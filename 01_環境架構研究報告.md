# 機器狗 MuJoCo 模擬專案 — 環境與架構研究報告

> 產出日期：2026-07-01（定案版）
> 對象機器：EndeavourOS（Arch 系）· i7-1360P · Intel Iris Xe（無 NVIDIA）· 16 GB RAM
> 內容：決策定案、硬體評估、軟體架構分析、MuJoCo 入門教學、三階段任務技術地圖、環境建置步驟

## 目錄

1. [一句話結論（TL;DR）](#1-一句話結論tldr)
2. [決策定案](#2-決策定案)
3. [硬體評估](#3-硬體評估)
4. [軟體架構分析：ROS1 vs ROS2 vs 不用 ROS](#4-軟體架構分析ros1-vs-ros2-vs-不用-ros)
5. [MuJoCo 入門教學](#5-mujoco-入門教學)
6. [宇樹機器狗模型現況](#6-宇樹機器狗模型現況)
7. [走路控制器：為何選 MJPC](#7-走路控制器為何選-mjpc)
8. [三階段任務技術地圖](#8-三階段任務技術地圖)
9. [環境建置步驟](#9-環境建置步驟)
10. [決策紀錄](#10-決策紀錄)
- [附錄 A：備案路線 R（Colab 訓練一次 → 本機推論）](#附錄-a備案路線-rcolab-訓練一次--本機推論)
- [附錄 B：參考資料](#附錄-b參考資料)

---

## 1. 一句話結論（TL;DR）

1. **不用 ROS1。** ROS1（Noetic）已於 **2025-05-31 正式 EOL**，官方只支援 Ubuntu 20.04，在你的 Arch 系統上原生安裝非常痛苦。本專案三階段任務用 **純 MuJoCo Python（`pip install mujoco`）** 就能完成。
2. **未來若真需要 ROS 生態**（rviz / tf / 訊息匯流排 / 真機驅動），請用 **ROS2（Jazzy/Humble）+ RoboStack**，不要 ROS1。
3. **硬體結論：** 你的機器**能**跑 MuJoCo 模擬、渲染與走路控制（推論）；但**不能**做大規模 RL 訓練（需 NVIDIA CUDA GPU）。
4. **走路方式定案：** 宇樹「自帶走路（Sport Mode）」只在真機、模擬裡沒有；且**四足沒有官方可下載即用的 RL 權重**。因此走路採用 **MJPC**（DeepMind `mujoco_mpc`，內建 Unitree 四足走路，純本機 CPU、免訓練）。

---

## 2. 決策定案

（以下為與你確認後的最終方向，本報告其餘章節均依此展開。）

| 決策點 | 定案 | 依據 |
|--------|------|------|
| 是否用 ROS | **初期不用**，純 MuJoCo Python；未來需要才上 ROS2 | §4 |
| 機器狗型號 | **不限 Go2，四足能走即可**（以最快看到成果為準） | §6 |
| 走路控制器 | **主路線 = MJPC**（免訓練、純本機 CPU）；**備案 = Colab 訓練一次→本機推論** 保留 | §7、附錄A |
| GPU / 雲端 | 無本機 NVIDIA；**以本機為主**，Colab 僅備案時一次性使用 | §3 |

**整體技術路線一覽：**

```
任務1  純 MuJoCo Python 環境   → 看到宇樹四足站在地面（pip install mujoco）
任務2  編譯 MJPC              → Quadruped 任務，狗在 CPU 上走路（免訓練）
任務3  XML 加 IMU+magnetometer → compass 解算 yaw → 外層 P/PI 航向校正走直線
備案   路線 R（Colab RL）      → 需要「真 RL 策略」時再啟用（附錄A）
```

---

## 3. 硬體評估

| 項目 | 你的規格 | 評估 | 說明 |
|------|----------|------|------|
| CPU | i7-1360P（12核/16緒） | ✅ 良好 | 單隻機器狗的 MuJoCo 物理模擬 + 控制器可即時執行 |
| GPU | **Intel Iris Xe 內顯，無 CUDA** | ⚠️ 關鍵限制 | 渲染 OK；但 **RL 訓練、Isaac Sim、MJX/GPU 平行模擬都不能用** |
| RAM | 16 GB（**無 swap**） | ⚠️ 夠但偏緊 | 建議加 swap（見 §9） |
| 硬碟 | 剩 37 GB（已用 85%） | ⚠️ 偏緊 | conda 環境 + 模型 + 編譯會吃空間，建議先清出 ≥ 20 GB |
| OS | EndeavourOS（Arch 系） | ⚠️ | 對 ROS1 不友善；對 conda/MuJoCo/MJPC 沒問題 |
| Python | miniforge 3.10 | ✅ 有利 | 已有 conda，建環境方便 |

### 三種工作負載的可行性判定

| 工作負載 | 需要 CUDA？ | 你的機器可行？ | 結論 |
|----------|-----------|----------------|------|
| MuJoCo 渲染（互動視窗 / 離屏） | ❌ 用 OpenGL | ✅ 可行 | Iris Xe 支援 OpenGL 4.6 |
| 走路控制（MJPC / 策略推論，單機器狗） | ❌ | ✅ 可行 | CPU 即時 |
| **訓練 RL 走路策略** | ✅ **需要** | ❌ **不可行** | 需 NVIDIA；改用 MJPC 或上雲訓練（附錄A） |

> **關鍵提醒：** MuJoCo 的渲染是 **OpenGL**，不是 CUDA，所以「看模擬畫面」在你的內顯上沒問題。CUDA 只有在「一次平行模擬幾千隻機器狗來訓練」時才需要。

---

## 4. 軟體架構分析：ROS1 vs ROS2 vs 不用 ROS

### 4.1 你原本的想法（MuJoCo + ROS1）合理嗎？

**結論：技術上做得到，但不建議，屬於「逆風」選擇。** 原因：

- **ROS1 已 EOL。** ROS PMC 官方訂 Noetic 生命週期終點為 **2025-05-31**（與 Ubuntu 20.04 同日）。之後沒有官方更新，社群套件也逐漸停止維護。
- **官方只支援 Ubuntu 20.04。** 你是 Arch，沒有官方二進位。
- **Arch 上裝 ROS1 特別痛。** AUR 的 `ros-*` 套件已無人維護；從原始碼對 Arch 的滾動式工具鏈（新版 glibc/Python/Boost）編譯很脆弱、容易壞。
- 唯一相對可行的 ROS1-on-Arch 路線是 **RoboStack（用 conda/mamba 裝 `ros-noetic`）**，二進位自成一格、不依賴系統工具鏈。但 RoboStack 官方重心已移到 ROS2，Noetic 屬「legacy」通道，能撐多久不確定。

### 4.2 選項比較

| 面向 | ROS1 Noetic | ROS2（Jazzy/Humble） | **不用 ROS（純 MuJoCo Python）** |
|------|-------------|----------------------|-------------------------------------|
| 維護狀態 | ❌ 已 EOL | ✅ 活躍（Jazzy 支援到 2029） | ✅ MuJoCo 官方持續維護 |
| Arch 相容性 | ❌ 很差（僅 RoboStack 勉強） | 🟡 中（RoboStack 良好支援） | ✅ 完美（pip 一行） |
| 上手難度 | 高 | 中 | **低** |
| 完成本專案三任務所需？ | 否 | 否 | ✅ **足夠** |
| 何時才值得用 | 幾乎不建議 | 需要 tf/rviz/多節點/真機驅動時 | 專案初期最佳起點 |

### 4.3 分階段架構

```
階段 A（現在～完成三任務）：純 MuJoCo Python
   pip install mujoco
   └─ 載入宇樹模型（MuJoCo Menagerie）
   └─ 走路控制（MJPC）
   └─ 內建 IMU / 磁力計感測器
   └─ 用 viewer 看畫面、matplotlib 畫軌跡

階段 B（未來，若需要）：加上 ROS2（不是 ROS1）
   micromamba + robostack-jazzy
   └─ mujoco_ros2_control 把 MuJoCo 接進 ROS2
   └─ rviz 視覺化、tf、rosbag 記錄、真機部署
```

> **為什麼先不用 ROS？** ROS 的價值在「多程序訊息匯流排 + 生態工具（tf/rviz/驅動）」。目前三個任務是「單一模擬 + 單一控制器 + 讀感測器」，在 MuJoCo Python 裡就是幾十行程式碼，硬套 ROS 只會多一層安裝與除錯負擔。等要接真機或做多節點系統時，再加 ROS2 是很自然的升級。

---

## 5. MuJoCo 入門教學

### 5.1 名詞與定位

**MuJoCo**（Multi-Joint dynamics with Contact）是 Google DeepMind 維護的 **物理引擎**，專長是「有接觸的多關節剛體動力學」——正好適合機器人足式運動。2022 年開源、免費。

- **不是** 像 Gazebo 那樣「開箱即用的模擬器 GUI 應用」，而是一個 **函式庫**：你用 Python/C++ 呼叫它推進物理、讀狀態、下控制命令。
- 它自帶輕量的 **互動檢視器（`mujoco.viewer`）** 與 **離屏渲染**（OpenGL）。

### 5.2 三個核心概念

1. **MJCF / XML 模型檔**：機器人與世界用一個 `.xml`（MJCF 格式）描述——身體、關節、幾何、致動器（馬達）、**感測器**都寫在裡面。宇樹的 `.xml` 從 MuJoCo Menagerie 下載即可。
2. **`mjModel`（模型，不變）與 `mjData`（狀態，每步變）**：
   - `model` = 結構（關節數、質量、感測器定義…），載入後固定。
   - `data` = 當下狀態（關節角度 `qpos`、速度 `qvel`、感測器讀值 `sensordata`…），每步 `mj_step` 後更新。
3. **模擬迴圈**：核心就是這個 loop
   ```python
   import mujoco, mujoco.viewer
   model = mujoco.MjModel.from_xml_path("scene.xml")   # 讀模型
   data  = mujoco.MjData(model)                        # 建狀態
   with mujoco.viewer.launch_passive(model, data) as v:
       while v.is_running():
           data.ctrl[:] = my_controller(data)          # 1) 算控制命令（餵給馬達）
           mujoco.mj_step(model, data)                  # 2) 推進一個物理步
           v.sync()                                     # 3) 更新畫面
   ```
   - `data.ctrl` = 你下給致動器的命令（力矩或目標角度）。
   - `data.sensordata` = 讀感測器（IMU、磁力計等）。

### 5.3 感測器（對任務3特別重要）

MuJoCo **原生內建** 這些感測器，寫在 XML 的 `<sensor>` 區塊，掛在某個 `site` 上：

| 感測器類型 | 讀到什麼 | 對應真實元件 |
|-----------|---------|-------------|
| `accelerometer` | 3軸線性加速度（區域座標） | 加速度計 |
| `gyro` | 3軸角速度 | 陀螺儀 |
| `magnetometer` | 3軸磁通量向量（區域座標） | **磁力計/指南針** |
| `framequat` | 該 frame 的姿態四元數 | （直接拿到絕對姿態） |

→ **「9軸 IMU（含 compass）」= `accelerometer` + `gyro` + `magnetometer` 掛在同一個 site。** MuJoCo 沒有獨立的 "compass" 型別，`magnetometer` 就是它的三軸電子羅盤（磁向量投影到機身座標，可解算航向角 yaw）。

> 小技巧：只想「知道現在朝哪個方向」，用 `framequat`（或 `framezaxis`）直接讀絕對姿態最省事；但你的任務明確要求「用 IMU 的 compass 校正」，所以用 `magnetometer` 模擬真實羅盤，更貼近題目。

### 5.4 生態系工具（知道有這些就好）

- **MuJoCo Menagerie**：DeepMind 官方高品質機器人模型庫（含宇樹 A1/Go1/Go2）。
- **MuJoCo MPC（MJPC）**：DeepMind 官方即時模型預測控制框架，內建四足走路任務——**本專案走路用它**。
- **MuJoCo Playground / MJX**：GPU 加速 RL 訓練框架（**需 NVIDIA**，本機不適用於訓練，僅備案時上 Colab 用）。

---

## 6. 宇樹機器狗模型現況

| 來源 | 內容 | 適合你？ |
|------|------|----------|
| **MuJoCo Menagerie** (`google-deepmind/mujoco_menagerie`) | 官方高品質模型，含宇樹 **A1 / Go1 / Go2**（各12自由度）。**B1/B2/Aliengo 不在裡面** | ✅ 取得機器狗模型的首選 |
| **`unitreerobotics/unitree_mujoco`** | 官方模擬器（C++/Python）+ 地形工具 + SDK 橋接。**只提供低階控制，範例只有站/趴，沒有走路步態** | 🟡 想模擬真機 SDK 行為時用 |
| **`unitreerobotics/unitree_rl_gym`** | RL 訓練 + Sim2Sim 到 MuJoCo。**內建預訓練權重只有 G1/H1/H1_2（人形）；四足沒有官方現成權重** | 🟡 人形有現成、四足需自己訓練 |
| **`unitreerobotics/unitree_rl_mjlab`** | 官方新版、直接以 MuJoCo 為後端的 RL 框架，支援 Go2 等 | 🟡 訓練框架（需 GPU 訓練） |
| 社群：`unitree-go2-mjx-rl`、`darshmenon/quadruped-dog-rl` 等 | Go2 在 MuJoCo 的走路訓練/策略 | 🟡 可參考/借用（附錄A 相關） |

---

## 7. 走路控制器：為何選 MJPC

### 7.1 重要澄清：模擬裡沒有「宇樹自帶走路」

你原本假設「用機器狗自帶的行走功能就能走路」。**在模擬環境裡這個假設不成立：**

- 宇樹真機的 **Sport Mode / 高階走路（`SportClient`）跑在機身內的運動控制服務上，封閉、不開放到模擬**。`unitree_mujoco` 只能接**低階**（關節力矩/位置）控制，官方範例也只有站起/趴下，**沒有內建走路步態**。
- 所以在 MuJoCo 裡要讓狗走路，必須**自己提供一個低階控制器**。

### 7.2 查證定案：四足沒有官方現成的 RL 走路權重

實際查證各 repo 後確認：

- `unitree_rl_gym` 提交的預訓練 `.pt/.onnx` **只有人形 G1/H1/H1_2**；四足 Go1/Go2/A1 **沒有** 官方現成權重。
- MuJoCo Playground 的 Go1 走路策略 **沒有 committed 權重、且訓練需要 NVIDIA GPU**。

→ 「下載即用、CPU 直接跑的四足 RL 走路權重」在官方管道基本不存在。因此「現成 + RL + 四足 + 純本機」四者無法同時成立。

### 7.3 結論：主路線用 MJPC

| | **主路線 = MJPC** | 備案 = Colab 訓練→本機推論（附錄A） |
|---|---|---|
| 原理 | 取樣式 MPC（Predictive Sampling），**非學習** | RL（PPO） |
| 訓練？ | **完全免訓練** | 上 Colab 訓一次（免費 GPU，數分鐘） |
| 本機需求 | **純 CPU 即時跑、零雲端** | 之後永遠本機 CPU 推論；僅一次雲端訓練 |
| 現成程度 | 內建 Unitree 四足走路任務，編譯後即可看狗走路 | 需跑 notebook、匯出、接推論腳本 |
| 缺點 | 需從原始碼**編譯**（非 pip）；是 MPC 不是 RL | 有一次性雲端步驟；接線較多 |

> 選 MJPC 的理由：最貼合你「本機為主、最快看到成果、盡量不靠雲端」。它免訓練、免 GPU、免下載權重，內建就有 Unitree 四足走路。備案 R（真 RL）保留於附錄A。

---

## 8. 三階段任務技術地圖

```
任務1：環境建置
  └─ 建 conda 環境 → pip install mujoco → 下載 mujoco_menagerie
  └─（暫不裝 ROS；未來需要再上 ROS2/RoboStack）
  ✅ 驗收：能開 viewer 看到宇樹四足站在地面上

任務2：讓機器狗走路（MJPC）
  └─ 編譯 mujoco_mpc → 開 Quadruped 任務
  └─（進階）用 MJPC Python API 在程式中驅動、給方向指令
  ✅ 驗收：狗能持續向前走一段距離

任務3：走直線（IMU/compass 姿態校正）
  └─ 在模型 XML 幫機身加 accelerometer + gyro + magnetometer（掛同一 site）
  └─ 從 magnetometer 解算航向角 yaw
  └─ 先觀察「不校正時會偏一邊」的現象並記錄軌跡
  └─ 加航向控制迴圈：用 yaw 誤差修正走路的「轉向命令」
  ✅ 驗收：對比「有/無校正」軌跡，校正後明顯更直
```

> 補充：任務3「狗會往一邊偏」在模擬裡通常來自控制器方向偏差、左右腿摩擦/接觸不對稱、或積分漂移。用 compass 讀絕對航向 → 算與目標航向誤差 → 回授成轉向指令，是標準做法（外層航向 P/PI 控制器包住內層走路控制器）。

---

## 9. 環境建置步驟

### 9.1 前置（會改到系統，需 sudo）

```bash
# 加 swap（16GB 無 swap 偏險，建議做），建立 8GB swapfile：
sudo fallocate -l 8G /swapfile && sudo chmod 600 /swapfile \
  && sudo mkswap /swapfile && sudo swapon /swapfile

# 另外：請先清出硬碟空間至 ≥ 20 GB 餘裕（目前僅剩 37GB / 85%）
```

### 9.2 任務1：純 MuJoCo Python 環境

```bash
# 1) 用 conda/mamba 建乾淨環境（你已有 miniforge）
mamba create -n rbtdog python=3.11 -y
mamba activate rbtdog

# 2) 安裝 MuJoCo 官方 Python 套件
pip install mujoco

# 3) 快速自我測試
python -c "import mujoco; print('MuJoCo', mujoco.__version__)"
python -m mujoco.viewer          # 應跳出互動視窗（拖曳可轉視角）

# 4) 取得宇樹機器狗模型並開啟
git clone https://github.com/google-deepmind/mujoco_menagerie.git
python -m mujoco.viewer --mjcf=mujoco_menagerie/unitree_go2/scene.xml
#    → 看到 Go2 站在地面上（任務1 驗收）
```

> 註：`python -m mujoco.viewer` 需要能開圖形視窗（本機桌面環境即可）。若之後要遠端/無頭執行，再改用離屏渲染。

### 9.3 任務2：編譯 MJPC（走路引擎）

MJPC 需從原始碼編譯（非 pip）。Arch 上：

> ⚠️ 本機（Arch + CMake 4.x + GCC 16）踩過 3 個雷，**務必照下面帶旗標的版本做**，否則會 configure 失敗、編譯中斷、或編成功但一開 Quadruped 就 segfault。

```bash
# 1) 安裝建置工具（Arch 若久未更新，DB 過期會報 404，需 -Syu 一起裝）
sudo pacman -Syu base-devel cmake git ninja

# 2) 取得原始碼
cd /home/huang/rbtdog_sim
git clone --depth 1 https://github.com/google-deepmind/mujoco_mpc.git
cd mujoco_mpc

# 3) 先跑一次 configure 把相依抓下來（會因雷①失敗前需先加旗標；此處已含）
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
      -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
      -DCMAKE_C_FLAGS_RELEASE="-O2 -DNDEBUG -fno-strict-aliasing" \
      -DCMAKE_CXX_FLAGS_RELEASE="-O2 -DNDEBUG -fno-strict-aliasing"

# 4) 刪掉 MuJoCo 的 -Werror（雷②），並確認 LTO 關閉（雷③）
#    編輯 build/_deps/mujoco-src/cmake/MujocoOptions.cmake：
#      - 移除 EXTRA_COMPILE_OPTIONS 裡的 "-Werror" 那一行
#      - 把 "if(NOT CMAKE_INTERPROCEDURAL_OPTIMIZATION ...) set(... ON)" 改成 set(... OFF)
#    然後重跑一次上面的 cmake configure

# 5) 編譯
cmake --build build -j8                            # 記憶體不足時改 -j4

# 6) 啟動互動 GUI，選 Quadruped 任務即可看到狗走路（任務2 驗收）
./build/bin/mjpc
```

**三個雷區（本機實際踩過，皆因 Arch 滾動工具鏈太新 vs MJPC 舊碼）：**

- **① CMake 4.x 太新**：相依 qhull 用舊語法，configure 報 `Compatibility with CMake < 3.5 has been removed`。→ 加 `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`。
- **② GCC 16 + MuJoCo `-Werror`**：MuJoCo 內附源碼有新 GCC 才報的警告（`engine_print.c` discarded-qualifiers），`-Werror` 使其中斷。→ 刪 `MujocoOptions.cmake` 的 `-Werror` 行。
- **③（最關鍵）GCC 16 + strict-aliasing → Quadruped segfault**：MJPC 的 `ReinterpretAsInt/Double`（用 `int*` 讀 `double` 做型別雙關）在 GCC 16 預設 `-fstrict-aliasing` 下被錯誤最佳化，`gait` 讀成垃圾值 → `kGaitPhase[gait]` 越界，**一開 Quadruped 就崩**。→ 編譯加 **`-fno-strict-aliasing`**（放在 `-O2` 之後才生效）。註：LTO 關閉、降 `-O2` 都無效，唯一有效的是這個旗標。

- MJPC 也提供 **Python API**（`python/` 目錄），任務3 要用程式讀 compass、下方向指令時會用到。
- ⚠️ 編譯較吃資源：先確認已加 swap、硬碟 ≥ 10 GB 餘裕。編譯一次即可，之後直接跑 `./build/bin/mjpc`。

### 9.4 建置順序建議

1. §9.2 純 MuJoCo 環境 → 看到四足站好（任務1）
2. §9.3 編譯 MJPC → 看到走路（任務2）
3. 在模型 XML 加 IMU / magnetometer → 航向校正（任務3）

---

## 10. 決策紀錄

（下列問題已於 2026-07-01 與你確認，記錄備查。）

| # | 問題 | 你的決定 |
|---|------|----------|
| 1 | 是否接受初期不用 ROS、未來需要才 ROS2？ | ✅ 接受，初期純 MuJoCo |
| 2 | 任務2 一定要 Go2，還是四足能走即可？ | 四足能走即可（最快） |
| 3 | 走路控制器走哪條路線？ | 先以 **MJPC（路線B）** 為主，另一條（Colab RL）保留備用 |
| 4 | 是否有可用 NVIDIA GPU？ | 無本機 GPU；可用 Colab，但**以本機為主、盡量不訓練** |

---

## 附錄 A：備案路線 R（Colab 訓練一次 → 本機推論）

保留備用，若之後想要「真正的 RL 策略」而非 MPC，走這條：

1. **雲端訓練（一次性）**：開 `mujoco_playground` 的 [locomotion Colab notebook](https://colab.research.google.com/github/google-deepmind/mujoco_playground/blob/main/learning/notebooks/locomotion.ipynb)，用免費 GPU 訓練 **Unitree Go1 joystick** 走路策略（數分鐘～數十分鐘）。
2. **匯出策略**：把訓好的 policy 匯出（Brax/JAX → 存權重，或轉 ONNX）。
3. **本機推論**：在本機 `rbtdog` conda 環境用 `mujoco`（CPU）載入 Go1 模型 + 讀策略權重，跑模擬迴圈（`data.ctrl = policy(obs)` → `mj_step`）。之後 100% 本機。
4. **任務3 相容**：joystick 策略輸入為「前進速度 + 側向速度 + yaw rate」，可用 magnetometer 解算的航向誤差回授到 yaw rate 指令做校正。

> 與主路線差別：MJPC 免訓練、免雲端、當下就能跑，但是 MPC；路線 R 是學習型策略、貼近真機部署流程，但需一次雲端訓練與較多接線工作。

---

## 附錄 B：參考資料

- ROS Noetic EOL（2025-05-31）：<https://discourse.openrobotics.org/t/ros-noetic-end-of-life-may-31-2025/43160>
- RoboStack（conda 裝 ROS）：<https://robostack.github.io/> · Noetic：<https://robostack.github.io/noetic.html>
- MuJoCo 官方文件（XML/感測器/渲染）：<https://mujoco.readthedocs.io/>
- MuJoCo Menagerie（宇樹模型）：<https://github.com/google-deepmind/mujoco_menagerie>
- MuJoCo MPC（MJPC，走路主路線）：<https://github.com/google-deepmind/mujoco_mpc>
- unitree_mujoco（低階模擬器）：<https://github.com/unitreerobotics/unitree_mujoco>
- unitree_rl_gym（RL + Sim2Sim，僅人形有預訓練權重）：<https://github.com/unitreerobotics/unitree_rl_gym>
- unitree_rl_mjlab（官方 MuJoCo RL）：<https://github.com/unitreerobotics/unitree_rl_mjlab>
- MuJoCo Playground（GPU RL，需 NVIDIA；備案用）：<https://playground.mujoco.org/>
- mujoco_ros2_control（未來 ROS2 整合）：<https://github.com/moveit/mujoco_ros2_control>
