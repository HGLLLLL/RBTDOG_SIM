# 智元 D1 EDU **輪足版（ZSL-1w）** CPG-RL 平地走路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **修訂 2（2026-08-10）**：機型由點足版（ZSL-1）改為**輪足版（ZSL-1w）**，四顆輪子熔接鎖死當圓腳走路。
> Task 1/2/3 全部改寫；Task 4/5/6/8 的程式碼透過 `d1_model` 取常數，結構不變（僅 Task 4 一處斷言範圍調整）；
> Task 7/9 的內容更新。修訂 1（點足版）見 git commit `211401b`。

**Goal:** 把 task4 在 Go2 上驗證過的 CPG-RL 論文標準版移植到智元 D1 EDU 輪足版（ZSL-1w），四顆輪子鎖死當 71 mm 圓腳，訓出平地走路權重，且 observation 只用未來 LowLevel 可能提供的量。

**Architecture:** 以 zsibot 的 ZSL-1W URDF 真實質量/慣量為準，手寫 menagerie 風格 MJCF（`d1_edu_w.xml` + `scene.xml` + `scene_mjx.xml`）。URDF 的四個輪子關節**不建立**，輪子當成無關節子 body 熔接在小腿末端（質量與慣量完整保留），動作空間維持 12 維。動作經 CPG 產生腳端偏移，再用「home 姿態數值 Jacobian 求逆」轉成關節角，交給 MJCF 內建位置伺服（kp=80/kd=1）。訓練在 Colab MJX，推論在本機 CPU。

**Tech Stack:** MuJoCo 3.10.0 / MJX / Brax PPO / JAX / numpy / pytest。本機 conda env `rbtdog`（CPU only），訓練在 Colab GPU。

**設計規格：** `docs/superpowers/specs/2026-08-10-d1-edu-cpg-rl-design.md`

## Global Constraints

- 執行環境一律 `conda run --no-capture-output -n rbtdog python ...`（本機無 GPU，`jax.devices()` 只有 `CpuDevice`）。
- **不得修改 task4 / task3 任何既有檔案。** task6 完全獨立，不 import task3 的 `Go2Gait`。
- 腿序常數固定為 `LEGS = ["FL", "FR", "RL", "RR"]`，全專案唯一，定義在 `task6/inference/d1_model.py`。
- 質量、慣量、關節原點、關節軸、關節限位**一律照 `zsl-1w/urdf/ZSL-1W.urdf` 原值**，不得參考 Go2 或點足版代換。
- 關節限位（rad）：abad `[-0.4887, 0.4887]`、hip `[-1.152, 2.967]`、knee `[-2.723, -0.602]`。
- knee 關節軸為 `0 1 0`（**與點足版 ZSL-1 的 `0 -1 0` 相反**，與 Go2 同向）。
- home 關節角 `HOME3 = [0.0, 1.05, -2.00]`、`HOME12 = HOME3 * 4`；機身站立高 0.2948 m。
- **URDF 的四個 `*_FOOT_JOINT`（連續旋轉的輪子）一律不建立**；輪子以無關節子 body 熔接在小腿末端，質量 0.90130429 kg 與慣量完整保留。動作空間因此維持 12 維。
- 輪子碰撞用 `cylinder`（半徑 0.0710、半高 0.0240、軸沿 y），不是 sphere。
- 致動器 kp=80、kd=1、`forcerange` ±28 N·m（全 12 關節相同）。
- **不得出現 `apply_pd()`**：增益寫死在 MJCF，訓練與推論共用同一組位置伺服。
- `OBS_DIM = 73`、`ACT_DIM = 12`。
- CPG 常數：`MU_MIN=1.0, MU_MAX=2.0, OMEGA_MIN=0.0, OMEGA_MAX=4.5, A_CONV=50.0, D_STEP=0.12, **D_STEP_Y=0.09**, G_C=0.08, G_P=0.01, W_COUP=8.0, N_CPG_SUB=4`。
- **前後步幅 `D_STEP=0.12` 與側向擺幅 `D_STEP_Y=0.09` 是兩個常數**（論文原式共用一個 d）。
  原因：abad 角只由側向偏移決定（靈敏度 4.468 rad/m），而本機 abad 行程僅 ±28°（Go2 為 ±60°），
  沿用 0.12 會讓 abad 峰值達 0.536 rad、超出 ctrlrange ±0.4687 達 14%。
  0.09 對應峰值 0.402 rad（86% 行程，留 14% 餘裕），且不犧牲前後步幅。
- `NOMINAL_HEIGHT = 0.2695`：實際站定高度。keyframe 的 z=0.2948 是純運動學值，
  kp=80 的靜態撓度會讓機身沉降約 2.5 cm。**訓練的高度獎勵必須用 NOMINAL_HEIGHT，不是 key_qpos[2]。**
- `CTRL_DT=0.02`、`SIM_DT=0.004`。
- 所有註解與文件用繁體中文。

### 規格未涵蓋、本計畫新增的假設（需在 Task 3 回報實測結果）

URDF 沒有提供**轉子慣量（armature）**與**關節阻尼（damping）**。本計畫採 `armature="0.01"`、`damping="0.1"`
作為起始值（armature 沿用 menagerie Go2 的 0.01；damping 取小值以免與致動器 kd=1 重複計入阻尼）。
Task 3 的站姿測試與 Task 5 的開迴路測試若顯示不穩或過阻尼，**回報數據給使用者再決定**，不得自行調參後默默通過。

---

## File Structure

| 檔案 | 責任 |
|---|---|
| `task6/model/d1_edu_w/d1_edu_w.xml` | 機器人本體 MJCF：body 樹、慣量、關節、碰撞、致動器、感測器、home keyframe |
| `task6/model/d1_edu_w/scene.xml` | 本機 CPU 場景（含 skybox / 地板材質 / 燈光） |
| `task6/model/d1_edu_w/scene_mjx.xml` | MJX 訓練場景（同上，`<option>` 走 MJX 參數） |
| `task6/model/d1_edu_w/meshes/*.STL` | 17 個視覺網格（zsl-1w） |
| `task6/model/d1_edu_w/LICENSE` | zsibot BSD-3 授權原文 |
| `task6/model/d1_edu_w/SOURCE.md` | 資產來源與 commit 記錄 |
| `task6/inference/d1_model.py` | **單一事實來源**：路徑、`LEGS`、`HOME3/HOME12`、CPG 常數、`OBS_DIM`、`TAU_CONTACT`、`make_model()` |
| `task6/inference/cpg_d1.py` | CPG 動力學 + 動作解碼 + IK 常數 + 關節目標角（純函式，無 I/O） |
| `task6/inference/obs_d1.py` | 73 維 observation 組裝 + 力矩觸地判定 |
| `task6/inference/cpg_openloop_d1.py` | 關卡 3：開迴路 CPG rollout，驗證模型/IK/PD/CPG，並輸出膝力矩分佈 |
| `task6/inference/local_infer_d1.py` | 關卡 6：載入權重、本機推論、輸出影片與指標 |
| `task6/notebooks/cpg_rl_d1_colab.ipynb` | Colab MJX 訓練 |
| `task6/tests/test_model.py` | 關卡 1、2：MJCF 對帳與站姿穩定 |
| `task6/tests/test_cpg_d1.py` | CPG / IK 純函式單元測試 |
| `task6/tests/test_obs_d1.py` | obs 維度與欄位順序測試 |
| `task6/docs/D1_EDU_規格與模型對照.md` | 規格對帳表、SDK 腿序三方對照、sim2real 部署注意事項 |
| `task6/README.md` | 使用流程 |

依賴方向：`d1_model.py` ← `cpg_d1.py` / `obs_d1.py` ← `cpg_openloop_d1.py` / `local_infer_d1.py`。無循環依賴。

---

## Task 1: 換成輪足版模型資產

**背景：** 專案原本匯入了點足版（ZSL-1）資產並建了 MJCF，使用者其後告知實機是輪足版。
本任務**刪除點足版資產、換成輪足版**。

**Files:**
- Delete: `task6/model/d1_edu/`（整個目錄：舊點足版 meshes / LICENSE / SOURCE.md / d1_edu.xml）
- Delete: `task6/tests/test_model.py`（舊點足版 MJCF 對帳測試，Task 2 會重寫）
- Create: `task6/model/d1_edu_w/meshes/` （17 個 STL，從 zsibot repo `zsl-1w/` 複製）
- Create: `task6/model/d1_edu_w/LICENSE`
- Create: `task6/model/d1_edu_w/SOURCE.md`
- Modify: `task6/tests/test_assets.py`（改指向 `d1_edu_w`）
- 保留：`task6/inference/__init__.py`（已存在，不動）

**Interfaces:**
- Consumes: 無
- Produces: `task6/model/d1_edu_w/meshes/{BASE,FL_ABAD,FL_HIP,FL_KNEE,FL_FOOT,FR_*,RR_*,RL_*}_LINK.STL` 共 17 檔

**注意：** zsl-1w 的 meshes 目錄有 **18** 個檔，其中 `BASE_LINK_ori.STL` **未被 URDF 引用**，不要複製（YAGNI）。

- [ ] **Step 1: 寫失敗的測試**

Create `task6/tests/test_assets.py`:

```python
"""資產完整性測試：17 個 STL、授權、來源記錄都要在。"""
from pathlib import Path

MODEL_DIR = Path(__file__).resolve().parents[1] / "model" / "d1_edu_w"

EXPECTED_MESHES = [
    "BASE_LINK.STL",
    *[f"{leg}_{part}_LINK.STL"
      for leg in ("FL", "FR", "RL", "RR")
      for part in ("ABAD", "HIP", "KNEE", "FOOT")],
]


def test_all_17_meshes_present():
    missing = [n for n in EXPECTED_MESHES if not (MODEL_DIR / "meshes" / n).is_file()]
    assert missing == [], f"缺少網格: {missing}"
    assert len(EXPECTED_MESHES) == 17


def test_only_referenced_meshes_copied():
    """zsl-1w 上游有 18 個 STL，BASE_LINK_ori.STL 未被 URDF 引用，不該複製進來。"""
    got = sorted(p.name for p in (MODEL_DIR / "meshes").glob("*.STL"))
    assert got == sorted(EXPECTED_MESHES), f"網格清單不符，多餘或缺少: {set(got) ^ set(EXPECTED_MESHES)}"


def test_license_and_source_recorded():
    assert (MODEL_DIR / "LICENSE").is_file()
    src = (MODEL_DIR / "SOURCE.md").read_text(encoding="utf-8")
    assert "zsibot/genisom_model" in src
    assert "BSD-3" in src
    assert "zsl-1w" in src, "SOURCE.md 必須寫明取用的是輪足版 zsl-1w 目錄"


def test_point_foot_assets_removed():
    """點足版資產必須已刪除，避免兩份模型並存造成誤用。"""
    assert not (MODEL_DIR.parent / "d1_edu").exists(), "舊的點足版目錄 task6/model/d1_edu 尚未刪除"
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_assets.py -v`
Expected: FAIL — `缺少網格: [...]`（17 個全缺，因為 `d1_edu_w` 目錄還不存在）

- [ ] **Step 3: 刪除點足版資產、取得輪足版**

```bash
cd /home/huang/rbtdog_sim
git rm -r -q task6/model/d1_edu task6/tests/test_model.py
mkdir -p task6/model/d1_edu_w/meshes

TMP=$(mktemp -d)
git clone --depth 1 https://github.com/zsibot/genisom_model.git "$TMP/genisom"
# 只複製 URDF 有引用的 17 個；BASE_LINK_ori.STL 不要
for n in BASE_LINK \
         FL_ABAD FL_HIP FL_KNEE FL_FOOT \
         FR_ABAD FR_HIP FR_KNEE FR_FOOT \
         RL_ABAD RL_HIP RL_KNEE RL_FOOT \
         RR_ABAD RR_HIP RR_KNEE RR_FOOT ; do
    case "$n" in BASE_LINK) f="BASE_LINK.STL";; *) f="${n}_LINK.STL";; esac
    cp "$TMP/genisom/zsl-1w/meshes/$f" task6/model/d1_edu_w/meshes/
done
cp "$TMP/genisom/LICENSE" task6/model/d1_edu_w/LICENSE
git -C "$TMP/genisom" rev-parse HEAD
rm -rf "$TMP"
ls task6/model/d1_edu_w/meshes | wc -l   # 應為 17
```

`git rev-parse HEAD` 印出的 hash 要填進 Step 4 的 `<commit>`。

- [ ] **Step 4: 寫 SOURCE.md**

Create `task6/model/d1_edu_w/SOURCE.md`（把 Step 3 印出的 commit 填進 `<commit>`）:

```markdown
# D1 EDU 輪足版（ZSL-1w）模型資產來源

## 網格與運動學

- 來源：https://github.com/zsibot/genisom_model  目錄 **`zsl-1w/`**（輪足版）
- commit：`<commit>`
- 授權：BSD-3-Clause（見同目錄 `LICENSE`）
- 取用內容：17 個 STL 視覺網格；質量/慣量/關節參數抄自同目錄 `urdf/ZSL-1W.urdf`
- 未取用：上游 `meshes/` 另有 `BASE_LINK_ori.STL`，URDF 未引用，故未複製。

## 為什麼不是官方 repo

智元官方 SDK repo（https://github.com/AgibotTech/agibot_D1_Edu-Ultra , commit `db8accd`）
**不含任何 URDF / mesh / 模擬模型**，只有 `demo/`、`docs/`、`include/`、`lib/`、`site/`。

## 輪足版與點足版的差異（同一產品線的兩種構型）

| 項目 | ZSL-1 點足 | **ZSL-1w 輪足（本專案採用）** |
|---|---|---|
| URDF 自由度 | 12 | 16（每腿多一顆連續旋轉的輪）|
| 總質量 | 15.19 kg | **20.56 kg** |
| 足端 | 0.063 kg 圓球 | **0.901 kg 輪**（半徑 71 mm、寬 48 mm）|
| 小腿質量 | 0.207 kg | 0.765 kg |
| 膝關節軸 | `0 -1 0` | **`0 1 0`** |

## 與官網規格的對帳

官網 D1 Pro/Edu 標示 15.5 kg，本 URDF 為 20.56 kg。差額 5.06 kg
≈ 4 ×[(0.901 − 0.063) + (0.765 − 0.207)] = 5.58 kg，正好是輪子與加粗小腿相對點足版的增重。
判定：**官網數字是點足版**，兩份資料不衝突。

其餘可對帳項目（輪足版與點足版共用）：

| 項目 | zsibot URDF | 官網 D1 Edu 規格 |
|---|---|---|
| abad 行程 | ±28.0° | ±28° |
| hip 行程 | −66.0°~170.0° | −170°~66°（符號約定相反）|
| knee 行程 | −156.0°~−34.5° | 35°~156° |
| 機身長 | 0.5555 m | 站立 0.635 m（含腿）|
| 機型代號 | `ZSL-1W` | SDK 輪足版代號 `zsl-1w` |
```

- [ ] **Step 5: 執行測試確認通過**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_assets.py -v`
Expected: PASS（4 passed）

- [ ] **Step 6: Commit**

```bash
cd /home/huang/rbtdog_sim
git add -A task6/
git commit -m "feat(task6)!: 機型改為輪足版 ZSL-1w，換掉點足版資產

使用者告知實機為輪足版。刪除 d1_edu/(點足版 meshes+MJCF) 與其對帳測試，
改匯入 zsl-1w 的 17 個網格到 d1_edu_w/。"
```

---

## Task 2: `d1_edu_w.xml` 本體 MJCF（關卡 1）

**Files:**
- Create: `task6/model/d1_edu_w/d1_edu_w.xml`
- Create: `task6/tests/test_model.py`（Task 1 已刪掉舊的點足版版本，這裡重新建立）

**Interfaces:**
- Consumes: `task6/model/d1_edu_w/meshes/*.STL`（Task 1）
- Produces: MJCF 名稱契約，後續所有任務依賴 ——
  - body：`base`、`{LEG}_abad`、`{LEG}_hip`、`{LEG}_knee`、`{LEG}_wheel`
  - joint：`{LEG}_abad_joint`、`{LEG}_hip_joint`、`{LEG}_knee_joint`（**輪子沒有關節**）
  - actuator：`{LEG}_abad`、`{LEG}_hip`、`{LEG}_knee`（順序 FL, FR, RL, RR × abad, hip, knee，共 12 個）
  - 輪子碰撞 geom：`FL`、`FR`、`RL`、`RR`（沿用短名，供 IK 與觸地判定引用）
  - site：`imu`
  - keyframe：`home`（index 0）

- [ ] **Step 1: 寫失敗的測試**

Create `task6/tests/test_model.py`:

```python
"""關卡 1：MJCF 與 URDF 對帳。數值全部來自 zsl-1w/urdf/ZSL-1W.urdf（輪足版）。"""
from pathlib import Path

import mujoco
import numpy as np
import pytest

XML = str(Path(__file__).resolve().parents[1] / "model" / "d1_edu_w" / "d1_edu_w.xml")
LEGS = ["FL", "FR", "RL", "RR"]

URDF_TOTAL_MASS = 20.559          # 17 個 link 質量總和（含四顆 0.9013 kg 的輪）
WHEEL_RADIUS = 0.0710             # STL 實測
LIMITS = {                        # rad，URDF <limit lower/upper>
    "abad": (-0.4887, 0.4887),
    "hip": (-1.152, 2.967),
    "knee": (-2.723, -0.602),
}


@pytest.fixture(scope="module")
def model():
    return mujoco.MjModel.from_xml_path(XML)


def test_freejoint_present_and_dof_count(model):
    # 沒有 freejoint 的話 nq=12 且 BASE_LINK 會被熔進 worldbody
    assert model.nq == 19, f"nq 應為 19（7 自由基座 + 12 關節），實得 {model.nq}"
    assert model.nv == 18
    assert model.nu == 12, f"輪子不建關節，致動器應為 12 個，實得 {model.nu}"


def test_wheels_have_no_joint(model):
    """輪子熔接鎖死：URDF 的 4 個 *_FOOT_JOINT 不得出現在 MJCF。"""
    names = [mujoco.mj_id2name(model, mujoco.mjtObj.mjOBJ_JOINT, i) for i in range(model.njnt)]
    assert not [n for n in names if n and ("wheel" in n or "foot" in n.lower())], \
        f"輪子不該有關節，實際關節清單：{names}"
    # 1 個 freejoint + 12 個 hinge
    assert model.njnt == 13, f"關節總數應為 13（1 freejoint + 12 hinge），實得 {model.njnt}"


def test_total_mass_matches_urdf(model):
    total = float(model.body_mass.sum())
    assert total == pytest.approx(URDF_TOTAL_MASS, rel=0.01), (
        f"總質量 {total:.3f} kg 與 URDF 的 {URDF_TOTAL_MASS} kg 差超過 1%；"
        "最常見原因是缺 freejoint 導致 BASE_LINK 的 6.716 kg 被熔進 world，"
        "或是漏掉四顆 0.9013 kg 的輪子"
    )


def test_wheel_mass_preserved(model):
    """鎖死不等於簡化掉：四顆輪的質量與慣量都要在。"""
    for leg in LEGS:
        bid = mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_BODY, f"{leg}_wheel")
        assert bid >= 0, f"找不到 body {leg}_wheel"
        assert float(model.body_mass[bid]) == pytest.approx(0.90130429, rel=1e-6)


@pytest.mark.parametrize("leg", LEGS)
@pytest.mark.parametrize("part", ["abad", "hip", "knee"])
def test_joint_limits_match_urdf(model, leg, part):
    jid = mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_JOINT, f"{leg}_{part}_joint")
    assert jid >= 0, f"找不到關節 {leg}_{part}_joint"
    lo, hi = model.jnt_range[jid]
    assert (lo, hi) == pytest.approx(LIMITS[part], abs=1e-4)


@pytest.mark.parametrize("leg", LEGS)
def test_knee_axis_is_positive_y(model, leg):
    """輪足版 ZSL-1w 的膝軸是 +y（點足版 ZSL-1 是 -y）。抄錯會導致 home 姿態站不起來。"""
    jid = mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_JOINT, f"{leg}_knee_joint")
    assert model.jnt_axis[jid] == pytest.approx([0.0, 1.0, 0.0], abs=1e-9)


def test_actuator_order_and_gains(model):
    expected = [f"{leg}_{part}" for leg in LEGS for part in ("abad", "hip", "knee")]
    got = [mujoco.mj_id2name(model, mujoco.mjtObj.mjOBJ_ACTUATOR, i) for i in range(model.nu)]
    assert got == expected, "致動器順序必須是 FL,FR,RL,RR × abad,hip,knee"
    # 位置伺服：tau = kp*(ctrl - q) - kd*qd，kp=80 kd=1（原廠 demo 值）
    assert model.actuator_gainprm[:, 0] == pytest.approx(80.0)
    assert model.actuator_biasprm[:, 1] == pytest.approx(-80.0)
    assert model.actuator_biasprm[:, 2] == pytest.approx(-1.0)
    assert model.actuator_forcerange[:, 0] == pytest.approx(-28.0)
    assert model.actuator_forcerange[:, 1] == pytest.approx(28.0)


def test_named_entities_exist(model):
    for leg in LEGS:
        for suffix in ("abad", "hip", "knee", "wheel"):
            assert mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_BODY, f"{leg}_{suffix}") >= 0
        assert mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_GEOM, leg) >= 0, f"缺輪子碰撞 geom {leg}"
    assert mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_BODY, "base") >= 0
    assert mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_SITE, "imu") >= 0


def test_wheel_collision_is_cylinder_with_correct_radius(model):
    """鎖死的輪子接觸地面是一條線，用 cylinder 不是 sphere。"""
    for leg in LEGS:
        gid = mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_GEOM, leg)
        assert model.geom_type[gid] == mujoco.mjtGeom.mjGEOM_CYLINDER, f"{leg} 應為 cylinder"
        assert float(model.geom_size[gid][0]) == pytest.approx(WHEEL_RADIUS, abs=1e-6)
        assert float(model.geom_size[gid][1]) == pytest.approx(0.0240, abs=1e-6)


def test_home_keyframe(model):
    assert model.nkey >= 1
    qpos = model.key_qpos[0]
    assert qpos[7:19] == pytest.approx([0.0, 1.05, -2.00] * 4, abs=1e-6)
    assert model.key_ctrl[0] == pytest.approx([0.0, 1.05, -2.00] * 4, abs=1e-6)


def test_home_pose_wheels_are_on_the_ground(model):
    """home keyframe 的機身高度要讓四顆輪剛好觸地（誤差 < 5 mm）。"""
    data = mujoco.MjData(model)
    mujoco.mj_resetDataKeyframe(model, data, 0)
    mujoco.mj_forward(model, data)
    wheel_z = [data.geom_xpos[mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_GEOM, leg)][2]
               for leg in LEGS]
    clearance = np.array(wheel_z) - WHEEL_RADIUS
    assert np.abs(clearance).max() < 5e-3, f"四輪離地/陷地量 {clearance}（應接近 0）"
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_model.py -v`
Expected: 全部 ERROR / FAIL — 找不到 `d1_edu_w.xml`

- [ ] **Step 3: 寫 MJCF**

Create `task6/model/d1_edu_w/d1_edu_w.xml`。慣量全部抄自 `ZSL-1W.urdf`，
`fullinertia` 順序為 `ixx iyy izz ixy ixz iyz`。

**四顆輪子刻意不建關節**（見 Global Constraints）：`{LEG}_wheel` 是沒有 joint 的子 body，
質量與慣量完整保留，碰撞用 `cylinder`（軸沿 y，故 `quat` 繞 x 轉 90°）。

```xml
<mujoco model="d1_edu_w">
  <compiler angle="radian" meshdir="meshes" autolimits="true"/>

  <option timestep="0.004" cone="pyramidal" impratio="100" iterations="1" ls_iterations="5">
    <flag eulerdamp="disable"/>
  </option>

  <default>
    <default class="d1w">
      <geom friction="0.6" margin="0.001" condim="1"/>
      <!-- armature / damping：URDF 未提供，見計畫 Global Constraints 的假設說明 -->
      <joint armature="0.01" damping="0.1"/>
      <general biastype="affine" gainprm="80 0 0" biasprm="0 -80 -1" forcerange="-28 28"/>

      <default class="abad">
        <joint axis="1 0 0" range="-0.4887 0.4887"/>
        <general ctrlrange="-0.4687 0.4687"/>
      </default>
      <default class="hip">
        <joint axis="0 1 0" range="-1.152 2.967"/>
        <general ctrlrange="-1.132 2.947"/>
      </default>
      <default class="knee">
        <joint axis="0 1 0" range="-2.723 -0.602"/>
        <general ctrlrange="-2.703 -0.622"/>
      </default>

      <default class="visual">
        <geom type="mesh" contype="0" conaffinity="0" group="2" rgba="0.75 0.75 0.75 1"/>
      </default>
      <default class="collision">
        <geom group="3"/>
        <default class="wheel">
          <geom type="cylinder" size="0.0710 0.0240" quat="0.70710678 0.70710678 0 0"
                priority="1" condim="6" solimp="0.015 1 0.031" friction="0.8 0.02 0.01"/>
        </default>
      </default>
    </default>
  </default>

  <asset>
    <mesh file="BASE_LINK.STL"/>
    <mesh file="FL_ABAD_LINK.STL"/> <mesh file="FL_HIP_LINK.STL"/>
    <mesh file="FL_KNEE_LINK.STL"/> <mesh file="FL_FOOT_LINK.STL"/>
    <mesh file="FR_ABAD_LINK.STL"/> <mesh file="FR_HIP_LINK.STL"/>
    <mesh file="FR_KNEE_LINK.STL"/> <mesh file="FR_FOOT_LINK.STL"/>
    <mesh file="RL_ABAD_LINK.STL"/> <mesh file="RL_HIP_LINK.STL"/>
    <mesh file="RL_KNEE_LINK.STL"/> <mesh file="RL_FOOT_LINK.STL"/>
    <mesh file="RR_ABAD_LINK.STL"/> <mesh file="RR_HIP_LINK.STL"/>
    <mesh file="RR_KNEE_LINK.STL"/> <mesh file="RR_FOOT_LINK.STL"/>
  </asset>

  <worldbody>
    <body name="base" pos="0 0 0.2948" childclass="d1w">
      <camera name="track" pos="0.85 -1.47 0.92" xyaxes="0.866 0.5 0 -0.171 0.296 0.94" mode="trackcom"/>
      <inertial pos="0.0050538027 -0.000631701 0.0050408732" mass="6.7155497"
        fullinertia="0.030172666 0.088387421 0.10431072 -0.00028335181 0.000084157847 -0.00056913307"/>
      <freejoint/>
      <geom mesh="BASE_LINK" class="visual"/>
      <geom type="box" size="0.2778 0.1114 0.0687" pos="0.00445 0 0.0217" class="collision"/>
      <site name="imu" pos="0 0 0"/>

      <!-- ================= FL ================= -->
      <body name="FL_abad" pos="0.17449 0.062 0">
        <inertial pos="-0.027074453 -0.015458828 0.0000094167931" mass="0.45409769"
          fullinertia="0.000251027 0.0004874808 0.00054102144 0.000153060 0.0000000321747 -0.0000000208886"/>
        <joint name="FL_abad_joint" class="abad"/>
        <geom mesh="FL_ABAD_LINK" class="visual"/>
        <body name="FL_hip" pos="0 0.097412 0">
          <inertial pos="-0.0018322311 -0.050798328 -0.01620581" mass="1.34038"
            fullinertia="0.0055878127 0.0036181265 0.0028642756 0.000140269 -0.000271226 0.0011403235"/>
          <joint name="FL_hip_joint" class="hip"/>
          <geom mesh="FL_HIP_LINK" class="visual"/>
          <geom type="capsule" fromto="0 0 -0.02 0 0 -0.19" size="0.030" class="collision"/>
          <body name="FL_knee" pos="0 0 -0.2">
            <inertial pos="0.00296222 -0.01016197 -0.18400768" mass="0.76510830"
              fullinertia="0.00433034 0.00438029 0.00042299 -0.00001722 -0.00011502 -0.00027115"/>
            <joint name="FL_knee_joint" class="knee"/>
            <geom mesh="FL_KNEE_LINK" class="visual"/>
            <geom type="capsule" fromto="0 0 -0.02 0 0 -0.20" size="0.022" class="collision"/>
            <!-- 輪子：URDF 的 FL_FOOT_JOINT 為連續旋轉，本設計刻意不建立關節（鎖輪當圓腳），
                 但質量 0.90130429 kg 與慣量完整保留 -->
            <body name="FL_wheel" pos="0 0 -0.21366">
              <inertial pos="0 0.04488028 0" mass="0.90130429"
                fullinertia="0.00142854 0.00247230 0.00142858 0 0 0"/>
              <geom mesh="FL_FOOT_LINK" class="visual"/>
              <geom name="FL" class="wheel" pos="0 0.04488028 0"/>
            </body>
          </body>
        </body>
      </body>

      <!-- ================= FR ================= -->
      <body name="FR_abad" pos="0.1745 -0.062 0">
        <inertial pos="-0.027074453 0.015458828 -0.0000094167931" mass="0.45409769"
          fullinertia="0.000251027 0.0004874808 0.00054102144 -0.000153060 -0.0000000321747 -0.0000000208886"/>
        <joint name="FR_abad_joint" class="abad"/>
        <geom mesh="FR_ABAD_LINK" class="visual"/>
        <body name="FR_hip" pos="0 -0.097399 0">
          <inertial pos="-0.0018322311 0.050798328 -0.01620581" mass="1.34038"
            fullinertia="0.0055878127 0.0036181265 0.0028642756 -0.000140269 -0.000271226 -0.0011403235"/>
          <joint name="FR_hip_joint" class="hip"/>
          <geom mesh="FR_HIP_LINK" class="visual"/>
          <geom type="capsule" fromto="0 0 -0.02 0 0 -0.19" size="0.030" class="collision"/>
          <body name="FR_knee" pos="0 0 -0.2">
            <inertial pos="0.00294408 0.01016197 -0.18399731" mass="0.76510852"
              fullinertia="0.00432973 0.00437978 0.00042308 0.00001787 -0.00011551 0.00027077"/>
            <joint name="FR_knee_joint" class="knee"/>
            <geom mesh="FR_KNEE_LINK" class="visual"/>
            <geom type="capsule" fromto="0 0 -0.02 0 0 -0.20" size="0.022" class="collision"/>
            <!-- 輪子：URDF 的 FR_FOOT_JOINT 為連續旋轉，本設計刻意不建立關節（鎖輪當圓腳），
                 但質量 0.90130429 kg 與慣量完整保留 -->
            <body name="FR_wheel" pos="0 0 -0.21366">
              <inertial pos="0 -0.04488028 0" mass="0.90130429"
                fullinertia="0.00142854 0.00247230 0.00142858 0 0 0"/>
              <geom mesh="FR_FOOT_LINK" class="visual"/>
              <geom name="FR" class="wheel" pos="0 -0.04488028 0"/>
            </body>
          </body>
        </body>
      </body>

      <!-- ================= RL ================= -->
      <body name="RL_abad" pos="-0.1745 0.062 0">
        <inertial pos="0.027074453 -0.015458828 -0.0000094167931" mass="0.45409769"
          fullinertia="0.000251027 0.0004874808 0.00054102144 -0.000153060 0.0000000321747 0.0000000208886"/>
        <joint name="RL_abad_joint" class="abad"/>
        <geom mesh="RL_ABAD_LINK" class="visual"/>
        <body name="RL_hip" pos="0 0.09735 0">
          <inertial pos="-0.0018322311 -0.050798328 -0.01620581" mass="1.34038"
            fullinertia="0.0055878127 0.0036181265 0.0028642756 0.000140269 -0.000271226 0.0011403235"/>
          <joint name="RL_hip_joint" class="hip"/>
          <geom mesh="RL_HIP_LINK" class="visual"/>
          <geom type="capsule" fromto="0 0 -0.02 0 0 -0.19" size="0.030" class="collision"/>
          <body name="RL_knee" pos="0 0 -0.2">
            <inertial pos="0.00296222 -0.01016197 -0.18400768" mass="0.76510830"
              fullinertia="0.00433034 0.00438029 0.00042299 -0.00001722 -0.00011502 -0.00027115"/>
            <joint name="RL_knee_joint" class="knee"/>
            <geom mesh="RL_KNEE_LINK" class="visual"/>
            <geom type="capsule" fromto="0 0 -0.02 0 0 -0.20" size="0.022" class="collision"/>
            <!-- 輪子：URDF 的 RL_FOOT_JOINT 為連續旋轉，本設計刻意不建立關節（鎖輪當圓腳），
                 但質量 0.90130429 kg 與慣量完整保留 -->
            <body name="RL_wheel" pos="0 0 -0.21366">
              <inertial pos="0 0.04488028 0" mass="0.90130429"
                fullinertia="0.00142854 0.00247230 0.00142858 0 0 0"/>
              <geom mesh="RL_FOOT_LINK" class="visual"/>
              <geom name="RL" class="wheel" pos="0 0.04488028 0"/>
            </body>
          </body>
        </body>
      </body>

      <!-- ================= RR ================= -->
      <body name="RR_abad" pos="-0.1745 -0.062 0">
        <inertial pos="0.027074453 0.015458828 0.0000094167931" mass="0.45409769"
          fullinertia="0.000251027 0.0004874808 0.00054102144 0.000153060 -0.0000000321747 0.0000000208886"/>
        <joint name="RR_abad_joint" class="abad"/>
        <geom mesh="RR_ABAD_LINK" class="visual"/>
        <body name="RR_hip" pos="0 -0.09735 0">
          <inertial pos="-0.0018322311 0.050798328 -0.01620581" mass="1.34038"
            fullinertia="0.0055878127 0.0036181265 0.0028642756 -0.000140269 -0.000271226 -0.0011403235"/>
          <joint name="RR_hip_joint" class="hip"/>
          <geom mesh="RR_HIP_LINK" class="visual"/>
          <geom type="capsule" fromto="0 0 -0.02 0 0 -0.19" size="0.030" class="collision"/>
          <body name="RR_knee" pos="0 0 -0.2">
            <inertial pos="0.00294408 0.01016197 -0.18399731" mass="0.76510852"
              fullinertia="0.00432973 0.00437978 0.00042308 0.00001787 -0.00011551 0.00027077"/>
            <joint name="RR_knee_joint" class="knee"/>
            <geom mesh="RR_KNEE_LINK" class="visual"/>
            <geom type="capsule" fromto="0 0 -0.02 0 0 -0.20" size="0.022" class="collision"/>
            <!-- 輪子：URDF 的 RR_FOOT_JOINT 為連續旋轉，本設計刻意不建立關節（鎖輪當圓腳），
                 但質量 0.90130429 kg 與慣量完整保留 -->
            <body name="RR_wheel" pos="0 0 -0.21366">
              <inertial pos="0 -0.04488028 0" mass="0.90130429"
                fullinertia="0.00142854 0.00247230 0.00142858 0 0 0"/>
              <geom mesh="RR_FOOT_LINK" class="visual"/>
              <geom name="RR" class="wheel" pos="0 -0.04488028 0"/>
            </body>
          </body>
        </body>
      </body>
    </body>
  </worldbody>

  <actuator>
    <general class="abad" name="FL_abad" joint="FL_abad_joint"/>
    <general class="hip"  name="FL_hip"  joint="FL_hip_joint"/>
    <general class="knee" name="FL_knee" joint="FL_knee_joint"/>
    <general class="abad" name="FR_abad" joint="FR_abad_joint"/>
    <general class="hip"  name="FR_hip"  joint="FR_hip_joint"/>
    <general class="knee" name="FR_knee" joint="FR_knee_joint"/>
    <general class="abad" name="RL_abad" joint="RL_abad_joint"/>
    <general class="hip"  name="RL_hip"  joint="RL_hip_joint"/>
    <general class="knee" name="RL_knee" joint="RL_knee_joint"/>
    <general class="abad" name="RR_abad" joint="RR_abad_joint"/>
    <general class="hip"  name="RR_hip"  joint="RR_hip_joint"/>
    <general class="knee" name="RR_knee" joint="RR_knee_joint"/>
  </actuator>

  <sensor>
    <gyro site="imu" name="imu_gyro"/>
    <accelerometer site="imu" name="imu_acc"/>
    <framequat objtype="site" objname="imu" name="imu_quat"/>
    <framepos objtype="site" objname="imu" name="odom_pos"/>
  </sensor>

  <keyframe>
    <key name="home"
         qpos="0 0 0.2948 1 0 0 0  0 1.05 -2.00  0 1.05 -2.00  0 1.05 -2.00  0 1.05 -2.00"
         ctrl="0 1.05 -2.00  0 1.05 -2.00  0 1.05 -2.00  0 1.05 -2.00"/>
  </keyframe>
</mujoco>
```

- [ ] **Step 4: 執行測試**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_model.py -v`
Expected: PASS

`test_home_pose_wheels_are_on_the_ground` 若失敗，**只准調整 keyframe 的機身 z**（第 3 個數字，目前 0.2948），
用測試印出的 `clearance` 直接加減；**不准動關節角、碰撞半徑或慣量**。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/model/d1_edu_w/d1_edu_w.xml task6/tests/test_model.py
git commit -m "feat(task6): D1 EDU 輪足版本體 MJCF + 與 URDF 對帳測試（關卡 1）"
```

---

## Task 3: 場景檔與 `d1_model.py`（關卡 2：站姿穩定）

**Files:**
- Create: `task6/model/d1_edu_w/scene.xml`
- Create: `task6/model/d1_edu_w/scene_mjx.xml`
- Create: `task6/inference/d1_model.py`
- Modify: `task6/tests/test_model.py`（附加站姿測試）

**Interfaces:**
- Consumes: `d1_edu_w.xml` 的名稱契約（Task 2）
- Produces:
  - `d1_model.LEGS: list[str]`
  - `d1_model.HOME3: np.ndarray (3,)`、`d1_model.HOME12: np.ndarray (12,)`
  - `d1_model.SCENE: str`、`d1_model.SCENE_MJX: str`
  - `d1_model.MU_MIN, MU_MAX, OMEGA_MIN, OMEGA_MAX, A_CONV, D_STEP, G_C, G_P, W_COUP, N_CPG_SUB: float/int`
  - `d1_model.CTRL_DT, SIM_DT: float`
  - `d1_model.OBS_DIM: int = 73`、`d1_model.ACT_DIM: int = 12`
  - `d1_model.TAU_CONTACT: float`（Task 5 決定最終值，本任務先設 8.0）
  - `d1_model.make_model(mjx: bool = False) -> mujoco.MjModel`

- [ ] **Step 1: 寫失敗的測試**

Append to `task6/tests/test_model.py`:

```python
# ---------- 關卡 2：站姿穩定 ----------
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "inference"))


def test_d1_model_constants_are_single_source_of_truth():
    import d1_model

    assert d1_model.LEGS == ["FL", "FR", "RL", "RR"]
    assert d1_model.HOME3 == pytest.approx([0.0, 1.05, -2.00])
    assert d1_model.HOME12 == pytest.approx([0.0, 1.05, -2.00] * 4)
    assert d1_model.OBS_DIM == 73
    assert d1_model.ACT_DIM == 12
    assert (d1_model.CTRL_DT, d1_model.SIM_DT) == (0.02, 0.004)


def test_make_model_both_variants_compile():
    import d1_model

    for mjx in (False, True):
        m = d1_model.make_model(mjx=mjx)
        assert m.nq == 19 and m.nu == 12
        assert mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_GEOM, "floor") >= 0


def test_home_pose_is_statically_stable():
    """關卡 2：站姿穩定。

    量測方式是「先沉降 1 秒，再量之後 1 秒的漂移」，而不是量總位移。
    原因：kp=80 的位置伺服在 20.56 kg 下必然有靜態撓度，機身會比 keyframe 的
    幾何高度低約 2.5 cm 後停在平衡點。那是物理，不是不穩定。
    真正要測的是「沉降後有沒有停住」。
    （實測基準：沉降 2.47 cm，之後 1 秒 z 峰對峰 1.36 mm，末速 -0.0004 m/s。）
    """
    import d1_model

    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    d.ctrl[:] = d1_model.HOME12
    mujoco.mj_forward(m, d)
    z_start = float(d.qpos[2])

    n1 = int(1.0 / m.opt.timestep)
    for _ in range(n1):
        mujoco.mj_step(m, d)
    z_settled = float(d.qpos[2])

    zs = []
    for _ in range(n1):
        mujoco.mj_step(m, d)
        zs.append(float(d.qpos[2]))
    zs = np.array(zs)

    sag = z_start - z_settled
    assert sag < 0.04, (
        f"沉降 {sag*100:.1f} cm 過大（>4 cm），機器人可能正在塌陷。"
        "kp=80/kd=1 為原廠值不得擅改；請回報數據給使用者決定。"
    )
    assert (zs.max() - zs.min()) < 0.003, (
        f"沉降後 1 秒內 z 峰對峰 {(zs.max()-zs.min())*1000:.2f} mm（應 < 3 mm），機身仍在抖動。"
    )
    assert float(d.qvel[2]) == pytest.approx(0.0, abs=0.02), "站定後垂直速度應趨近 0"


def test_home_pose_body_stays_level():
    """站定後機身不應傾倒（重力在機身系的 z 分量應接近 -1）。"""
    import d1_model

    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    d.ctrl[:] = d1_model.HOME12
    mujoco.mj_forward(m, d)
    for _ in range(int(2.0 / m.opt.timestep)):
        mujoco.mj_step(m, d)
    base = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, "base")
    grav_z_in_body = -d.xmat[base].reshape(3, 3)[2, 2]
    assert grav_z_in_body < -0.99, f"機身傾斜過大 (重力 z 分量 {grav_z_in_body:.4f})"
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_model.py -v -k "d1_model or make_model or statically"`
Expected: FAIL — `ModuleNotFoundError: No module named 'd1_model'`

- [ ] **Step 3: 寫兩個場景檔**

Create `task6/model/d1_edu_w/scene.xml`:

```xml
<mujoco model="d1_edu_w scene">
  <include file="d1_edu_w.xml"/>

  <statistic center="0 0 0.1" extent="0.8"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="-130" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>
  </worldbody>
</mujoco>
```

Create `task6/model/d1_edu_w/scene_mjx.xml`（內容與 `scene.xml` 相同 —— `d1_edu_w.xml` 的 `<option>`
已經是 MJX 參數，兩者差異保留給未來需要時分化。使用者已確認保留此重複，不要改成 include）:

```xml
<mujoco model="d1_edu_w scene (mjx)">
  <include file="d1_edu_w.xml"/>

  <statistic center="0 0 0.1" extent="0.8"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="-130" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"/>
  </worldbody>
</mujoco>
```

- [ ] **Step 4: 寫 `d1_model.py`**

Create `task6/inference/d1_model.py`:

```python
"""D1 EDU 輪足版（ZSL-1w）模型與常數的單一事實來源。

所有 task6 腳本與 Colab notebook 的常數都必須與本檔一致，否則訓練與推論會對不起來。
數值出處：
  - 幾何/質量/慣量/限位：zsibot/genisom_model 的 zsl-1w/urdf/ZSL-1W.urdf
  - kp/kd：AgibotTech/agibot_D1_Edu-Ultra 的 demo/zsl-1/python/examples/lowlevel_demo.py:68-73
  - CPG 常數：沿用 task4 論文標準版（cpg_rl_paper_colab.ipynb）
"""
from pathlib import Path

import mujoco
import numpy as np

_MODEL_DIR = Path(__file__).resolve().parents[1] / "model" / "d1_edu_w"
SCENE = str(_MODEL_DIR / "scene.xml")
SCENE_MJX = str(_MODEL_DIR / "scene_mjx.xml")

# ---- 機構 ----
LEGS = ["FL", "FR", "RL", "RR"]        # 見 task6/docs 對照表（官方文件腿序自相矛盾，尚未定案）
HOME3 = np.array([0.0, 1.05, -2.00])   # abad, hip, knee；knee 軸為 +y，故 hip 為正（與點足版相反）
HOME12 = np.tile(HOME3, 4)
WHEEL_RADIUS = 0.0710                  # 輪半徑(m)；輪子在模擬中熔接鎖死，當成圓腳

# ---- 控制 ----
CTRL_DT = 0.02      # 50 Hz，落在 SDK 建議的 20~50 Hz 內
SIM_DT = 0.004
KP, KD = 80.0, 1.0  # 原廠 demo 值（取自點足版 lowlevel_demo；輪足版無 LowLevel demo）不得擅改
TAU_MAX = 28.0      # URDF effort；官網 48 N·m 為峰值，取保守值

# ---- CPG（與 task4 論文標準版逐項相同）----
MU_MIN, MU_MAX = 1.0, 2.0
OMEGA_MIN, OMEGA_MAX = 0.0, 4.5
A_CONV = 50.0
D_STEP = 0.12      # 前後步幅尺度
D_STEP_Y = 0.09    # 側向擺幅尺度：本機 abad 行程僅 ±28°(Go2 ±60°)，沿用 0.12 會超限 14%
G_C = 0.08
G_P = 0.01
W_COUP = 8.0
N_CPG_SUB = 4
PHASE_OFFSET = np.array([0.0, np.pi, np.pi, 0.0])   # trot：FL, FR, RL, RR

# ---- 觀測 ----
OBS_DIM = 73        # 76 − 3（移除實機拿不到的機身線速度）
ACT_DIM = 12
TAU_CONTACT = 8.0   # 膝關節力矩觸地門檻(N·m)；Task 5 由開迴路力矩分佈定案


def make_model(mjx: bool = False) -> mujoco.MjModel:
    """建立 D1 EDU 模型。mjx=True 回傳訓練用場景，False 回傳本機渲染場景。"""
    return mujoco.MjModel.from_xml_path(SCENE_MJX if mjx else SCENE)


def foot_geom_ids(m: mujoco.MjModel) -> list[int]:
    """四顆輪子碰撞 geom 的 id，順序同 LEGS。

    輪子鎖死當圓腳，功能上等同足端，故沿用 foot_* 命名供 IK 與觸地判定引用。
    """
    return [mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_GEOM, leg) for leg in LEGS]


def knee_actuator_ids(m: mujoco.MjModel) -> list[int]:
    """四個膝致動器的 id，順序同 LEGS。用於力矩觸地判定。"""
    return [mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_ACTUATOR, f"{leg}_knee") for leg in LEGS]
```

- [ ] **Step 5: 執行測試**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_model.py -v`
Expected: PASS（全部）

若 `test_home_pose_is_statically_stable` 失敗：**停下來回報**機身 z 的起訖值與 `d.qvel[2]`，
連同「是否懷疑 armature/damping 假設」一起交給使用者決定，不得自行改 kp/kd。

- [ ] **Step 6: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/model/d1_edu_w/scene.xml task6/model/d1_edu_w/scene_mjx.xml \
        task6/inference/d1_model.py task6/tests/test_model.py
git commit -m "feat(task6): 場景檔 + d1_model 常數單一來源 + 站姿穩定測試（關卡 2）"
```

---

## Task 4: `cpg_d1.py` — CPG 動力學與 IK

**Files:**
- Create: `task6/inference/cpg_d1.py`
- Test: `task6/tests/test_cpg_d1.py`

**Interfaces:**
- Consumes: `d1_model` 的全部常數與 `make_model()`
- Produces:
  - `cpg_init() -> dict`（鍵 `rx, rx_d, ry, ry_d, theta`，每個 shape (4,)）
  - `cpg_step(c: dict, mux, muy, omega, dt: float) -> dict`
  - `act_to_cmd(a: np.ndarray) -> tuple[np.ndarray, np.ndarray, np.ndarray]`（各 shape (4,)）
  - `leg_ik_consts(m: mujoco.MjModel) -> tuple[np.ndarray, np.ndarray]`（shape (4,3) 與 (4,3,3)）
  - `joint_targets(c: dict, f0s, jinvs) -> np.ndarray`（shape (12,)）
  - `qinv(q)`, `qrot(q, v)`, `w2b(q, v)`

- [ ] **Step 1: 寫失敗的測試**

Create `task6/tests/test_cpg_d1.py`:

```python
"""CPG / IK 純函式測試。"""
import sys
from pathlib import Path

import numpy as np
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "inference"))

import cpg_d1
import d1_model


def test_cpg_init_shapes_and_trot_phase():
    c = cpg_d1.cpg_init()
    for k in ("rx", "rx_d", "ry", "ry_d", "theta"):
        assert c[k].shape == (4,), k
    # trot：FL 與 RR 同相、FR 與 RL 同相、兩組差 pi
    assert c["theta"] == pytest.approx([0.0, np.pi, np.pi, 0.0])


def test_cpg_amplitude_converges_to_mu():
    """rx 應收斂到指令 mu。"""
    c = cpg_d1.cpg_init()
    mu = np.full(4, 1.8)
    for _ in range(200):
        c = cpg_d1.cpg_step(c, mu, mu, np.zeros(4), d1_model.CTRL_DT)
    assert c["rx"] == pytest.approx(mu, abs=1e-3)
    assert c["ry"] == pytest.approx(mu, abs=1e-3)


def test_cpg_phase_advances_with_omega():
    """omega=1 Hz 跑 0.25 秒，相位應前進約 pi/2。"""
    c = cpg_d1.cpg_init()
    om = np.ones(4)
    for _ in range(int(0.25 / d1_model.CTRL_DT)):
        c = cpg_d1.cpg_step(c, np.full(4, 1.5), np.full(4, 1.5), om, d1_model.CTRL_DT)
    assert c["theta"][0] == pytest.approx(np.pi / 2, abs=0.15)


def test_act_to_cmd_saturates_into_declared_ranges():
    lo = cpg_d1.act_to_cmd(np.full(12, -50.0))
    hi = cpg_d1.act_to_cmd(np.full(12, +50.0))
    assert lo[0] == pytest.approx(np.full(4, d1_model.MU_MIN), abs=1e-6)
    assert hi[0] == pytest.approx(np.full(4, d1_model.MU_MAX), abs=1e-6)
    assert lo[2] == pytest.approx(np.full(4, d1_model.OMEGA_MIN), abs=1e-6)
    assert hi[2] == pytest.approx(np.full(4, d1_model.OMEGA_MAX), abs=1e-6)


def test_leg_ik_consts_home_wheel_is_below_hip():
    """home 姿態下輪心應在髖正下方約 0.224 m，並向外偏 0.142 m。

    實測基準：f0 = (0.0003, ±0.1423, -0.2238)，Jacobian 條件數 2.6。
    y 的 0.142 m 包含輪子相對小腿末端向外 4.5 cm 的安裝偏移，抄漏會讓步幅算錯。
    """
    m = d1_model.make_model()
    f0s, jinvs = cpg_d1.leg_ik_consts(m)
    assert f0s.shape == (4, 3)
    assert jinvs.shape == (4, 3, 3)
    for k in range(4):
        assert abs(f0s[k][0]) < 0.02, f"腿 {k} 的輪心未在髖正下方 (x={f0s[k][0]:.3f})"
        assert -0.24 < f0s[k][2] < -0.20, f"腿 {k} 髖到輪心距離異常 (z={f0s[k][2]:.3f})"
        assert 0.13 < abs(f0s[k][1]) < 0.155, f"腿 {k} 輪子橫向偏移異常 (y={f0s[k][1]:.3f})"
    left = [f0s[0][1], f0s[2][1]]
    right = [f0s[1][1], f0s[3][1]]
    assert all(v > 0 for v in left) and all(v < 0 for v in right), "左右腿的 y 偏移方向反了"


def test_ik_jacobian_moves_foot_in_requested_direction():
    """把輪心往 +x 推 2 cm，正向運動學算回來的位移應吻合（實測誤差 0.89 mm）。"""
    import mujoco

    m = d1_model.make_model()
    d = mujoco.MjData(m)
    f0s, jinvs = cpg_d1.leg_ik_consts(m)
    want = np.array([0.02, 0.0, 0.0])
    q3 = d1_model.HOME3 + jinvs[0] @ want

    mujoco.mj_resetDataKeyframe(m, d, 0)
    d.qpos[7:10] = q3
    mujoco.mj_forward(m, d)
    gid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_GEOM, "FL")
    hid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, "FL_abad")
    got = d.geom_xpos[gid] - d.xpos[hid] - f0s[0]
    assert got == pytest.approx(want, abs=3e-3)


def test_joint_targets_returns_12_and_stays_in_limits():
    import mujoco

    m = d1_model.make_model()
    f0s, jinvs = cpg_d1.leg_ik_consts(m)
    c = cpg_d1.cpg_init()
    om = np.full(4, 2.0)
    for _ in range(100):
        c = cpg_d1.cpg_step(c, np.full(4, 2.0), np.full(4, 2.0), om, d1_model.CTRL_DT)
        q = cpg_d1.joint_targets(c, f0s, jinvs)
        assert q.shape == (12,)
        assert np.all(q >= m.jnt_range[1:, 0] - 1e-6), "關節目標角低於下限"
        assert np.all(q <= m.jnt_range[1:, 1] + 1e-6), "關節目標角超過上限"
        # 還要在致動器 ctrlrange 內才不會被 clip（abad 的 ctrlrange 比 jnt_range 更緊）
        assert np.all(q >= m.actuator_ctrlrange[:, 0] - 1e-6), "關節目標角低於 ctrlrange"
        assert np.all(q <= m.actuator_ctrlrange[:, 1] + 1e-6), "關節目標角超過 ctrlrange"


def test_w2b_rotates_gravity_into_body_frame():
    """機身繞 y 轉 90 度後，世界的 -z 重力在機身系應變成 +x 方向。"""
    q = np.array([np.cos(np.pi / 4), 0.0, np.sin(np.pi / 4), 0.0])
    got = cpg_d1.w2b(q, np.array([0.0, 0.0, -1.0]))
    assert got == pytest.approx([1.0, 0.0, 0.0], abs=1e-9)
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_cpg_d1.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'cpg_d1'`

- [ ] **Step 3: 實作**

Create `task6/inference/cpg_d1.py`:

```python
"""D1 EDU 的 CPG 動力學、動作解碼與腿部 IK。純函式，不做任何 I/O。

與 task4 論文標準版逐行對應；差別只在 HOME3 與模型名稱由 d1_model 提供。
IK 用「home 姿態的數值 Jacobian 求逆」，對關節正負號自動免疫，
因此 D1 的 knee 軸為 -y、hip 正負號與 Go2 相反都不需要特別處理。
"""
import mujoco
import numpy as np

from d1_model import (A_CONV, D_STEP, D_STEP_Y, G_C, G_P, HOME3, LEGS, MU_MAX,
                      MU_MIN, N_CPG_SUB, OMEGA_MAX, OMEGA_MIN, PHASE_OFFSET, W_COUP)

PHI = PHASE_OFFSET[None, :] - PHASE_OFFSET[:, None]   # (4,4) 目標相位差


def cpg_init() -> dict:
    return {"rx": np.full(4, 1.5), "rx_d": np.zeros(4),
            "ry": np.full(4, 1.5), "ry_d": np.zeros(4),
            "theta": PHASE_OFFSET.copy()}


def cpg_step(c: dict, mux, muy, omega, dt: float) -> dict:
    rx, rxd, ry, ryd, th = (c["rx"].copy(), c["rx_d"].copy(),
                            c["ry"].copy(), c["ry_d"].copy(), c["theta"].copy())
    h = dt / N_CPG_SUB
    for _ in range(N_CPG_SUB):
        rxd += (A_CONV * (A_CONV / 4 * (mux - rx) - rxd)) * h
        rx += rxd * h
        ryd += (A_CONV * (A_CONV / 4 * (muy - ry) - ryd)) * h
        ry += ryd * h
        rbar = 0.5 * (rx + ry)
        diff = th[None, :] - th[:, None] - PHI
        th = th + (2 * np.pi * omega + W_COUP * np.sum(rbar[None, :] * np.sin(diff), 1)) * h
    return {"rx": rx, "rx_d": rxd, "ry": ry, "ry_d": ryd, "theta": th % (2 * np.pi)}


def act_to_cmd(a: np.ndarray):
    """12 維動作 → 每腿 (mux, muy, omega)。"""
    a = np.tanh(a).reshape(4, 3)
    mux = (a[:, 0] + 1) / 2 * (MU_MAX - MU_MIN) + MU_MIN
    muy = (a[:, 1] + 1) / 2 * (MU_MAX - MU_MIN) + MU_MIN
    om = (a[:, 2] + 1) / 2 * (OMEGA_MAX - OMEGA_MIN) + OMEGA_MIN
    return mux, muy, om


def leg_ik_consts(m: mujoco.MjModel):
    """在 home 姿態對每腿求 3x3 數值 Jacobian（腳掌相對髖 對 三個關節角）。

    回傳 (f0s, jinvs)：f0s[k] 是第 k 腿 home 時腳掌相對髖的位置 (3,)，
    jinvs[k] 是 Jacobian 的逆 (3,3)。
    """
    d = mujoco.MjData(m)
    f0s, jinvs = [], []
    for k, leg in enumerate(LEGS):
        jb = 7 + 3 * k
        gid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_GEOM, leg)
        hip = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, f"{leg}_abad")

        def foot(q3):
            mujoco.mj_resetDataKeyframe(m, d, 0)
            d.qpos[jb:jb + 3] = q3
            mujoco.mj_forward(m, d)
            return (d.geom_xpos[gid] - d.xpos[hip]).copy()

        f0 = foot(HOME3)
        e = 1e-3
        J = np.zeros((3, 3))
        for j in range(3):
            dq = np.zeros(3)
            dq[j] = e
            J[:, j] = (foot(HOME3 + dq) - foot(HOME3 - dq)) / (2 * e)
        f0s.append(f0)
        jinvs.append(np.linalg.inv(J))
    return np.array(f0s), np.array(jinvs)


def joint_targets(c: dict, f0s, jinvs) -> np.ndarray:
    """CPG 狀態 → 12 個關節目標角。"""
    th = c["theta"]
    fx = 2 * (c["rx"] - MU_MIN) / (MU_MAX - MU_MIN) - 1
    fy = 2 * (c["ry"] - MU_MIN) / (MU_MAX - MU_MIN) - 1
    dx = -D_STEP * fx * np.cos(th)
    dy = D_STEP_Y * fy * np.cos(th)   # 側向用較小的尺度，見 d1_model.D_STEP_Y
    dz = np.where(np.sin(th) > 0, G_C * np.sin(th), G_P * np.sin(th))
    off = np.stack([dx, dy, dz], -1)
    q = np.zeros((4, 3))
    for k in range(4):
        q[k] = HOME3 + jinvs[k] @ off[k]
    return q.reshape(12)


def qinv(q):
    return np.array([q[0], -q[1], -q[2], -q[3]])


def qrot(q, v):
    u = q[1:4]
    t = 2 * np.cross(u, v)
    return v + q[0] * t + np.cross(u, t)


def w2b(q, v):
    """世界向量轉到機身座標系。"""
    return qrot(qinv(q), v)
```

- [ ] **Step 4: 執行測試**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_cpg_d1.py -v`
Expected: PASS（8 passed）

若 `test_joint_targets_returns_12_and_stays_in_limits` 失敗，代表 `D_STEP`/`G_C` 對 D1 的腿長過大，
**回報實際超限的關節與數值**，不要直接改常數。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/inference/cpg_d1.py task6/tests/test_cpg_d1.py
git commit -m "feat(task6): CPG 動力學 + 數值 Jacobian IK + 單元測試"
```

---

## Task 5: 開迴路 CPG 驗證（關卡 3）與 `TAU_CONTACT` 定值

**Files:**
- Create: `task6/inference/cpg_openloop_d1.py`
- Modify: `task6/inference/d1_model.py:TAU_CONTACT`（依實測結果改值）

**Interfaces:**
- Consumes: `cpg_d1.*`、`d1_model.*`
- Produces: `cpg_openloop_d1.rollout(secs, omega, video) -> dict`
  （鍵：`dist`, `fell`, `foot_lift`, `final_z`, `tau_swing_p95`, `tau_stance_p05`）

這是整個計畫最重要的早期關卡：**不花任何 GPU 時數**就能一次驗證 MJCF、home 姿態、
IK Jacobian、位置伺服增益、CPG 常數五件事。

- [ ] **Step 1: 寫腳本**

Create `task6/inference/cpg_openloop_d1.py`:

```python
"""關卡 3：開迴路 CPG rollout（不含 RL），驗證模型/IK/PD/CPG 常數是否正確。

用法：
  conda run --no-capture-output -n rbtdog python task6/inference/cpg_openloop_d1.py --secs 8 --video

通過標準：前進 > 1 m、抬腳 0.03~0.06 m、不跌倒。
順帶輸出擺動相/站立相的膝力矩分佈，用來決定 d1_model.TAU_CONTACT。
"""
import argparse
import os
import sys
from pathlib import Path

import numpy as np

sys.path.insert(0, str(Path(__file__).resolve().parent))

import mujoco

import cpg_d1
import d1_model

OUT_DIR = Path(__file__).resolve().parents[1] / "outputs"


def rollout(secs: float = 8.0, omega: float = 2.0, mu: float = 1.8,
            video: bool = False) -> dict:
    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    mujoco.mj_forward(m, d)

    f0s, jinvs = cpg_d1.leg_ik_consts(m)
    foot_gid = d1_model.foot_geom_ids(m)
    knee_aid = d1_model.knee_actuator_ids(m)
    n_sub = int(round(d1_model.CTRL_DT / m.opt.timestep))

    def apply(q_des):
        d.ctrl[:] = q_des                      # 位置伺服；PD 在 MJCF 內，無軟體 PD
        for _ in range(n_sub):
            mujoco.mj_step(m, d)

    for _ in range(int(0.5 / d1_model.CTRL_DT)):   # 先站穩 0.5 秒
        apply(d1_model.HOME12)

    ren = cam = None
    frames = []
    if video:
        os.environ.setdefault("MUJOCO_GL", "egl")
        ren = mujoco.Renderer(m, 480, 640)
        cam = mujoco.MjvCamera()
        mujoco.mjv_defaultFreeCamera(m, cam)

    c = cpg_d1.cpg_init()
    x0 = float(d.qpos[0])
    fz_min = fz_max = float(d.geom_xpos[foot_gid[0]][2])
    fell = None
    tau_swing, tau_stance = [], []

    for i in range(int(secs / d1_model.CTRL_DT)):
        c = cpg_d1.cpg_step(c, np.full(4, mu), np.full(4, mu),
                            np.full(4, omega), d1_model.CTRL_DT)
        apply(cpg_d1.joint_targets(c, f0s, jinvs))

        # 以 CPG 相位當作觸地的真值標籤：sin(theta) > 0 為擺動相
        swing = np.sin(c["theta"]) > 0
        tau = np.abs(d.actuator_force[knee_aid])
        tau_swing.extend(tau[swing].tolist())
        tau_stance.extend(tau[~swing].tolist())

        grav = cpg_d1.w2b(d.qpos[3:7], np.array([0.0, 0.0, -1.0]))
        if grav[2] > -0.4 and fell is None:
            fell = i * d1_model.CTRL_DT
        fz = float(d.geom_xpos[foot_gid[0]][2])
        fz_min, fz_max = min(fz_min, fz), max(fz_max, fz)

        if ren is not None and i % 2 == 0:
            cam.lookat[:] = [d.qpos[0], d.qpos[1], 0.3]
            cam.distance, cam.elevation, cam.azimuth = 2.5, -20, 90
            ren.update_scene(d, cam)
            frames.append(ren.render())

    res = {
        "dist": float(d.qpos[0]) - x0,
        "fell": fell,
        "foot_lift": fz_max - fz_min,
        "final_z": float(d.qpos[2]),
        "tau_swing_p95": float(np.percentile(tau_swing, 95)) if tau_swing else 0.0,
        "tau_stance_p05": float(np.percentile(tau_stance, 5)) if tau_stance else 0.0,
    }

    print(f"[開迴路] 前進={res['dist']:+.2f} m  "
          f"跌倒={'是 @%.1fs' % fell if fell else '否'}  "
          f"FL 抬腳={res['foot_lift']:.3f} m  末端機身高={res['final_z']:.3f} m")
    print(f"[力矩] 擺動相 |tau_knee| p95={res['tau_swing_p95']:.2f} N·m   "
          f"站立相 |tau_knee| p05={res['tau_stance_p05']:.2f} N·m")
    print(f"[建議] TAU_CONTACT = {(res['tau_swing_p95'] + res['tau_stance_p05']) / 2:.1f}  "
          f"（兩者中點；若 p95 >= p05 代表兩相未分離，需回報）")

    if frames:
        import imageio.v2 as iio
        OUT_DIR.mkdir(parents=True, exist_ok=True)
        out = OUT_DIR / "openloop_cpg.mp4"
        iio.mimsave(str(out), frames, fps=25, codec="libx264")
        print("[影片]", out)
    return res


if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--secs", type=float, default=8.0)
    ap.add_argument("--omega", type=float, default=2.0)
    ap.add_argument("--mu", type=float, default=1.8)
    ap.add_argument("--video", action="store_true")
    rollout(**vars(ap.parse_args()))
```

- [ ] **Step 2: 執行關卡 3**

Run:
```bash
cd /home/huang/rbtdog_sim
conda run --no-capture-output -n rbtdog python task6/inference/cpg_openloop_d1.py --secs 8 --video
```

Expected：印出四項指標與力矩分佈，並產生 `task6/outputs/openloop_cpg.mp4`。

**通過標準**：`前進 > 1.00 m`、`跌倒=否`、`FL 抬腳` 落在 0.03~0.06 m。

**未通過就停下來回報**，附上實際數值與影片；候選原因依序為
**(a) 鎖死輪子的接觸摩擦（線接觸易打滑，這是輪足版最可能的原因）**
(b) `D_STEP`/`G_C` 對這台的腿長不合 (c) home 機身高度 (d) armature/damping 假設。
不得自行連續調參後宣稱通過。

- [ ] **Step 3: 用實測結果定 `TAU_CONTACT`**

把 Step 2 印出的「建議 TAU_CONTACT」數值填進 `task6/inference/d1_model.py`：

```python
TAU_CONTACT = 8.0   # 膝關節力矩觸地門檻(N·m)；Task 5 由開迴路力矩分佈定案
```
改成（`<實測值>` 換成實際數字，並把註解寫成實際的 p95 / p05）：
```python
TAU_CONTACT = <實測值>  # 膝力矩觸地門檻(N·m)：擺動相 p95=<..> / 站立相 p05=<..> 的中點
```

若 `擺動相 p95 >= 站立相 p05`（兩相力矩沒分離），**停下來回報**——
代表力矩無法用來判觸地，需與使用者重新決定是否改回幾何判定或砍掉這 4 維。

- [ ] **Step 4: 加一個回歸測試鎖住結果**

Append to `task6/tests/test_cpg_d1.py`:

```python
def test_openloop_walks_forward_without_falling():
    """關卡 3 的回歸測試（較慢，約 10 秒）。"""
    import cpg_openloop_d1

    res = cpg_openloop_d1.rollout(secs=6.0, video=False)
    assert res["fell"] is None, f"開迴路 CPG 跌倒於 {res['fell']} s"
    assert res["dist"] > 0.8, f"6 秒只前進 {res['dist']:.2f} m"
    assert 0.02 < res["foot_lift"] < 0.08, f"抬腳量 {res['foot_lift']:.3f} m 不合理"
    assert res["tau_swing_p95"] < res["tau_stance_p05"], "擺動相與站立相的膝力矩未分離"
```

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_cpg_d1.py -v`
Expected: PASS（9 passed）

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/inference/cpg_openloop_d1.py task6/inference/d1_model.py \
        task6/tests/test_cpg_d1.py task6/outputs/openloop_cpg.mp4
git commit -m "feat(task6): 開迴路 CPG 驗證通過（關卡 3）+ 由實測力矩定 TAU_CONTACT"
```

---

## Task 6: `obs_d1.py` — 73 維觀測

**Files:**
- Create: `task6/inference/obs_d1.py`
- Test: `task6/tests/test_obs_d1.py`

**Interfaces:**
- Consumes: `d1_model.*`、`cpg_d1.w2b`
- Produces:
  - `foot_contact(actuator_force: np.ndarray, knee_aid: list[int]) -> np.ndarray`（shape (4,) float32，0/1）
  - `build_obs(d, c, cmd, last_a, knee_aid) -> np.ndarray`（shape (73,) float32）
  - `OBS_LAYOUT: list[tuple[str, int]]`（欄位名稱與長度，供測試與文件使用）

- [ ] **Step 1: 寫失敗的測試**

Create `task6/tests/test_obs_d1.py`:

```python
"""73 維 observation 測試。維度與欄位順序一旦改變，訓練好的權重就失效。"""
import sys
from pathlib import Path

import mujoco
import numpy as np
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "inference"))

import cpg_d1
import d1_model
import obs_d1


def test_layout_sums_to_73_and_has_no_base_linear_velocity():
    total = sum(n for _, n in obs_d1.OBS_LAYOUT)
    assert total == d1_model.OBS_DIM == 73
    names = [k for k, _ in obs_d1.OBS_LAYOUT]
    assert "base_linvel" not in names, "機身線速度實機拿不到，不得放進 obs"
    assert names == ["gravity", "gyro", "joint_pos", "joint_vel",
                     "cmd", "last_action", "foot_contact", "cpg"]


def test_build_obs_shape_and_dtype():
    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    mujoco.mj_forward(m, d)
    knee_aid = d1_model.knee_actuator_ids(m)
    obs = obs_d1.build_obs(d, cpg_d1.cpg_init(),
                           np.array([0.6, 0.0, 0.0], np.float32),
                           np.zeros(12), knee_aid)
    assert obs.shape == (73,)
    assert obs.dtype == np.float32
    assert np.all(np.isfinite(obs))


def test_joint_pos_block_is_deviation_from_home():
    """home 姿態下關節角區塊應全為 0（存的是相對 HOME12 的偏差）。"""
    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    mujoco.mj_forward(m, d)
    knee_aid = d1_model.knee_actuator_ids(m)
    obs = obs_d1.build_obs(d, cpg_d1.cpg_init(),
                           np.zeros(3, np.float32), np.zeros(12), knee_aid)
    assert obs[6:18] == pytest.approx(np.zeros(12), abs=1e-9)


def test_gravity_block_points_down_when_upright():
    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    mujoco.mj_forward(m, d)
    knee_aid = d1_model.knee_actuator_ids(m)
    obs = obs_d1.build_obs(d, cpg_d1.cpg_init(),
                           np.zeros(3, np.float32), np.zeros(12), knee_aid)
    assert obs[0:3] == pytest.approx([0.0, 0.0, -1.0], abs=1e-6)


def test_foot_contact_uses_absolute_torque_threshold():
    knee_aid = [2, 5, 8, 11]
    force = np.zeros(12)
    force[2] = d1_model.TAU_CONTACT + 1.0      # 超過門檻 → 觸地
    force[5] = -(d1_model.TAU_CONTACT + 1.0)   # 負號同樣算觸地（取絕對值）
    force[8] = d1_model.TAU_CONTACT - 1.0      # 未達門檻 → 未觸地
    force[11] = 0.0
    got = obs_d1.foot_contact(force, knee_aid)
    assert got == pytest.approx([1.0, 1.0, 0.0, 0.0])
    assert got.dtype == np.float32
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_obs_d1.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'obs_d1'`

- [ ] **Step 3: 實作**

Create `task6/inference/obs_d1.py`:

```python
"""D1 EDU 的 73 維 observation。

設計約束：每一個欄位都必須是實機 LowLevel SDK 拿得到的量。
（官方 FAQ：Highlevel 與 Lowlevel 不可同時使用，所以只能用 LowLevel 的 getter。）

| 欄位 | 維度 | 實機來源 |
|---|---|---|
| gravity      | 3  | getQuaternion() → 轉機身系 |
| gyro         | 3  | getBodyGyro() |
| joint_pos    | 12 | motorState.q_*  減 HOME12 |
| joint_vel    | 12 | motorState.qd_* |
| cmd          | 3  | 自產 |
| last_action  | 12 | 自存 |
| foot_contact | 4  | |motorState.tau_knee_fb| > TAU_CONTACT |
| cpg          | 24 | 自算 |

刻意不含機身線速度：LowLevel 沒有這個量（getBodyVelocity 只存在於 HighLevel）。
reward 仍可使用模擬真值速度，因為 reward 只在模擬計算。
"""
import numpy as np

from cpg_d1 import w2b
from d1_model import HOME12, TAU_CONTACT

OBS_LAYOUT = [
    ("gravity", 3),
    ("gyro", 3),
    ("joint_pos", 12),
    ("joint_vel", 12),
    ("cmd", 3),
    ("last_action", 12),
    ("foot_contact", 4),
    ("cpg", 24),
]

_DOWN = np.array([0.0, 0.0, -1.0])


def foot_contact(actuator_force: np.ndarray, knee_aid) -> np.ndarray:
    """膝關節力矩門檻判觸地。

    取絕對值是因為 D1 的 knee 軸為 -y，站立相力矩符號與 Go2 相反；
    用絕對值可免除符號約定爭議，且模擬（actuator_force）與實機（tau_knee_fb）公式一致。
    """
    tau = np.abs(np.asarray(actuator_force)[knee_aid])
    return (tau > TAU_CONTACT).astype(np.float32)


def build_obs(d, c: dict, cmd: np.ndarray, last_a: np.ndarray, knee_aid) -> np.ndarray:
    """組 73 維 observation。d 為 mujoco.MjData（或具備同名欄位的物件）。"""
    quat = d.qpos[3:7]
    return np.concatenate([
        w2b(quat, _DOWN),            # gravity 3
        d.qvel[3:6],                 # gyro 3
        d.qpos[7:19] - HOME12,       # joint_pos 12
        d.qvel[6:18],                # joint_vel 12
        cmd,                         # cmd 3
        last_a,                      # last_action 12
        foot_contact(d.actuator_force, knee_aid),   # foot_contact 4
        c["rx"], c["rx_d"], c["ry"], c["ry_d"],     # cpg 16
        np.sin(c["theta"]), np.cos(c["theta"]),     # cpg 8
    ]).astype(np.float32)
```

- [ ] **Step 4: 執行測試**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests/test_obs_d1.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/inference/obs_d1.py task6/tests/test_obs_d1.py
git commit -m "feat(task6): 73 維 observation（移除機身線速度、觸地改力矩判定）"
```

---

## Task 7: Colab 訓練 notebook

**Files:**
- Create: `task6/notebooks/cpg_rl_d1w_colab.ipynb`

**Interfaces:**
- Consumes: `task6/model/d1_edu_w/*`（notebook 內用 `git clone` 取本專案或手動上傳）
- Produces: `cpg_rl_d1w_params.pkl`（Brax PPO params）

notebook 的常數必須與 `d1_model.py` 逐項相同；notebook 是自足的（Colab 不 import 本地模組），
因此常數是**刻意的重複**，Task 9 的文件要註明「改一邊就要改另一邊」。

- [ ] **Step 1: 建立 notebook 骨架與環境格**

Create `task6/notebooks/cpg_rl_d1w_colab.ipynb`，Cell 1（markdown）:

```markdown
# CPG-RL 訓練：智元 D1 EDU **輪足版（ZSL-1w）** · MJX · Colab GPU

移植自 task4 的 Go2 論文標準版。四顆輪子在模擬中熔接鎖死，當成 71 mm 圓腳走路，
因此動作空間仍是 12 維。與 Go2 版的差異：

| 項目 | Go2（task4）| D1 輪足（本檔）|
|---|---|---|
| home 關節角 | `[0, 0.9, -1.8]` | `[0, 1.05, -2.00]` |
| PD | `apply_pd()` 覆寫 90/3 | XML 內建 kp=80/kd=1（原廠 demo 值），**無 apply_pd** |
| 力矩上限 | 23.7 / knee 45.43 | 28 全關節 |
| 負重 DR | 0~8 kg | 0~5 kg（官方額定 payload）|
| 總質量 | 15 kg | **20.56 kg**（四顆輪各 0.9 kg）|
| obs | 76 維 | **73 維**（移除實機拿不到的機身線速度）|
| 觸地判定 | 腳掌世界高度 < 3 cm | **膝關節力矩 > TAU_CONTACT** |
| IMU DR | 無 | 重力/角速度加雜訊與偏差 |

執行階段 → 變更類型 → GPU。先跑 Smoke test 再開訓練。
```

Cell 2（code）:

```python
import os
os.environ["MUJOCO_GL"] = "egl"
!pip install -q mujoco mujoco-mjx brax mediapy
print("done")
```

Cell 3（code）:

```python
import jax
print("JAX", jax.__version__, "devices:", jax.devices())   # 要看到 cuda
```

Cell 4（code）— 取得模型檔：

```python
import os, subprocess
REPO = "https://github.com/<你的-remote>/rbtdog_sim.git"   # 或改用左側檔案面板手動上傳 task6/model/
if not os.path.exists("rbtdog_sim"):
    subprocess.run(["git", "clone", "--depth", "1", REPO], check=True)
SCENE = "rbtdog_sim/task6/model/d1_edu_w/scene_mjx.xml"
print("model exists:", os.path.exists(SCENE))
```

- [ ] **Step 2: 常數格與 CPG（JAX 版）**

Cell 5（code）:

```python
import jax.numpy as jnp
import numpy as np

MU_MIN, MU_MAX = 1.0, 2.0
OMEGA_MIN, OMEGA_MAX = 0.0, 4.5
A_CONV = 50.0
D_STEP, D_STEP_Y = 0.12, 0.09    # 前後 / 側向；側向較小是因 abad 行程僅 ±28°
G_C, G_P = 0.08, 0.01
NOMINAL_HEIGHT = 0.2695          # 實際站定高度（keyframe 的 0.2948 是純運動學值）
W_COUP = 8.0
N_CPG_SUB = 4
CTRL_DT, SIM_DT = 0.02, 0.004
KP_NOM, KD_NOM = 80.0, 1.0          # 原廠 demo 值
TAU_MAX = 28.0
TAU_CONTACT = 8.0                   # ← 用 d1_model.py 的實測定案值覆寫
OBS_DIM, ACT_DIM = 73, 12
HOME3_np = np.array([0.0, 1.05, -2.00])
HOME12 = jnp.array(list(HOME3_np) * 4)
LEGS = ["FL", "FR", "RL", "RR"]
KNEE_ACT_IDX = [2, 5, 8, 11]

PHASE_OFFSET = jnp.array([0.0, jnp.pi, jnp.pi, 0.0])
PHI = PHASE_OFFSET[None, :] - PHASE_OFFSET[:, None]


def cpg_init():
    return {"rx": jnp.full(4, 1.5), "rx_d": jnp.zeros(4),
            "ry": jnp.full(4, 1.5), "ry_d": jnp.zeros(4),
            "theta": PHASE_OFFSET}


def cpg_step(c, mux, muy, omega, dt):
    rx, rxd, ry, ryd, th = c["rx"], c["rx_d"], c["ry"], c["ry_d"], c["theta"]
    h = dt / N_CPG_SUB
    for _ in range(N_CPG_SUB):
        rxd = rxd + A_CONV * (A_CONV / 4.0 * (mux - rx) - rxd) * h
        rx = rx + rxd * h
        ryd = ryd + A_CONV * (A_CONV / 4.0 * (muy - ry) - ryd) * h
        ry = ry + ryd * h
        rbar = 0.5 * (rx + ry)
        diff = th[None, :] - th[:, None] - PHI
        coup = jnp.sum(rbar[None, :] * jnp.sin(diff), axis=1)
        th = th + (2.0 * jnp.pi * omega + W_COUP * coup) * h
    return {"rx": rx, "rx_d": rxd, "ry": ry, "ry_d": ryd,
            "theta": jnp.mod(th, 2.0 * jnp.pi)}


def action_to_cpg_cmd(action):
    a = jnp.tanh(action).reshape(4, 3)
    mux = (a[:, 0] + 1) / 2 * (MU_MAX - MU_MIN) + MU_MIN
    muy = (a[:, 1] + 1) / 2 * (MU_MAX - MU_MIN) + MU_MIN
    om = (a[:, 2] + 1) / 2 * (OMEGA_MAX - OMEGA_MIN) + OMEGA_MIN
    return mux, muy, om
```

- [ ] **Step 3: IK 常數格**

Cell 6（code）:

```python
import mujoco

def leg_ik_consts(xml):
    m = mujoco.MjModel.from_xml_path(xml); d = mujoco.MjData(m)
    f0s, jinvs = [], []
    for k, leg in enumerate(LEGS):
        jb = 7 + 3 * k
        gid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_GEOM, leg)
        hip = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, leg + "_abad")
        def foot(q3):
            mujoco.mj_resetDataKeyframe(m, d, 0)
            d.qpos[jb:jb + 3] = q3; mujoco.mj_forward(m, d)
            return (d.geom_xpos[gid] - d.xpos[hip]).copy()
        f0 = foot(HOME3_np); e = 1e-3; J = np.zeros((3, 3))
        for j in range(3):
            dq = np.zeros(3); dq[j] = e
            J[:, j] = (foot(HOME3_np + dq) - foot(HOME3_np - dq)) / (2 * e)
        f0s.append(f0); jinvs.append(np.linalg.inv(J))
    return np.array(f0s, np.float32), np.array(jinvs, np.float32)

F0S_np, JINVS_np = leg_ik_consts(SCENE)
print("f0 每腿(x,y,z 相對髖):\n", np.round(F0S_np, 3))
```

- [ ] **Step 4: MJX 環境格**

Cell 7（code）—— reward 逐項沿用 task4 論文版；**不新增也不刪減 reward 項**：

```python
import functools
from brax.envs.base import Env, State
from mujoco import mjx

N_FRAMES = int(round(CTRL_DT / SIM_DT))
PUSH_EVERY = 100          # 每 100 控制步(=2s) 注入一次速度擾動
PUSH_VEL = 0.6
GRAV_NOISE = 0.02         # 重力向量雜訊 sigma（IMU 為原始資料、精度一般）
GYRO_NOISE = 0.10         # 角速度雜訊 sigma (rad/s)
GYRO_BIAS = 0.05          # 每 episode 取樣一次的角速度偏差上限 (rad/s)


def _qinv(q): return jnp.array([q[0], -q[1], -q[2], -q[3]])
def _qrot(q, v):
    u = q[1:4]; t = 2.0 * jnp.cross(u, v); return v + q[0] * t + jnp.cross(u, t)
def w2b(quat, v): return _qrot(_qinv(quat), v)


class D1wCpgEnv(Env):
    def __init__(self, f0s, jinvs):
        m = mujoco.MjModel.from_xml_path(SCENE)
        m.opt.timestep = SIM_DT
        self._mj = m
        self.sys = mjx.put_model(m)
        self._init_q = jnp.array(m.key_qpos[0])
        self._lo = jnp.array(m.actuator_ctrlrange[:, 0])
        self._hi = jnp.array(m.actuator_ctrlrange[:, 1])
        self._f0s = jnp.array(f0s)
        self._jinvs = jnp.array(jinvs)

    # ---- 動作 → 關節目標角 ----
    def _joint_targets(self, c):
        th = c["theta"]
        fx = 2 * (c["rx"] - MU_MIN) / (MU_MAX - MU_MIN) - 1
        fy = 2 * (c["ry"] - MU_MIN) / (MU_MAX - MU_MIN) - 1
        dx = -D_STEP * fx * jnp.cos(th)
        dy = D_STEP_Y * fy * jnp.cos(th)
        dz = jnp.where(jnp.sin(th) > 0, G_C * jnp.sin(th), G_P * jnp.sin(th))
        off = jnp.stack([dx, dy, dz], -1)
        q = jax.vmap(lambda ji, o: jnp.array(HOME3_np) + ji @ o)(self._jinvs, off)
        return q.reshape(12)

    # ---- 73 維 obs（欄位順序必須與 task6/inference/obs_d1.py 一致）----
    def _obs(self, data, c, cmd, last_a, gyro_bias):
        quat = data.qpos[3:7]
        grav = w2b(quat, jnp.array([0.0, 0.0, -1.0]))
        gyro = data.qvel[3:6] + gyro_bias
        contact = (jnp.abs(data.actuator_force[jnp.array(KNEE_ACT_IDX)]) > TAU_CONTACT
                   ).astype(jnp.float32)
        return jnp.concatenate([
            grav, gyro,
            data.qpos[7:19] - HOME12, data.qvel[6:18],
            cmd, last_a, contact,
            c["rx"], c["rx_d"], c["ry"], c["ry_d"],
            jnp.sin(c["theta"]), jnp.cos(c["theta"]),
        ])

    def reset(self, rng):
        k_cmd, k_bias, k_noise = jax.random.split(rng, 3)
        data = mjx.make_data(self.sys).replace(qpos=self._init_q,
                                               ctrl=jnp.array(self._mj.key_ctrl[0]))
        data = mjx.forward(self.sys, data)
        cmd = jnp.array([jax.random.uniform(k_cmd, minval=0.3, maxval=1.0), 0.0, 0.0])
        gyro_bias = jax.random.uniform(k_bias, (3,), minval=-GYRO_BIAS, maxval=GYRO_BIAS)
        c = cpg_init()
        info = {"rng": k_noise, "c": c, "last_a": jnp.zeros(ACT_DIM),
                "cmd": cmd, "gyro_bias": gyro_bias, "step": 0}
        obs = self._obs(data, c, cmd, jnp.zeros(ACT_DIM), gyro_bias)
        metrics = {"height": data.qpos[2], "vx": 0.0}
        return State(data, obs, jnp.zeros(()), jnp.zeros(()), metrics, info)

    def step(self, state, action):
        info = dict(state.info)
        mux, muy, om = action_to_cpg_cmd(action)
        c = cpg_step(info["c"], mux, muy, om, CTRL_DT)
        q_des = jnp.clip(self._joint_targets(c), self._lo, self._hi)

        data = state.pipeline_state
        rng, k_push, k_dir, k_obs = jax.random.split(info["rng"], 4)
        do_push = (info["step"] % PUSH_EVERY) == (PUSH_EVERY - 1)
        ang = jax.random.uniform(k_dir, minval=0.0, maxval=2 * jnp.pi)
        mag = jax.random.uniform(k_push, minval=0.0, maxval=PUSH_VEL)
        kick = jnp.where(do_push,
                         jnp.array([mag * jnp.cos(ang), mag * jnp.sin(ang), 0.0]),
                         jnp.zeros(3))
        data = data.replace(qvel=data.qvel.at[0:3].add(kick))

        def one(d, _):
            return mjx.step(self.sys, d.replace(ctrl=q_des)), None
        data, _ = jax.lax.scan(one, data, None, length=N_FRAMES)

        grav = w2b(data.qpos[3:7], jnp.array([0.0, 0.0, -1.0]))
        vb = w2b(data.qpos[3:7], data.qvel[0:3])      # reward 用真值速度（只在模擬）
        cmd = info["cmd"]

        r_vx = jnp.exp(-4.0 * (vb[0] - cmd[0]) ** 2)
        r_vy = jnp.exp(-4.0 * vb[1] ** 2)
        r_wz = jnp.exp(-4.0 * (data.qvel[5] - cmd[2]) ** 2)
        r_h = jnp.exp(-40.0 * (data.qpos[2] - NOMINAL_HEIGHT) ** 2)   # 用實際站定高度，非 keyframe
        c_act = jnp.sum((action - info["last_a"]) ** 2)
        c_tau = jnp.sum(data.actuator_force ** 2)
        reward = 1.5 * r_vx + 0.5 * r_vy + 0.5 * r_wz + 0.5 * r_h \
                 - 0.01 * c_act - 2e-4 * c_tau

        done = jnp.where(grav[2] > -0.4, 1.0, 0.0)

        noise = jax.random.normal(k_obs, (6,))
        obs = self._obs(data, c, cmd, action, info["gyro_bias"])
        obs = obs.at[0:3].add(GRAV_NOISE * noise[0:3])
        obs = obs.at[3:6].add(GYRO_NOISE * noise[3:6])

        info.update({"rng": rng, "c": c, "last_a": action, "step": info["step"] + 1})
        metrics = {"height": data.qpos[2], "vx": vb[0]}
        return state.replace(pipeline_state=data, obs=obs, reward=reward,
                             done=done, metrics=metrics, info=info)

    @property
    def observation_size(self): return OBS_DIM

    @property
    def action_size(self): return ACT_DIM

    @property
    def backend(self): return "mjx"
```

- [ ] **Step 5: Domain randomization 格**

Cell 8（code）:

```python
_mm = mujoco.MjModel.from_xml_path(SCENE)
BASE_ID = mujoco.mj_name2id(_mm, mujoco.mjtObj.mjOBJ_BODY, "base")

def domain_randomize(sys, rng):
    @jax.vmap
    def per_env(rng):
        k1, k2, k3, k4, k5 = jax.random.split(rng, 5)
        geom_friction = sys.geom_friction.at[:, 0].set(
            jax.random.uniform(k1, minval=0.3, maxval=1.0))
        kp = jax.random.uniform(k2, minval=60.0, maxval=100.0)    # 名目 80
        kd = jax.random.uniform(k3, minval=0.5, maxval=2.0)       # 名目 1
        gain = sys.actuator_gainprm.at[:, 0].set(kp)
        bias = sys.actuator_biasprm.at[:, 1].set(-kp).at[:, 2].set(-kd)
        body_mass = sys.body_mass * jax.random.uniform(
            k4, (sys.nbody,), minval=0.8, maxval=1.2)             # 連桿質量 ±20%
        payload = jax.random.uniform(k5, minval=0.0, maxval=5.0)  # 官方額定 payload 5 kg
        body_mass = body_mass.at[BASE_ID].add(payload)
        return geom_friction, gain, bias, body_mass
    gf, gain, bias, bm = per_env(rng)
    in_axes = jax.tree_util.tree_map(lambda x: None, sys)
    in_axes = in_axes.replace(geom_friction=0, actuator_gainprm=0,
                              actuator_biasprm=0, body_mass=0)
    sys = sys.replace(geom_friction=gf, actuator_gainprm=gain,
                      actuator_biasprm=bias, body_mass=bm)
    return sys, in_axes
print("domain_randomize ready")
```

- [ ] **Step 6: Smoke test 格（關卡 4）**

Cell 9（code）:

```python
env = D1wCpgEnv(F0S_np, JINVS_np)
s = jax.jit(env.reset)(jax.random.PRNGKey(0))
print("obs shape:", s.obs.shape, "(應為 (73,))")
s = jax.jit(env.step)(s, jnp.zeros(12))
print("reward:", float(s.reward), "done:", float(s.done), "height:", float(s.metrics["height"]))
assert s.obs.shape == (73,), "obs 維度不對，回頭對 _obs 的欄位順序"
assert np.isfinite(float(s.reward)), "reward 出現 NaN/Inf"
print("PASSED")
```

- [ ] **Step 7: 訓練格（關卡 5）**

Cell 10（code）:

```python
import functools, time
from brax.training.agents.ppo import train as ppo
from brax.training.agents.ppo import networks as ppo_networks

env = D1wCpgEnv(F0S_np, JINVS_np)
network_factory = functools.partial(
    ppo_networks.make_ppo_networks,
    policy_hidden_layer_sizes=(256, 256, 128),
    value_hidden_layer_sizes=(256, 256, 256))

train_fn = functools.partial(
    ppo.train, num_timesteps=120_000_000, num_evals=20, episode_length=1000,
    num_envs=2048, batch_size=256, num_minibatches=32, unroll_length=20,
    num_updates_per_batch=4, learning_rate=3e-4, entropy_cost=1e-2,
    discounting=0.97, normalize_observations=True,
    network_factory=network_factory, randomization_fn=domain_randomize, seed=0)

_t0 = time.time(); rewards = []
def progress(step, metrics):
    r = float(metrics.get("eval/episode_reward", 0.0)); rewards.append((step, r))
    print(f"step {step:>10,}  reward {r:8.2f}  ({time.time()-_t0:.0f}s)")

make_inference_fn, params, _ = train_fn(environment=env, progress_fn=progress)
print("training done")
```

Cell 11（code）:

```python
import matplotlib.pyplot as plt
plt.plot([s for s, _ in rewards], [r for _, r in rewards], marker="o")
plt.xlabel("env steps"); plt.ylabel("eval reward"); plt.grid(True); plt.show()
```

Cell 12（code）— 存權重:

```python
from brax.io import model
model.save_params("cpg_rl_d1w_params.pkl", params)
try:
    from google.colab import files; files.download("cpg_rl_d1w_params.pkl")
except Exception as e:
    print("左側檔案面板右鍵下載 cpg_rl_d1w_params.pkl。", e)
```

- [ ] **Step 8: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/notebooks/cpg_rl_d1w_colab.ipynb
git commit -m "feat(task6): Colab MJX 訓練 notebook（輪足版、73 維 obs、kp80/kd1、payload 5kg、IMU 雜訊 DR）"
```

- [ ] **Step 9: 交給使用者執行關卡 4、5**

告知使用者：上傳 notebook 到 Colab（執行階段選 GPU），依序跑格子，
Cell 9 的 Smoke test 印出 `PASSED` 才繼續 Cell 10 訓練，
訓完把 `cpg_rl_d1w_params.pkl` 下載放到 `task6/weights/`。

---

## Task 8: `local_infer_d1.py` — 本機推論（關卡 6）

**Files:**
- Create: `task6/inference/local_infer_d1.py`

**Interfaces:**
- Consumes: `d1_model.*`、`cpg_d1.*`、`obs_d1.build_obs`、`task6/weights/cpg_rl_d1w_params.pkl`
- Produces: `local_infer_d1.load_policy(path) -> Callable[[np.ndarray], np.ndarray]`、
  `local_infer_d1.run(args) -> dict`

- [ ] **Step 1: 實作**

Create `task6/inference/local_infer_d1.py`:

```python
"""關卡 6：本機 CPU 推論，載入 Colab 訓練的權重，輸出影片與指標。

用法：
  # 沒權重時先測管線（固定動作）
  conda run --no-capture-output -n rbtdog python task6/inference/local_infer_d1.py --dummy --secs 8 --video
  # 有權重
  conda run --no-capture-output -n rbtdog python task6/inference/local_infer_d1.py \
      --params task6/weights/cpg_rl_d1w_params.pkl --secs 20 --video --push
"""
import argparse
import os
import sys
from pathlib import Path

import numpy as np

sys.path.insert(0, str(Path(__file__).resolve().parent))

import mujoco

import cpg_d1
import d1_model
import obs_d1

OUT_DIR = Path(__file__).resolve().parents[1] / "outputs"
PUSHES = [(4.0, 0.2, 60.0), (9.0, 0.2, -60.0), (14.0, 0.2, 80.0)]   # (t0, 持續秒, 側向力 N)


def load_policy(path: str):
    import functools

    import jax
    from brax.io import model
    from brax.training.acme import running_statistics
    from brax.training.agents.ppo import networks as ppo_networks

    factory = functools.partial(ppo_networks.make_ppo_networks,
                                policy_hidden_layer_sizes=(256, 256, 128),
                                value_hidden_layer_sizes=(256, 256, 256))
    net = factory(d1_model.OBS_DIM, d1_model.ACT_DIM,
                  preprocess_observations_fn=running_statistics.normalize)
    pol = ppo_networks.make_inference_fn(net)(model.load_params(path), deterministic=True)
    jpol = jax.jit(pol)
    key = jax.random.PRNGKey(0)

    def infer(obs):
        return np.asarray(jpol(jax.numpy.asarray(obs), key)[0])

    infer(np.zeros(d1_model.OBS_DIM, np.float32))    # 先 warm up 編譯
    return infer


def run(args) -> dict:
    m = d1_model.make_model()
    d = mujoco.MjData(m)
    mujoco.mj_resetDataKeyframe(m, d, 0)
    mujoco.mj_forward(m, d)

    f0s, jinvs = cpg_d1.leg_ik_consts(m)
    foot_gid = d1_model.foot_geom_ids(m)
    knee_aid = d1_model.knee_actuator_ids(m)
    base_id = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_BODY, "base")
    n_sub = int(round(d1_model.CTRL_DT / m.opt.timestep))

    if args.dummy or not args.params:
        if not args.params and not args.dummy:
            print("[warn] 無 --params，改用 dummy 固定動作測管線")
        fixed = np.tile([0.69, 0.0, -0.111], 4).astype(np.float32)
        def infer(obs): return fixed
    else:
        print(f"[info] 載入 {args.params}")
        infer = load_policy(args.params)
        print("[info] ok")

    def apply(q_des):
        d.ctrl[:] = q_des
        for _ in range(n_sub):
            mujoco.mj_step(m, d)

    for _ in range(int(0.5 / d1_model.CTRL_DT)):
        apply(d1_model.HOME12)

    ren = cam = None
    frames = []
    if args.video:
        os.environ.setdefault("MUJOCO_GL", "egl")
        ren = mujoco.Renderer(m, 480, 640)
        cam = mujoco.MjvCamera()
        mujoco.mjv_defaultFreeCamera(m, cam)

    c = cpg_d1.cpg_init()
    last_a = np.zeros(d1_model.ACT_DIM)
    cmd = np.array([args.vx, 0.0, 0.0], np.float32)
    x0 = float(d.qpos[0])
    fz_min = fz_max = float(d.geom_xpos[foot_gid[0]][2])
    fell = None

    for i in range(int(args.secs / d1_model.CTRL_DT)):
        t = i * d1_model.CTRL_DT
        d.xfrc_applied[base_id] = 0.0
        if args.push:
            for t0, dur, fy in PUSHES:
                if t0 <= t < t0 + dur:
                    d.xfrc_applied[base_id, 1] = fy

        obs = obs_d1.build_obs(d, c, cmd, last_a, knee_aid)
        act = infer(obs)
        mux, muy, om = cpg_d1.act_to_cmd(act)
        c = cpg_d1.cpg_step(c, mux, muy, om, d1_model.CTRL_DT)
        apply(cpg_d1.joint_targets(c, f0s, jinvs))
        last_a = act

        grav = cpg_d1.w2b(d.qpos[3:7], np.array([0.0, 0.0, -1.0]))
        if grav[2] > -0.4 and fell is None:
            fell = t
        fz = float(d.geom_xpos[foot_gid[0]][2])
        fz_min, fz_max = min(fz_min, fz), max(fz_max, fz)

        if ren is not None and i % 2 == 0:
            cam.lookat[:] = [d.qpos[0], d.qpos[1], 0.3]
            cam.distance, cam.elevation, cam.azimuth = 2.5, -20, 90
            ren.update_scene(d, cam)
            frames.append(ren.render())

    d.xfrc_applied[base_id] = 0.0
    res = {"dist": float(d.qpos[0]) - x0, "lateral": float(d.qpos[1]),
           "fell": fell, "foot_lift": fz_max - fz_min, "final_z": float(d.qpos[2])}
    print(f"[result] 前進={res['dist']:+.2f} m  側偏={res['lateral']:+.2f} m  "
          f"跌倒={'是 @%.1fs' % fell if fell else '否'}  "
          f"FL 抬腳={res['foot_lift']:.3f} m  末端機身高={res['final_z']:.3f} m")

    if frames:
        import imageio.v2 as iio
        OUT_DIR.mkdir(parents=True, exist_ok=True)
        tag = "_push" if args.push else ""
        out = OUT_DIR / f"infer_d1w{tag}.mp4"
        iio.mimsave(str(out), frames, fps=25, codec="libx264")
        print("[影片]", out)
    return res


if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--params", type=str, default="")
    ap.add_argument("--dummy", action="store_true")
    ap.add_argument("--secs", type=float, default=20.0)
    ap.add_argument("--vx", type=float, default=0.6)
    ap.add_argument("--video", action="store_true")
    ap.add_argument("--push", action="store_true")
    run(ap.parse_args())
```

- [ ] **Step 2: 用 dummy 驗證管線（不需權重）**

Run:
```bash
cd /home/huang/rbtdog_sim
conda run --no-capture-output -n rbtdog python task6/inference/local_infer_d1.py --dummy --secs 6 --video
```
Expected: 印出 `[result] ...` 一行，產生 `task6/outputs/infer_d1w.mp4`，且不拋例外。
（dummy 是固定動作，走不走得好不重要，這一步只驗證管線接得起來。）

- [ ] **Step 3: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/inference/local_infer_d1.py
git commit -m "feat(task6): 本機 CPU 推論腳本（關卡 6）"
```

- [ ] **Step 4: 權重回來後跑關卡 6**

拿到 `task6/weights/cpg_rl_d1w_params.pkl` 後執行：
```bash
cd /home/huang/rbtdog_sim
conda run --no-capture-output -n rbtdog python task6/inference/local_infer_d1.py \
    --params task6/weights/cpg_rl_d1w_params.pkl --secs 20 --video
conda run --no-capture-output -n rbtdog python task6/inference/local_infer_d1.py \
    --params task6/weights/cpg_rl_d1w_params.pkl --secs 20 --video --push
```
把兩份指標與影片交給使用者。

---

## Task 9: 文件與 README

**Files:**
- Create: `task6/docs/D1_EDU_W_規格與模型對照.md`
- Create: `task6/README.md`

**Interfaces:**
- Consumes: 前八個任務的全部產出
- Produces: 無程式介面

- [ ] **Step 1: 寫規格與對照文件**

Create `task6/docs/D1_EDU_W_規格與模型對照.md`:

```markdown
# 智元 D1 EDU 輪足版（ZSL-1w）規格、模型與 SDK 對照

## 1. 官方規格（來源：https://www.agibot.com/products/D1_Pro）

| 項目 | 數值 |
|---|---|
| 重量 | 15.5 kg（含電池）← **點足版數字**，輪足版 URDF 為 20.56 kg |
| 站立尺寸 | 635 × 360 × 420 mm |
| 自由度 | 12（每腿 abad / hip / knee）＋ 輪足版另有 4 顆輪 |
| 關節峰值扭矩 | 48 N·m |
| 關節行程 | 側擺 ±28°、大腿 −170°~66°、小腿 35°~156° |
| 最大速度 | 3.5 m/s ｜ 最大爬坡 40° ｜ 連續上階 16 cm |
| 額定負載 | 5 kg |

質量差異的解釋：20.56 − 15.5 ≈ 5.06 kg ≈ 4 ×[(0.901 − 0.063) + (0.765 − 0.207)]，
正好是輪子與加粗小腿相對點足版的增重，兩份資料不衝突。

## 2. 模擬模型採用值

| 項目 | 值 | 出處 |
|---|---|---|
| 總質量 | 20.56 kg | ZSL-1W.urdf 17 個 link 之和 |
| 輪 | 半徑 0.0710 m、寬 0.0480 m、每顆 0.90130429 kg | STL 實測 + URDF |
| 力矩上限 | 28 N·m | URDF `effort`（官網 48 為峰值，取保守值）|
| kp / kd | 80 / 1 | 點足版官方 demo `lowlevel_demo.py:68-73` ⚠ 見下 |
| home 關節角 | `[0, +1.05, −2.00]` | 解輪心在髖正下方；膝軸 +y |
| home 機身高 | 0.2948 m（幾何值）| 實際站定後因位置伺服撓度沉降至約 0.270 m |
| armature / damping | 0.01 / 0.1 | **URDF 未提供，為假設值** |

⚠ **kp/kd 的來源限制**：輪足版 SDK 沒有 LowLevel demo，這組值取自**點足版**。
已由關卡 2、3 驗證可用（沉降 2.47 cm 後穩定，膝力矩 7.3 N·m 遠低於 28 上限），
但若日後取得輪足版官方值應以官方為準。

## 3. ⚠️ 輪足版 SDK 沒有 LowLevel —— 目前沒有部署路徑

官方文件 `docs/deploy.md:168` 原文：

> **「ZSL-1w 不提供 LowLevel 接口，因此仅有 `highlevel_demo`。」**

三路交叉驗證（SDK repo commit `db8accd`，v0.2.7 為最新）：

| 檢查點 | 點足版 zsl-1 | 輪足版 zsl-1w |
|---|---|---|
| `include/<機型>/lowlevel.h` | ✅ 有 | ❌ 只有 `highlevel.h` |
| `docs/api_<機型>.md` | 有「2. LowLevel函数介绍」| ❌ 全篇只有 HighLevel |
| `demo/<機型>/` | lowlevel + highlevel | ❌ 只有 highlevel |

**結論**：輪足版實機**能讀**十六個關節的角度/速度/力矩與 IMU，
但**不能下發任何關節指令**，只能用 `move(vx, vy, yaw_rate)` / `crawl` / `climb` 等原廠內建動作。

→ **本專案訓練出的 CPG-RL policy 目前無法部署到輪足版實機。**
模擬訓練的價值不受影響，但部署需等原廠釋出輪足版 LowLevel，或取得特殊韌體。

附帶影響：「鎖住輪子當圓腳」這個組態本身也需要對輪馬達下指令，
因此**該組態目前只存在於模擬中**。

## 4. observation 為什麼仍排除機身線速度

輪足版 `HighLevel` **讀得到** `getBodyVelocity()`。但能讀不等於能部署——
部署必須靠尚未存在的 LowLevel，而依點足版 LowLevel 的先例，**LowLevel 沒有線速度**
（`getBodyVelocity` 只存在於 HighLevel，且官方 FAQ 明示 High/Low 不可並用）。

因此採保守設計：obs 不含機身線速度，維持 73 維，一旦輪足版 LowLevel 釋出即可直接使用。

| obs 欄位 | 維度 | 未來 LowLevel 應可提供 |
|---|---|---|
| gravity | 3 | 四元數轉機身系 |
| gyro | 3 | IMU |
| joint_pos | 12 | 關節角減 HOME12 |
| joint_vel | 12 | 關節速度 |
| cmd | 3 | 自產 |
| last_action | 12 | 自存 |
| foot_contact | 4 | `\|膝關節力矩\|` > `TAU_CONTACT` |
| cpg | 24 | 自算 |
| **合計** | **73** | |

輪子在模擬中被熔接、無自由度，故不進 obs。
reward 仍用模擬真值速度（reward 只在模擬計算）。

## 5. ⚠️ 腿序：官方文件自相矛盾，尚未定案

| 來源 | 腿順序 |
|---|---|
| `include/zsl-1/lowlevel.h:30`（點足版 `motorState` 註解）| FL, FR, RL, RR |
| `docs/architecture.md`「关节控制命令说明」| FR, FL, RR, RL |
| `include/lowlevel/lowlevel.h` enum | FR, FL, RR, RL |
| `ZSL-1W.urdf` joint 名稱前綴 | FBL(→FL), FAR(→FR), RAR(→RR), RBL(→RL) |

模擬採 `LEGS = ["FL", "FR", "RL", "RR"]`，內部自洽，不影響訓練。
URDF 的 joint 前綴與其 child link 名稱不一致，**以 child link 名稱為準**。
**sim2real 前必須用實機驗證後才能部署，不得憑文件推測。**

## 6. 部署注意事項（來源：官方 FAQ / deploy.md）

- 控制下發建議 20~50 Hz；本專案 `CTRL_DT = 0.02` = 50 Hz。
- **3 秒未收到 SDK 資料，機器會自動切阻尼模式趴下** → 控制迴圈不可中斷。
- IMU 為原始資料、精度一般 → 訓練已加入雜訊與偏差 DR。
- 四元數轉尤拉角順序為 ZYX。
- 手柄遙控與 SDK 不可同時使用，SDK 優先權較高。
- 運控程式升級後需重新設定 `SDK_CONFIG` 與 `SDK_CLIENT_IP`。

## 7. 常數雙寫警告

`task6/inference/d1_model.py` 與 `task6/notebooks/cpg_rl_d1w_colab.ipynb` 各有一份常數
（Colab 不 import 本地模組，屬刻意重複，使用者已確認保留）。
**改一邊就必須改另一邊**，否則訓練與推論會對不起來。
最容易出錯的三個：`HOME3`、`TAU_CONTACT`、`OBS_DIM`。
```

- [ ] **Step 2: 寫 README**

Create `task6/README.md`:

```markdown
# task6 — 智元 D1 EDU 輪足版（ZSL-1w）CPG-RL 走路

把 task4 在 Go2 上驗證過的 CPG-RL 論文標準版移植到智元 D1 EDU **輪足版**。
四顆輪子在模擬中熔接鎖死，當成 71 mm 圓腳走路，動作空間維持 12 維。
訓練在 Colab GPU（MJX），推論在本機 CPU。**與 task4 完全獨立，不共用程式碼。**

## ⚠️ 先讀這個

輪足版 SDK **沒有 LowLevel 介面**（官方 `deploy.md:168` 明載），
因此本專案訓練出的 policy **目前無法部署到實機**。
模擬訓練與驗證不受影響。詳見 `docs/D1_EDU_W_規格與模型對照.md` 第 3 節。

## 資料夾

```
task6/
├── model/d1_edu_w/   MJCF + 網格 + 來源記錄
├── inference/        d1_model(常數) / cpg_d1(CPG+IK) / obs_d1(73維obs)
│                     cpg_openloop_d1(關卡3) / local_infer_d1(關卡6)
├── notebooks/        cpg_rl_d1w_colab.ipynb
├── tests/            pytest
├── weights/          訓練好的權重
├── outputs/          影片與圖
└── docs/             規格、SDK 對照、部署注意事項
```

## 與 Go2（task4）的關鍵差異

| 項目 | Go2 | D1 EDU 輪足 |
|---|---|---|
| 總質量 | 15 kg | 20.56 kg（四顆輪各 0.9 kg）|
| home 關節角 | `[0, 0.9, -1.8]` | `[0, 1.05, -2.00]` |
| 足端 | 17.5 mm 球 | **71 mm 輪（熔接鎖死）**，碰撞用 cylinder |
| PD | `apply_pd()` 覆寫 90/3 | XML 內建 80/1（原廠值），**無 apply_pd** |
| 力矩上限 | 23.7 / knee 45.43 | 28 全關節 |
| obs | 76 維 | 73 維（移除實機拿不到的機身線速度）|
| 觸地判定 | 腳掌世界高度 | 膝關節力矩門檻 |

## 使用流程

```bash
# 1. 跑測試（關卡 1~3）
conda run --no-capture-output -n rbtdog python -m pytest task6/tests -v

# 2. 開迴路 CPG 驗證 + 影片（關卡 3）
conda run --no-capture-output -n rbtdog python task6/inference/cpg_openloop_d1.py --secs 8 --video

# 3. 訓練（Colab GPU）：上傳 notebooks/cpg_rl_d1w_colab.ipynb，
#    先跑 Smoke test 印出 PASSED 再開訓練，訓完下載權重放 weights/

# 4. 本機推論（關卡 6）
conda run --no-capture-output -n rbtdog python task6/inference/local_infer_d1.py \
    --params task6/weights/cpg_rl_d1w_params.pkl --secs 20 --video --push
```

## 站姿的正常行為

home keyframe 的幾何高度是 0.2948 m，但實際站定後機身會沉降到約 0.270 m。
這是 kp=80 位置伺服在 20.56 kg 下的靜態撓度，**是物理不是 bug**；
沉降後 1 秒內 z 峰對峰僅約 1.4 mm。
```

- [ ] **Step 3: 全套測試通過確認**

Run: `cd /home/huang/rbtdog_sim && conda run --no-capture-output -n rbtdog python -m pytest task6/tests -v`
Expected: 全部 PASS

- [ ] **Step 4: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task6/docs task6/README.md
git commit -m "docs(task6): 規格與 SDK 對照表、部署注意事項、README"
```

---

## Self-Review 紀錄

**Spec 覆蓋檢查**

| Spec 章節 | 對應任務 |
|---|---|
| §1 資料來源與授權 | Task 1（`SOURCE.md` + `LICENSE` + 測試）|
| §2 交付結構 | Task 1（骨架）、各任務建檔 |
| §3.1–3.2 MJCF 逐項規格 | Task 2 |
| §3.3 freejoint 必要性 | Task 2（`test_freejoint_present_and_dof_count`、`test_total_mass_matches_urdf`）|
| §3.4 home 姿態推導 | Task 2（`test_home_keyframe`）、Task 4（`test_leg_ik_consts_home_foot_is_below_hip`）|
| §3.5 PD 用原廠值、刪 `apply_pd` | Task 2（`test_actuator_order_and_gains`）、Global Constraints |
| §3.6 腿序矛盾不猜 | Task 3（`LEGS` 單一常數）、Task 9（對照表 + 實機驗證步驟）|
| §4.1 沿用 task4 配方 | Task 7 Cell 5–7 |
| §4.2 九項必改 | Task 7（逐項對照表列在 Cell 1）|
| §5.1 實機能力硬約束 | Task 9 第 5 節 |
| §5.2 obs 逐項對帳、73 維 | Task 6 |
| §5.3 部署約束 | Task 9 第 6 節 |
| §6 六道關卡 | 關卡 1→Task 2、2→Task 3、3→Task 5、4/5→Task 7、6→Task 8 |
| §7 分工 | Task 7 Step 9、Task 8 Step 4 |
| §8 YAGNI | 全計畫無地形、無輪足、無實機部署 |
| §9 已知未決事項 | 腿序→Task 9；`TAU_CONTACT`→Task 5 Step 3；kp/kd 穩定性→Task 3 Step 5；力矩上限→Task 7 訓練後回報 |

無缺口。額外新增 spec 未涵蓋的 armature/damping 假設，已明列於 Global Constraints 並綁定回報義務。

**Placeholder 掃描**：無 TBD / TODO / 「類似 Task N」/ 無程式碼的程式步驟。
Task 1 Step 4 的 `<commit>` 與 Task 5 Step 3 的 `<實測值>` 是**必須由執行結果填入的實測數字**，
兩處都在同一任務內給了取得該數字的確切指令，不是待辦。

**型別一致性**：`LEGS` / `HOME3` / `HOME12` / `OBS_DIM` / `TAU_CONTACT` 只在 `d1_model.py` 定義一次；
`cpg_init` 回傳的字典鍵（`rx, rx_d, ry, ry_d, theta`）在 Task 4、6、7 三處用法一致；
`leg_ik_consts` 回傳 `(f0s, jinvs)` 形狀 `(4,3)` / `(4,3,3)` 在 Task 4、5、7、8 一致；
MJCF 名稱契約（`{LEG}_abad` body、`{LEG}` foot geom、`{LEG}_knee` actuator）在 Task 2 定義，
Task 3–8 全數依此引用。
