# D1 Max CPG 基準凍結 ＋ CPG-RL 訓練 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 D1 Max 已驗證不跌倒的 `walk` 步態凍結成唯一真實來源，並產出一本可直接在 Colab 跑的 CPG-RL 訓練 notebook，其觀測層每一欄都是實機底層拿得到的量。

**Architecture:** 三層。(1) `gait_baseline.py` 凍結步態常數，`cpg_walk_max` 與 notebook 都引用它。(2) `make_mjx_model.py` 程式化把官方 MJCF 轉成 MJX 可跑的訓練模型（碰撞網格換原始形狀、力矩致動器換位置致動器、拿掉網格相依），並用四段式對照量出模擬對模擬的落差。(3) `obs_max.py` 定義 68 維觀測層，Colab 訓練端與本機推論端共用同一份欄位順序。

**Tech Stack:** Python 3.11 / MuJoCo 3.10 / MJX / brax 0.14.2 / JAX / pytest / conda 環境 `rbtdog`

## Global Constraints

- 所有指令一律用 `conda run --no-capture-output -n rbtdog python ...`，工作目錄是 repo 根目錄 `/home/huang/rbtdog_sim`。
- **brax 版本鎖死 `0.14.2`**、jax `<0.10`（Colab 端）。brax 載到 activation 不匹配的權重**不會報錯**，只會讓 policy 靜默錯亂。
- **不得手改** `task7/model/zgws/zgws.xml`（官方原檔）。所有模型改動一律由 `make_mjx_model.py` 程式化產生。
- 常數一律從 `task7/inference/max_model.py` 取，不得在各檔各自寫死。
- 腿序一律 `LEGS = ("FR", "FL", "RR", "RL")`；SHM 是 `FL,FR,BL,BR`，**一律按名稱對應，不按索引**。
- 12 個腿關節在 `qpos` 裡**不連續**，一律用 `mm.LEG_QPOS_IDX` / `mm.LEG_QVEL_IDX`，不得用連續切片。
- 行進速度一律引用 `speed_travel`，**不得引用 `speed_path`**（高估 68%）。
- 註解與文件用繁體中文，風格對齊 `task7/` 既有檔案：寫「為什麼」與「踩過什麼坑」，不寫「這行做什麼」。
- 每個 Task 結束時 commit，訊息結尾加上：
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

## 已量到的基準數字（給對照用，不是要重算的）

`conda run -n rbtdog python -c "...cw.rollout(gait='walk', secs=20.0)"`（單次、無擾動）：

| 指標 | 值 |
|---|---|
| `speed_travel` | 0.1471 m/s |
| `bounce` | 16.7 mm |
| `pitch_mean` | −0.025° |
| `support` | 3.204 |
| `yaw` | +6.0°（**噪的，±3°，不可拿來比對**） |
| `fell` / `lim_pct` / `tau_pct` / `reach_pct` | None / 0 / 0 / 0 |
| 牆鐘時間 | 約 2.3 s |

solver 迭代數敏感度（已實測，`iterations=100/ls=50` 預設 vs `6/6`）：
`travel 0.1471→0.1467`、`bounce 16.7→17.1`、`support 3.204→3.206`。**可忽略**。

---

## File Structure

| 檔案 | 責任 |
|---|---|
| `task7/inference/gait_baseline.py` | **新**。凍結的 walk 步態常數，唯一真實來源 |
| `task7/inference/cpg_walk_max.py` | **改**。`GAITS["walk"]` 改為引用 `gait_baseline`；`Robot` / `rollout` 增加 `scene` / `actuator_mode` / `solver_iters` 三個參數 |
| `task7/inference/cpg_sweep_max.py` | **改**。新增 `--plan g1` 對照計畫 |
| `task7/model/zgws/make_mjx_model.py` | **新**。程式化產生 MJX 訓練模型 |
| `task7/model/zgws/zgws_mjx.xml` | **新（產生物，進版控）**。無網格相依，Colab clone 完即可用 |
| `task7/model/zgws/scene_flat_mjx.xml` | **新（產生物，進版控）** |
| `task7/inference/obs_max.py` | **新**。68 維觀測層唯一定義 |
| `task7/inference/local_infer_max.py` | **新**。載權重，在**原始網格模型**上回放／錄影／量測 |
| `task7/notebooks/cpg_rl_max_colab.ipynb` | **新**。使用者拿去 Colab 跑的東西 |
| `task7/tests/test_gait_baseline.py` | **新** |
| `task7/tests/test_mjx_model.py` | **新** |
| `task7/tests/test_obs_max.py` | **新** |
| `task7/docs/基準步態凍結_D1Max_walk_2026-08-27.md` | **新** |
| `task7/docs/MJX模型對照_2026-08-27.md` | **新**。G1 數據 |
| `task7/docs/現場操作卡_IMU平放複核.md` | **新** |
| `task7/docs/CPG-RL_D1Max_設計_2026-08-27.md` | **新**。總結，掛進 HANDOFF |
| `task7/HANDOFF.md` | **改**。加入本線的入口 |

---

## Task 1: 凍結基準步態

**Files:**
- Create: `task7/inference/gait_baseline.py`
- Modify: `task7/inference/cpg_walk_max.py:125-140`
- Test: `task7/tests/test_gait_baseline.py`

**Interfaces:**
- Produces: `gait_baseline.BASELINE`（`dict`，含 `gait, duty, omega, mu_x, mu_y, d_step, d_step_y, x_off, g_c, z_sag, wheel_mode` 十一個鍵）、
  `gait_baseline.walk_gait()`（回傳 `cpg_walk_max.GAITS["walk"]` 用的 dict，含 `phase` 物件）、
  `gait_baseline.MU_Y`、`gait_baseline.D_STEP_Y`

- [ ] **Step 1: 寫失敗測試**

建立 `task7/tests/test_gait_baseline.py`：

```python
"""基準步態凍結的回歸測試。

為什麼要有這支：掃描期間曾發生「跑到一半共用 MJCF 被別條線改掉」
（commit 533e91a 改了膝關節 range），事後只因為 `lim_pct` 這個診斷欄一直有印
才判定得出來。步態常數比 MJCF 更容易被順手改掉，而改掉之後 RL 的基準就悄悄變了。
"""
import sys
from pathlib import Path

import numpy as np
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "inference"))

import cpg_max  # noqa: E402
import cpg_walk_max as cw  # noqa: E402
import gait_baseline as gb  # noqa: E402


def test_baseline_values_frozen():
    """每一個數字都有判準來源，改動必須連同文件一起改。"""
    assert gb.BASELINE == {
        "gait": "walk",
        "duty": 0.80,
        "omega": 1.4,
        "mu_x": 1.80,
        "mu_y": 1.50,
        "d_step": 0.10,
        "d_step_y": 0.12,
        "x_off": -0.040,
        "g_c": 0.08,
        "z_sag": 0.0325,
        "wheel_mode": "damp",
    }


def test_cpg_walk_max_uses_baseline():
    """cpg_walk_max 的 walk 必須是引用而不是另一份抄本。"""
    g = cw.GAITS["walk"]
    assert g["duty"] == gb.BASELINE["duty"]
    assert g["omega"] == gb.BASELINE["omega"]
    assert g["mu_x"] == gb.BASELINE["mu_x"]
    assert g["x_off"] == gb.BASELINE["x_off"]
    assert g["d_step"] == gb.BASELINE["d_step"]
    assert g["g_c"] == gb.BASELINE["g_c"]
    assert g["phase"] is cpg_max.PHASE_WALK
    assert cw.MU_Y == gb.BASELINE["mu_y"]
    assert cw.D_STEP_Y == gb.BASELINE["d_step_y"]


def test_z_sag_matches_static_sag():
    """z_sag 與 max_model.STATIC_SAG 綁死，不可以各寫各的。"""
    import max_model as mm
    assert gb.BASELINE["z_sag"] == mm.STATIC_SAG


@pytest.mark.slow
def test_baseline_walks_without_falling():
    """8 秒煙霧測試：不跌倒、四個診斷指標全 0。

    只跑 8 秒是因為完整驗收在 `cpg_sweep_max --plan base` 做（12 擾動 × 180 秒）。
    這裡只擋「有人把常數改到步態當場垮掉」這種等級的錯。
    """
    r = cw.rollout(gait="walk", secs=8.0, quiet=True)
    assert r["fell"] is None
    assert r["lim_pct"] == 0.0
    assert r["tau_pct"] == 0.0
    assert r["reach_pct"] == 0.0
    assert r["support"] > 3.0
    assert np.isfinite(r["speed_travel"]) and r["speed_travel"] > 0.10
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_gait_baseline.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'gait_baseline'`

- [ ] **Step 3: 建立 `task7/inference/gait_baseline.py`**

```python
"""D1 Max 基準步態（`walk`）的凍結參數 —— **唯一真實來源**。

這組參數是 RL 的基準與對照組。`cpg_walk_max.GAITS["walk"]` 與
Colab 訓練 notebook 都必須引用這裡，不得各自寫死。

## 為什麼要凍結

`walk` 在多擾動掃描（`task7/outputs/sweep_*.json`）中 20 / 60 / 120 / **180 秒**
每格 12 個擾動**全部 0 跌倒**，關節超限 / 力矩飽和 / IK 縮限全部 0.00%。
也就是「平地不跌倒」已經達成且複驗過。RL 要拿它當起點與對照，
所以它必須是一個**不會被順手改掉**的常數集合，而不是散在腳本裡的預設值。

## 每個數字的判準來源（全部是多擾動掃描的結論，不是單次樣本）

| 參數 | 判準 |
|---|---|
| `duty=0.80` | ≤0.70 是 **12/12 全跌**；0.80 彈跳最小（17.2 mm，0.85 是 21.1、0.90 是 19.9） |
| `omega=1.4` | 再高就帶進 0.4–0.9 °/s 的偏航，且符號不可預測 |
| `mu_y=1.50` | → `fy = 0`，橫向偏移恰為 0（有測試釘到 1e-15）；1.75 是 **12/12 全跌** |
| `x_off=-0.040` | 平均俯仰**單調過零**在 −40.8 mm（12 擾動，格內全距 0.01–0.02°、雜訊僅佔 2%） |
| `g_c=0.08` | 實際離地約 100 mm，對上原廠 `leg_height=0.10`；再大俯仰開始劣化 |
| `z_sag` | ＝ `max_model.STATIC_SAG`，位置伺服的靜態撓度補償，**只加在擺動相** |
| `wheel_mode="damp"` | 輪子給位置增益實測會造成 **+39° 偏航失控**（原廠那個 Kp=60 是搭配「每步重給目標角」的 RL 策略用的） |

⚠️ **配平點 `x_off` 會隨三件事移動**：輪摩擦、腿關節摩擦、`d_step`。
   動到任何一個都必須用 `cpg_sweep_max.py --plan trim` 重掃，不可以沿用。

⚠️ **不要用 `speed_path` 引用行進速度**，它把機身左右搖擺算成前進，實測高估 68%。
   行進速度一律看 `speed_travel`（以一個步態週期為步長重算的路徑長）。
"""
import cpg_max
from max_model import STATIC_SAG

# 凍結值。純量字典，好比對、好序列化、好在 notebook 裡 assert。
BASELINE = {
    "gait": "walk",
    "duty": 0.80,
    "omega": 1.4,
    "mu_x": 1.80,
    "mu_y": 1.50,
    "d_step": 0.10,
    "d_step_y": 0.12,
    "x_off": -0.040,
    "g_c": 0.08,
    "z_sag": STATIC_SAG,
    "wheel_mode": "damp",
}

MU_Y = BASELINE["mu_y"]
D_STEP_Y = BASELINE["d_step_y"]


def walk_gait() -> dict:
    """`cpg_walk_max.GAITS["walk"]` 用的 dict（含相位物件）。

    相位是 ndarray，不放進 `BASELINE` 是為了讓 `BASELINE` 保持可直接 `==` 比對。
    """
    return dict(phase=cpg_max.PHASE_WALK, duty=BASELINE["duty"],
                omega=BASELINE["omega"], mu_x=BASELINE["mu_x"],
                x_off=BASELINE["x_off"], d_step=BASELINE["d_step"],
                g_c=BASELINE["g_c"])
```

- [ ] **Step 4: 改 `cpg_walk_max.py` 引用它**

在 import 區（`import cpg_max` 之後）加：

```python
import gait_baseline as gb
```

把 `GAITS` 的 `"walk"` 那筆與其下的 `MU_Y` / `D_STEP_Y` 改成：

```python
GAITS = {
    # ★ walk 是**基準步態**，參數凍結在 `gait_baseline.py`，判準來源也寫在那裡。
    #   要改它必須連同 `docs/基準步態凍結_D1Max_walk_2026-08-27.md` 一起改。
    "walk": gb.walk_gait(),
    # ★ walk_fast 的配平點與 walk **不一樣**（2026-08-26 用 12 擾動重掃才看到）。
    #   平均俯仰過零：walk −41 mm、walk_fast **−46 mm**。之前兩組共用 −40 是有偏差的
    #   （walk_fast 在 −40 的平均俯仰是 −0.12°±0.01，−46 是 +0.05°±0.00）。
    #   配平點會隨 d_step 移動，不是只隨摩擦移動。
    "walk_fast": dict(phase=cpg_max.PHASE_WALK, duty=0.80, omega=1.4, mu_x=1.80,
                      x_off=-0.046, d_step=0.13, g_c=0.08),
    # trot 的 x_off 沒有重掃 —— 它的指標是混沌的，掃了也選不出東西。
    "trot": dict(phase=cpg_max.PHASE_TROT, duty=0.50, omega=3.0, mu_x=1.80,
                 x_off=-0.050, d_step=0.10, g_c=0.08),
}
MU_Y = gb.MU_Y          # → fy = 0，直線走路不需要橫擺（不歸零是側偏的主因）
D_STEP_Y = gb.D_STEP_Y  # 橫擺尺度。ABAD 力臂約 0.41 m（D1 EDU 只有 0.22），比 task6 寬鬆
SETTLE_S = 1.5    # 開走前先站穩。這台 41 kg，比 task6 的 0.8 s 需要更久
```

- [ ] **Step 5: 跑測試確認通過**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_gait_baseline.py -q`
Expected: 4 passed

- [ ] **Step 6: 跑既有測試確認沒破壞任何東西**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/ -q`
Expected: 全過（既有 36 項 + 新增 4 項）

- [ ] **Step 7: Commit**

```bash
git add task7/inference/gait_baseline.py task7/inference/cpg_walk_max.py task7/tests/test_gait_baseline.py
git commit -m "$(cat <<'EOF'
feat(task7): 凍結 walk 為基準步態，參數集中到 gait_baseline

walk 在 180s x 12 擾動已 0 跌倒，把它從「腳本裡的預設值」升成
「有測試釘住的常數」，RL 才有不會被順手改掉的對照組。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: `Robot` 支援替代模型與位置致動器

**Files:**
- Modify: `task7/inference/cpg_walk_max.py:152-241`（`_model`、`Robot.__init__`、`Robot.step`）、`:328-347`（`rollout` 簽章）
- Test: `task7/tests/test_mjx_model.py`（本 Task 只加 `Robot` 相關的兩個測試，模型產生的測試在 Task 3）

**Interfaces:**
- Consumes: Task 1 的 `gait_baseline`
- Produces:
  - `cpg_walk_max._model(scene: str | None = None) -> mujoco.MjModel`（**按 scene 路徑分別快取**）
  - `cpg_walk_max.Robot(..., scene: str | None = None, actuator_mode: str = "torque_pd", solver_iters: tuple[int, int] | None = None)`
  - `cpg_walk_max.rollout(..., scene=None, actuator_mode="torque_pd", solver_iters=None)`

- [ ] **Step 1: 寫失敗測試**

建立 `task7/tests/test_mjx_model.py`：

```python
"""MJX 訓練模型與 Robot 的替代模型支援。"""
import sys
from pathlib import Path

import numpy as np
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "inference"))

import cpg_walk_max as cw  # noqa: E402
import max_model as mm  # noqa: E402


def test_model_cache_same_scene_is_shared():
    """同一個場景要拿到同一份模型（快取是擋 OOM 的，不是微優化）。

    跨場景的隔離另外由 `test_model_cache_is_per_scene` 驗（需要 MJX 場景，在 Task 3）。
    """
    a = cw._model()
    b = cw._model(mm.SCENE)
    assert a is b


def test_solver_iters_override_is_restored():
    """solver 迭代數覆寫必須在下一次建 Robot 時還原，否則會滲進後續 rollout。

    這是 `--friction 0.3` 滲透那個坑的同一類：上一格的設定悄悄留在快取模型上，
    而四個診斷指標全是乾淨的，事後看不出來。
    """
    r1 = cw.Robot(solver_iters=(6, 6))
    assert (r1.m.opt.iterations, r1.m.opt.ls_iterations) == (6, 6)
    r2 = cw.Robot()
    assert (r2.m.opt.iterations, r2.m.opt.ls_iterations) == (100, 50)
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_mjx_model.py -q`
Expected: FAIL，`TypeError: _model() takes 0 positional arguments` 與 `Robot.__init__() got an unexpected keyword argument 'solver_iters'`

- [ ] **Step 3: 改 `_model()` 成按場景快取，並把 solver 選項納入還原清單**

把 `cpg_walk_max.py` 的 `_MODEL / _PRISTINE / _model()` 整段（約 152–164 行）換成：

```python
# ⚠️ 這不是微優化，是**擋當機的**。一次 `make_model()` 要吃掉約 0.9 GB（網格），
#    而 `Robot()` 原本每個 rollout 都重建一次。單一進程掃 30 組會讓 RSS 一路衝到
#    1.6 GB 以上（glibc arena 不還給 OS），平行掃就直接把 16 GB 的機器 OOM 掉
#    —— 2026-08-26 就是這樣把開發機弄當的。
#
# 模型本身在整場掃描裡是**不變的**，會被改寫的只有下面幾個欄位。所以快取一份，
# 每次建 Robot 時**先還原成 MJCF 的原值再套用覆寫** —— 少了「還原」這步，上一次
# `--friction 0.3` 的設定會靜默滲進下一次跑，而且四個診斷指標全是乾淨的。
#
# ★ 快取是**按場景路徑分開**的。MJX 對照要同時用兩個場景（原始網格 / 圓柱），
#   共用單一快取會讓第二個場景**靜默拿到第一個場景的模型**。
_CACHE: dict[str, tuple] = {}


def _model(scene: str | None = None):
    """取得共用模型，並把可被改寫的欄位還原成 MJCF 的原值。"""
    path = scene or mm.SCENE
    if path not in _CACHE:
        m = mujoco.MjModel.from_xml_path(path)
        _CACHE[path] = (m, m.geom_friction.copy(), m.dof_frictionloss.copy(),
                        int(m.opt.iterations), int(m.opt.ls_iterations))
    m, gf, fl, it, ls = _CACHE[path]
    m.geom_friction[:] = gf
    m.dof_frictionloss[:] = fl
    m.opt.iterations, m.opt.ls_iterations = it, ls
    return m
```

- [ ] **Step 4: 改 `Robot.__init__` 接受三個新參數**

把 `Robot.__init__` 的簽章與開頭改成（其餘覆寫邏輯不動）：

```python
    def __init__(self, friction: float = None, wheel_friction: float = None,
                 leg_friction: float = None, abad_friction: float = None,
                 scene: str = None, actuator_mode: str = "torque_pd",
                 solver_iters: tuple = None):
        self.m = _model(scene)
        self.actuator_mode = actuator_mode
        if solver_iters is not None:
            # MuJoCo 預設 100/50。MJX 沒有提早收斂，迭代數是固定成本，
            # 訓練模型必須調低。已實測對 walk 的影響可忽略
            # （travel 0.1471→0.1467、bounce 16.7→17.1、support 3.204→3.206）。
            self.m.opt.iterations, self.m.opt.ls_iterations = solver_iters
        if friction is not None:
            ...  # 以下原樣不動
```

並在 `self.d = mujoco.MjData(self.m)` 之前加一道契約檢查：

```python
        assert actuator_mode in ("torque_pd", "position"), \
            f"actuator_mode 只能是 torque_pd / position，收到 {actuator_mode!r}"
        # position 模式要求模型的致動器本身是 affine 的位置伺服。
        # ⚠️ biastype 不是 affine 時 `ctrl` 會被當**力矩**直接施加，機器人當場塌掉，
        #    而且不會有任何錯誤訊息（task4 地形版踩過）。
        if actuator_mode == "position":
            bt = self.m.actuator_biastype[mm.LEG_ACT_IDX]
            assert np.all(bt == mujoco.mjtBias.mjBIAS_AFFINE), \
                f"position 模式需要 affine 致動器，實得 biastype={bt}"
```

- [ ] **Step 5: 改 `Robot.step` 支援 position 模式**

把 `Robot.step` 的內迴圈（`for _ in range(self.n_sub):` 整段）改成：

```python
        if self.actuator_mode == "position":
            # 模型內建的 position 致動器**每個物理步**自己算 PD，
            # 也就是 500 Hz —— 剛好等於原廠 controller_dt。我們只要把目標角放進 ctrl。
            # 輪子用 velocity 致動器，目標速度恆 0 ＝ 純阻尼，與 torque_pd 的 damp 等價。
            d.ctrl[mm.LEG_ACT_IDX] = q_des
            d.ctrl[mm.WHEEL_ACT_IDX] = 0.0
            for _ in range(self.n_sub):
                mujoco.mj_step(m, d)
                # 飽和診斷改用致動器實際出力（position 模式下我們算不到 tau）
                self.n_tau += int(np.sum(
                    np.abs(d.actuator_force[mm.LEG_ACT_IDX]) > self.tau_max - 1e-6))
                self.n_tau_tot += 12
            return

        for _ in range(self.n_sub):
            ...  # 原本的 torque_pd 內迴圈，原樣不動
```

⚠️ `wheel_mode` 在 `position` 模式下被忽略（模型已固定成阻尼），
在 `step` 開頭加一行擋掉誤用：

```python
        if self.actuator_mode == "position" and wheel_mode != "damp":
            raise ValueError("position 模式的輪子由模型的 velocity 致動器固定成阻尼，"
                             f"不支援 wheel_mode={wheel_mode!r}")
```

- [ ] **Step 6: 改 `rollout` 把三個參數傳下去**

`rollout` 簽章加三個關鍵字（放在 `quiet` 之前）：

```python
            z_sag: float = None, video: bool = False,
            scene: str = None, actuator_mode: str = "torque_pd",
            solver_iters: tuple = None, quiet: bool = False) -> dict:
```

並把建 `Robot` 那行改成：

```python
    r = Robot(friction=friction, wheel_friction=wheel_friction,
              leg_friction=leg_friction, abad_friction=abad_friction,
              scene=scene, actuator_mode=actuator_mode, solver_iters=solver_iters)
```

在回傳的 `res` 字典裡加兩欄，讓對照數據自己帶著身分（掃描 JSON 事後才判讀得出來）：

```python
        "actuator_mode": actuator_mode,
        "scene": scene or mm.SCENE,
```

- [ ] **Step 7: 跑測試確認通過**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_mjx_model.py task7/tests/ -q`
Expected: 全過

- [ ] **Step 8: 手動確認 torque_pd 路徑數字沒變**

Run:
```bash
conda run --no-capture-output -n rbtdog python -c "
import sys; sys.path.insert(0,'task7/inference')
import cpg_walk_max as cw
r=cw.rollout(gait='walk', secs=20.0, quiet=True)
print('%.4f %.1f %+.3f %.3f'%(r['speed_travel'], r['bounce']*1000, r['pitch_mean'], r['support']))"
```
Expected: `0.1471 16.7 -0.025 3.204`（與重構前逐位相同）

- [ ] **Step 9: Commit**

```bash
git add task7/inference/cpg_walk_max.py task7/tests/test_mjx_model.py
git commit -m "$(cat <<'EOF'
feat(task7): Robot 支援替代場景、位置致動器與 solver 迭代數覆寫

為 MJX 訓練模型的四段式對照鋪路。模型快取改成按場景路徑分開——
共用單一快取會讓第二個場景靜默拿到第一個場景的模型，而診斷指標全是乾淨的。
solver 迭代數也納入還原清單，同一個滲透坑。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: MJX 訓練模型產生器

**Files:**
- Create: `task7/model/zgws/make_mjx_model.py`
- Create（產生物，進版控）: `task7/model/zgws/zgws_mjx.xml`、`task7/model/zgws/scene_flat_mjx.xml`
- Modify: `task7/tests/test_mjx_model.py`（加模型不變量測試）

**Interfaces:**
- Consumes: `task7/model/zgws/zgws.xml`（唯讀）、`max_model.WHEEL_RADIUS`
- Produces: `make_mjx_model.build(src: str, dst: str) -> dict`（回傳替換摘要，供測試與文件引用）、
  `max_model.SCENE_MJX`（新常數，指向 `scene_flat_mjx.xml`）

**背景（為什麼要有這個 Task）：** 官方 MJCF 的碰撞幾何是網格，`BASE_LINK` **98,569 頂點**、
四個輪各 **31,730 頂點**。MJX 的 plane–convex 碰撞是逐頂點算的，2048 個平行環境會 OOM。
MuJoCo 自己在 `mjx.put_model` 時就會警告 coplanar face 超過 20 頂點。
已確認 MJX 的碰撞函式表支援 `(PLANE, CYLINDER)`。

- [ ] **Step 1: 寫失敗測試**

在 `task7/tests/test_mjx_model.py` 追加：

```python
MJX_XML = Path(__file__).resolve().parents[1] / "model" / "zgws" / "zgws_mjx.xml"
MJX_SCENE = Path(__file__).resolve().parents[1] / "model" / "zgws" / "scene_flat_mjx.xml"


@pytest.fixture(scope="module")
def pair():
    import mujoco
    a = mujoco.MjModel.from_xml_path(mm.SCENE)
    b = mujoco.MjModel.from_xml_path(str(MJX_SCENE))
    return a, b


def test_model_cache_is_per_scene():
    """兩個場景必須拿到不同的模型物件。

    `_model()` 原本只快取一份。加了第二個場景之後若沿用單一快取，
    第二次呼叫會**靜默拿到上一個場景的模型** —— 而所有診斷指標都會是乾淨的，
    G1 對照會變成「同一個模型跟自己比」而看起來完美通過。
    """
    a = cw._model(mm.SCENE)
    b = cw._model(mm.SCENE_MJX)
    assert a is not b
    assert a is cw._model(mm.SCENE)      # 快取沒有被第二個場景擠掉


def test_mjx_model_has_no_mesh(pair):
    """訓練模型必須零網格相依。

    網格要靠 fetch_assets.sh 從官方發布包抓 2.1 GB 才拿得到，
    Colab 每次開機都要重抓。拿掉網格之後 XML 自己就是完整模型，clone 完即可用。
    """
    _, b = pair
    assert b.nmesh == 0


def test_mass_and_inertia_unchanged(pair):
    """換碰撞幾何**不可以**動到質量與慣量。

    每個 body 都有明寫的 <inertial>，所以 geom 的 density 本來就不參與計算——
    但這是一個「以為如此」的推論，必須變成會失敗的測試。
    """
    a, b = pair
    np.testing.assert_allclose(a.body_mass, b.body_mass, rtol=0, atol=0)
    np.testing.assert_allclose(a.body_inertia, b.body_inertia, rtol=0, atol=0)
    np.testing.assert_allclose(a.body_ipos, b.body_ipos, rtol=0, atol=0)


def test_joints_identical(pair):
    """關節數量、限位、frictionloss、armature 一項都不能變。"""
    a, b = pair
    assert (a.nq, a.nv) == (b.nq, b.nv)
    np.testing.assert_allclose(a.jnt_range, b.jnt_range)
    np.testing.assert_allclose(a.dof_frictionloss, b.dof_frictionloss)
    np.testing.assert_allclose(a.dof_armature, b.dof_armature)


def test_actuators_are_affine_servos():
    """12 腿是 position、4 輪是 velocity，且全部 affine。

    biastype 不是 affine 時 ctrl 會被當力矩直接施加，機器人當場塌掉且不報錯。
    """
    import mujoco
    b = mujoco.MjModel.from_xml_path(str(MJX_SCENE))
    assert b.nu == 16
    leg = b.actuator_biasprm[mm.LEG_ACT_IDX]
    wheel = b.actuator_biasprm[mm.WHEEL_ACT_IDX]
    assert np.all(b.actuator_biastype == mujoco.mjtBias.mjBIAS_AFFINE)
    # position 致動器：biasprm = [0, -kp, -kv]
    np.testing.assert_allclose(-leg[:, 1], np.tile(mm.KP3, 4))
    np.testing.assert_allclose(-leg[:, 2], np.tile(mm.KD3, 4))
    # velocity 致動器：biasprm = [0, 0, -kv]
    np.testing.assert_allclose(-wheel[:, 2], np.full(4, mm.KD_WHEEL))


def test_wheel_collision_is_cylinder_with_measured_radius(pair):
    """四輪碰撞體是圓柱，半徑用實算的 0.0961 m。

    規格書寫 0.09、原廠設定檔 feet_z_offset 寫 0.095、碰撞網格實算 0.0961。
    取實算值，因為觸地判定要跟模擬裡真的碰到地板的那個面一致。
    """
    import mujoco
    _, b = pair
    cyl = [i for i in range(b.ngeom)
           if b.geom_type[i] == mujoco.mjtGeom.mjGEOM_CYLINDER
           and (b.geom_contype[i] or b.geom_conaffinity[i])]
    assert len(cyl) == 4
    for i in cyl:
        assert abs(b.geom_size[i][0] - mm.WHEEL_RADIUS) < 1e-3


def test_mjx_model_loads_into_mjx():
    """能被 mjx.put_model 吃下去，且不再有網格碰撞體。"""
    import mujoco
    from mujoco import mjx
    b = mujoco.MjModel.from_xml_path(str(MJX_SCENE))
    sys_ = mjx.put_model(b)
    assert sys_ is not None
    ncoll_mesh = sum(1 for i in range(b.ngeom)
                     if b.geom_type[i] == mujoco.mjtGeom.mjGEOM_MESH
                     and (b.geom_contype[i] or b.geom_conaffinity[i]))
    assert ncoll_mesh == 0


def test_generator_is_reproducible(tmp_path):
    """重跑產生器要得到逐字元相同的檔案 —— 產生物才可以進版控。"""
    sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "model" / "zgws"))
    import make_mjx_model
    out = tmp_path / "zgws_mjx.xml"
    make_mjx_model.build(str(make_mjx_model.SRC), str(out))
    assert out.read_text() == MJX_XML.read_text()
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_mjx_model.py -q`
Expected: FAIL，找不到 `scene_flat_mjx.xml`

- [ ] **Step 3: 寫產生器 `task7/model/zgws/make_mjx_model.py`**

```python
#!/usr/bin/env python3
"""從官方 `zgws.xml` 產生 MJX 可訓練的模型 `zgws_mjx.xml`。

## 為什麼需要這支

官方 MJCF 的碰撞幾何是網格，實測：

| 碰撞 geom | 頂點數 |
|---|---|
| `BASE_LINK` | **98,569** |
| 四個 `*_FOOT_LINK`（輪） | 各 **31,730** |

MJX 的 plane–convex 碰撞是**逐頂點**計算的。2048 個平行環境 × 98,569 頂點，
單一中間張量就是數百 MB —— Colab GPU 上不是慢到不能用就是 OOM。
`mjx.put_model` 本身雖然過得去（實測 2.6 s），MuJoCo 自己就會警告
`coplanar face with more than 20 vertices ... may lead to performance issues`。

**現有 CPG 模擬跑得動不能當作 MJX 可行的證據** —— 那是 CPU MuJoCo、單一環境。

## 改了哪三件事

1. **碰撞幾何**：四輪網格 → 圓柱（半徑取實算的 `WHEEL_RADIUS`，半寬與中心由
   編譯後的 `geom_aabb` 實算，不手填）；`BASE_LINK` 網格 → 方塊（同樣由 aabb 實算）。
   已確認 MJX 的碰撞函式表支援 `(PLANE, CYLINDER)`。
2. **致動器**：16 個 `<motor>` → 12 個 `<position kp kv>` ＋ 4 個 `<velocity kv>`。
   `<position>` 每個**物理步**（500 Hz）算一次 PD，剛好等於原廠 `controller_dt`；
   若改在控制步（50 Hz）算，kp=120 會是完全不同的系統。
3. **拿掉所有網格**：視覺網格（`contype=0 conaffinity=0 density=0`）一併移除，
   連 `<asset><mesh>` 也不留。這樣 XML 自己就是完整模型，**Colab clone 完即可用**，
   不必為了 54 MB 的 STL 去抓 2.1 GB 的官方發布包。

## 不改的事

質量、慣量、關節限位、`frictionloss`、`armature`、site、sensor 全部原樣。
每個 body 都有明寫的 `<inertial>`，所以換 geom 不影響質量分佈 ——
這一點由 `test_mass_and_inertia_unchanged` 釘死，不靠推論。

## 用法

    conda run --no-capture-output -n rbtdog python task7/model/zgws/make_mjx_model.py

產生物**進版控**（因為沒有網格相依，檔案小且可重現，有 `test_generator_is_reproducible` 釘住）。
"""
import sys
import xml.etree.ElementTree as ET
from pathlib import Path

import mujoco
import numpy as np

HERE = Path(__file__).resolve().parent
SRC = HERE / "zgws.xml"
DST = HERE / "zgws_mjx.xml"
SCENE_SRC = HERE / "scene_flat.xml"
SCENE_DST = HERE / "scene_flat_mjx.xml"

sys.path.insert(0, str(HERE.parents[1] / "inference"))
from max_model import (KD3, KD_WHEEL, KP3, LEGS, PREFIX,  # noqa: E402
                       TAU_MAX3, TAU_MAX_WHEEL, WHEEL_RADIUS)

# MJX 沒有提早收斂，solver 迭代數是固定成本。MuJoCo 預設 100/50 在 MJX 上是災難。
# 已實測對 walk 的影響可忽略（travel 0.1471→0.1467、bounce 16.7→17.1、support 3.204→3.206）。
SOLVER_ITERATIONS, SOLVER_LS_ITERATIONS = 6, 6

# 圓柱預設軸是 +z，輪關節軸是 +y。繞 x 轉 90° 把 z 帶到 −y（圓柱對稱，正負不影響）。
_Q_Z_TO_Y = "0.7071068 0.7071068 0 0"


def _aabb(m: mujoco.MjModel, mesh_name: str):
    """回傳某個「掛在 body 上、用指定網格」的碰撞 geom 的 (中心, 半邊長)。

    用編譯後的 `geom_aabb` 而不是自己算網格頂點：MuJoCo 編譯時會把網格重新定位到
    自己的參考框，自己算頂點很容易差一個偏移，而那個偏移會變成「輪子埋進地裡 5 mm」
    這種**不會報錯、只會讓步態變鈍**的錯誤。
    """
    mid = mujoco.mj_name2id(m, mujoco.mjtObj.mjOBJ_MESH, mesh_name)
    assert mid >= 0, f"找不到網格 {mesh_name}"
    for i in range(m.ngeom):
        if (m.geom_type[i] == mujoco.mjtGeom.mjGEOM_MESH and m.geom_dataid[i] == mid
                and (m.geom_contype[i] or m.geom_conaffinity[i])):
            a = m.geom_aabb[i]
            return np.array(a[:3]), np.array(a[3:])
    raise AssertionError(f"{mesh_name} 沒有可碰撞的 geom")


def _fmt(v) -> str:
    """XML 數值格式。固定 6 位小數 —— 產生物要進版控，格式必須可重現。"""
    return " ".join(f"{float(x):.6f}" for x in np.atleast_1d(v))


def build(src: str = str(SRC), dst: str = str(DST)) -> dict:
    """讀官方 XML，寫出 MJX 版，回傳替換摘要。"""
    m = mujoco.MjModel.from_xml_path(src)     # 只用來查 aabb，不改它
    tree = ET.parse(src)
    root = tree.getroot()
    summary = {"wheels": [], "base": None, "removed_mesh_geoms": 0}

    # --- 1. 拿掉整個 <asset>（裡面只有 mesh） ---
    for asset in root.findall("asset"):
        root.remove(asset)

    # --- 2. 走訪所有 body，替換 mesh geom ---
    def walk(parent):
        for body in parent.findall("body"):
            for g in list(body.findall("geom")):
                if g.get("type") != "mesh":
                    continue
                mesh = g.get("mesh")
                collidable = g.get("contype") != "0"
                body.remove(g)
                summary["removed_mesh_geoms"] += 1
                if not collidable:
                    continue                       # 視覺網格：拿掉就好
                c, h = _aabb(m, mesh)
                if mesh.endswith("_FOOT_LINK"):
                    # 輪：圓柱。半徑用實算的 WHEEL_RADIUS（不是 aabb 的 x/z），
                    # 半寬用 aabb 的 y。
                    new = ET.SubElement(body, "geom")
                    new.set("name", f"{mesh}_COLL")
                    new.set("type", "cylinder")
                    new.set("size", _fmt([WHEEL_RADIUS, h[1]]))
                    new.set("pos", _fmt(c))
                    new.set("quat", _Q_Z_TO_Y)
                    new.set("rgba", "1 1 1 1")
                    summary["wheels"].append(
                        {"mesh": mesh, "radius": WHEEL_RADIUS, "half_width": float(h[1]),
                         "pos": c.tolist(), "aabb_half": h.tolist()})
                else:
                    # 機身：方塊
                    new = ET.SubElement(body, "geom")
                    new.set("name", f"{mesh}_COLL")
                    new.set("type", "box")
                    new.set("size", _fmt(h))
                    new.set("pos", _fmt(c))
                    new.set("rgba", "1 1 1 1")
                    summary["base"] = {"mesh": mesh, "half": h.tolist(), "pos": c.tolist()}
            walk(body)

    for wb in root.findall("worldbody"):
        walk(wb)

    # --- 3. 換致動器 ---
    for act in root.findall("actuator"):
        root.remove(act)
    act = ET.SubElement(root, "actuator")
    for k, leg in enumerate(LEGS):
        p = PREFIX[leg]
        for j, (kp, kd, tau) in enumerate(zip(KP3, KD3, TAU_MAX3)):
            e = ET.SubElement(act, "position")
            e.set("name", f"{p}_{('ABAD', 'HIP', 'KNEE')[j]}_LINK")
            e.set("joint", f"{p}_{('ABAD', 'HIP', 'KNEE')[j]}_JOINT")
            e.set("kp", f"{kp:.1f}")
            e.set("kv", f"{kd:.1f}")
            e.set("forcerange", f"{-tau:.1f} {tau:.1f}")
        e = ET.SubElement(act, "velocity")
        e.set("name", f"{p}_FOOT_LINK")
        e.set("joint", f"{p}_FOOT_JOINT")
        e.set("kv", f"{KD_WHEEL:.1f}")
        e.set("forcerange", f"{-TAU_MAX_WHEEL:.1f} {TAU_MAX_WHEEL:.1f}")

    # --- 4. solver 選項 ---
    opt = root.find("option")
    if opt is None:
        opt = ET.Element("option")
        root.insert(1, opt)
    opt.set("iterations", str(SOLVER_ITERATIONS))
    opt.set("ls_iterations", str(SOLVER_LS_ITERATIONS))

    # --- 5. 寫檔 ---
    root.set("model", "zgws_mjx")
    ET.indent(tree, space="  ")
    header = ("<!--\n"
              "  ⚠️ 這個檔案是產生的，不要手改。\n"
              "  產生器：task7/model/zgws/make_mjx_model.py（跑它會逐字元覆蓋本檔）\n"
              "  來源：  task7/model/zgws/zgws.xml（官方 MATRiX v0.1.2 原檔）\n"
              "  用途：  MJX / Colab 訓練。碰撞網格換成原始形狀、力矩致動器換成位置伺服、\n"
              "          完全不依賴 STL —— clone 完就能用，不必抓 2.1 GB 發布包。\n"
              "  差異的量化對照見 task7/docs/MJX模型對照_2026-08-27.md。\n"
              "-->\n")
    Path(dst).write_text(header + ET.tostring(root, encoding="unicode") + "\n")
    return summary


def build_scene(dst: str = str(SCENE_DST)) -> None:
    """把 scene_flat.xml 的 include 換成 MJX 版，其餘原樣。"""
    text = SCENE_SRC.read_text()
    text = text.replace('<include file="zgws.xml"/>', '<include file="zgws_mjx.xml"/>')
    text = text.replace("zgws flat scene", "zgws flat scene (mjx)")
    Path(dst).write_text(
        "<!-- ⚠️ 產生檔，不要手改。產生器：make_mjx_model.py -->\n" + text)


if __name__ == "__main__":
    s = build()
    build_scene()
    print(f"[產生] {DST}")
    print(f"  移除網格 geom {s['removed_mesh_geoms']} 個")
    for w in s["wheels"]:
        print(f"  輪 {w['mesh']:>16}  半徑 {w['radius']:.4f}  半寬 {w['half_width']:.4f} m")
    b = s["base"]
    print(f"  機身 {b['mesh']}  半邊長 {[round(v, 4) for v in b['half']]} m")
    print(f"[產生] {SCENE_DST}")
```

- [ ] **Step 4: 跑產生器**

Run: `conda run --no-capture-output -n rbtdog python task7/model/zgws/make_mjx_model.py`
Expected: 印出「移除網格 geom 22 個」、四個輪的半徑 0.0961 與半寬、機身半邊長，以及兩個產生檔路徑。

- [ ] **Step 5: 在 `max_model.py` 加 `SCENE_MJX` 常數**

在 `SCENE = str(_MODEL_DIR / "scene_flat.xml")` 之後加：

```python
# MJX / Colab 訓練用的場景。碰撞網格換成原始形狀、致動器換成位置伺服、無 STL 相依。
# ⚠️ 它與 SCENE **不是同一個物理模型**，差異的量化對照見
#    `task7/docs/MJX模型對照_2026-08-27.md`。引用數字時要標明是哪一個。
SCENE_MJX = str(_MODEL_DIR / "scene_flat_mjx.xml")
```

- [ ] **Step 6: 跑測試確認通過**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_mjx_model.py -q`
Expected: 全過（9 項）

如果 `test_mass_and_inertia_unchanged` 失敗，**不要改容差** —— 那代表某個 body 沒有明寫
`<inertial>`、質量真的被 geom density 改掉了，必須回頭在該 body 補 `<inertial>` 抄原值。

- [ ] **Step 7: 檢查有沒有多出寄生接觸**

機身方塊比原本的網格凸包**胖**，可能與 hip/knee 的方塊產生原本不存在的接觸。

Run:
```bash
conda run --no-capture-output -n rbtdog python -c "
import sys; sys.path.insert(0,'task7/inference')
import numpy as np, mujoco, max_model as mm, cpg_walk_max as cw
for scene, mode in ((mm.SCENE,'torque_pd'), (mm.SCENE_MJX,'position')):
    r = cw.Robot(scene=scene, actuator_mode=mode)
    import cpg_max, leg_kin
    ks, f0 = leg_kin.knee_sign_of(mm.HOME), leg_kin.home_foot(mm.HOME)
    q = cpg_max.stand_targets(ks, f0, -0.040)
    r.reset_standing(q, mm.NOMINAL_HEIGHT_KIN+0.005)
    for _ in range(150): r.step(q)
    pairs = set()
    for i in range(r.d.ncon):
        c = r.d.contact[i]
        pairs.add(tuple(sorted((mujoco.mj_id2name(r.m, mujoco.mjtObj.mjOBJ_BODY, r.m.geom_bodyid[c.geom1]) or 'world',
                                mujoco.mj_id2name(r.m, mujoco.mjtObj.mjOBJ_BODY, r.m.geom_bodyid[c.geom2]) or 'world'))))
    print(scene.split('/')[-1], 'ncon', r.d.ncon, sorted(pairs))"
```
Expected: 兩邊的接觸對**都只有 world ↔ 四個 `*_FOOT_LINK`**。
若 MJX 版多出 `base_link ↔ *_KNEE_LINK` 之類的對，在產生器的 Step 3 之後加：

```python
    # 機身方塊是網格凸包的外接盒，比原形狀胖，可能與腿的方塊產生原本沒有的接觸。
    # 這些對在原始模型裡不存在，留著會讓步態多出一組憑空的約束力。
    ct = ET.SubElement(root, "contact")
    for leg in LEGS:
        for part in ("HIP", "KNEE"):
            ex = ET.SubElement(ct, "exclude")
            ex.set("body1", "base_link")
            ex.set("body2", f"{PREFIX[leg]}_{part}_LINK")
```

然後重跑 Step 4、6、7。

- [ ] **Step 8: Commit**

```bash
git add task7/model/zgws/make_mjx_model.py task7/model/zgws/zgws_mjx.xml \
        task7/model/zgws/scene_flat_mjx.xml task7/inference/max_model.py \
        task7/tests/test_mjx_model.py
git commit -m "$(cat <<'EOF'
feat(task7): MJX 訓練模型產生器 —— 官方 MJCF 不能直接餵 MJX

碰撞網格 BASE_LINK 98,569 頂點、四輪各 31,730 頂點，MJX 的 plane-convex
是逐頂點算的，2048 環境會 OOM。換成圓柱與方塊、致動器換成 affine 位置伺服、
順便拿掉全部 STL 相依（Colab clone 完即可用，不必抓 2.1 GB 發布包）。

質量與慣量不變由測試釘死，不靠推論。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: G1 對照 —— 量出模擬對模擬的落差

**Files:**
- Modify: `task7/inference/cpg_sweep_max.py`（`build_plan` 加 `g1`、`--plan` 的 choices）
- Create: `task7/docs/MJX模型對照_2026-08-27.md`
- Create（輸出）: `task7/outputs/sweep_g1_20s.json`

**Interfaces:**
- Consumes: Task 2 的 `rollout(scene=, actuator_mode=, solver_iters=)`、Task 3 的 `max_model.SCENE_MJX`

**為什麼是四段而不是 A/B：** 訓練模型一次改了三件事（碰撞形狀、致動器、solver 迭代數）。
直接 A/B 只會得到「有差」或「沒差」，差了也不知道是哪一項造成的。
四段式讓每一步只動一個變因。

- [ ] **Step 1: 在 `build_plan` 加 `g1` 計畫**

在 `build_plan` 的 `if name == "drift":` 之前插入：

```python
    if name == "g1":
        # ★ MJX 訓練模型的落差量測。四段各只動一個變因：
        #   A  原始網格 + 迴圈內 PD + 預設 solver      ← 基準（既有全部結論的來源）
        #   B1 圓柱/方塊 + 迴圈內 PD + 預設 solver      ← 只換碰撞形狀
        #   B2 圓柱/方塊 + 模型內位置伺服 + 預設 solver  ← 再換致動器
        #   B3 圓柱/方塊 + 模型內位置伺服 + 6/6 solver   ← 訓練實際要用的模型
        # 直接 A/B 只會得到「有差」，差了也不知道是三項裡的哪一項。
        import max_model as _mm
        P.append(("G1/模型", "A 原始網格", dict(S, gait="walk")))
        P.append(("G1/模型", "B1 圓柱+外部PD",
                  dict(S, gait="walk", scene=_mm.SCENE_MJX, actuator_mode="torque_pd")))
        P.append(("G1/模型", "B2 圓柱+位置伺服",
                  dict(S, gait="walk", scene=_mm.SCENE_MJX, actuator_mode="position")))
        P.append(("G1/模型", "B3 訓練模型",
                  dict(S, gait="walk", scene=_mm.SCENE_MJX, actuator_mode="position",
                       solver_iters=(6, 6))))
```

並把 `--plan` 的 `choices` 加上 `"g1"`。

⚠️ `_run()` 會對 `x_off` 加擾動，它讀的是 `cw.GAITS[kw["gait"]]["x_off"]`，
四段共用同一個 `gait="walk"`，所以四段的擾動序列一致、可逐 seed 對照。**不要改這裡。**

- [ ] **Step 2: 跑 G1**

Run:
```bash
conda run --no-capture-output -n rbtdog python task7/inference/cpg_sweep_max.py --plan g1 --seeds 12
```
Expected: 印出四列（每列 12 擾動的中位數與全距），輸出 `task7/outputs/sweep_g1_20s.json`。
預估牆鐘時間 4 × 12 × 3.5 s ÷ worker 數，約 1–3 分鐘。

- [ ] **Step 3: 判讀，寫 `task7/docs/MJX模型對照_2026-08-27.md`**

文件必須包含：

1. **§1 為什麼要換模型**：碰撞網格頂點數表（98,569 / 31,730 × 4）、MuJoCo 的 coplanar 警告原文、
   「CPU 單環境跑得動不等於 MJX 可行」這句。
2. **§2 四段對照表**：`n_fell`、`speed_travel`（含全距）、`bounce`、`pitch_mean`、`support`、`min_lift`。
   **不要列偏航** —— 它在 12 擾動下全距 3.6–11.7°，佔訊號 20–60%，拿來比對是在比雜訊。
3. **§3 判定**：對照 §7 的通過條件（跌倒數全 0；其餘四項差異 ≤ ±15%）。
   **逐項寫出實際百分比**，不要只寫「通過」。
4. **§4 每一段各自貢獻了多少差異**：B1−A、B2−B1、B3−B2 三個增量。
5. 若有任何一項超出 ±15%：**不要放寬容差**，在文件寫下超出的項目與可能原因，
   並停下來回報，不要進 Task 5。

- [ ] **Step 4: Commit**

```bash
git add task7/inference/cpg_sweep_max.py task7/outputs/sweep_g1_20s.json \
        task7/docs/MJX模型對照_2026-08-27.md
git commit -m "$(cat <<'EOF'
feat(task7): G1 四段對照 —— 量出 MJX 訓練模型與原始模型的落差

四段各只動一個變因（碰撞形狀 / 致動器 / solver 迭代數），
直接 A/B 差了也不知道是哪一項造成的。偏航不列入判準（雜訊佔 20-60%）。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 觀測層 `obs_max.py`

**Files:**
- Create: `task7/inference/obs_max.py`
- Test: `task7/tests/test_obs_max.py`

**Interfaces:**
- Consumes: `max_model`（`LEG_QPOS_IDX`、`LEG_QVEL_IDX`、`HOME12`）、`cpg_max.w2b`
- Produces: `obs_max.OBS_LAYOUT`（list of `(name, dim)`）、`obs_max.OBS_DIM = 68`、
  `obs_max.ACT_DIM = 12`、`obs_max.build_obs(d, c, cmd, last_a) -> np.ndarray(68, float32)`、
  `obs_max.slice_of(name) -> slice`

- [ ] **Step 1: 寫失敗測試**

建立 `task7/tests/test_obs_max.py`：

```python
"""68 維觀測層。

觀測層是 sim2real 最容易靜默壞掉的地方：`np.concatenate` 對長度錯誤的輸入
不會報錯，會安靜產生錯誤維度的 obs，而錯誤維度的 obs 會讓訓練好的權重直接失效。
"""
import sys
from pathlib import Path

import numpy as np
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "inference"))

import cpg_max  # noqa: E402
import max_model as mm  # noqa: E402
import obs_max  # noqa: E402


def test_layout_sums_to_dim():
    assert sum(d for _, d in obs_max.OBS_LAYOUT) == obs_max.OBS_DIM == 68


def test_layout_order_is_frozen():
    """欄位順序改了，舊權重就報廢。改順序必須是一個刻意的、會失敗的決定。"""
    assert obs_max.OBS_LAYOUT == [
        ("gravity", 3), ("gyro", 3), ("joint_pos", 12), ("joint_vel", 12),
        ("cmd", 2), ("last_action", 12), ("cpg", 24)]


def test_slice_of():
    assert obs_max.slice_of("gravity") == slice(0, 3)
    assert obs_max.slice_of("cmd") == slice(30, 32)
    assert obs_max.slice_of("cpg") == slice(44, 68)


def _fake_data():
    import mujoco
    m = mm.make_model()
    d = mujoco.MjData(m)
    d.qpos[mm.LEG_QPOS_IDX] = mm.HOME12
    d.qpos[3:7] = [1.0, 0.0, 0.0, 0.0]
    mujoco.mj_forward(m, d)
    return d


def test_build_obs_dim_and_dtype():
    d = _fake_data()
    c = cpg_max.cpg_init(cpg_max.PHASE_WALK)
    o = obs_max.build_obs(d, c, np.array([0.15, 0.0]), np.zeros(12))
    assert o.shape == (68,)
    assert o.dtype == np.float32


def test_gravity_is_down_when_level():
    """機身水平時重力向量應為 (0, 0, -1)。四元數順序弄錯這一條會當場失敗。"""
    d = _fake_data()
    c = cpg_max.cpg_init(cpg_max.PHASE_WALK)
    o = obs_max.build_obs(d, c, np.array([0.0, 0.0]), np.zeros(12))
    np.testing.assert_allclose(o[obs_max.slice_of("gravity")],
                               [0.0, 0.0, -1.0], atol=1e-6)


def test_joint_pos_is_zero_at_home():
    """HOME 姿態時 joint_pos 應全 0（它是相對 HOME12 的偏差）。"""
    d = _fake_data()
    c = cpg_max.cpg_init(cpg_max.PHASE_WALK)
    o = obs_max.build_obs(d, c, np.array([0.0, 0.0]), np.zeros(12))
    np.testing.assert_allclose(o[obs_max.slice_of("joint_pos")], np.zeros(12), atol=1e-9)


def test_wrong_cmd_size_is_rejected():
    """維度錯必須當場擋下來，不可以讓 concatenate 靜默產生 67 或 69 維。"""
    d = _fake_data()
    c = cpg_max.cpg_init(cpg_max.PHASE_WALK)
    with pytest.raises(AssertionError):
        obs_max.build_obs(d, c, np.array([0.1, 0.0, 0.0]), np.zeros(12))
    with pytest.raises(AssertionError):
        obs_max.build_obs(d, c, np.array([0.1, 0.0]), np.zeros(16))


def test_leg_indices_are_not_contiguous():
    """釘住「腿關節位址不連續」這件事本身 —— 有人改成 qpos[7:19] 會在這裡失敗。"""
    assert list(mm.LEG_QPOS_IDX) != list(range(7, 19))
    assert set(mm.LEG_QPOS_IDX).isdisjoint(set(mm.WHEEL_QPOS_IDX))
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_obs_max.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'obs_max'`

- [ ] **Step 3: 寫 `task7/inference/obs_max.py`**

```python
"""D1 Max 的 68 維 observation —— Colab 訓練端與本機推論端的**共用定義**。

## 設計約束：每一欄都必須是實機底層真的拿得到的量

實機的路徑是「凍結 `mc_ctrl` → 直接讀寫 `/dev/shm`」，高層 SDK 在那之後就死了。

| 欄位 | 維度 | 實機來源 | 驗證程度 |
|---|---|---|---|
| `gravity` | 3 | `/dev/shm/imu_central` quat（**xyzw**）轉機身系 | ⚠️ 見下 |
| `gyro` | 3 | `/dev/shm/imu_central` gyro | ⚠️ 見下 |
| `joint_pos` | 12 | `/dev/shm/joint_state` pos − `HOME12` | ★ 實機驗過是 1 kHz 活的串流 |
| `joint_vel` | 12 | `/dev/shm/joint_state` vel | ★ 同上 |
| `cmd` | 2 | `(vx, wz)`，自產 | — |
| `last_action` | 12 | 自存 | — |
| `cpg` | 24 | `rx, rx_d, ry, ry_d, sin θ, cos θ`，自算 | — |

⚠️ **IMU 那兩欄尚未驗證**：`imu_central` 目前只有離線快照解碼過，**沒驗過是不是活的
串流**；而且 `xyzw` 的順序是「取樣當下機身剛好水平」推出來的，不是刻意做的平放實驗。
四元數順序若錯 → 重力向量翻掉 → policy 直接廢掉。
上實機前必須先跑 `task7/docs/現場操作卡_IMU平放複核.md`。

## 刻意不含的三項

1. **機身線速度** —— 底層沒有這個量（高層 SDK 才有，接管後就死了）。
   reward 仍可使用模擬真值速度，因為 reward 只在模擬計算、訓練完就丟棄。
2. **足端觸地布林** —— 沒有感測器。task6 在同型輪足（D1 EDU）上實測四個候選訊號
   全部不可用：0.9 kg 的輪子讓**擺動相**的膝力矩與位置誤差**大於站立相**，訊號方向是反的
   （膝力矩站立相 p05=1.55 N·m、擺動相 p95=8.76 N·m）。
3. **輪子角速度** —— 實機讀得到，但實機從未走過路，輪速在真實地面（地毯 vs 磁磚）
   的行為沒有任何資料可對照，而 policy 又控制不了它（輪子固定阻尼）。
   放進去等於押一個沒驗證過的量。

## 為什麼 cmd 是 2 維而不是 3 維

只放實際會變的兩個量 `(vx, wz)`。側移 `vy` 不在動作範圍內：ABAD 行程只有
−0.697 ~ +0.523 rad 且左右鏡像，本輪不做全向移動。
放一個恆為 0 的欄位只是讓網路多學一個常數。
"""
import numpy as np

from cpg_max import w2b
from max_model import HOME12, LEG_QPOS_IDX, LEG_QVEL_IDX

ACT_DIM = 12          # 每腿 (mux, muy, omega)

# ⚠️ 順序凍結。改順序 = 既有權重全部報廢，必須是一個刻意的決定。
OBS_LAYOUT = [
    ("gravity", 3),
    ("gyro", 3),
    ("joint_pos", 12),
    ("joint_vel", 12),
    ("cmd", 2),
    ("last_action", ACT_DIM),
    ("cpg", 24),
]
OBS_DIM = sum(d for _, d in OBS_LAYOUT)

_DOWN = np.array([0.0, 0.0, -1.0])


def slice_of(name: str) -> slice:
    """某個欄位在 obs 向量裡的位置。給測試與除錯用，推論路徑不需要。"""
    i = 0
    for n, d in OBS_LAYOUT:
        if n == name:
            return slice(i, i + d)
        i += d
    raise KeyError(f"沒有這個欄位：{name}")


def build_obs(d, c: dict, cmd, last_a) -> np.ndarray:
    """組 68 維 observation。`d` 為 `mujoco.MjData`（或具備同名欄位的物件）。

    ⚠️ 入口一定要擋維度。`np.concatenate` 對長度錯誤的輸入**不會報錯**，
       會靜默產生錯誤維度的 obs，而錯誤維度的 obs 會讓訓練好的權重直接失效。
    """
    cmd = np.asarray(cmd, dtype=np.float64).reshape(-1)
    last_a = np.asarray(last_a, dtype=np.float64).reshape(-1)
    assert cmd.size == 2, f"cmd 應為 2 維 (vx, wz)，實得 {cmd.size}"
    assert last_a.size == ACT_DIM, f"last_a 應為 {ACT_DIM} 維，實得 {last_a.size}"

    quat = d.qpos[3:7]
    o = np.concatenate([
        w2b(quat, _DOWN),                  # gravity 3
        d.qvel[3:6],                       # gyro 3
        # ⚠️ 輪關節夾在腿關節中間，位址不連續。`qpos[7:19]` 會把輪角當關節角讀進去，
        #    IK 變奇異矩陣而不報錯（task6 踩過）。
        d.qpos[LEG_QPOS_IDX] - HOME12,     # joint_pos 12
        d.qvel[LEG_QVEL_IDX],              # joint_vel 12
        cmd,                               # cmd 2
        last_a,                            # last_action 12
        c["rx"], c["rx_d"], c["ry"], c["ry_d"],       # cpg 16
        np.sin(c["theta"]), np.cos(c["theta"]),       # cpg 8
    ]).astype(np.float32)
    assert o.size == OBS_DIM, f"obs 應為 {OBS_DIM} 維，實得 {o.size}"
    return o
```

- [ ] **Step 4: 跑測試確認通過**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_obs_max.py -q`
Expected: 9 passed

- [ ] **Step 5: Commit**

```bash
git add task7/inference/obs_max.py task7/tests/test_obs_max.py
git commit -m "$(cat <<'EOF'
feat(task7): 68 維觀測層 —— 每一欄都是實機底層拿得到的量

刻意排除機身線速度（底層沒有）、足端觸地（task6 實測四個候選訊號全不可用，
輪子太重讓擺動相的力矩大於站立相、訊號方向是反的）、輪速（實機零資料可對照）。
IMU 兩欄標明尚未驗證是活串流、xyzw 順序也還沒做平放實驗。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Colab notebook —— 安裝、常數、CPG 的 JAX 版

**Files:**
- Create: `task7/notebooks/cpg_rl_max_colab.ipynb`（本 Task 建到第 6 格）

**Interfaces:**
- Produces（notebook 內的全域名稱，Task 7、8 會用到）：
  `SCENE`、`CTRL_DT`、`SIM_DT`、`N_FRAMES`、`DUTY`、`X_OFF`、`Z_SAG`、`G_C`、`G_P`、
  `D_STEP`、`D_STEP_Y`、`MU_MIN`、`MU_MAX`、`OMEGA_MIN`、`OMEGA_MAX`、`A_CONV`、
  `W_COUP`、`N_CPG_SUB`、`PHASE_WALK`、`HOME_np`、`HOME12_np`、`KNEE_SIGN_np`、
  `F0S_np`、`LEG_QPOS_IDX`、`LEG_QVEL_IDX`、`OBS_DIM`、`ACT_DIM`、
  `cpg_init()`、`cpg_step(c, mux, muy, omega, dt)`、`duty_remap(th, duty)`、
  `foot_targets_j(c)`、`ik_j(k, p, knee_sign)`、`joint_targets_j(c)`

**參照原型：** `task6/notebooks/cpg_rl_d1w_colab.ipynb` 的第 0–5 格。
逐格照抄結構，把下面列出的差異套上去。

- [ ] **Step 1: 建 notebook 骨架與第 0 格（markdown）**

用 `nbformat` 建立空 notebook，第 0 格寫成 markdown，內容必須包含：

- 標題：`# CPG-RL 訓練：智元 D1 Max（中狗 · ZSM-1w） · MJX · Colab GPU`
- **與 task6（D1 EDU）的差異表**，至少含這六列：

| 項目 | task6 D1 EDU | **本檔 D1 Max** |
|---|---|---|
| 訓練模型 | 官方 XML 直接用 | ★ **`zgws_mjx.xml`**（產生物）。官方 XML 的碰撞網格 98,569 頂點，MJX 會 OOM |
| 佔空比 | 沒有 `duty_remap`（等效 0.5） | ★ **`duty=0.80` 必須進 env**。這台 duty ≤0.70 是 **12/12 全跌** |
| IK | home 附近線性化 Jacobian | ★ **解析式**。抬腿 100 mm，線性化誤差會汙染抬腿量這個指標 |
| HOME | 四腿共用一組三元組 | ★ **(4,3) 每腿各自**，前後反號的 X 型 |
| 致動器 | XML 內建 kp=80/kd=1 | `position` kp 60/120/120、kd 1.0；輪 `velocity` kv 0.5 |
| obs | 69 維，cmd 3 | **68 維，cmd 2 = (vx, wz)** |

- **基準對照數字**：`walk` 開迴路 `speed_travel` **0.148 m/s**、偏航 −0.5 ~ −0.9 °/s、
  180 s × 12 擾動 0 跌倒 —— RL 要打敗的是「偏航」那一項，速度是附帶。
- **⚠️ 不要引用 `speed_path`**（高估 68%）。

- [ ] **Step 2: 第 1 格 —— 安裝（照抄 task6 第 1 格，原樣不改）**

版本鎖死的理由與註解**逐字保留**。`os.environ["MUJOCO_GL"] = "egl"` 也保留。

- [ ] **Step 3: 第 2 格 —— 版本斷言（照抄 task6 第 2 格，原樣不改）**

- [ ] **Step 4: 第 3 格 —— clone repo**

照抄 task6 第 3 格，並在後面追加兩行與**一句說明**：

```python
import sys
sys.path.insert(0, f"{DEST}/task7/inference")

# ★ 不需要 fetch_assets.sh。訓練模型 zgws_mjx.xml 完全不依賴 STL
#   （官方模型要 54 MB 網格，而那要從 2.1 GB 的發布包抓）。
SCENE = f"{DEST}/task7/model/zgws/scene_flat_mjx.xml"
import os
assert os.path.exists(SCENE), f"找不到訓練模型 {SCENE}；分支對嗎？"
```

- [ ] **Step 5: 第 4 格 —— 常數**

從 repo 直接 import，**不要在 notebook 裡重打一遍**（重打 = 第二份真實來源）：

```python
import numpy as np
import jax.numpy as jnp

import gait_baseline as gb
import leg_kin
import max_model as mm
import obs_max
from cpg_max import PHASE_WALK

CTRL_DT, SIM_DT = mm.CTRL_DT, mm.SIM_DT      # 0.02 / 0.002
N_FRAMES = int(round(CTRL_DT / SIM_DT))      # = 10（task6 是 5，這台計算量約 2 倍）
A_CONV, W_COUP, N_CPG_SUB = mm.A_CONV, mm.W_COUP, mm.N_CPG_SUB
MU_MIN, MU_MAX = mm.MU_MIN, mm.MU_MAX
G_P = mm.G_P

# ---- 基準步態（凍結在 gait_baseline.py，判準來源也寫在那裡）----
DUTY = gb.BASELINE["duty"]            # 0.80 ⚠️ ≤0.70 是 12/12 全跌，不可以動
X_OFF = gb.BASELINE["x_off"]          # −0.040 m，平均俯仰過零點
G_C = gb.BASELINE["g_c"]              # 0.08
Z_SAG = gb.BASELINE["z_sag"]          # 0.0325，只加在擺動相
D_STEP, D_STEP_Y = gb.BASELINE["d_step"], gb.BASELINE["d_step_y"]

# ---- 動作空間 ----
# ω 上限的依據是 12 擾動掃描：1.8 → 0.316 m/s 但彈跳 +48%、支撐腳 −0.3；
# 2.0 速度反而掉回 0.281。給到 2.0 有餘裕又不會掃進劣化區。
# 混沌區（trot、duty=0.5，速度在 0.11–0.46 m/s 之間跳）因 DUTY 鎖死而不可達。
OMEGA_MIN, OMEGA_MAX = 0.0, 2.0

HOME_np = np.array(mm.HOME)
HOME12_np = np.array(mm.HOME12)
KNEE_SIGN_np = leg_kin.knee_sign_of(mm.HOME)      # 前腿 −1、後腿 +1
F0S_np = leg_kin.home_foot(mm.HOME)               # (4,3) 每腿各自的基準足端
LEG_QPOS_IDX = jnp.array(mm.LEG_QPOS_IDX)
LEG_QVEL_IDX = jnp.array(mm.LEG_QVEL_IDX)
LEG_ACT_IDX = jnp.array(mm.LEG_ACT_IDX)
WHEEL_ACT_IDX = jnp.array(mm.WHEEL_ACT_IDX)
OBS_DIM, ACT_DIM = obs_max.OBS_DIM, obs_max.ACT_DIM   # 68 / 12

print(f"N_FRAMES={N_FRAMES}  OBS_DIM={OBS_DIM}  ACT_DIM={ACT_DIM}  DUTY={DUTY}")
assert (OBS_DIM, ACT_DIM) == (68, 12)
```

- [ ] **Step 6: 第 5 格 —— CPG 與解析式 IK 的 JAX 版**

這一格是本 notebook 與 task6 差最多的地方，全部要新寫：

```python
PHI = jnp.array(PHASE_WALK)[None, :] - jnp.array(PHASE_WALK)[:, None]


def cpg_init():
    return {"rx": jnp.full(4, 1.5), "rx_d": jnp.zeros(4),
            "ry": jnp.full(4, 1.5), "ry_d": jnp.zeros(4),
            "theta": jnp.array(PHASE_WALK)}


def cpg_step(c, mux, muy, omega, dt):
    """與 task7/inference/cpg_max.make_cpg_step 逐行相同，只是換成 jnp。

    ⚠️ 耦合項會把相位拉回 PHI 定義的關係，所以**只改初始相位是無效的**，
       必須連耦合矩陣一起換（task6 的教訓）。
    """
    rx, rxd, ry, ryd, th = c["rx"], c["rx_d"], c["ry"], c["ry_d"], c["theta"]
    h = dt / N_CPG_SUB
    for _ in range(N_CPG_SUB):
        rxd = rxd + A_CONV * (A_CONV / 4.0 * (mux - rx) - rxd) * h
        rx = rx + rxd * h
        ryd = ryd + A_CONV * (A_CONV / 4.0 * (muy - ry) - ryd) * h
        ry = ry + ryd * h
        rbar = 0.5 * (rx + ry)
        diff = th[None, :] - th[:, None] - PHI
        th = th + (2 * jnp.pi * omega
                   + W_COUP * jnp.sum(rbar[None, :] * jnp.sin(diff), 1)) * h
    return {"rx": rx, "rx_d": rxd, "ry": ry, "ry_d": ryd,
            "theta": jnp.mod(th, 2 * jnp.pi)}


def duty_remap(th, duty):
    """相位重映射成「擺動相佔一圈的 (1−duty)、站立相佔 duty」。

    ★ 這一段 task6 的 env **沒有**（等效 duty=0.5）。D1 Max 實測 duty ≤ 0.70
      是 12/12 全跌，照抄 task6 會得到一台永遠站不住的狗。
    """
    ph = jnp.mod(th, 2 * jnp.pi) / (2 * jnp.pi)
    sw = 1.0 - duty
    return jnp.where(ph < sw,
                     jnp.pi * ph / sw,
                     jnp.pi + jnp.pi * (ph - sw) / duty)


def act_to_cmd(a):
    """12 維動作 → 每腿 (mux, muy, omega)。"""
    a = jnp.tanh(a).reshape(4, 3)
    mux = (a[:, 0] + 1) / 2 * (MU_MAX - MU_MIN) + MU_MIN
    muy = (a[:, 1] + 1) / 2 * (MU_MAX - MU_MIN) + MU_MIN
    om = (a[:, 2] + 1) / 2 * (OMEGA_MAX - OMEGA_MIN) + OMEGA_MIN
    return mux, muy, om


# ---- 解析式 IK（與 task7/inference/leg_kin.ik_ex 逐行相同，換成 jnp）----
# ⚠️ 不用 task6 那套 home 附近的線性化 Jacobian。這台抬腿 100 mm、連桿 0.26+0.28 m，
#    線性化誤差會直接變成「我以為抬了 100 mm、實際是別的數字」——
#    而抬腿量正是 reward 的 r_clr 要獎勵的東西，用有偏差的 IK 去量它會自己騙自己。
SIDE_X_j = jnp.array(mm.SIDE_X)
SIDE_Y_j = jnp.array(mm.SIDE_Y)
L_T, L_S = mm.L_THIGH, mm.L_SHANK
A2H_X, A2F_Y = mm.ABAD_TO_HIP_X, mm.ABAD_TO_FOOT_Y
REACH_LO = abs(L_T - L_S) + 1e-6
REACH_HI = L_T + L_S - 1e-6


def ik_j(k, p, knee_sign):
    yp = SIDE_Y_j[k] * A2F_Y
    px, py, pz = p[0], p[1], p[2]
    zp2 = jnp.maximum(px * 0.0 + py * py + pz * pz - yp * yp, 0.0)
    zp = -jnp.sqrt(zp2)
    q1 = jnp.arctan2(yp * pz - zp * py, yp * py + zp * pz)

    xp = px - SIDE_X_j[k] * A2H_X
    r = jnp.sqrt(xp * xp + zp * zp)
    r_new = jnp.clip(r, REACH_LO, REACH_HI)
    scale = jnp.where(r > 1e-12, r_new / r, 1.0)
    xp, zp = xp * scale, zp * scale
    r2 = r_new * r_new

    cos_q3 = (r2 - L_T ** 2 - L_S ** 2) / (2 * L_T * L_S)
    q3 = jnp.sign(knee_sign) * jnp.arccos(jnp.clip(cos_q3, -1.0, 1.0))
    a = -(L_T + L_S * jnp.cos(q3))
    b = -L_S * jnp.sin(q3)
    q2 = jnp.arctan2(a * xp - b * zp, a * zp + b * xp)
    return jnp.array([q1, q2, q3])


F0S_j = jnp.array(F0S_np)
KNEE_SIGN_j = jnp.array(KNEE_SIGN_np)


def joint_targets_j(c):
    """CPG 狀態 → 12 個關節目標角。"""
    th = duty_remap(c["theta"], DUTY)
    fx = 2 * (c["rx"] - MU_MIN) / (MU_MAX - MU_MIN) - 1
    fy = 2 * (c["ry"] - MU_MIN) / (MU_MAX - MU_MIN) - 1
    dx = -D_STEP * fx * jnp.cos(th) + X_OFF
    dy = D_STEP_Y * fy * jnp.cos(th)
    # ★ z_sag 只加在擺動相。站立相就是靠位置伺服的追蹤誤差在出力撐機身的，
    #   站立相也補等於把機身放掉。沒補的後果是「命令抬 100 mm、實際只抬 67 mm」，
    #   而且超限／飽和／IK縮限／相位鎖定四個診斷指標全是乾淨的，完全看不出問題。
    dz = jnp.where(jnp.sin(th) > 0, (G_C + Z_SAG) * jnp.sin(th), G_P * jnp.sin(th))
    tgt = F0S_j + jnp.stack([dx, dy, dz], -1)
    q = jnp.stack([ik_j(k, tgt[k], KNEE_SIGN_j[k]) for k in range(4)])
    return q.reshape(12)
```

- [ ] **Step 7: 驗證 JAX 版與 numpy 版逐點一致**

這是本 notebook 最重要的一個檢查：**JAX 版與已驗證的 numpy 版必須算出同一個東西**，
否則訓練出來的 policy 在本機推論端會是另一回事。在第 5 格結尾追加：

```python
# ★ 對照已驗證的 numpy 版。差異必須小於 1e-6 rad（0.00006°）。
# 沒有這個檢查的話，JAX 移植的任何一個號差錯都會安靜地變成「訓練與推論是兩台機器人」。
import cpg_max as _cm
_c_np = _cm.cpg_init(PHASE_WALK)
_step_np = _cm.make_cpg_step(PHASE_WALK)
_c_j = cpg_init()
for _ in range(37):          # 跑 37 步，把暫態也涵蓋進去
    _c_np = _step_np(_c_np, np.full(4, 1.8), np.full(4, 1.5), np.full(4, 1.4), CTRL_DT)
    _c_j = cpg_step(_c_j, jnp.full(4, 1.8), jnp.full(4, 1.5), jnp.full(4, 1.4), CTRL_DT)
_q_np, _ = _cm.joint_targets(_c_np, F0S_np, X_OFF, G_C, D_STEP, D_STEP_Y, DUTY,
                             KNEE_SIGN_np, Z_SAG)
_q_j = np.asarray(joint_targets_j(_c_j))
_err = np.abs(_q_np - _q_j).max()
print(f"JAX vs numpy 關節角最大差 {_err:.3e} rad")
assert _err < 1e-6, "JAX 版的 CPG/IK 與 numpy 版不一致，訓練與推論會是兩台機器人"
```

- [ ] **Step 8: 本機跑一次前 6 格確認可執行**

Run:
```bash
conda run --no-capture-output -n rbtdog python -c "
import json, sys
sys.path.insert(0, 'task7/inference')
nb = json.load(open('task7/notebooks/cpg_rl_max_colab.ipynb'))
src = '\n'.join(''.join(c['source']) for c in nb['cells'][4:6] if c['cell_type']=='code')
exec(compile(src, 'nb', 'exec'), {'__name__':'__main__'})"
```
Expected: 印出 `N_FRAMES=10 OBS_DIM=68 ACT_DIM=12 DUTY=0.8` 與 `JAX vs numpy 關節角最大差 ...`，
斷言全過。（第 1–3 格是 Colab 專用的安裝與 clone，本機不跑。）

- [ ] **Step 9: Commit**

```bash
git add task7/notebooks/cpg_rl_max_colab.ipynb
git commit -m "$(cat <<'EOF'
feat(task7): CPG-RL notebook 前半 —— 常數、CPG/IK 的 JAX 版

常數一律從 repo import 不重打（重打 = 第二份真實來源）。
duty_remap 是與 task6 最關鍵的差異：task6 的 env 沒有它（等效 duty=0.5），
而這台 duty <= 0.70 是 12/12 全跌。
加了 JAX vs numpy 的逐點對照，否則移植錯號會安靜地變成兩台機器人。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Colab notebook —— env、reward、domain randomization

**Files:**
- Modify: `task7/notebooks/cpg_rl_max_colab.ipynb`（加第 6–8 格）

**Interfaces:**
- Consumes: Task 6 的全域名稱
- Produces: `MaxCpgEnv`（brax `Env` 子類）、`domain_randomize(sys, rng)`、`env`（已實例化）

**參照原型：** `task6/notebooks/cpg_rl_d1w_colab.ipynb` 第 6、7、8 格。
reward v4 的四項懲罰與其**全部說明註解**逐字保留（它們記錄了 v1→v4 每一次踩到的坑），
下面只列必須改的地方。

- [ ] **Step 1: 第 6 格 —— env**

以 task6 第 6 格為底，套上這些差異：

1. **初始站姿**：task6 用 `m.key_qpos[0]`，D1 Max 的 MJCF **`nkey=0`**，沒有 keyframe。
   改成在 env `__init__` 裡用 numpy MuJoCo 先站定 1.5 秒，把結果凍成常數：

```python
    def _settled_qpos(self):
        """先在 CPU MuJoCo 上站定 1.5 秒，把落地後的 qpos 當成 reset 的起點。

        ⚠️ 不能直接用純運動學的 HOME 高度：位置伺服有靜態撓度（41 kg、kp 有限），
           直接放上去會先掉 40 mm，每個 episode 的前 0.5 秒都在處理這個瞬態。
           task7/inference/cpg_walk_max.rollout 也是先 SETTLE_S=1.5 秒才開走。
        """
        import mujoco
        import cpg_max as _cm
        m = mujoco.MjModel.from_xml_path(SCENE)
        d = mujoco.MjData(m)
        q = _cm.stand_targets(KNEE_SIGN_np, F0S_np, X_OFF)
        d.qpos[:3] = [0.0, 0.0, mm.NOMINAL_HEIGHT_KIN + 0.005]
        d.qpos[3:7] = [1.0, 0.0, 0.0, 0.0]
        d.qpos[mm.LEG_QPOS_IDX] = q
        mujoco.mj_forward(m, d)
        for _ in range(int(1.5 / SIM_DT)):
            d.ctrl[mm.LEG_ACT_IDX] = q
            d.ctrl[mm.WHEEL_ACT_IDX] = 0.0
            mujoco.mj_step(m, d)
        assert abs(d.qpos[2] - mm.NOMINAL_HEIGHT) < 0.05, \
            f"站定高度 {d.qpos[2]:.3f} 與預期 {mm.NOMINAL_HEIGHT:.3f} 差太多"
        return jnp.array(d.qpos)
```

   `__init__` 裡 `self._init_q = self._settled_qpos()`。

2. **ctrl 的語意**：task6 的 XML 是位置致動器、`ctrl` 就是目標角；本檔相同，
   但**輪子要另外給 0**：

```python
        ctrl = jnp.zeros(16).at[LEG_ACT_IDX].set(q_des).at[WHEEL_ACT_IDX].set(0.0)
```

3. **`_obs` 改成 68 維**，欄位順序**必須**與 `obs_max.OBS_LAYOUT` 一致：

```python
    def _obs(self, data, c, cmd, last_a, gyro_bias):
        # ⚠️ 欄位順序必須與 task7/inference/obs_max.py 的 OBS_LAYOUT 逐項一致。
        #    本機推論端 local_infer_max.py 用的是那一份。
        quat = data.qpos[3:7]
        grav = w2b(quat, jnp.array([0.0, 0.0, -1.0]))
        gyro = data.qvel[3:6] + gyro_bias
        return jnp.concatenate([
            grav, gyro,
            data.qpos[LEG_QPOS_IDX] - jnp.array(HOME12_np),
            data.qvel[LEG_QVEL_IDX],
            cmd,                       # 2 維 (vx, wz)
            last_a,
            c["rx"], c["rx_d"], c["ry"], c["ry_d"],
            jnp.sin(c["theta"]), jnp.cos(c["theta"]),
        ])
```

   並在 `__init__` 結尾加 `assert self.observation_size == 68`。

4. **指令抽樣**（`reset`）：

```python
        # 60% 的 episode 抽 wz = 0（＝「給我走直」）——開迴路 CPG 繞不過去的
        # 0.4–0.9 °/s 偏航就是 RL 這條線要解的主要問題，走直的樣本必須夠多。
        k_vx, k_wz, k_zero = jax.random.split(k_cmd, 3)
        vx = jax.random.uniform(k_vx, minval=0.05, maxval=0.40)
        wz = jnp.where(jax.random.uniform(k_zero) < 0.6, 0.0,
                       jax.random.uniform(k_wz, minval=-0.4, maxval=0.4))
        cmd = jnp.array([vx, wz])
```

5. **動作延遲**（task6 沒有這一項）：

```python
        # ★ 實機的鏈路是「我們寫 shm → 1 kHz daemon 讀 → 馬達」，一定有延遲，
        #   而 daemon 還有 500 ms 的過期判定。延遲量沒量過，所以用「隨機 0 或 1 個
        #   控制步」把它蓋住，而不是押一個沒根據的定值。
        info["prev_action"] = action                 # step 開頭先存
        act = jnp.where(info["delay"] > 0.5, info["prev_action"], action)
```
   `delay` 在 `reset` 抽一次：`jax.random.bernoulli(k_delay, 0.5).astype(jnp.float32)`。

6. **終止門檻**：

```python
# 低姿護欄。由 task4 Go2 的 0.18 / 0.30 等比例換到 D1 Max 站立高 0.48 m → 0.288，取 0.29。
# 沒有這條的話，「塌腰趴著慢慢挪」是個很好爬的 local optimum。
MIN_HEIGHT = 0.29
```
   翻倒仍用 `mm.FALL_GRAV_Z`（−0.4）。

7. **reward**：保留 task6 v4 的 `W_PITCH / W_PITCHRATE / W_VZ / W_OMEGA_VAR / W_CLR`
   與**全部註解**，並加上偏航率追蹤與側向速度懲罰：

```python
# ---- D1 Max 新增：偏航率追蹤 ----
# 這是本次做 RL 的主要理由。開迴路 CPG 的偏航 0.4–0.9 °/s **調參繞不過去**：
# x_off 配平掃 20 mm 完全不變號、d_step 單調沒有過零點、ω 在 1.8 變號但 2.0 又變回來
# （那是分歧不是趨勢）。所以航向必須由回授來，而 RL 拿得到 gyro。
W_YAW = 1.0          # × exp(−(wz − cmd_wz)² / 0.05)
W_VY = 0.5           # × vy²，側向速度
W_VX = 1.5           # × exp(−(vx − cmd_vx)² / 0.02)
```

   ⚠️ 在這一格結尾用註解明寫：**這些權重是照 D1 EDU（20.6 kg）的量值訂的，
   這台 41 kg，第一輪訓練後必須依 `progress` 印出的實測值重新校準。**

8. **輪子離地淨空**（`r_clr`）：`WHEEL_RADIUS = mm.WHEEL_RADIUS`（0.0961，
   不是 task6 的 0.0710），並保留 task6 那段「reward 可以用上帝視角、obs 不行」的說明。

- [ ] **Step 2: 第 7 格 —— domain randomization**

以 task6 第 7 格為底，改成：

```python
_mm_model = mujoco.MjModel.from_xml_path(SCENE)
BASE_ID = mujoco.mj_name2id(_mm_model, mujoco.mjtObj.mjOBJ_BODY, "base_link")
assert BASE_ID >= 0, "找不到 base_link"
LEG_DOF = np.array(mm.LEG_QVEL_IDX)
WHEEL_DOF = np.array(mm.WHEEL_QVEL_IDX)
KP_NOM = np.tile(np.array(mm.KP3), 4)      # 60/120/120 × 4
KD_NOM = np.tile(np.array(mm.KD3), 4)


def domain_randomize(sys, rng):
    @jax.vmap
    def per_env(rng):
        k1, k2, k3, k4, k5, k6, k7 = jax.random.split(rng, 7)
        # 地面摩擦。掃描實測 0.3 會跌、≥0.5 能走，所以下界取 0.4 —— 讓它學會
        # 在「勉強能走」的地面上走，但不要把整段訓練泡在必跌的條件裡。
        geom_friction = sys.geom_friction.at[:, 0].set(
            jax.random.uniform(k1, minval=0.4, maxval=1.4))

        # PD 增益 ±20%。position 致動器：gainprm[0]=kp、biasprm=[0,−kp,−kv]。
        scale_kp = jax.random.uniform(k2, minval=0.8, maxval=1.2)
        kv = jax.random.uniform(k3, minval=0.5, maxval=2.0)
        kp_leg = jnp.array(KP_NOM) * scale_kp
        gain = sys.actuator_gainprm.at[jnp.array(mm.LEG_ACT_IDX), 0].set(kp_leg)
        bias = (sys.actuator_biasprm
                .at[jnp.array(mm.LEG_ACT_IDX), 1].set(-kp_leg)
                .at[jnp.array(mm.LEG_ACT_IDX), 2].set(-kv))

        # 質量。★ payload 0–5 kg 同時吸收兩件事：官方額定 5 kg 負載，
        #   以及 MJCF 38.8 kg 與規格書 41 kg 之間那 2.2 kg 的缺口（可能是沒算雙電池）。
        body_mass = sys.body_mass * jax.random.uniform(
            k4, (sys.nbody,), minval=0.9, maxval=1.1)
        body_mass = body_mass.at[BASE_ID].add(
            jax.random.uniform(k5, minval=0.0, maxval=5.0))

        # 關節靜摩擦。★ 腿的 1.85/1.5/1.5 之中，ABAD 那個 1.85 掃描已證明是
        #   **下界不是量測值**（正向那次沒有掙脫：速度整段停在量化底噪 +0.001 rad/s、
        #   力矩到最後還在上升）。所以必須用範圍蓋住，不能當定值。
        fl = sys.dof_frictionloss
        fl = fl.at[LEG_DOF].multiply(jax.random.uniform(k6, minval=0.5, maxval=1.5))
        # 輪：兩天兩種條件量到 0.153 / 0.170，取 0.10–0.25。
        fl = fl.at[WHEEL_DOF].set(jax.random.uniform(k7, minval=0.10, maxval=0.25))
        return geom_friction, gain, bias, body_mass, fl

    gf, gain, bias, bm, fl = per_env(rng)
    in_axes = jax.tree_util.tree_map(lambda x: None, sys)
    in_axes = in_axes.replace(geom_friction=0, actuator_gainprm=0,
                              actuator_biasprm=0, body_mass=0, dof_frictionloss=0)
    sys = sys.replace(geom_friction=gf, actuator_gainprm=gain,
                      actuator_biasprm=bias, body_mass=bm, dof_frictionloss=fl)
    return sys, in_axes


print("domain_randomize ready")
```

- [ ] **Step 3: 第 8 格 —— 冒煙測試（照抄 task6 第 8 格並改斷言）**

```python
env = MaxCpgEnv()

# ⚠️ biastype 不是 affine 時 ctrl 會被當力矩直接施加，機器人直接塌掉且不報錯。
assert env.sys.actuator_biastype[0] == mujoco.mjtBias.mjBIAS_AFFINE
assert env.observation_size == 68 and env.action_size == 12

s = jax.jit(env.reset)(jax.random.PRNGKey(0))
print("reset ok, obs", s.obs.shape, "height", float(s.pipeline_state.qpos[2]))
s = jax.jit(env.step)(s, jnp.zeros(12))
print("step ok, reward", float(s.reward), "done", int(s.done))
assert abs(float(s.pipeline_state.qpos[2]) - mm.NOMINAL_HEIGHT) < 0.06, \
    "起步高度不對——站定流程沒生效"
```

- [ ] **Step 4: 本機用 MJX CPU 跑冒煙測試**

Run:
```bash
conda run --no-capture-output -n rbtdog python -c "
import json, sys
sys.path.insert(0,'task7/inference')
nb = json.load(open('task7/notebooks/cpg_rl_max_colab.ipynb'))
cells = [''.join(c['source']) for c in nb['cells'] if c['cell_type']=='code']
src = '\n'.join(cells[3:7])   # 第 4、5、6、7、8 格（跳過安裝與 clone）
g = {'__name__':'__main__', 'DEST':'.'}
exec(compile(src.replace('f\"{DEST}/task7/model/zgws/scene_flat_mjx.xml\"',
     \"'task7/model/zgws/scene_flat_mjx.xml'\"), 'nb', 'exec'), g)"
```
Expected: `reset ok` 與 `step ok` 兩行都印出來，全部斷言通過。
（CPU 上 jit 編譯約 1–3 分鐘，屬正常。）

⚠️ 如果 `reset` 之後高度偏離 `NOMINAL_HEIGHT` 超過 60 mm，**不要調寬斷言**。
那代表 MJX 模型的位置伺服與 CPU MuJoCo 的行為不同，要回頭查 Task 4 的 G1 數據。

- [ ] **Step 5: Commit**

```bash
git add task7/notebooks/cpg_rl_max_colab.ipynb
git commit -m "$(cat <<'EOF'
feat(task7): CPG-RL notebook env + reward + domain randomization

reward 保留 task6 v4 的四項懲罰與全部說明註解，加上偏航率追蹤——
開迴路那 0.4-0.9 度/秒的偏航調參繞不過去，這是做 RL 的主要理由。
DR 新增動作延遲隨機化（task6 沒有）與關節靜摩擦範圍（ABAD 的 1.85 是下界不是量測值）。
MJCF nkey=0，reset 起點改成先在 CPU MuJoCo 站定 1.5 秒。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Colab notebook —— 訓練與匯出

**Files:**
- Modify: `task7/notebooks/cpg_rl_max_colab.ipynb`（加第 9–11 格）

**Interfaces:**
- Consumes: Task 7 的 `env`、`domain_randomize`
- Produces: `params`（PPO 權重）、`cpg_rl_max_params.pkl`

- [ ] **Step 1: 第 9 格 —— 訓練**

以 task6 第 9 格為底，改三處：

```python
train_fn = functools.partial(
    ppo.train, num_timesteps=60_000_000, num_evals=20, episode_length=1000,
    num_envs=2048, batch_size=256, num_minibatches=32, unroll_length=20,
    num_updates_per_batch=4, learning_rate=3e-4, entropy_cost=1e-2,
    discounting=0.97, normalize_observations=True,
    network_factory=network_factory, randomization_fn=domain_randomize, seed=0)
```

`progress` 改成印本檔要校準的量，並**印出實測速率**：

```python
_t0 = time.time(); rewards = []
def progress(step, metrics):
    r = float(metrics.get("eval/episode_reward", 0.0)); rewards.append((step, r))
    L = float(metrics.get("eval/avg_episode_length", 1.0)) or 1.0
    el = time.time() - _t0
    # ⚠️ 這台每控制步 10 個物理步（task6 是 5），每步計算量約 2 倍。
    #    第一個 eval 就看得出實際速率 —— 若 60M 要跑超過 3 小時，砍到 40M。
    rate = step / max(el, 1e-9)
    print(f"step {step:>11,}  reward {r:8.2f}  "
          f"pitch {float(metrics.get('eval/episode_pitch',0))/L:5.2f}°  "
          f"clr {float(metrics.get('eval/episode_clr',0))/L:5.1f}mm  "
          f"vz {float(metrics.get('eval/episode_vz',0))/L:5.3f}  "
          f"yawerr {float(metrics.get('eval/episode_yawerr',0))/L:5.3f}rad/s  "
          f"vxerr {float(metrics.get('eval/episode_vxerr',0))/L:5.3f}m/s  "
          f"({el:.0f}s, {rate/1e3:.0f}k步/s → 60M 約需 {60e6/max(rate,1)/60:.0f} 分)")
```

env 的 `metrics` 必須同時輸出 `pitch / clr / vz / yawerr / vxerr` 五項
（Task 7 第 6 格的 `State.metrics` 要一併補上）。

- [ ] **Step 2: 第 10 格 —— 曲線（照抄 task6 第 10 格，原樣不改）**

- [ ] **Step 3: 第 11 格 —— 匯出**

```python
from brax.io import model
model.save_params("cpg_rl_max_params.pkl", params)
print("已存 cpg_rl_max_params.pkl")
print("放到 repo 的 task7/weights/ 之後，用本機推論端驗收：")
print("  conda run --no-capture-output -n rbtdog python task7/inference/local_infer_max.py \\")
print("      --params task7/weights/cpg_rl_max_params.pkl --secs 20 --video")
try:
    from google.colab import files; files.download("cpg_rl_max_params.pkl")
except Exception as e:
    print("左側檔案面板右鍵下載 cpg_rl_max_params.pkl。", e)
```

- [ ] **Step 4: 檢查 notebook 結構完整**

Run:
```bash
conda run --no-capture-output -n rbtdog python -c "
import json
nb = json.load(open('task7/notebooks/cpg_rl_max_colab.ipynb'))
print('cells', len(nb['cells']))
for i,c in enumerate(nb['cells']):
    print(i, c['cell_type'], len(''.join(c['source'])))
import nbformat; nbformat.validate(nbformat.read('task7/notebooks/cpg_rl_max_colab.ipynb', as_version=4))
print('nbformat OK')"
```
Expected: 12 格、`nbformat OK`

- [ ] **Step 5: Commit**

```bash
git add task7/notebooks/cpg_rl_max_colab.ipynb
git commit -m "$(cat <<'EOF'
feat(task7): CPG-RL notebook 訓練與匯出格

progress 會印實測步率與「60M 約需幾分鐘」——這台每控制步 10 個物理步
（task6 是 5），計算量約 2 倍，第一個 eval 就要能當場決定要不要砍到 40M。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 本機推論端 `local_infer_max.py`

**Files:**
- Create: `task7/inference/local_infer_max.py`
- Test: 追加到 `task7/tests/test_obs_max.py`

**Interfaces:**
- Consumes: `obs_max`、`cpg_max`、`leg_kin`、`max_model`、`gait_baseline`
- Produces: `local_infer_max.run(args) -> dict`（回傳的指標鍵與 `cpg_walk_max.rollout` 對齊：
  `speed_travel / bounce / pitch_mean / support / yaw / fell / min_lift`）

**參照原型：** `task6/inference/local_infer_d1.py`（含它開頭那段「brax 版本不匹配會靜默錯亂」的說明，**逐字保留**）。

- [ ] **Step 1: 寫失敗測試（追加到 `test_obs_max.py`）**

```python
def test_local_infer_dummy_runs():
    """--dummy 模式（固定動作）要能跑完 3 秒且回傳完整指標。

    這條擋的是「推論管線本身壞掉」，與權重好不好無關 ——
    沒有權重的時候也要能驗證管線。
    """
    import argparse
    import local_infer_max
    args = argparse.Namespace(params="", dummy=True, secs=3.0, vx=0.15, wz=0.0,
                              video=False, scene=None)
    r = local_infer_max.run(args)
    for k in ("speed_travel", "bounce", "pitch_mean", "support", "yaw", "fell"):
        assert k in r, f"缺少指標 {k}"


def test_local_infer_uses_mesh_scene_by_default():
    """預設必須跑**原始網格模型**，不是訓練用的圓柱模型。

    訓練模型是為了 MJX 才簡化的。驗收如果也跑簡化模型，等於用同一個近似
    去驗證那個近似 —— 落差永遠量不到。
    """
    import local_infer_max
    assert local_infer_max.DEFAULT_SCENE == mm.SCENE
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/test_obs_max.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'local_infer_max'`

- [ ] **Step 3: 寫 `task7/inference/local_infer_max.py`**

結構照 `task6/inference/local_infer_d1.py`，逐項替換：

| task6 | task7 |
|---|---|
| `d1_model` | `max_model as mm` |
| `cpg_d1` | `cpg_max` + `leg_kin`（解析式 IK） |
| `obs_d1` | `obs_max` |
| `OBS_DIM` 69 | `obs_max.OBS_DIM` 68 |
| 線性化 IK | `cpg_max.joint_targets(...)`（**與訓練端同一條軌跡鏈**） |
| XML 內建位置致動器 | **`cpg_walk_max.Robot(actuator_mode="torque_pd")`**（原始網格模型是力矩致動器） |
| `cmd` 3 維 | `cmd` 2 維 `(vx, wz)` |

必寫的三件事：

```python
DEFAULT_SCENE = mm.SCENE      # ★ 原始網格模型，不是訓練用的 scene_flat_mjx.xml

# 推論端的軌跡鏈必須與訓練端逐項相同，否則 policy 學到的動作會被解成別的關節角。
# 這裡直接呼叫 cpg_max.joint_targets（訓練端是它的 JAX 版，兩者有逐點對照測試）。
```

```python
def load_policy(path: str):
    """網路結構必須與 Colab 訓練時逐項相同：policy (256,256,128)、value (256,256,256)、
    normalize_observations=True。

    不匹配的後果分兩種，差別很大：
      - **隱藏層大小**對不上 → flax 丟 ScopeParamShapeError，當場停住，安全。
      - **activation 與動作分布類型**對不上 → 參數形狀完全相同，brax **不會報錯**，
        權重照樣載入，只是 policy 行為錯亂而毫無訊息。
    而 activation 與分布由 **brax 的版本**決定，不由本檔的參數決定 ——
    所以 notebook 鎖死 `brax==0.14.2` 並在裝完後斷言，是這個靜默失敗的唯一防線。
    """
```

- [ ] **Step 3b: 把統計段抽成共用函式**

指標計算**不要重寫**。把 `cpg_walk_max.rollout` 從 `pit = np.asarray(pitch)` 到組出
`res` 字典為止那一段（約 423–482 行）抽成模組層級的：

```python
def summarize(gait, params: dict, samples: dict, d, r, n_reach, secs) -> dict:
    """把一次 rollout 的取樣資料整理成指標字典。

    ⚠️ 開迴路（`rollout`）與 RL 推論（`local_infer_max`）**共用這一份**。
    複製第二份的話兩邊遲早分岔，而分岔之後「RL 比開迴路快 x%」就不可信了 ——
    那個百分比會同時包含真實差異與兩份統計程式的差異，事後分不開。
    """
```

`rollout` 改成呼叫它。**驗收：`rollout` 的輸出必須逐位不變。**

Run:
```bash
conda run --no-capture-output -n rbtdog python -c "
import sys; sys.path.insert(0,'task7/inference')
import cpg_walk_max as cw
r=cw.rollout(gait='walk', secs=20.0, quiet=True)
print('%.4f %.1f %+.3f %.3f'%(r['speed_travel'], r['bounce']*1000, r['pitch_mean'], r['support']))"
```
Expected: `0.1471 16.7 -0.025 3.204`（與 Task 2 Step 8 逐位相同）

CLI：

```python
    ap.add_argument("--params", type=str, default="")
    ap.add_argument("--dummy", action="store_true", help="不載權重，用固定動作測管線")
    ap.add_argument("--secs", type=float, default=20.0)
    ap.add_argument("--vx", type=float, default=0.15, help="前進速度指令 m/s")
    ap.add_argument("--wz", type=float, default=0.0, help="偏航率指令 rad/s")
    ap.add_argument("--video", action="store_true")
    ap.add_argument("--scene", type=str, default=None,
                    help="覆寫場景；預設是**原始網格模型**。"
                         "給 scene_flat_mjx.xml 可與訓練條件對照")
```

- [ ] **Step 4: 跑測試確認通過**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/ -q`
Expected: 全過

- [ ] **Step 5: 手動跑 dummy 管線**

Run:
```bash
conda run --no-capture-output -n rbtdog python task7/inference/local_infer_max.py --dummy --secs 5
```
Expected: 印出與 `cpg_walk_max` 同格式的四行指標，不崩潰。
（`--dummy` 是固定動作，走得爛是正常的，這一步只驗管線。）

- [ ] **Step 6: Commit**

```bash
git add task7/inference/local_infer_max.py task7/tests/test_obs_max.py \
        task7/inference/cpg_walk_max.py
git commit -m "$(cat <<'EOF'
feat(task7): 本機推論端 —— 在原始網格模型上驗收 RL 權重

預設跑原始網格模型而不是訓練用的圓柱模型：驗收若也跑簡化模型，
等於用同一個近似去驗證那個近似，落差永遠量不到。
統計段抽成 summarize() 兩邊共用，不複製第二份——兩份統計遲早分岔，
分岔之後「RL 比開迴路快 x%」就不可信了。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: 文件與交接

**Files:**
- Create: `task7/docs/基準步態凍結_D1Max_walk_2026-08-27.md`
- Create: `task7/docs/現場操作卡_IMU平放複核.md`
- Create: `task7/docs/CPG-RL_D1Max_設計_2026-08-27.md`
- Modify: `task7/HANDOFF.md`、`task7/README.md`

- [ ] **Step 1: 跑基準複驗掃描**

Run:
```bash
conda run --no-capture-output -n rbtdog python task7/inference/cpg_sweep_max.py --plan base --seeds 12
```
Expected: `walk` 那列 `n_fell = 0/12`。把輸出貼進基準凍結文件。

- [ ] **Step 2: 寫 `task7/docs/基準步態凍結_D1Max_walk_2026-08-27.md`**

必須包含：

1. **§0 一句話**：`walk` 在 180 s × 12 擾動 0 跌倒，本文把它凍結成唯一真實來源。
2. **§1 凍結的參數表**（十一項）＋ **每一項的判準來源**（照 `gait_baseline.py` 的 docstring）。
3. **§2 複驗數據**：Step 1 的輸出、以及 20 / 60 / 120 / 180 秒的 `n_fell` 與 `speed_travel`
   （從既有的 `outputs/sweep_drift_20s.json` 取，不必重跑）。
4. **§3 已知限制**，逐條寫清楚**不要當作已解決**：
   - 偏航 −0.5 ~ −0.9 °/s，**開迴路調參繞不過去**（x_off 掃 20 mm 不變號、
     d_step 單調無過零、ω 在 1.8 變號但 2.0 又變回來 —— 那是分歧不是趨勢）
   - **只在平地驗過**，斜坡／台階／不平地面完全沒測
   - **模擬質量 38.821 kg vs 規格書 41 kg**，增益與配平是照 38.8 調的
   - `STATIC_SAG` 與 `KP3` 綁死，改增益必須重掃
   - **實機一步都沒走過**
5. **§4 改動守則**：動到輪摩擦 / 腿摩擦 / `d_step` 任一個，都必須用 `--plan trim` 重掃 `x_off`。

- [ ] **Step 3: 寫 `task7/docs/現場操作卡_IMU平放複核.md`**

風格對齊既有的 `現場操作卡_*.md`。必須包含：

1. **為什麼要做**：`imu_central` 目前只有離線快照解碼過，**沒驗過是不是活的串流**；
   `xyzw` 順序是「取樣當下機身剛好水平」推出來的，不是刻意做的平放實驗。
   四元數順序錯 → 重力向量翻掉 → policy 直接廢掉。
   **這張卡不執行不影響訓練，但不執行就不可以上實機。**
2. **T0 活性**：讀 30 秒 `imu_central`，確認 `acc/gyro/quat` 的數值真的在變、
   且 `|q| = 1.000`。（對照組：`joint_state` 的 1 kHz 活性是已驗過的。）
3. **T1 平放**：狗確實平放地面，記錄 `acc` 應為 `(≈0, ≈0, ≈+9.8)`，
   用 `xyzw` 解出的 roll/pitch 應 ≈ 0；用 `wxyz` 解會得到明顯錯誤的角度（先前實測 −30.6°）。
4. **T2 前傾 / T3 側傾**：分別墊高後方 / 一側約 10 cm，
   確認 `xyzw` 解出的 pitch / roll **符號與量值都對得上加速度計**。
   ⚠️ 判準是「**兩個獨立來源互相對照**」，不是「數字看起來合理」。
5. **記錄表格**（空白的，現場填）與**中止條件**：任何一項對不上就停，不要硬推。
6. 用到的工具：`task7/realbot/shm_io.py`（唯讀開啟，不需要 root）。

- [ ] **Step 4: 寫 `task7/docs/CPG-RL_D1Max_設計_2026-08-27.md`**

必須包含：

1. **§1 觀測層 68 維表**與**刻意排除的三項及理由**（照 `obs_max.py` 的 docstring）。
2. **§2 動作空間**：12 維、`duty` 固定 0.80 且**這是與 task6 最關鍵的差異**。
3. **§3 訓練模型**：指向 `MJX模型對照_2026-08-27.md`，摘要 G1 的四段結果。
4. **§4 reward 與 DR**：權重表 ＋「**權重照 D1 EDU 訂的，這台 41 kg，
   第一輪後必須重新校準**」的警告。
5. **§5 驗收關卡 G3–G6**（訓練完成後由使用者執行）：
   - G3：`local_infer_max.py` 在**原始網格模型**上 20 / 60 / 180 s × 12 擾動，跌倒數 0
   - G4：`cmd_wz=0` 時的偏航率**必須顯著優於開迴路的 −0.5 ~ −0.9 °/s**
   - G5：速度一律用 `speed_travel` 量，**不可用 `speed_path`**
   - G6：錄影 `task7/outputs/cpg_rl_max.mp4`
6. **§6 未解決**：IMU 未複核、實機未走過、只有平地、輪子沒納入動作空間。

- [ ] **Step 5: 更新 `task7/HANDOFF.md`**

在「▶ 明天第一件事」之後、「一句話現況」之前插入一節：

```markdown
## ▶ 另一條線：CPG-RL（模擬，不碰實機）

**基準步態已凍結**：`walk`，180 s × 12 擾動 0 跌倒，參數在
`inference/gait_baseline.py`，判準來源在 `docs/基準步態凍結_D1Max_walk_2026-08-27.md`。

**訓練 notebook 已備妥**：`notebooks/cpg_rl_max_colab.ipynb`，丟 Colab GPU 直接跑。

★ **官方 MJCF 不能直接餵 MJX**（碰撞網格 98,569 頂點會 OOM），
訓練用的是產生出來的 `model/zgws/zgws_mjx.xml`，落差量在
`docs/MJX模型對照_2026-08-27.md`。

⚠️ **上實機前必做**：`docs/現場操作卡_IMU平放複核.md`。
`imu_central` 還沒驗過是活的串流，`xyzw` 順序也只是「取樣當下剛好水平」推出來的。
順序錯 → 重力向量翻掉 → policy 直接廢掉。
```

並在「已完成」表格追加三列（基準凍結 / MJX 模型 / 訓練 notebook）。

- [ ] **Step 6: 更新 `task7/README.md`**

在檔案清單加入本次新增的六個檔案，各一行說明。

- [ ] **Step 7: 全測試 + commit**

Run: `conda run --no-capture-output -n rbtdog python -m pytest task7/tests/ -q`
Expected: 全過

```bash
git add task7/docs/ task7/HANDOFF.md task7/README.md
git commit -m "$(cat <<'EOF'
docs(task7): 基準步態凍結、IMU 複核操作卡、CPG-RL 設計與交接

IMU 那張卡是上實機的硬性前提：imu_central 目前只有離線快照解碼過，
沒驗過是活的串流，xyzw 順序也只是「取樣當下剛好水平」推出來的，
不是刻意做的平放實驗。順序錯 policy 直接廢掉。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 完成後交給使用者的東西

1. `task7/notebooks/cpg_rl_max_colab.ipynb` —— 丟 Colab GPU 跑，約 60M steps。
   第一個 eval 就會印出「60M 約需幾分鐘」，可當場決定要不要砍到 40M。
2. 訓練完下載 `cpg_rl_max_params.pkl`，放到 `task7/weights/`，然後：
   ```bash
   conda run --no-capture-output -n rbtdog python task7/inference/local_infer_max.py \
       --params task7/weights/cpg_rl_max_params.pkl --secs 20 --video
   ```
3. G3–G6 的驗收數字回填到 `docs/CPG-RL_D1Max_設計_2026-08-27.md`。
