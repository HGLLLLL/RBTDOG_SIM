# CPG-RL v3.1 統一運動學產生器 —— 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `task7/inference/rl_env_v3.py` 的離散雙模式產生器換成「一條運動學公式＋每腿活動度」的 v3.1 產生器（平移四腿踏步、弧線內側前腿踏步、三軸疊加），輪子回速度伺服＋力矩殘差，先用原廠錄檔回放驗模擬器，G0 過了才上 Colab。

**Architecture:** 指令 (vx, vy, wz) → 活動度 (a_lat, a_turn, a_wheel, s4, s_arc) → 每腿活動度 s_k、步向量 vec_k = T·(vy·ŷ + rot_frac·wz×r_k)、相位偏移（原廠兩組圓周插值）、輪速（差速×gate＋平移圖案＋RL 殘差）。RL 仍是 12 維殘差、obs 76 維。原廠回放腳本獨立於 env，用 MuJoCo 直接跑。

**Tech Stack:** Python 3.11（conda env `rbtdog`）、MuJoCo / MJX、JAX、brax 0.14.2、numpy、scipy（`minimum_filter1d`）、pytest。

Spec：`docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md`。

## Global Constraints

- 所有指令在 repo 根目錄 `/home/huang/rbtdog_sim` 執行；python 一律 `conda run -n rbtdog python …`、測試 `conda run -n rbtdog python -m pytest task7/tests/… -q`。
- 腿序陷阱：MJCF／env 序 **FR, FL, RR, RL**；SHM／錄檔序 **fl, fr, bl, br**（`bl` = RL、`br` = RR）。跨界一律按名稱對應，不按 index。
- 控制器座標 = MJCF 關節座標（`coord.to_ctrl` 後可直接當 qpos／ctrl）。
- 腿增益不動：模擬 kp 60／250／250、kd 2.0（`scene_flat_mjx_v3.xml`）。輪子回速度伺服 kv 1.0、前饋 0.13、死區 0.3（`scene_flat_mjx_v3.xml`，`wheel_pos=False` 預設）。
- 動作 12 維、obs 76 維不變；obs 第 34–36 為 cmd、第 37–38 由 `[u, 1−u]` 改為 `[s4, s_arc]`。
- 護欄不動：TAU_BAR 58、ERR_BAR 0.45、TAU_KILL 90、Z_SAG 0.036。
- 步頻名目 2.0 Hz、duty 0.5、抬高名目 0.040（v3.0 G0 驗過 kp250 要這麼深；原廠 22 mm 留 `factory_lift`）。
- 每個 Task 結尾 commit；commit 訊息結尾加：
  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b
  ```

## 檔案地圖

| 檔 | 責任 | 動作 |
|---|---|---|
| `task7/inference/ref_extract.py` | 錄檔 → 參考資料集 | 加 `swing_events31` / `analyze31` / `gains_of`，輸出 `ds["v31"]`、`ds["summary"]["v31"]` |
| `task7/tests/test_ref_extract_v31.py` | 擺動事件法的合成訊號測試 | 新 |
| `task7/inference/diag/replay_factory.py` | 原廠錄檔在 MuJoCo 開迴路回放 | 新 |
| `task7/tests/test_replay_factory.py` | 回放模型增益、smoke | 新 |
| `task7/inference/rl_env_v3.py` | v3.1 env | 產生器區段整段重寫；`reset`/`step`/`_obs`/`_sample_cmd` 改 |
| `task7/tests/test_rl_env_v3.py` | env 單元＋短 rollout | 整檔重寫 |
| `task7/inference/diag/g0_v3.py` | G0 零動作＋掃描 | 改 |
| `task7/notebooks/build_nb_v3.py` → `cpg_rl_v3_colab.ipynb` | Colab | 改文字與 G0 格 |
| `task7/docs/results/L_*.md`、`docs/superpowers/specs/2026-09-09-*.md` | 勘誤 | 加段落 |
| `task7/docs/figs/gen_figP12_v3_arch.py` | 架構圖 | 改文字重畫 |
| `task7/HANDOFF.md` | 交接 | 頂節更新 |

---

### Task 1: ref_extract —— 擺動事件法、四腿相位、弧線、增益表

**Files:**
- Modify: `task7/inference/ref_extract.py`（在 `analyze_transition` 之後、`build` 之前插入新函式；`build` 與 `md` 加 v31 段）
- Create: `task7/tests/test_ref_extract_v31.py`

**Interfaces:**
- Produces: `swing_events31(z: np.ndarray) -> list[tuple[int, int, float]]`（起、迄索引、apex m）；`analyze31(path) -> dict`；`gains_of(rec, i0, i1) -> dict`；資料集新增 `ds["v31"][tag] -> list[dict]`、`ds["summary"]["v31"]`，鍵見 Step 3。Task 3 的常數測試讀 `summary.v31`。

- [ ] **Step 1: 寫失敗測試**

```python
# task7/tests/test_ref_extract_v31.py
"""v3.1 擺動事件法（滾動最低點基線、時長 0.04–0.35 s）與相位計算的合成訊號測試。"""
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))
pytest.importorskip("scipy")
import ref_extract as rx  # noqa: E402

HZ = 500.0


def _foot_z(period_s, swing_s, apex, n_steps, drift=0.0):
    """站姿 z=-0.44，每週期一次 swing_s 的正弦抬腳；drift 是慢的姿態漂移（m/s），事件法要能忽略它。"""
    n = int(n_steps * period_s * HZ) + int(HZ)
    t = np.arange(n) / HZ
    z = -0.44 + drift * t
    for k in range(n_steps):
        t0 = 0.5 + k * period_s
        m = (t >= t0) & (t < t0 + swing_s)
        z[m] += apex * np.sin(np.pi * (t[m] - t0) / swing_s)
    return z


def test_swing_events31_counts_period_and_apex_with_drift():
    z = _foot_z(period_s=0.48, swing_s=0.10, apex=0.023, n_steps=10, drift=0.02)
    ev = rx.swing_events31(z)
    assert len(ev) == 10
    starts = np.array([s for s, _, _ in ev]) / HZ
    assert abs(np.median(np.diff(starts)) - 0.48) < 0.01
    assert all(0.018 < a < 0.026 for _, _, a in ev)


def test_swing_events31_rejects_long_posture_changes():
    z = _foot_z(period_s=1.0, swing_s=0.8, apex=0.03, n_steps=3)   # 0.8 s 的「抬」是姿態變化，不是步
    assert rx.swing_events31(z) == []


def test_phases_relative_to_ref():
    ref = np.array([100, 340, 580, 820])                            # 週期 240 tick
    other = np.array([148, 388, 628])                               # 落後 0.2 週期
    assert abs(rx.phase_rel(other, ref, 240) - 0.2) < 0.01
    assert abs(rx.phase_rel(ref + 230, ref, 240) - (230 / 240)) < 0.01
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_ref_extract_v31.py -q`
Expected: FAIL，`AttributeError: module 'ref_extract' has no attribute 'swing_events31'`

- [ ] **Step 3: 實作**

在 `task7/inference/ref_extract.py` 的 `analyze_transition` 之後加：

```python
# ---------------------------------------------------------------- v3.1（2026-09-15）：擺動事件法、四腿相位、弧線、增益
LIFT31 = 0.012                 # 相對 1 s 滾動最低點抬 > 12 mm
DUR31 = (0.04, 0.35)           # 擺動時長（s）；更長的是姿態變化
FOOT_X_BODY = {"fl": 0.376, "fr": 0.376, "bl": -0.358, "br": -0.358}   # 站姿足端 x（機身座標，m）；旋轉運動學用


def swing_events31(z: np.ndarray) -> list:
    """→ [(s, e, apex_m)]。基線 = 1 s 滾動最低點（吃掉姿態漂移），抬 > LIFT31、時長在 DUR31 內。"""
    from scipy.ndimage import minimum_filter1d
    h = z - minimum_filter1d(z, int(HZ))
    up = h > LIFT31
    d = np.diff(up.astype(int))
    starts, ends = np.nonzero(d == 1)[0] + 1, np.nonzero(d == -1)[0] + 1
    if ends.size and starts.size and ends[0] < starts[0]:
        ends = ends[1:]
    lo, hi = DUR31[0] * HZ, DUR31[1] * HZ
    return [(int(s), int(e), float(h[s:e].max())) for s, e in zip(starts, ends) if lo <= e - s <= hi]


def phase_rel(starts: np.ndarray, ref_starts: np.ndarray, period_ticks: float) -> float:
    """每個 start 對最近一個 ref start 的落後（週期比例），圓形平均。"""
    ph = []
    for s in starts:
        k = np.searchsorted(ref_starts, s, side="right") - 1
        if 0 <= k < ref_starts.size:
            ph.append(((s - ref_starts[k]) / period_ticks) % 1.0)
    return circ_mean(ph) if ph else float("nan")


def gains_of(rec, i0: int, i1: int) -> dict:
    """動作段的增益（取 fl；16 顆同步切換）。"""
    def u(name, k):
        return sorted(set(np.round(rec.j[name][k][i0:i1], 2).tolist()))
    return dict(abad_kp=u("fl1_hip_roll", "kp"), hip_kp=u("fl2_hip_pitch", "kp"), knee_kp=u("fl3_knee_pitch", "kp"),
                leg_kd=u("fl2_hip_pitch", "kd"), wheel_kp=u("fl4_foot", "kp"), wheel_kd=u("fl4_foot", "kd"),
                wheel_ff_p98=float(np.percentile(np.abs(rec.j["fl4_foot"]["ff"][i0:i1]), 98)))


def analyze31(path: str) -> dict:
    rec, q, tau, wv = rga.load(path)
    m = rga.moving_mask(rec, q, wv)
    idx = np.nonzero(m)[0]
    i0, i1 = int(idx[0]), int(idx[-1])
    P = {l: foot_xyz(q, l) for l in LEGS}
    ev = {l: [e for e in swing_events31(P[l][:, 2]) if i0 <= e[0] <= i1] for l in LEGS}
    out = {"label": json.loads(Path(path).read_text(encoding="utf-8")).get("label", ""), "legs": {}, "gains": gains_of(rec, i0, i1)}
    # 穩態偏航：|gyro_z| ≥ 50% 的 p95 那些時刻
    gz = rec.gyro[:, 2]
    thr = 0.5 * np.percentile(np.abs(gz[i0:i1]), 95)
    ss = np.zeros(rec.n, bool)
    ss[i0:i1] = np.abs(gz[i0:i1]) >= max(thr, 0.05)
    if ss.sum() < 100:
        ss[i0:i1] = True
    out["yaw_rate_deg_s"] = float(np.degrees(gz[ss].mean()))
    out["wheel_mean_rad_s"] = {l: float(wv[ss, k].mean()) for k, l in enumerate(LEGS)}
    periods = []
    for l in LEGS:
        st = np.array([s for s, _, _ in ev[l]])
        if st.size >= 3:
            dp = np.diff(st) / HZ
            periods += dp[dp < 1.0].tolist()
    period = float(np.median(periods)) if periods else float("nan")
    out["period_s"], out["freq_hz"] = period, (1.0 / period if period == period and period > 0 else float("nan"))
    ref_leg = max(LEGS, key=lambda l: len(ev[l]))
    out["ref_leg"] = ref_leg
    ref_starts = np.array([s for s, _, _ in ev[ref_leg]])
    for l in LEGS:
        E = ev[l]
        out["legs"][l] = dict(
            n=len(E),
            swing_dur_s=float(np.median([(e - s) / HZ for s, e, _ in E])) if E else float("nan"),
            apex_m=float(np.median([a for _, _, a in E])) if E else float("nan"),
            dx_m=float(np.median([P[l][e - 1, 0] - P[l][s, 0] for s, e, _ in E])) if E else float("nan"),
            dy_m=float(np.median([P[l][e - 1, 1] - P[l][s, 1] for s, e, _ in E])) if E else float("nan"),
            phase=phase_rel(np.array([s for s, _, _ in E]), ref_starts, period * HZ) if E and period == period else float("nan"),
            abad_ptp_deg=float(np.degrees(np.ptp(q[l]["1_hip_roll"][i0:i1]))),
            foot_y_mean_m=float(P[l][ss, 1].mean()), foot_y_idle_m=float(P[l][:i0, 1].mean()) if i0 > 50 else float("nan"))
    # 旋轉運動學：踏步做掉的比例 = 量到的 dy / (T · ω · x_foot)
    w = abs(np.radians(out["yaw_rate_deg_s"]))
    if period == period and w > 0.3 and out["legs"][ref_leg]["n"] >= 3:
        dy_kin = period * w * abs(FOOT_X_BODY[ref_leg])
        out["rot_step_frac"] = float(abs(out["legs"][ref_leg]["dy_m"]) / dy_kin)
    else:
        out["rot_step_frac"] = float("nan")
    return out
```

在 `build()` 的 `for k in ("lat_to_fwd", "turn_to_fwd"):` 迴圈之後、`# 彙總` 之前加：

```python
    ds["v31"] = {k: [analyze31(_path(t, logdir)) for t in FILES[k]]
                 for k in ("turn_left", "turn_right", "lat_left", "lat_right", "arc_left", "arc_right", "fwd")}
```

在 `build()` 最後 `return ds` 之前加：

```python
    v = ds["v31"]
    tl, ll = v["turn_left"][0], v["lat_left"][0]
    def _ph(f):
        # 相對 fl 的相位（fl 若不是 ref_leg 就平移）
        base = f["legs"]["fl"]["phase"]
        return {l: float((f["legs"][l]["phase"] - base) % 1.0) for l in LEGS}
    def _apex_ratio(f, dom):
        d = np.nanmean([f["legs"][l]["apex_m"] for l in dom]); s = np.nanmean([f["legs"][l]["apex_m"] for l in LEGS if l not in dom])
        return float(s / d)
    arc = [f for k in ("arc_left", "arc_right") for f in v[k]]
    ds["summary"]["v31"] = {
        "phase_turn_left": _ph(tl), "phase_lat_left": _ph(ll),
        "apex_ratio_turn": _apex_ratio(tl, ("fl", "br")), "apex_ratio_lat": _apex_ratio(ll, ("fl", "bl")),
        "rot_step_frac_turn": float(np.nanmedian([f["rot_step_frac"] for f in v["turn_left"] + v["turn_right"]])),
        "arc_inner_front_hz": float(np.nanmedian([f["freq_hz"] for f in arc])),
        "arc_inner_front_apex_m": float(np.nanmedian([f["legs"][f["ref_leg"]]["apex_m"] for f in arc])),
        "arc_inner_front_leg": [f["ref_leg"] for f in arc],
        "lat_lead_dy_m": float(np.nanmean([ll["legs"][l]["dy_m"] for l in ("fl", "bl")])),
        "lat_trail_dy_m": float(np.nanmean([ll["legs"][l]["dy_m"] for l in ("fr", "br")])),
        "gains_motion": tl["gains"],
        "gains_fwd": v["fwd"][0]["gains"],
    }
```

在 `md()` 的 `return "\n".join(L)` 之前加：

```python
    V = S.get("v31")
    if V:
        L += ["", "## v3.1（擺動事件法）", "",
              f"- 原地左轉相位（相對 fl）：{ {l: round(p, 2) for l, p in V['phase_turn_left'].items()} }；次要腿抬高比 {V['apex_ratio_turn']:.2f}；踏步做掉的旋轉位移比例 {V['rot_step_frac_turn']:.2f}",
              f"- 左平移相位：{ {l: round(p, 2) for l, p in V['phase_lat_left'].items()} }；次要腿抬高比 {V['apex_ratio_lat']:.2f}；移動側每步 dy {V['lat_lead_dy_m'] * 1000:+.0f} mm、另一側 {V['lat_trail_dy_m'] * 1000:+.0f} mm",
              f"- 弧線：內側前腿 {V['arc_inner_front_leg']} 踏步 {V['arc_inner_front_hz']:.2f} Hz、抬 {V['arc_inner_front_apex_m'] * 1000:.0f} mm",
              f"- 動作段增益：{V['gains_motion']}",
              f"- 前進段增益：{V['gains_fwd']}"]
        L += ["", "| 檔 | 偏航 °/s | 步頻 | fl n/apex/dy | fr | bl | br | 輪 fl/fr/bl/br |", "|---|---|---|---|---|---|---|---|"]
        for k, files in ds["v31"].items():
            for f in files:
                cells = [f"{f['legs'][l]['n']}/{f['legs'][l]['apex_m'] * 1000:.0f}/{f['legs'][l]['dy_m'] * 1000:+.0f}" for l in LEGS]
                L.append(f"| {f['label']} | {f['yaw_rate_deg_s']:+.0f} | {f['freq_hz']:.2f} | " + " | ".join(cells)
                         + " | " + "/".join(f"{f['wheel_mean_rad_s'][l]:+.1f}" for l in LEGS) + " |")
```

- [ ] **Step 4: 跑測試確認通過**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_ref_extract_v31.py -q`
Expected: `3 passed`

- [ ] **Step 5: 重新產生資料集並檢查數字**

Run: `conda run -n rbtdog python task7/inference/ref_extract.py --out task7/outputs/ref_gait_dataset.json --md task7/outputs/ref_gait_dataset.md | tail -25`
Expected（容差內）：原地左轉相位 fl 0／fr ≈ 0.25／bl ≈ 0.59／br ≈ 0.06；左平移 fr ≈ 0.19／bl ≈ 0.51／br ≈ 0.72；apex_ratio 0.6–0.8；rot_step_frac_turn 0.15–0.30；arc_inner_front_hz 2.1–2.5；gains_motion abad_kp [60]、hip_kp [120]、leg_kd [1.0]、wheel_kp [0]、wheel_kd [0.1]。
若相位差超過 0.08，先看 `ref_leg` 是不是 fl（不是就檢查 `_ph` 的平移），不要改容差。

- [ ] **Step 6: 既有測試仍過**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py::test_ref_constants_match_dataset -q`
Expected: `1 passed`（舊鍵都還在）

- [ ] **Step 7: Commit**

```bash
git add task7/inference/ref_extract.py task7/tests/test_ref_extract_v31.py task7/outputs/ref_gait_dataset.json task7/outputs/ref_gait_dataset.md
git commit -m "feat(task7): ref_extract v3.1 —— 擺動事件法（滾動最低點）、四腿相位、弧線內前腿、旋轉踏步比例、原廠動作段增益表"
```

---

### Task 2: 原廠錄檔在 MuJoCo 開迴路回放（spec §5 步驟 0）

**Files:**
- Create: `task7/inference/diag/replay_factory.py`
- Create: `task7/tests/test_replay_factory.py`
- Create（執行產物）: `task7/outputs/replay_factory_trip21.md`

**Interfaces:**
- Produces: `make_model(mu=None) -> mujoco.MjModel`（腿 kp 60/120/120、kd 1.0；輪致動器關掉）；`load_ctrl_frame(path) -> (rec, q(N,12), des(N,12), tau_w(N,4), v_w(N,4), lift_rec(N,4))`（MJCF 序）；`replay(m, q, des, tau_w, i0, n) -> dict(yaw_rad_s(n,), clr(n,4), wheel_v(n,4), fell(bool))`；`main()` 寫 md 表。
- Consumes: `max_model`（`SCENE_MJX`、`LEG_ACT_IDX`、`WHEEL_ACT_IDX`、`LEG_QPOS_IDX`、`WHEEL_QVEL_IDX`、`PREFIX`、`_id`）、`m6_rec.load`、`coord.to_ctrl`／`coord.SIGN`、`kin.fk`、`ref_gait_analysis.moving_mask`。

- [ ] **Step 1: 寫失敗測試**

```python
# task7/tests/test_replay_factory.py
"""原廠回放：模型增益要等於原廠動作段那組、輪致動器要關掉；有錄檔時 smoke 跑 0.2 s。"""
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))
sys.path.insert(0, str(ROOT / "inference" / "diag"))
import replay_factory as rf  # noqa: E402
import max_model as mm  # noqa: E402

LOG = ROOT / "logs" / "m_logs_trip21" / "M6_20260909_161316.json"   # ref_turn_left


def test_make_model_has_factory_motion_gains_and_free_wheels():
    m = rf.make_model()
    kp = m.actuator_gainprm[mm.LEG_ACT_IDX, 0]
    assert np.allclose(kp, np.tile([60.0, 120.0, 120.0], 4))
    assert np.allclose(-m.actuator_biasprm[mm.LEG_ACT_IDX, 2], 1.0)
    assert np.all(m.actuator_gainprm[mm.WHEEL_ACT_IDX] == 0.0) and np.all(m.actuator_biasprm[mm.WHEEL_ACT_IDX] == 0.0)


@pytest.mark.skipif(not LOG.exists(), reason="trip21 錄檔不在")
def test_replay_smoke():
    rec, q, des, tau_w, v_w, lift = rf.load_ctrl_frame(str(LOG))
    assert q.shape == (rec.n, 12) and tau_w.shape == (rec.n, 4) and lift.shape == (rec.n, 4)
    assert abs(rec.hz - 500.0) < 25.0
    m = rf.make_model()
    i0 = int(rec.n * 0.4)
    r = rf.replay(m, q, des, tau_w, i0, 100)
    assert r["yaw_rad_s"].shape == (100,) and r["clr"].shape == (100, 4)
    assert np.all(np.isfinite(r["clr"])) and not r["fell"]
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_replay_factory.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'replay_factory'`

- [ ] **Step 3: 實作**

```python
# task7/inference/diag/replay_factory.py
"""原廠錄檔在 MuJoCo 開迴路回放（spec v3.1 §5 步驟 0）：檢查模擬器的輪地接觸能不能重現原廠動作。

腿：位置伺服 kp 60/120/120、kd 1.0（`scene_flat_mjx.xml` 就是這組，= 原廠動作段的增益），ctrl = 錄檔 des。
輪：原廠 kd 0.1 幾乎是力矩控制、v_des 沒錄到 → 把錄檔實測 τ（正 = 向前滾）直接加到輪 DOF（qfrc_applied），輪致動器關掉。
起點：qpos 腿 = 錄檔 q、des = q 定住 1 s，再逐 tick（500 Hz = 錄檔率）餵 des／τ，回放 `--window` 秒；每檔 `--starts` 個起點。
判讀（spec §5 步驟 0）：原地轉回放偏航 ≥ 50% 原廠且不摔 → 輪地模型可信；< 25% → 先掃 --mu。

    conda run -n rbtdog python task7/inference/diag/replay_factory.py --window 1.5 --starts 3
    conda run -n rbtdog python task7/inference/diag/replay_factory.py --tags 161316 --mu 0.6
"""
from __future__ import annotations

import argparse
import sys
from pathlib import Path

import mujoco
import numpy as np

HERE = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(HERE))
sys.path.insert(0, str(HERE.parent / "realbot"))
import coord  # noqa: E402
import kin  # noqa: E402
import m6_rec  # noqa: E402
import max_model as mm  # noqa: E402
import ref_gait_analysis as rga  # noqa: E402

SHM_OF = {"FR": "fr", "FL": "fl", "RR": "br", "RL": "bl"}
KINDS = ("1_hip_roll", "2_hip_pitch", "3_knee_pitch")
TAGS = {"161316": "turn_left", "161342": "turn_right", "161123": "lat_left", "161223": "lat_right",
        "161544": "arc_left", "161711": "arc_right", "160911": "fwd_fast"}


def load_ctrl_frame(path: str):
    """→ (rec, q, des, tau_w, v_w, lift_rec)，關節 MJCF 序、控制器座標；輪量正 = 向前滾；lift_rec = FK 高度相對 1 s 滾動最低點。"""
    from scipy.ndimage import minimum_filter1d
    rec = m6_rec.load(path)
    q = np.stack([coord.to_ctrl(SHM_OF[L] + k, rec.j[SHM_OF[L] + k]["q"]) for L in mm.LEGS for k in KINDS], 1)
    des = np.stack([coord.to_ctrl(SHM_OF[L] + k, rec.j[SHM_OF[L] + k]["des"]) for L in mm.LEGS for k in KINDS], 1)
    tau_w = np.stack([rec.j[SHM_OF[L] + "4_foot"]["tau"] / coord.SIGN[coord.KIND_WHEEL][SHM_OF[L]] for L in mm.LEGS], 1)
    v_w = np.stack([rec.j[SHM_OF[L] + "4_foot"]["v"] / coord.SIGN[coord.KIND_WHEEL][SHM_OF[L]] for L in mm.LEGS], 1)
    z = np.stack([[kin.fk(SHM_OF[L], *row)[2] for row in q[:, 3 * i:3 * i + 3]] for i, L in enumerate(mm.LEGS)], 1)
    lift = z - minimum_filter1d(z, int(rga.HZ), axis=0)
    return rec, q, des, tau_w, v_w, lift


def make_model(mu: float | None = None) -> mujoco.MjModel:
    m = mujoco.MjModel.from_xml_path(mm.SCENE_MJX)
    kp = m.actuator_gainprm[mm.LEG_ACT_IDX, 0]
    assert np.allclose(kp, np.tile([60.0, 120.0, 120.0], 4)), f"scene_flat_mjx 腿增益不是原廠動作組：{kp}"
    assert np.allclose(-m.actuator_biasprm[mm.LEG_ACT_IDX, 2], 1.0)
    m.actuator_gainprm[mm.WHEEL_ACT_IDX, :] = 0.0
    m.actuator_biasprm[mm.WHEEL_ACT_IDX, :] = 0.0
    if mu is not None:
        m.geom_friction[:, 0] = mu
    return m


def _settle(m, d, q12, secs=1.0):
    d.qpos[:] = 0.0
    d.qpos[2], d.qpos[3] = 0.56, 1.0
    d.qpos[mm.LEG_QPOS_IDX] = q12
    d.qvel[:] = 0.0
    d.ctrl[:] = 0.0
    d.ctrl[mm.LEG_ACT_IDX] = q12
    for _ in range(int(secs / m.opt.timestep)):
        mujoco.mj_step(m, d)


def replay(m, q, des, tau_w, i0: int, n: int) -> dict:
    d = mujoco.MjData(m)
    _settle(m, d, q[i0])
    base = mm._id(m, mujoco.mjtObj.mjOBJ_BODY, "base_link")
    gids = [mm._id(m, mujoco.mjtObj.mjOBJ_GEOM, f"{mm.PREFIX[L]}_FOOT_LINK_COLL") for L in mm.LEGS]
    r = float(m.geom_size[gids[0]][0])
    yaw, clr, wv, fell = np.zeros(n), np.zeros((n, 4)), np.zeros((n, 4)), False
    for j in range(n):
        i = min(i0 + j, len(des) - 1)
        d.ctrl[mm.LEG_ACT_IDX] = des[i]
        d.qfrc_applied[mm.WHEEL_QVEL_IDX] = tau_w[i]
        mujoco.mj_step(m, d)
        yaw[j] = d.qvel[5]
        clr[j] = d.geom_xpos[gids, 2] - r
        wv[j] = d.qvel[mm.WHEEL_QVEL_IDX]
        if d.xmat[base].reshape(3, 3)[2, 2] < 0.5 or d.qpos[2] < 0.25:
            fell = True
            break
    return dict(yaw_rad_s=yaw[:j + 1], clr=clr[:j + 1], wheel_v=wv[:j + 1], fell=fell, height=float(d.qpos[2]))


def run_file(path: str, window: float, starts: int, mu=None) -> list:
    rec, q, des, tau_w, v_w, lift = load_ctrl_frame(path)
    m = make_model(mu)
    n = int(window * rga.HZ)
    mv = rga.moving_mask(rec, _q_dict(rec), v_w)
    idx = np.nonzero(mv)[0]
    a, b = int(idx[0]) + int(rga.HZ), int(idx[-1]) - n
    if b <= a:
        a, b = int(idx[0]), max(int(idx[0]) + 1, int(idx[-1]) - n)
    rows = []
    for i0 in np.linspace(a, b, starts).astype(int):
        r = replay(m, q, des, tau_w, i0, n)
        k = len(r["yaw_rad_s"])
        rows.append(dict(
            t0=float(rec.t[i0]), fell=r["fell"], secs=k / rga.HZ,
            yaw_sim=float(np.degrees(r["yaw_rad_s"].mean())), yaw_rec=float(np.degrees(rec.gyro[i0:i0 + k, 2].mean())),
            lift_sim=(r["clr"].max(0) * 1000).round(0).tolist(), lift_rec=(lift[i0:i0 + k].max(0) * 1000).round(0).tolist(),
            wheel_sim=r["wheel_v"].mean(0).round(1).tolist(), wheel_rec=v_w[i0:i0 + k].mean(0).round(1).tolist()))
    return rows


def _q_dict(rec):
    """ref_gait_analysis.moving_mask 要的 q dict（shm 名、控制器座標）。"""
    return {l: {k: coord.to_ctrl(l + k, rec.j[l + k]["q"]) for k in coord.LEG_KINDS} for l in rga.LEGS}


def md(results: dict, mu) -> str:
    L = [f"# 原廠錄檔 MuJoCo 開迴路回放（trip21；腿 kp 60/120/120 kd 1.0、輪 = 錄檔 τ；μ {mu if mu is not None else 'MJCF 預設'}）", "",
         "| 檔 | 起點 s | 回放 s | 偏航 模擬/原廠 °/s | 比 | 抬腳 模擬 mm (FR,FL,RR,RL) | 抬腳 原廠 | 輪速 模擬 | 輪速 原廠 | 摔 |",
         "|---|---|---|---|---|---|---|---|---|---|"]
    for tag, rows in results.items():
        for r in rows:
            ratio = r["yaw_sim"] / r["yaw_rec"] if abs(r["yaw_rec"]) > 3 else float("nan")
            L.append(f"| {TAGS[tag]} | {r['t0']:.1f} | {r['secs']:.2f} | {r['yaw_sim']:+.0f} / {r['yaw_rec']:+.0f} | {ratio:.2f} | "
                     f"{r['lift_sim']} | {r['lift_rec']} | {r['wheel_sim']} | {r['wheel_rec']} | {'是' if r['fell'] else ''} |")
    L += ["", "判讀：原地轉比 ≥ 0.5 且不摔 → 輪地模型可信、G0 門檻照 spec；比 < 0.25 或輪被刮地鎖死 → 先掃 --mu 0.6/0.8。開迴路回放閉迴路策略會發散，只信前 1.5 s、三個起點一致才算。"]
    return "\n".join(L)


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--logdir", default=str(HERE.parent / "logs" / "m_logs_trip21"))
    ap.add_argument("--tags", nargs="*", default=list(TAGS))
    ap.add_argument("--window", type=float, default=1.5)
    ap.add_argument("--starts", type=int, default=3)
    ap.add_argument("--mu", type=float, default=None)
    ap.add_argument("--out", default=str(HERE.parent / "outputs" / "replay_factory_trip21.md"))
    a = ap.parse_args()
    results = {}
    for tag in a.tags:
        path = next(Path(a.logdir).glob(f"M6_*_{tag}.json"))
        results[tag] = run_file(str(path), a.window, a.starts, a.mu)
        for r in results[tag]:
            print(f"{TAGS.get(tag, tag):11s} t0 {r['t0']:5.1f} yaw {r['yaw_sim']:+5.0f}/{r['yaw_rec']:+5.0f} lift {r['lift_sim']} / {r['lift_rec']} fell {r['fell']}")
    txt = md(results, a.mu)
    out = Path(a.out) if a.mu is None else Path(a.out).with_name(f"replay_factory_trip21_mu{a.mu}.md")
    out.write_text(txt + "\n", encoding="utf-8")
    print("→", out)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 跑測試確認通過**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_replay_factory.py -q`
Expected: `2 passed`（smoke 約 10 s）

- [ ] **Step 5: 跑回放，先 MJCF 預設摩擦**

Run: `conda run -n rbtdog python task7/inference/diag/replay_factory.py --window 1.5 --starts 3 2>&1 | tail -30`
Expected: 每檔三列；記下原地轉的偏航比。三種結果的處置：
- 比 ≥ 0.5、不摔：模擬器可信，進 Task 3。
- 0.25 ≤ 比 < 0.5：可信度中等，G0 的偏航門檻乘這個比（記在 §8）。
- 比 < 0.25 或摔：再跑 `--mu 0.6` 與 `--mu 0.8`；哪個 μ 讓比進 0.5 以上，就把它寫進 Task 3 的 `REF["floor_mu"]` 預設與 DR 範圍。

- [ ] **Step 6: 把結果寫進 spec §8**

在 `docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md` 末尾加 `## 8. 執行紀錄`，第一小節「8.1 原廠回放（步驟 0）」貼上 md 表的原地轉、平移、弧線各一列與結論（三選一），並寫明 G0 偏航門檻是否打折。

- [ ] **Step 7: Commit**

```bash
git add task7/inference/diag/replay_factory.py task7/tests/test_replay_factory.py task7/outputs/replay_factory_trip21*.md docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md
git commit -m "feat(task7): replay_factory —— 原廠錄檔 MuJoCo 開迴路回放（腿 kp120 PD、輪施加錄檔 τ）；spec §8.1 記結果"
```

---

### Task 3: env 產生器純函式（活動度、步向量、相位、輪速、足端目標）

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（第 1–11 行 docstring；第 41–58 行 `REF`；第 62 行 `MODE_*` 刪；第 64 行 `WHEEL_RES`；第 74–82 行 `W`／`METRIC_KEYS`；第 94–200 行 `_vec4` 到 `joint_targets` 整段換掉）
- Rewrite: `task7/tests/test_rl_env_v3.py`（本 Task 先寫純函式測試；Task 4 補 env 測試）

**Interfaces:**
- Produces（Task 4 的 `reset`/`step` 用）：
  - `activity(cmd, ref=None) -> dict(lat, turn, wheel, s4, arc)`，每個是純量 jnp。
  - `leg_activity(cmd, A) -> (4,)` s_k。
  - `step_gain(s) -> (4,)` g_k = clip(s_k / 0.2, 0, 1)。
  - `phase_offsets(cmd, A) -> (4,)`；`slew_phase(ph, ph_tgt, max_step=0.02) -> (4,)`。
  - `kin_step_vec(cmd, T, ref=None) -> (4,2)`。
  - `posture_offset(cmd, A, ref=None) -> (4,)` y 偏移。
  - `wheel_cmd(cmd, A, res, ref=None) -> (4,)` rad/s。
  - `step_pattern(cmd, ref=None) -> dict(A, s, g, vec, ph, wheel0, hz, duty, lift, post_y)`。
  - `foot_targets(theta, amp, g, vec, lift4, sway, u, foot_x, foot_z, duty, post_y) -> (4,3)`。
  - `act_split(a)` 的 `wres` 改為 `WHEEL_RES * tanh(a[8:12])`，單位 rad/s，`WHEEL_RES = 2.0`。
  - 常數：`PH_TURN_L`, `PH_LAT_L`, `W_LAT_L`, `W_TURN_L`, `W_ARC_L`, `DOM_TURN_L`, `LAT_WHEEL_L`, `FOOT_XY_BODY`, `SIDE_Y_j`, `MIRROR_LR`。
- 刪除：`mode_of`, `wheel_cmd_wheelmode`, `TURN_STEP_L`, `TURN_WHEEL_L`, `LAT_STEP_L`, `MODE_VY_THR/MODE_WZ_THR/MODE_VX_THR`。

- [ ] **Step 1: 寫失敗測試（整檔重寫，先只放純函式部分）**

```python
# task7/tests/test_rl_env_v3.py
"""v3.1 統一運動學產生器：活動度表、步向量方向、相位鏡像與插值、輪速 gate、足端目標、常數對資料集；env 形狀與短 rollout。"""
import json
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))
jax = pytest.importorskip("jax")
jnp = jax.numpy
import rl_env_v3 as v3  # noqa: E402

DS = ROOT / "outputs" / "ref_gait_dataset.json"
FR, FL, RR, RL = 0, 1, 2, 3
C = lambda vx, vy, wz: jnp.array([vx, vy, wz])   # noqa: E731


def A(cmd):
    return v3.activity(cmd)


def test_activity_table():
    a = A(C(0.5, 0, 0));      assert float(a["s4"]) == 0 and float(a["arc"]) == 0 and float(a["wheel"]) == 1
    a = A(C(0, 0, 1.3));      assert float(a["s4"]) == 1 and float(a["arc"]) == 0
    a = A(C(0, 0.08, 0));     assert float(a["s4"]) == 1 and float(a["arc"]) == 0
    a = A(C(0.5, 0, 0.5));    assert abs(float(a["arc"]) - 0.5 / 1.3) < 1e-6 and float(a["s4"]) == 0   # 弧線
    a = A(C(0.3, 0.06, 0));   assert abs(float(a["s4"]) - 0.75) < 1e-6 and float(a["arc"]) == 0       # 斜走
    a = A(C(0, 0, 0));        assert float(a["s4"]) == 0 and float(a["arc"]) == 0


def test_leg_activity_dominant_and_secondary():
    cmd = C(0, 0, 1.3); s = np.asarray(v3.leg_activity(cmd, A(cmd)))
    assert s[FL] == 1 and s[RR] == 1 and abs(s[FR] - 0.7) < 1e-6 and abs(s[RL] - 0.7) < 1e-6   # 左轉主導 FL+RR
    cmd = C(0, 0, -1.3); s = np.asarray(v3.leg_activity(cmd, A(cmd)))
    assert s[FR] == 1 and s[RL] == 1 and abs(s[FL] - 0.7) < 1e-6
    cmd = C(0, 0.08, 0); s = np.asarray(v3.leg_activity(cmd, A(cmd)))
    assert s[FL] == 1 and s[RL] == 1 and abs(s[FR] - 0.7) < 1e-6 and abs(s[RR] - 0.7) < 1e-6   # 左移主導 FL+RL
    cmd = C(0.5, 0, 1.3); s = np.asarray(v3.leg_activity(cmd, A(cmd)))
    assert s[FL] == 1 and abs(s[RR] - 0.5) < 1e-6 and s[FR] == 0 and s[RL] == 0               # 左弧線：內前 FL、對角 RR
    cmd = C(0.5, 0, -1.3); s = np.asarray(v3.leg_activity(cmd, A(cmd)))
    assert s[FR] == 1 and abs(s[RL] - 0.5) < 1e-6 and s[FL] == 0 and s[RR] == 0
    cmd = C(0.5, 0, 0); assert float(jnp.max(v3.leg_activity(cmd, A(cmd)))) == 0


def test_kin_step_vec_directions():
    T = 0.5
    v = np.asarray(v3.kin_step_vec(C(0, 0, 1.3), T))
    assert v[FL][0] < 0 and v[FL][1] > 0 and v[RR][0] > 0 and v[RR][1] < 0          # 左轉：FL 往左後、RR 往右前
    assert v[FR][0] < 0 and v[FR][1] > 0 and v[RL][0] > 0 and v[RL][1] < 0
    assert abs(v[FL][1] - v3.REF["rot_step_frac"] * T * 1.3 * float(v3.FOOT_XY_BODY[FL, 0])) < 1e-6
    v = np.asarray(v3.kin_step_vec(C(0, 0.08, 0), T))
    assert np.allclose(v[:, 1], 0.04) and np.allclose(v[:, 0], 0.0)                  # 左移：四腿同 +40 mm
    v = np.asarray(v3.kin_step_vec(C(0.5, 0, 0), T))
    assert np.allclose(v, 0.0)                                                        # 純 vx 不踏步


def test_phase_offsets_mirror_and_blend():
    ph_t = np.asarray(v3.phase_offsets(C(0, 0, 1.3), A(C(0, 0, 1.3))))
    assert np.allclose(ph_t, np.asarray(v3.PH_TURN_L))
    ph_tr = np.asarray(v3.phase_offsets(C(0, 0, -1.3), A(C(0, 0, -1.3))))
    assert np.allclose(ph_tr, np.asarray(v3.PH_TURN_L)[[FL, FR, RL, RR]])
    ph_l = np.asarray(v3.phase_offsets(C(0, 0.08, 0), A(C(0, 0.08, 0))))
    assert np.allclose(ph_l, np.asarray(v3.PH_LAT_L))
    mix = np.asarray(v3.phase_offsets(C(0, 0.08, 1.3), A(C(0, 0.08, 1.3))))
    for k in range(4):
        d1 = abs((mix[k] - ph_l[k] + 0.5) % 1 - 0.5); d2 = abs((mix[k] - ph_t[k] + 0.5) % 1 - 0.5)
        assert d1 <= abs((ph_t[k] - ph_l[k] + 0.5) % 1 - 0.5) + 1e-6 and d2 <= abs((ph_t[k] - ph_l[k] + 0.5) % 1 - 0.5) + 1e-6


def test_slew_phase_is_rate_limited_and_circular():
    ph = jnp.array([0.0, 0.0, 0.0, 0.0]); tgt = jnp.array([0.5, 0.01, 0.95, 0.0])
    out = np.asarray(v3.slew_phase(ph, tgt))
    assert abs(out[0] - 0.02) < 1e-6 and abs(out[1] - 0.01) < 1e-6 and abs(out[2] - 0.98) < 1e-6 and out[3] == 0


def test_wheel_cmd_gate_lateral_pattern_and_residual():
    z = jnp.zeros(4)
    w = np.asarray(v3.wheel_cmd(C(0.5, 0, 0), A(C(0.5, 0, 0)), z))
    assert np.allclose(w, 0.5 / v3.REF["r_wheel"])
    w = np.asarray(v3.wheel_cmd(C(0, 0, 1.3), A(C(0, 0, 1.3)), z))
    assert w[FR] > 4.0 and w[RL] < -4.0 and abs(w[FL]) < 1e-6 and abs(w[RR]) < 1e-6      # 左轉：主導對 FL/RR 的輪 0、FR/RL 2 倍
    w = np.asarray(v3.wheel_cmd(C(0.5, 0, 0.5), A(C(0.5, 0, 0.5)), z))
    assert w[FR] > w[FL] > 0 and w[RR] > w[RL] > 0                                       # 弧線：右快左慢、四輪都轉
    w = np.asarray(v3.wheel_cmd(C(0.05, 0, 0), A(C(0.05, 0, 0)), z))
    assert np.allclose(w, 0.05 / v3.REF["r_wheel"])                                      # 慢直走 gate 不作怪
    w = np.asarray(v3.wheel_cmd(C(0, 0.08, 0), A(C(0, 0.08, 0)), z))
    assert w[FL] > 3.0 and w[RL] < -2.0 and abs(w[FR]) < 1e-6 and abs(w[RR]) < 1e-6      # 左移圖案
    w2 = np.asarray(v3.wheel_cmd(C(0, 0.08, 0), A(C(0, 0.08, 0)), jnp.array([1.0, -1.0, 0.5, 0.0])))
    assert np.allclose(w2 - w, [1.0, -1.0, 0.5, 0.0])


def test_posture_offset_arc_only():
    p = np.asarray(v3.posture_offset(C(0.5, 0, 1.3), A(C(0.5, 0, 1.3))))
    assert p[FL] < 0 and p[RL] > 0 and p[FR] == 0 and p[RR] == 0                          # 左弧線：內側 FL 往中線(−y)、RL 往外(+y)
    p = np.asarray(v3.posture_offset(C(0.5, 0, -1.3), A(C(0.5, 0, -1.3))))
    assert p[FR] > 0 and p[RR] < 0 and p[FL] == 0
    assert np.allclose(np.asarray(v3.posture_offset(C(0, 0, 1.3), A(C(0, 0, 1.3)))), 0.0)  # 原地轉沒有


def test_act_split_wheel_residual_units():
    a = jnp.zeros(12).at[8].set(10.0)
    assert abs(float(v3.act_split(a)["wres"][0]) - v3.WHEEL_RES) < 1e-4 and v3.WHEEL_RES == 2.0


def test_step_pattern_keys_and_foot_targets_shape():
    P = v3.step_pattern(C(0.3, 0.06, 0.4))
    assert set(P) >= {"A", "s", "g", "vec", "ph", "wheel0", "hz", "duty", "lift", "post_y"}
    feet = v3.foot_targets(jnp.zeros(4), jnp.ones(4), P["g"], P["vec"], P["lift"], jnp.zeros(2), 1.0, jnp.zeros(4), 0.0, P["duty"], P["post_y"])
    assert feet.shape == (4, 3)
    feet0 = v3.foot_targets(jnp.zeros(4), jnp.ones(4), jnp.zeros(4), jnp.zeros((4, 2)), jnp.zeros(4), jnp.zeros(2), 0.0, jnp.zeros(4), 0.0, 0.5, jnp.zeros(4))
    assert np.allclose(np.asarray(feet0), np.asarray(v3.STANCE_FEET_j))


@pytest.mark.skipif(not DS.exists(), reason="資料集未產生")
def test_ref_constants_match_dataset():
    d = json.loads(DS.read_text(encoding="utf-8"))["summary"]
    assert abs(v3.REF["L_eff"] - d["L_eff_m"]) < 0.02
    q = np.array(d["wheel_stance_q12"])
    assert abs(v3.REF["stance_q12"][1] - q[1]) < 0.02 and abs(v3.REF["stance_q12"][2] - q[2]) < 0.02
    V = d["v31"]
    pt, pl = V["phase_turn_left"], V["phase_lat_left"]
    for name, shm, ph in (("PH_TURN_L", pt, v3.PH_TURN_L), ("PH_LAT_L", pl, v3.PH_LAT_L)):
        for L, s in (("FR", "fr"), ("FL", "fl"), ("RR", "br"), ("RL", "bl")):
            assert abs((float(ph[v3.LEG_IDX[L]]) - shm[s] + 0.5) % 1 - 0.5) < 0.08, (name, L)
    assert abs(float(v3.W_TURN_L[FR]) - V["apex_ratio_turn"]) < 0.15
    assert abs(float(v3.W_LAT_L[FR]) - V["apex_ratio_lat"]) < 0.15
    assert abs(v3.REF["factory_hz_turn"] - d["turn_left"]["freq_hz"]) < 0.15
    g = V["gains_motion"]
    assert g["abad_kp"] == [60.0] and g["hip_kp"] == [120.0] and g["leg_kd"] == [1.0] and g["wheel_kp"] == [0.0] and g["wheel_kd"] == [0.1]


def test_obs_layout_pinned():
    """狗上 obs 組裝要照這個切；改了就要一起改 realbot。第 37–38 格是 [s4, s_arc]。"""
    assert v3.ACT_DIM == 12
    assert 3 + 3 + 12 + 12 + 4 + 3 + 2 + 1 + 12 + 24 == 76
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "not env_"`
Expected: 多數 FAIL，`AttributeError: module 'rl_env_v3' has no attribute 'activity'`

- [ ] **Step 3: 實作 —— 改 docstring、REF、常數、純函式**

`task7/inference/rl_env_v3.py` 第 1–11 行 docstring 換成：

```python
"""CPG-RL v3.1：統一運動學產生器，模仿原廠步態（spec `docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md`）。

  - 指令 (vx, vy, wz) → 活動度 (a_lat, a_turn, a_wheel, s4, s_arc) → 每腿活動度 s_k（誰抬、抬多高），沒有離散模式
  - 每腳每週期位移 vec_k = T·(vy·ŷ + rot_step_frac·wz × r_k)：純 vx 位移全在 x、輪子滾掉、不抬腳；
    平移／原地轉／弧線有 y 分量才抬腳。旋轉只用踏步做掉一部分（原廠約 0.2，其餘刮地）
  - 相位：原廠兩組四腿偏移（原地轉／平移），依 a_lat:a_turn 圓周插值，左右鏡像
  - 輪子：速度伺服 kd 1.0 ＋ 前饋 0.13（M11 實機已驗）；wheel = 差速×gate ＋ 平移圖案 ＋ RL 殘差（±2 rad/s ≈ ±2 N·m）
  - 動作 12 維：[ω_scale, amp×4, lift, sway×2, wheel_res×4]；零動作＝純開迴路產生器（G0 有標準答案）
  - 觀測 76 維：gravity 3 | gyro 3 | jpos 12 | jvel 12 | wheel_vel 4 | cmd 3 | [s4, s_arc] 2 | head 1 | last_a 12 | cpg 24
原廠參考數字來自 `outputs/ref_gait_dataset.json`（常數釘在 REF／PH_*；tests 比對 json）。
本機 CPU 可跑 reset/step 做測試（慢）；訓練在 Colab GPU。
"""
```

第 41–58 行 `REF = dict(...)` 整段換成：

```python
REF = dict(
    # 輪行站姿（hip/knee 取資料集平均、ABAD 0）：前腿 hip +0.52 / knee −1.20，後腿鏡像
    stance_q12=np.array([0.0, 0.52, -1.20, 0.0, 0.52, -1.20, 0.0, -0.56, 1.20, 0.0, -0.56, 1.20]),
    L_eff=0.375, r_wheel=0.096, v_max=0.92,
    # 踏步名目（kp250 撓度下 v3.0 G0 掃出來的可行點）；原廠 2.5／2.1 Hz、duty 0.8、抬 22 mm 留作對照
    step_hz=2.0, duty=0.50, lift=0.040,
    factory_hz_turn=2.5, factory_hz_lat=2.1, factory_duty=0.80, factory_lift=0.022,
    # 旋轉需要的地面位移裡用踏步做掉的比例（原廠 ≈ 0.2、其餘輪子刮地；G0 掃 0.2/0.5/1.0）
    rot_step_frac=0.5,
    # 活動度分母
    a_ref=dict(vy=0.08, wz=1.3, vx=0.25),
    # 平移輪速圖案（左移 FL +Ω / RL −0.67Ω；運動學推不出，照錄檔）
    lat_wheel=dict(FL=1.0, RL=-0.67), lat_omega=3.3, lat_vy_ref=0.06,
    # 弧線姿態：內側前腳往中線 60 mm、內側後腳往外 30 mm（正 = 往中線）
    posture_arc=dict(front=0.06, rear=-0.03),
    trans_s=1.0,
)
```

第 62 行 `MODE_VY_THR, MODE_WZ_THR, MODE_VX_THR = ...` 刪掉。第 64 行改成：

```python
OMEGA_NOM_SCALE, AMP_SCALE, LIFT_SCALE, SWAY_MAX, SWAY_SLEW, WHEEL_RES = 0.28, 0.5, 0.4, 0.040, 0.004, 2.0   # WHEEL_RES：rad/s 偏移（kd 1.0 ≈ N·m）
STEP_GAIN_S = 0.2              # s_k ≥ 0.2 的腿吃完整步向量（四腿踏步時 stance 腳要同速，不能按 s_k 打折）
PH_SLEW = 0.02                 # 相位偏移每步最多改 0.02 週期
```

第 74–82 行 `W`／`METRIC_KEYS` 換成：

```python
W = dict(W_VX=2.0, W_VY=2.0, W_YAW=2.0, W_YAWI=0.5, W_HEAD=1.0, W_H=0.3, W_LIFT=0.5, W_STANCE=0.5,
         W_ROLL=150.0, W_PITCH=100.0, W_ROLLRATE=0.5, W_PITCHRATE=0.3, W_BIAS=100.0, W_SWAYBIAS=30.0,
         W_ACT=0.05, W_OMDOT=0.5, W_TAU=1e-5, W_TAUBAR=0.05, W_ERRBAR=1.0, W_KNEEV=0.02, W_MODE=2.0, W_VZ=0.05,
         VX_SIG2=0.02, VY_SIG2=0.005, YAW_SIG2=0.0005, YAW_SIG2_WIDE=0.02, YAW_INST_SIG2=0.05, HEAD_SIG=0.15,
         CMD_VX=(-0.4, 0.9), CMD_VY=(0.03, 0.10), CMD_WZ=(0.2, 1.3), P_VX=0.65, P_VY=0.30, P_WZ=0.50,
         P_SWITCH=0.4, RAMP_STEPS=50, BIAS_EMA=0.02)
METRIC_KEYS = ("height", "vx", "vy", "wz", "reward", "pitch", "roll", "mode", "s4", "s_arc", "clr_step", "clr_stance",
               "yawerr", "vxerr", "vyerr", "tau_pk", "err_pk", "knee_v", "omega", "sway_y", "roll_bias")
```

第 94–200 行（從 `def _vec4` 到 `def joint_targets` 的 `return`）整段換成：

```python
def _vec4(d: dict, default=0.0):
    out = np.full(4, default, dtype=float)
    for k, v in d.items():
        out[LEG_IDX[k]] = v
    return jnp.array(out)


# 序 FR, FL, RR, RL。錄檔（shm 序 fl, fr, bl, br；bl=RL、br=RR）：左轉 fl 0／fr .25／bl .59／br .06；左移 fl 0／fr .19／bl .51／br .72
PH_TURN_L = jnp.array([0.25, 0.00, 0.06, 0.59])
PH_LAT_L = jnp.array([0.19, 0.00, 0.72, 0.51])
W_LAT_L = jnp.array([0.7, 1.0, 0.7, 1.0])        # 左移：主導 FL+RL，另一側 0.7（抬高比 16/23）
W_TURN_L = jnp.array([0.7, 1.0, 1.0, 0.7])       # 左轉：主導對角 FL+RR
W_ARC_L = jnp.array([0.0, 1.0, 0.5, 0.0])        # 左弧線：內側前 FL 1.0、對角外側後 RR 0.5
DOM_TURN_L = jnp.array([0.0, 1.0, 1.0, 0.0])     # 左轉主導對（輪 gate 用）
LAT_WHEEL_L = _vec4(REF["lat_wheel"])
MIRROR_LR = jnp.array([1, 0, 3, 2])              # FR↔FL、RR↔RL
SIDE_Y_j = jnp.array(mm.SIDE_Y)                  # FR −1, FL +1, RR −1, RL +1
HIP_XY = jnp.array([[mm.HIP_X, -mm.HIP_Y], [mm.HIP_X, mm.HIP_Y], [-mm.HIP_X, -mm.HIP_Y], [-mm.HIP_X, mm.HIP_Y]])
FOOT_XY_BODY = HIP_XY + STANCE_FEET_j[:, :2]     # 站姿足端在機身座標 (4,2)：前 ≈ (+0.38, ±0.17)、後 ≈ (−0.36, ±0.17)
FRONT_j = jnp.array([1.0, 1.0, 0.0, 0.0])


def _mirror(v, right):
    """right 為 True（右向指令）→ 左右鏡像。"""
    return jnp.where(right, v[MIRROR_LR], v)


def activity(cmd, ref=None):
    """→ dict(lat, turn, wheel, s4, arc)，全是 0–1 純量。"""
    ar = (ref or REF)["a_ref"]
    a_lat = jnp.clip(jnp.abs(cmd[1]) / ar["vy"], 0.0, 1.0)
    a_turn = jnp.clip(jnp.abs(cmd[2]) / ar["wz"], 0.0, 1.0)
    a_wheel = jnp.clip(jnp.abs(cmd[0]) / ar["vx"], 0.0, 1.0)
    return dict(lat=a_lat, turn=a_turn, wheel=a_wheel,
                s4=jnp.maximum(a_lat, a_turn * (1.0 - a_wheel)), arc=a_turn * a_wheel)


def _lat_weight(A):
    return A["lat"] / (A["lat"] + A["turn"] + 1e-6)


def leg_activity(cmd, A):
    """每腿活動度 s_k (4,)：四腿踏步（平移／原地轉，主導 1.0 次要 0.7）與弧線（內前 1.0、對角後 0.5）取大。"""
    w_lat = _mirror(W_LAT_L, cmd[1] < 0)
    w_turn = _mirror(W_TURN_L, cmd[2] < 0)
    w_arc = _mirror(W_ARC_L, cmd[2] < 0)
    wl = _lat_weight(A)
    return jnp.maximum(A["s4"] * (wl * w_lat + (1.0 - wl) * w_turn), A["arc"] * w_arc)


def step_gain(s):
    """吃步向量的比例：s_k ≥ STEP_GAIN_S 就是 1（stance 腳要同速）。"""
    return jnp.clip(s / STEP_GAIN_S, 0.0, 1.0)


def phase_offsets(cmd, A):
    """四腿相位偏移目標 (4,)：原地轉組與平移組依 a_lat:a_turn 圓周插值；方向鏡像。"""
    ph_t = _mirror(PH_TURN_L, cmd[2] < 0)
    ph_l = _mirror(PH_LAT_L, cmd[1] < 0)
    wl = _lat_weight(A)
    z = wl * jnp.exp(2j * jnp.pi * ph_l) + (1.0 - wl) * jnp.exp(2j * jnp.pi * ph_t)
    return jnp.mod(jnp.angle(z) / (2 * jnp.pi), 1.0)


def slew_phase(ph, ph_tgt, max_step=PH_SLEW):
    d = jnp.mod(ph_tgt - ph + 0.5, 1.0) - 0.5
    return jnp.mod(ph + jnp.clip(d, -max_step, max_step), 1.0)


def kin_step_vec(cmd, T, ref=None):
    """每腳每週期踏步位移 (4,2) m：vy 全給踏步；旋轉只給 rot_step_frac；vx 不踏步（輪子滾）。"""
    rot = (ref or REF)["rot_step_frac"] * cmd[2]
    dx = T * (-rot * FOOT_XY_BODY[:, 1])
    dy = T * (cmd[1] + rot * FOOT_XY_BODY[:, 0])
    return jnp.stack([dx, dy], 1)


def posture_offset(cmd, A, ref=None):
    """弧線姿態：內側腿 y 偏移 (4,)，正值定義在 REF 是「往中線」。"""
    p = (ref or REF)["posture_arc"]
    inner = jnp.where(cmd[2] > 0, jnp.array([0.0, 1.0, 0.0, 1.0]), jnp.array([1.0, 0.0, 1.0, 0.0]))
    amount = FRONT_j * p["front"] + (1.0 - FRONT_j) * p["rear"]
    return A["arc"] * inner * amount * (-SIDE_Y_j)


def wheel_cmd(cmd, A, res, ref=None):
    """每輪 rad/s (4,)：差速 × gate ＋ 平移圖案 ＋ 殘差。gate：原地轉主導對 0、另兩輪 2；vx 大或沒轉時 1。"""
    ref = ref or REF
    diff = (cmd[0] - cmd[2] * SIDE_Y_j * ref["L_eff"] / 2) / ref["r_wheel"]
    dom = _mirror(DOM_TURN_L, cmd[2] < 0)
    gate = 1.0 + A["turn"] * (1.0 - A["wheel"]) * (1.0 - 2.0 * dom)
    lw = _mirror(LAT_WHEEL_L, cmd[1] < 0) * ref["lat_omega"] * jnp.clip(jnp.abs(cmd[1]) / ref["lat_vy_ref"], 0.0, 1.5)
    return diff * gate + lw + res


def wheel_ctrl(v_des):
    """實機控制律 τ = kv·(v_des − v) + τ_ff·sign 的 MJX 等價：ctrl = v_des + sign·τ_ff/kv；死區 0.3。"""
    dead = jnp.abs(v_des) < V_DEAD
    return jnp.where(dead, 0.0, v_des + jnp.sign(v_des) * TAU_FF / KV_WHEEL)


def step_pattern(cmd, ref=None):
    """指令 → 產生器設定 dict：A、s(4)、g(4)、vec(4,2)、ph(4) 目標、wheel0(4) 無殘差、hz、duty、lift(4)、post_y(4)。"""
    ref = ref or REF
    A = activity(cmd, ref)
    s = leg_activity(cmd, A)
    T = 1.0 / ref["step_hz"]
    return dict(A=A, s=s, g=step_gain(s), vec=kin_step_vec(cmd, T, ref), ph=phase_offsets(cmd, A),
                wheel0=wheel_cmd(cmd, A, jnp.zeros(4), ref), hz=ref["step_hz"], duty=ref["duty"],
                lift=s * ref["lift"], post_y=posture_offset(cmd, A, ref))


def duty_remap(th, duty):
    return v2.duty_remap(th, duty)


def act_split(a):
    """tanh 後的 12 維動作 → 各尺度。wres 是 rad/s 偏移（kd 1.0 下 ≈ N·m）。"""
    a = jnp.tanh(a)
    return dict(om=1.0 + OMEGA_NOM_SCALE * a[0], amp=1.0 + AMP_SCALE * a[1:5], lift=1.0 + LIFT_SCALE * a[5],
                sway=SWAY_MAX * a[6:8], wres=WHEEL_RES * a[8:12],
                foot_x=FOOT_OFF_MAX * a[1:5], foot_z=FOOT_OFF_MAX * a[5])


def foot_targets(theta, amp, g, vec, lift4, sway, u, foot_x, foot_z, duty, post_y):
    """(4,3) 足端目標（相對各腿 ABAD）。
    踏步：站姿 ＋ 姿態偏移 ＋ g·amp·vec 的往返（swing 往 +vec、stance 往 −vec）＋ 抬高（只在 swing，加 g·Z_SAG）；
    輪行：站姿 ＋ RL 小偏移。u = max_k s_k（斜率限制）做兩者混合。"""
    th = duty_remap(theta, duty)
    s = jnp.sin(th)
    prog = -0.5 * jnp.cos(th)                                  # −0.5 → +0.5 across swing, back across stance
    dxy = (vec * (amp * g)[:, None]) * prog[:, None]
    dz = jnp.where(s > 0, (lift4 + Z_SAG * g) * s, 0.0)
    step_off = jnp.concatenate([dxy, dz[:, None]], 1) + jnp.array([sway[0], sway[1], 0.0])
    post = jnp.stack([jnp.zeros(4), post_y, jnp.zeros(4)], 1)
    wheel_off = jnp.stack([foot_x, jnp.zeros(4), jnp.full(4, foot_z)], 1)
    return STANCE_FEET_j + post + u * step_off + (1.0 - u) * wheel_off


def joint_targets(feet):
    return jnp.stack([ik_j(k, feet[k], KNEE_SIGN_j[k]) for k in range(4)]).reshape(12)
```

- [ ] **Step 4: 跑純函式測試**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "not env_"`
Expected: 全 PASS（`test_ref_constants_match_dataset` 需要 Task 1 的資料集）。此時 `import rl_env_v3` 會因 `DualModeEnv.step` 還在呼叫舊函式而**不**報錯（只是還沒改），下一 Task 處理。

- [ ] **Step 5: Commit**

```bash
git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v3.py
git commit -m "feat(task7): rl_env_v3 v3.1 產生器純函式 —— 活動度、每腿活動度、運動學步向量、相位插值與限速、輪速 gate＋平移圖案＋殘差、弧線姿態"
```

---

### Task 4: env 接線（reset／step／obs／reward／指令抽樣）與短 rollout 測試

**Files:**
- Modify: `task7/inference/rl_env_v3.py`：`DualModeEnv.__init__`（`wheel_pos` 預設）、`_obs`、`_sample_cmd`、`reset`、`step`
- Modify: `task7/tests/test_rl_env_v3.py`（加 env 測試）

**Interfaces:**
- Consumes: Task 3 的 `step_pattern`, `slew_phase`, `foot_targets`, `wheel_cmd`, `act_split`。
- Produces: `DualModeEnv(scene=None, wheel_pos=False, ref=None, weights=None)`；`info` 多 `ph (4,)`、`s4`、`s_arc`；metrics 多 `s4`、`s_arc`；G0 腳本用 `env.reset` 後覆寫 `info["cmd"]/["cmd2"]/["t_switch"]` 即可（`u_mode`／`ph` 由 step 自己追）。

- [ ] **Step 1: 加失敗測試**

在 `task7/tests/test_rl_env_v3.py` 末尾加：

```python
def _rollout(env, cmd, steps, jit_reset, jit_step):
    s = jit_reset(jax.random.PRNGKey(0))
    s = s.replace(info={**s.info, "cmd": cmd, "cmd2": cmd, "t_switch": 10 ** 6})
    a = jnp.zeros(12)
    M = {k: [] for k in ("vx", "vy", "wz", "tau_pk", "clr_step", "clr_stance", "s4", "s_arc")}
    done = []
    for _ in range(steps):
        s = jit_step(s, a)
        for k in M:
            M[k].append(float(s.metrics[k]))
        done.append(float(s.done))
    return s, {k: np.array(v) for k, v in M.items()}, max(done)


def test_env_shapes_defaults_and_obs_mode_slots():
    env = v3.DualModeEnv()
    assert env.obs_dim == 76 and env.action_size == 12 and env.wheel_pos is False
    s = jax.jit(env.reset)(jax.random.PRNGKey(1))
    assert s.obs.shape == (76,) and s.info["ph"].shape == (4,)
    cmd = np.asarray(s.obs[34:37]); a = v3.activity(jnp.array(cmd))
    assert abs(float(s.obs[37]) - float(a["s4"])) < 1e-6 and abs(float(s.obs[38]) - float(a["arc"])) < 1e-6


def test_env_g0_wheel_and_turn():
    env = v3.DualModeEnv()
    jit_reset, jit_step = jax.jit(env.reset), jax.jit(env.step)
    s, M, done = _rollout(env, jnp.array([0.5, 0.0, 0.0]), 80, jit_reset, jit_step)
    assert done == 0.0 and M["tau_pk"].max() < v3.TAU_KILL
    assert abs(M["vx"][40:].mean() - 0.5) < 0.08 and M["clr_stance"][40:].max() < 10.0 and M["s4"].max() == 0.0
    s, M, done = _rollout(env, jnp.array([0.0, 0.0, 1.3]), 80, jit_reset, jit_step)
    assert done == 0.0 and M["tau_pk"].max() < v3.TAU_KILL
    assert np.degrees(M["wz"][40:].mean()) > 10.0 and M["s4"].max() == 1.0


def test_sample_cmd_covers_axes_and_combos():
    env = v3.DualModeEnv()
    f = jax.jit(jax.vmap(env._sample_cmd))
    c = np.asarray(f(jax.random.split(jax.random.PRNGKey(0), 4000)))
    nz = np.abs(c) > 1e-9
    assert 0.55 < nz[:, 0].mean() < 0.75 and 0.22 < nz[:, 1].mean() < 0.38 and 0.42 < nz[:, 2].mean() < 0.58
    assert (nz[:, 0] & nz[:, 1]).mean() > 0.1 and (nz[:, 0] & nz[:, 2]).mean() > 0.2      # 斜走、弧線都有
    assert c[:, 0].min() >= -0.4 and c[:, 0].max() <= 0.9 and np.abs(c[nz[:, 1], 1]).min() >= 0.03 and np.abs(c[nz[:, 2], 2]).min() >= 0.2
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "env_ or sample_cmd"`
Expected: FAIL（`step` 還呼叫不存在的 `mode_of`，或 `wheel_pos` 預設仍 True）

- [ ] **Step 3: 實作**

`DualModeEnv.__init__` 簽名改成 `def __init__(self, scene: str = None, wheel_pos: bool = False, ref: dict = None, weights: dict = None):`，docstring 第一句改成「`wheel_pos=False`（預設）：純速度伺服（scene_flat_mjx_v3，M11 實機已驗的 kd 1.0）；True：位置＋速度伺服（scene_flat_mjx_v3p，kp 60，實機未驗，v3.1 不用）。」

`_obs` 裡 `u = info["u_mode"]` 與 `info["cmd"], jnp.array([u, 1.0 - u]),` 兩處改成：

```python
        return jnp.concatenate([
            grav, gyro,
            data.qpos[LEG_QPOS_IDX] - STANCE_Q12_j,
            info["qvel_prev"],
            data.qvel[WHEEL_QVEL_IDX],
            info["cmd"], jnp.array([info["s4"], info["s_arc"]]),
            jnp.clip(info["head_err"], -1.0, 1.0)[None],
            last_a,
            c["amp"], jnp.sin(c["theta"]), jnp.cos(c["theta"]), c["vec"][:, 0], c["vec"][:, 1], c["wheel"] / 5.0,
        ])
```

`_sample_cmd` 整個換成：

```python
    def _sample_cmd(self, rng):
        """三軸獨立抽：vx 65%、vy 30%、wz 50% 非零；全零（站立）自然約 12%。"""
        k = jax.random.split(rng, 7)
        w = self.w
        sgn = lambda kk: jnp.sign(jax.random.uniform(kk) - 0.5)   # noqa: E731
        vx = jnp.where(jax.random.uniform(k[0]) < w["P_VX"],
                       jax.random.uniform(k[1], minval=w["CMD_VX"][0], maxval=w["CMD_VX"][1]), 0.0)
        vy = jnp.where(jax.random.uniform(k[2]) < w["P_VY"],
                       jax.random.uniform(k[3], minval=w["CMD_VY"][0], maxval=w["CMD_VY"][1]) * sgn(k[4]), 0.0)
        wz = jnp.where(jax.random.uniform(k[5]) < w["P_WZ"],
                       jax.random.uniform(k[6], minval=w["CMD_WZ"][0], maxval=w["CMD_WZ"][1]) * sgn(k[3]), 0.0)
        return jnp.array([vx, vy, wz])
```

`reset` 裡 `u0, _, _ = mode_of(cmd)` 改成 `P0 = step_pattern(cmd, self.ref)`；`info` dict 的 `"u_mode": u0,` 改成 `"u_mode": jnp.max(P0["s"]), "ph": P0["ph"], "s4": P0["A"]["s4"], "s_arc": P0["A"]["arc"],`。

`step` 從 `# ---- 模式與混合` 到 `c = dict(...)` 那段換成：

```python
        # ---- 產生器設定（連續，無模式）
        P = step_pattern(cmd, self.ref)
        u_tgt = jnp.max(P["s"])
        du = CTRL_DT / self.ref["trans_s"]
        u_mode = jnp.clip(info["u_mode"] + jnp.clip(u_tgt - info["u_mode"], -du, du), 0.0, 1.0)
        # ---- 動作（延遲、淡入）
        a_hist = jnp.concatenate([action[None], info["a_hist"][:2]], 0)
        act = a_hist[info["delay"]]
        u_ramp = jnp.clip(step_i / w["RAMP_STEPS"], 0.0, 1.0)
        act = u_ramp * act
        A = act_split(act)
        # ---- 相位：共同相位推進 ＋ 每腿偏移（偏移目標變了就限速追）
        om = P["hz"] * A["om"]
        ph = slew_phase(info["ph"], P["ph"])
        theta_free = info["c"]["theta"] + 2 * jnp.pi * om * CTRL_DT
        phi0 = theta_free[0] - ph[0]
        theta = jnp.where(u_mode > 0.01, phi0 + ph, info["c"]["theta"]) % (2 * jnp.pi)
        amp = info["c"]["amp"] + jnp.clip(P["g"] * A["amp"] - info["c"]["amp"], -0.05, 0.05)   # 振幅斜率
        sway = info["sway"] + jnp.clip(A["sway"] * u_mode - info["sway"], -SWAY_SLEW, SWAY_SLEW)
        lift4 = P["lift"] * A["lift"]
        feet = foot_targets(theta, amp, P["g"], P["vec"], lift4, sway, u_mode, A["foot_x"], A["foot_z"], P["duty"], P["post_y"])
        q_des = jnp.clip(joint_targets(feet), self._lo, self._hi)
        # ---- 輪速：差速×gate ＋ 平移圖案 ＋ 殘差（rad/s）
        wheel_v = wheel_cmd(cmd, P["A"], A["wres"], self.ref)
        wheel_theta = info["wheel_theta"] + wheel_v * CTRL_DT
        c = dict(theta=theta, amp=amp, vec=P["vec"] * amp[:, None], wheel=wheel_v)
        s_leg, g_leg, duty = P["s"], P["g"], P["duty"]
```

`step` 的 reward 段裡，把

```python
        sw = (jnp.sin(duty_remap(theta, duty)) > 0).astype(jnp.float32) * active
        clr_step = jnp.sum(sw * jnp.exp(-((clr - lift0) / 0.012) ** 2)) / jnp.maximum(jnp.sum(sw), 1.0)
        r_lift = u_mode * clr_step
        # 模式紀律：WHEEL 任何腿抬 > 10 mm；STEP 非踏步腿抬 > 10 mm
        lift_pen = jnp.sum(jnp.maximum(clr - 0.010, 0.0) ** 2 * ((1 - u_mode) + u_mode * (1 - active)))
        foot_dev = jnp.sum((foot_actual(q12) - STANCE_FEET_j) ** 2, 1)
        r_stance = (1 - u_mode) * jnp.mean(jnp.exp(-foot_dev / (0.02 ** 2)))
```

換成：

```python
        sw = (jnp.sin(duty_remap(theta, duty)) > 0).astype(jnp.float32) * g_leg
        wsw = sw * s_leg
        clr_step = jnp.sum(wsw * jnp.exp(-((clr - lift4) / 0.012) ** 2)) / jnp.maximum(jnp.sum(wsw), 1e-3)
        r_lift = u_mode * clr_step
        # 模式紀律（連續）：腿 k 抬 > 10 mm 的罰乘 (1 − s_k)
        lift_pen = jnp.sum(jnp.maximum(clr - 0.010, 0.0) ** 2 * (1.0 - s_leg))
        post = jnp.stack([jnp.zeros(4), P["post_y"], jnp.zeros(4)], 1)
        foot_dev = jnp.sum((foot_actual(q12) - STANCE_FEET_j - post) ** 2, 1)
        r_stance = (1 - u_mode) * jnp.mean(jnp.exp(-foot_dev / (0.02 ** 2)))
```

`info.update({...})` 加 `"ph": ph, "s4": P["A"]["s4"], "s_arc": P["A"]["arc"],`。

metrics 裡 `"clr_step": ...` 與 `"clr_stance": ...` 改成：

```python
                   "clr_step": jnp.sum(sw * clr) / jnp.maximum(jnp.sum(sw), 1.0) * 1000.0,
                   "clr_stance": jnp.max(clr * (1 - g_leg)) * 1000.0,
                   "s4": P["A"]["s4"], "s_arc": P["A"]["arc"],
```

- [ ] **Step 4: 跑全部 v3 測試**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q`
Expected: 全 PASS（兩個 rollout 測試 CPU 上各約 1–3 分鐘）。若 `test_env_g0_wheel_and_turn` 的原地轉偏航 < 10°/s，先跑 Task 5 的 G0 掃 `rot_step_frac`，不要在這裡調門檻。

- [ ] **Step 5: 全套測試沒有連帶壞掉**

Run: `conda run -n rbtdog python -m pytest task7/tests -q -x --ignore=task7/tests/test_rl_env_v3.py 2>&1 | tail -3`
Expected: 全 PASS（v2 env、realbot、kin 等都沒動）。若有 import `mode_of` 的舊測試／腳本，grep：`grep -rn "mode_of\|wheel_cmd_wheelmode" task7 --include=*.py` 應只剩 `diag/g0_v3.py`（Task 5 改）。

- [ ] **Step 6: Commit**

```bash
git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v3.py
git commit -m "feat(task7): rl_env_v3 v3.1 接線 —— 連續活動度取代模式、相位限速、obs [s4,s_arc]、連續模式紀律、三軸獨立指令抽樣、輪子回速度伺服"
```

---

### Task 5: G0 腳本改寫與掃描；結果進 spec §8.2

**Files:**
- Modify: `task7/inference/diag/g0_v3.py`（整檔重寫）
- Modify: spec §8

**Interfaces:**
- Consumes: `v3.DualModeEnv(ref=...)`, `v3.step_pattern`, metrics 鍵 `vx, vy, wz, roll, pitch, height, clr_step, clr_stance, tau_pk, err_pk, knee_v, s4, s_arc, reward`。
- Produces: CLI：`--steps 200 --rot-frac 0.5 --lift 0.04 --duty 0.5 --posture 1 --mu None --only ARC --sweep`；每 case 印一行並判 PASS/FAIL；`--sweep` 跑 rot_frac {0.2,0.5,1.0} × duty {0.5,0.6,0.7} 的原地轉與平移。

- [ ] **Step 1: 重寫腳本**

```python
# task7/inference/diag/g0_v3.py
"""v3.1 G0：零動作（純開迴路產生器）在六種指令下的行為，對 spec §5.1 門檻判 PASS/FAIL。CPU MJX。
    conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 200
    conda run -n rbtdog python task7/inference/diag/g0_v3.py --sweep            # rot_frac × duty，只跑 TURN/LAT
    conda run -n rbtdog python task7/inference/diag/g0_v3.py --posture 0 --only ARC
"""
import argparse, itertools, sys, time
from pathlib import Path
import jax, jax.numpy as jnp, numpy as np
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
import rl_env_v3 as v3

CASES = (("WHEEL vx0.5", (0.5, 0.0, 0.0)), ("ARC vx0.5 wz0.5", (0.5, 0.0, 0.5)), ("TURN wz1.3", (0.0, 0.0, 1.3)),
         ("LAT vy0.08", (0.0, 0.08, 0.0)), ("DIAG vx0.3 vy0.06", (0.3, 0.06, 0.0)), ("STAND", (0.0, 0.0, 0.0)))
KEYS = ("vx", "vy", "wz", "roll", "pitch", "clr_step", "clr_stance", "tau_pk", "err_pk", "knee_v", "height", "s4", "s_arc", "reward")


def run(env, cmd, steps, jit_reset, jit_step, seed=0):
    s = jit_reset(jax.random.PRNGKey(seed))
    s = s.replace(info={**s.info, "cmd": jnp.array(cmd), "cmd2": jnp.array(cmd), "t_switch": 10 ** 6})
    a = jnp.zeros(v3.ACT_DIM)
    M = {k: [] for k in KEYS}; WV, WC, LIFT = [], [], []
    done_at = None
    for i in range(steps):
        s = jit_step(s, a)
        for k in M:
            M[k].append(float(s.metrics[k]))
        WV.append(np.asarray(s.pipeline_state.qvel[v3.WHEEL_QVEL_IDX])); WC.append(np.asarray(s.info["c"]["wheel"]))
        LIFT.append(np.asarray(env._wheel_clearance(s.pipeline_state)) * 1000.0)
        if float(s.done) > 0 and done_at is None:
            done_at = i
    half = steps // 2
    out = {k: float(np.mean(v[half:])) for k, v in M.items()}
    out["tau_pk"] = float(np.max(M["tau_pk"])); out["err_pk"] = float(np.max(M["err_pk"])); out["knee_v"] = float(np.max(M["knee_v"]))
    out["roll_max"] = float(np.max(M["roll"][half:]))
    out["done_at"] = done_at
    out["yaw_deg_s"] = np.degrees(out["wz"])
    out["wheel_act"] = np.mean(WV[half:], 0).round(2); out["wheel_cmd"] = np.mean(WC[half:], 0).round(2)
    out["lift_max"] = np.max(LIFT[half:], 0).round(0)          # 每腿 (FR,FL,RR,RL) 最大離地 mm
    return out


def verdict(name, r, yaw_scale=1.0):
    """spec §5.1；yaw_scale 是步驟 0 回放決定的打折。"""
    ok = r["done_at"] is None and r["tau_pk"] < v3.TAU_KILL
    lm = r["lift_max"]
    if name.startswith("WHEEL"):
        ok &= r["vx"] >= 0.45 and lm.max() < 10
    elif name.startswith("ARC"):
        ok &= r["yaw_deg_s"] >= 20 * yaw_scale and lm[1] > 12            # 內側前腿 FL
    elif name.startswith("TURN"):
        ok &= r["yaw_deg_s"] >= 27 * yaw_scale and (lm > 12).sum() == 4 and r["tau_pk"] <= 75
    elif name.startswith("LAT"):
        ok &= abs(r["vy"]) >= 0.04 and r["tau_pk"] <= 75 and r["roll_max"] < 3.0
    elif name.startswith("DIAG"):
        ok &= r["vx"] >= 0.15 and r["vy"] >= 0.03
    return "PASS" if ok else "FAIL"


def main():
    ap = argparse.ArgumentParser(); ap.add_argument("--steps", type=int, default=200)
    ap.add_argument("--wheel-pos", action="store_true", dest="wheel_pos", help="用 kp 60 位置環模型（v3.1 預設不用）")
    ap.add_argument("--rot-frac", type=float, default=None, dest="rot_frac"); ap.add_argument("--lift", type=float, default=None)
    ap.add_argument("--duty", type=float, default=None); ap.add_argument("--hz", type=float, default=None)
    ap.add_argument("--posture", type=int, default=None, help="1 開 0 關（弧線姿態偏移）"); ap.add_argument("--mu", type=float, default=None)
    ap.add_argument("--yaw-scale", type=float, default=1.0, dest="yaw_scale"); ap.add_argument("--only", default="")
    ap.add_argument("--sweep", action="store_true")
    a = ap.parse_args()
    base = {k: v for k, v in dict(rot_step_frac=a.rot_frac, lift=a.lift, duty=a.duty, step_hz=a.hz, floor_mu=a.mu).items() if v is not None}
    if a.posture is not None:
        base["posture_arc"] = dict(front=0.06, rear=-0.03) if a.posture else dict(front=0.0, rear=0.0)
    grid = [base]
    if a.sweep:
        grid = [dict(base, rot_step_frac=rf, duty=d) for rf, d in itertools.product((0.2, 0.5, 1.0), (0.5, 0.6, 0.7))]
    for ref in grid:
        env = v3.DualModeEnv(wheel_pos=a.wheel_pos, ref=ref)
        jit_reset, jit_step = jax.jit(env.reset), jax.jit(env.step)
        print(f"\n== ref {ref}  wheel_pos {env.wheel_pos}  obs {env.obs_dim} act {env.action_size}")
        for name, cmd in CASES:
            if a.only and a.only not in name:
                continue
            if a.sweep and not (name.startswith("TURN") or name.startswith("LAT")):
                continue
            t0 = time.time(); r = run(env, cmd, a.steps, jit_reset, jit_step)
            print(f"{verdict(name, r, a.yaw_scale)} {name:18s} vx {r['vx']:+.2f} vy {r['vy']:+.3f} yaw {r['yaw_deg_s']:+.0f}°/s | roll {r['roll']:.1f}/{r['roll_max']:.1f} pitch {r['pitch']:.1f} h {r['height']:.3f} | "
                  f"lift {r['lift_max']} clr_step {r['clr_step']:.0f} | 輪 cmd {r['wheel_cmd']} act {r['wheel_act']} | tau_pk {r['tau_pk']:.0f} err {r['err_pk']:.2f} knee_v {r['knee_v']:.1f} | "
                  f"s4 {r['s4']:.2f} arc {r['s_arc']:.2f} R {r['reward']:+.2f} | done@{r['done_at']} ({time.time()-t0:.0f}s)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 先跑預設一輪**

Run: `conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 200 2>&1 | grep -v "^$"`
Expected: 六行，每行開頭 PASS/FAIL。把整段輸出存到 `task7/outputs/g0_v31_default.txt`（`| tee`）。

- [ ] **Step 3: 掃描**

Run:
```bash
conda run -n rbtdog python task7/inference/diag/g0_v3.py --sweep --steps 200 2>&1 | grep -v "^$" | tee task7/outputs/g0_v31_sweep.txt
conda run -n rbtdog python task7/inference/diag/g0_v3.py --only ARC --posture 0 --steps 200 2>&1 | grep -v "^$" | tee -a task7/outputs/g0_v31_sweep.txt
conda run -n rbtdog python task7/inference/diag/g0_v3.py --only ARC --posture 1 --steps 200 2>&1 | grep -v "^$" | tee -a task7/outputs/g0_v31_sweep.txt
```
Expected: 9 組 TURN/LAT ＋ 2 組 ARC。選法（照 spec §6 備案順序）：
1. LAT 全 FAIL 且是摔（done_at 非 None）→ 先看 duty 0.6/0.7 那列；仍摔 → 把 `PH_LAT_L` 的 FR 由 0.19 改 0.35 重跑（順序不變），記在 §8.2。
2. TURN 偏航最高且 tau_pk ≤ 75 的 rot_frac 當預設。
3. ARC 姿態偏移開 vs 關：偏航與 roll 都沒變好就 `posture_arc` 設 0。

- [ ] **Step 4: 把選定值寫回 REF 預設，重跑預設一輪確認六個 case 判定**

改 `REF["rot_step_frac"]`／`REF["duty"]`／`REF["posture_arc"]`（必要時 `PH_LAT_L`）。
Run: `conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 200 2>&1 | grep -v "^$" | tee task7/outputs/g0_v31_final.txt`
Expected: 六個 PASS。若 TURN 只差偏航門檻且步驟 0 回放的比 < 0.5，用 `--yaw-scale <比>` 重判並在 §8.2 寫明；其他 FAIL 不准靠改門檻過。

- [ ] **Step 5: 常數對資料集測試仍過、v3 全測**

Run: `conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q`
Expected: 全 PASS（`PH_LAT_L` 若改了 0.35，`test_ref_constants_match_dataset` 的 0.08 容差會 FAIL：這時把該測試的 FR 平移容差改成 `< 0.2` 並加註「G0 為了前對不同時離地拉開，spec §6」）。

- [ ] **Step 6: spec §8.2 與 commit**

在 spec §8 加「8.2 G0 掃描」：貼 `g0_v31_final.txt` 六行、掃描表（rot_frac × duty 的 TURN 偏航／tau_pk、LAT vy／摔）、選定值與理由。

```bash
git add task7/inference/diag/g0_v3.py task7/inference/rl_env_v3.py task7/tests/test_rl_env_v3.py task7/outputs/g0_v31_*.txt docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md
git commit -m "feat(task7): G0 v3.1 —— 六指令門檻判定、rot_frac×duty 掃描、弧線姿態開關；選定 REF 預設；spec §8.2"
```

---

### Task 6: Notebook 重建、文件勘誤、架構圖、HANDOFF

**Files:**
- Modify: `task7/notebooks/build_nb_v3.py`；重建 `task7/notebooks/cpg_rl_v3_colab.ipynb`
- Modify: `task7/docs/results/L_原廠步態參考錄製_2026-09-09.md`（頂端加勘誤）
- Modify: `docs/superpowers/specs/2026-09-09-cpg-rl-v3-dual-mode-design.md`（§7 頂端加勘誤）
- Modify: `task7/docs/figs/gen_figP12_v3_arch.py` → 重畫 `task7/docs/figs/圖P12_v3雙模式架構.png`
- Modify: `task7/HANDOFF.md`
- Delete: `task7/docs/image.png`（0 byte 誤建）

- [ ] **Step 1: notebook builder**

`task7/notebooks/build_nb_v3.py` 改三處：

`md0` 換成：

```python
md0 = f"""# CPG-RL **v3.1** 統一運動學產生器：智元 D1 Max · MJX · Colab GPU（2026-09-15）

模仿原廠運控：純前進＝腿站姿＋輪子；平移／原地轉＝四腿踏步（原廠相位）；邊走邊轉＝差速＋內側前腿踏步；三軸可疊加（斜走）。
設計 `docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md`（§0 是為什麼 v3.0 的模式表錯了）、
原廠參考 `task7/docs/results/L_*.md`（含 2026-09-15 勘誤）、輪子 `results/K_*.md`（M11）。

**env 住在 repo（`task7/inference/rl_env_v3.py`），本 notebook 只有：安裝 → clone → G0 → 訓練 → 存檔。**
動作 12 維、obs 76 維；零動作＝純開迴路產生器。腿 kp 60/250/250、輪子速度伺服 kd 1.0（M11 實機已驗）。
G0 標準答案（本機 CPU，spec §8.2）：見第 5 格的 assert。
"""
```

`import_src` 裡 `print("obs", ..., "wheel_pos", env.wheel_pos, "kp_wheel", env.kp_wheel)` 改成 `print("obs", env.obs_dim, "act", env.action_size, "wheel_pos", env.wheel_pos)`，並加一行 `assert env.wheel_pos is False`。

`g0_src` 換成（門檻數字照 Task 5 最終 REF 與 spec §8.2 填，下面是 spec §5.1 的門檻）：

```python
g0_src = '''# ---- G0：零動作在六種指令下的行為（同 diag/g0_v3.py；門檻 spec §5.1，本機結果 spec §8.2）
jit_reset, jit_step = jax.jit(env.reset), jax.jit(env.step)
def run(cmd, steps=200):
    s = jit_reset(jax.random.PRNGKey(0))
    s = s.replace(info={**s.info, "cmd": jnp.array(cmd), "cmd2": jnp.array(cmd), "t_switch": 10**6})
    a = jnp.zeros(v3.ACT_DIM); M = []
    for i in range(steps):
        s = jit_step(s, a); M.append({k: float(s.metrics[k]) for k in ("vx", "vy", "wz", "tau_pk", "roll", "clr_stance")} | {"done": float(s.done)})
    h = steps // 2
    return dict(vx=np.mean([m["vx"] for m in M[h:]]), vy=np.mean([m["vy"] for m in M[h:]]), yaw=np.degrees(np.mean([m["wz"] for m in M[h:]])),
                tau=max(m["tau_pk"] for m in M), clr_stance=max(m["clr_stance"] for m in M[h:]), fell=any(m["done"] > 0 for m in M))
R = {}
for name, cmd in (("WHEEL", (0.5, 0, 0)), ("ARC", (0.5, 0, 0.5)), ("TURN", (0, 0, 1.3)), ("LAT", (0, 0.08, 0)), ("DIAG", (0.3, 0.06, 0))):
    R[name] = r = run(cmd); print(name, {k: round(v, 3) if isinstance(v, float) else v for k, v in r.items()})
    assert not r["fell"], name
assert R["WHEEL"]["vx"] >= 0.45 and R["WHEEL"]["clr_stance"] < 10
assert R["ARC"]["yaw"] >= 20 and R["TURN"]["yaw"] >= 27 and abs(R["LAT"]["vy"]) >= 0.04
assert R["DIAG"]["vx"] >= 0.15 and R["DIAG"]["vy"] >= 0.03
'''
```

`train_src` 的 progress 列印加 `vyerr`：把 `vxerr {{ps('vxerr'):.3f}} yawerr {{ps('yawerr'):.3f}}` 改成 `vxerr {{ps('vxerr'):.3f}} vyerr {{ps('vyerr'):.3f}} yawerr {{ps('yawerr'):.3f}}`，`mode {{ps('mode'):.2f}}` 改成 `s4 {{ps('s4'):.2f}} arc {{ps('s_arc'):.2f}}`。

Run: `python3 task7/notebooks/build_nb_v3.py`
Expected: `→ .../cpg_rl_v3_colab.ipynb (9 cells)`

- [ ] **Step 2: L 報告勘誤**

在 `task7/docs/results/L_原廠步態參考錄製_2026-09-09.md` 的 `## 0.` 之前插入：

```markdown
> **⚠️ 勘誤（2026-09-15）**：§0 第三列「平移＝踏步為主、輪子輔助」與第四點「邊走邊轉是純差速不踏步」、§1 表的「原地旋轉＝對角一對踏步」都是整檔平均造成的誤判。
> 逐秒攤開（擺動事件法，`ref_extract.py` v3.1、`outputs/ref_gait_dataset.md` 的 v3.1 段）：
> - **平移四腿都踏**（移動側每步 +40 mm、擺動 0.10 s；另一側原地重放、擺動 0.20 s），相位 fl 0／fr 0.19／bl 0.51／br 0.72。
> - **原地轉四腿都踏**（主導對角對抬 23 mm、另兩腿 16 mm），相位 fl 0／fr 0.25／bl 0.59／br 0.06。
> - **弧線穩態偏航 18–52°/s，內側前腿持續踏步 2.2–2.4 Hz、側向重放 40–80 mm**；輪速外前／內後差 4–5 倍。
> - **動作段增益是 RL 那組**（ABAD 60／HIP·KNEE 120／kd 1.0、輪 kp 0 kd 0.1 ＋ 前饋），命令抬高比實際深 5–6 倍。
> 詳見 `docs/superpowers/specs/2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md` §0–§0.1。
```

- [ ] **Step 3: v3 spec §7 勘誤**

在 `docs/superpowers/specs/2026-09-09-cpg-rl-v3-dual-mode-design.md` 的 `## 7.` 標題下一行插入：

```markdown
> **⚠️ 勘誤（2026-09-15）**：發現 1（差速轉向要位置環）與發現 3（平移靠輪胎側滑、不可行）都錯了。前者是力矩權限不夠（原廠輪 kd 0.1 幾乎是力矩控制、後輪會負力矩煞車），後者是產生器把另一側兩腿釘死（原廠四腿都踏）。v3.0 從未訓練；由 `2026-09-15-cpg-rl-v3.1-unified-kinematic-generator-design.md` 取代 §1 與本節。
```

- [ ] **Step 4: 架構圖**

`task7/docs/figs/gen_figP12_v3_arch.py`：
- 第 57 行的 `box(2, 28, 24, 28, "模式選擇器", [...])` 標題改「活動度計算」，內容列改成 `["a_lat = |vy|/0.08", "a_turn = |wz|/1.3", "a_wheel = |vx|/0.25", "四腿踏步 s4 = max(a_lat, a_turn·(1−a_wheel))", "弧線 s_arc = a_turn·a_wheel", "－ 沒有離散模式、可疊加"]`。
- 第 65 行 `box(31, 47, 46, 46, "步態產生器（兩種走法，照原廠）", [...])` 標題改「步態產生器（一條運動學公式，照原廠）」，內容列改成 `["每腳每週期位移 D = T·(vy·ŷ + f·wz×r)", "x 分量 → 輪子滾；y 分量 → 抬腳重放", "純前進：不抬腳", "平移／原地轉：四腿踏步（原廠相位）", "邊走邊轉：內側前腿 ＋ 對角後腿踏步", "抬高 = s_k × 40 mm ＋ Z_SAG", "★ 旋轉只踏 f=rot_step_frac，其餘刮地"]`。
- 第 93 行 `box(82, 38, 24, 24, "輪子指令", [...])` 內容列改成 `["差速 (vx, wz) × gate", "原地轉：主導對輪 0、另兩輪 2×", "平移：同側前 +／後 −0.67", "＋ RL 殘差 ±2 rad/s（≈ ±2 N·m）", "速度伺服 kd 1.0（M11 已驗）", "－ 拿掉 kp 60 位置環"]`。
- 第 153 行標題改 `"CPG-RL v3.1 統一運動學產生器（2026-09-15 定案）"`，第 154 行改 `"紅框＝與 v3.0 不同的地方；行首 ＋ 新增、－ 拿掉、★ 關鍵決定"`。
- 底部「為什麼這樣改」列表改成三條：`"原廠平移／原地轉四腿都踏；弧線內側前腿踏步（trip21 錄檔逐秒攤開，L 報告勘誤）"`、`"原廠動作段增益 60/120/1.0、輪 kd 0.1 幾乎力矩控制；我們腿留 kp250、輪子改力矩權限（spec §0.1）"`、`"步驟 0：原廠錄檔 MuJoCo 回放驗輪地接觸（outputs/replay_factory_trip21.md）"`。
- 每個改過的 box 傳 `changed="v3.1"`。

Run: `conda run -n rbtdog python task7/docs/figs/gen_figP12_v3_arch.py`
Expected: 覆寫 `task7/docs/figs/圖P12_v3雙模式架構.png`；用 Read 工具看圖確認文字沒溢出框。

- [ ] **Step 5: HANDOFF 頂節**

`task7/HANDOFF.md`：`最後更新` 改 `2026-09-15（v3.1 統一運動學產生器，G0 <PASS 數>/6）`；在 `## ▶▶ 下次上班從這裡開始` 整節換成：

```markdown
## ▶▶ 下次上班從這裡開始（2026-09-15 收工）

**今天做完的**：重看 trip21 錄檔 → L 報告兩處結論錯（平移四腿都踏、弧線內側前腿踏步）、原廠動作段用軟增益＋大幅命令（spec v3.1 §0–§0.1）→
v3.1 統一運動學產生器（`inference/rl_env_v3.py`，12 維／76 維不變、obs 第 37–38 格改 [s4, s_arc]、輪子回速度伺服＋±2 rad/s 殘差）；
原廠回放 `diag/replay_factory.py`（結果 `outputs/replay_factory_trip21.md`、spec §8.1）；G0 六指令（`outputs/g0_v31_final.txt`、spec §8.2）。

**下一步（照順序）**：
1. Colab 訓 v3.1：`notebooks/cpg_rl_v3_colab.ipynb`（第 5 格 G0 五個 assert 要過）→ `weights/cpg_rl_v3_params.pkl`。
2. 本機補 `inference/local_infer_v3.py`（obs 76／act 12，G3–G7 加 vx／vy／wz 追蹤與斜走）。
3. 狗上 M9 v3 推論路徑（另開 spec）：obs 第 37–38 格用 `rl_env_v3.activity(cmd)` 的 numpy 版；輪子 = M11 律 kd 1.0 ＋ 前饋 0.13，殘差直接加 v_des。
4. 實機待驗：腿站著只給 vx 的承重輪行（打滑、L_eff、輪行 roll）。位置環 kp 60 那項**不用驗了**（v3.1 不用）。
5. 若 G0 的 TURN 只到原廠 1/3：是 kp250 撓度，等實機 kp 驗證，不在 env 解。

**已知硬限制**：輪速伺服 kd ≤ 1.0（M10）、kp250 撓度 36 mm（抬腿 +Z_SAG）、原廠 2.5 Hz 跳步做不到（名目 2.0 Hz／duty <G0 選定>／40 mm）、
RL 趟 `--vmax 18`、ABAD 承重外張 ±4.5°、M9 誤差護欄 0.45 rad（原廠 kp120 命令差 58°，不能照抄原廠命令）。
```

`<PASS 數>`／`<G0 選定>` 填 Task 5 的實際值。舊的 `## ▶ ★★★ 2026-09-09 晚：v3 雙模式 env 完成` 節保留，標題後加「（已被 v3.1 取代，見 spec 2026-09-15）」。

- [ ] **Step 6: 刪空檔、全測、commit**

```bash
git rm --cached -q task7/docs/image.png 2>/dev/null; rm -f task7/docs/image.png
conda run -n rbtdog python -m pytest task7/tests -q 2>&1 | tail -2
git add task7/notebooks/build_nb_v3.py task7/notebooks/cpg_rl_v3_colab.ipynb task7/docs/results/L_原廠步態參考錄製_2026-09-09.md docs/superpowers/specs/2026-09-09-cpg-rl-v3-dual-mode-design.md task7/docs/figs/gen_figP12_v3_arch.py "task7/docs/figs/圖P12_v3雙模式架構.png" task7/HANDOFF.md
git commit -m "docs(task7): v3.1 —— notebook 重建、L 報告與 v3 spec 勘誤、圖P12 改活動度架構、HANDOFF 起點"
```

Expected: 測試全 PASS；`git status` 乾淨。

---

## 自我檢查（寫完計畫後跑過）

- Spec 覆蓋：§0 勘誤 → Task 6；§0.1 輪子回速度伺服／殘差 ±2 → Task 3/4；步驟 0 回放 → Task 2；§2.1–2.6 → Task 3；§3 → Task 4；§4 測試 → Task 3/4；§5.1 G0 → Task 5；§7 檔案表每列都有 Task。
- 與 spec 的差異（已在 Task 3 註解與 spec §8 要補記）：步向量不用 KX 0.3，改 `rot_step_frac`（錄檔顯示旋轉每步位移只有運動學的 0.2，x/y 比例已是運動學比）；抬高名目 0.040 不是 0.023（v3.0 G0 證明 kp250 要這麼深）；站立 5% 不強制（三軸獨立抽樣自然 12%）。→ Task 5 Step 6 寫 spec §8 時一併補「8.0 與 spec 正文的差異」。
- 型別一致：`step_pattern` 回傳鍵 `A, s, g, vec, ph, wheel0, hz, duty, lift, post_y` 在 Task 3 測試、Task 4 step、Task 5 都一樣；`wheel_cmd(cmd, A, res, ref)` 三處同；`foot_targets` 11 個參數順序同。
