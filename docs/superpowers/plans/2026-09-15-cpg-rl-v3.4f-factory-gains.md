# CPG-RL v3.4f（馬達增益完全照原廠）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出一條與 v3.3 並存的訓練線 `gains="factory"`，腿 kp 60/120/120 kd 1.0、輪 kv 0.1，完全等於原廠錄檔動作段的增益，讓錄製的原廠命令可以 1:1 套用。

**Architecture:** 不 fork `rl_env_v3.py`。把四件「原本寫死成 kp250 假設」的東西參數化（模型 scene、domain randomization 的標稱增益、撓度相關常數、誤差護欄），再用一個 `gains` 開關選整組。輪子從速度空間改到力矩空間，因為 kv 0.1 下速度殘差沒有力矩權限。**v3.3 的預設路徑必須逐位元不變**，第一個 task 就是釘住這件事。

**Tech Stack:** Python 3.11 / conda env `rbtdog`、JAX + MJX + brax 0.14.2、MuJoCo、pytest。

**Spec:** `docs/superpowers/specs/2026-09-15-cpg-rl-v3.4f-factory-gains-design.md`

## Global Constraints

- 所有指令用 `conda run -n rbtdog python ...`，從 repo 根目錄 `/home/huang/rbtdog_sim` 執行，路徑寫 `task7/...`。
- **`gains="kp250"`（預設）的行為逐位元不變**。每個 task 結束前 golden 測試要過。
- 原廠動作段增益：腿 ABAD 60 / HIP 120 / KNEE 120、kd 1.0、前饋 0；輪 kp 0、kd 0.1、前饋 0.20–0.54 N·m。（spec §0.1，17 檔實測）
- 輪關節的 `frictionloss=0.13`、`damping=0.015`、`armature=0.004` 是 M11 實機辨識的**硬體特性**，任何版本都不准改。
- `ERR_BAR12` 只有 ABAD 那格改（0.60 → 1.05）；髖膝 0.45 不動。（spec §0.3：髖膝實測 max 0.32）
- `TAU_BAR=58.0`、`TAU_KILL=90.0`、`KNEE_V_BAR=14.0` 兩條線都不動。
- 註解與文件用繁體中文，跟現有檔案一致。
- `task7/tests/test_rl_env_v3.py` 整檔跑約 5 分鐘（CPU MJX 編譯慢），這是正常的，不要因為慢就跳過。

---

## 檔案結構

| 檔 | 責任 | 動作 |
|---|---|---|
| `task7/tests/test_rl_env_v3_golden.py` | 釘住 v3.3 的 rollout 數值，擋住所有重構的回歸 | 新增 |
| `task7/tests/data/rl_env_v3_golden.npz` | golden 數值 | 新增（產生物） |
| `task7/model/zgws/make_mjx_model.py` | 產模型 XML | 改：加 `WHEEL_MODEL_V3F` 與 `build_all()` 一段 |
| `task7/model/zgws/zgws_mjx_v3f.xml`、`scene_flat_mjx_v3f.xml` | 原廠增益模型 | 新增（產生物，不手改） |
| `task7/inference/max_model.py` | 模型路徑與增益常數 | 改：加 `SCENE_MJX_V3F` |
| `task7/inference/rl_env_v3.py` | env 本體 | 改：力矩空間輪控制律、四項參數化、`gains` 開關 |
| `task7/inference/diag/g0_v3.py` | 零動作開迴路驗收 | 改：加 `--gains` |
| `task7/inference/diag/replay_factory.py` | 原廠錄檔回放 | 改：加 `--actuator` 走 kv 0.1 致動器路徑 |
| `task7/notebooks/build_nb_v3.py` | 產 Colab notebook | 改：加 `--gains factory` |
| `task7/notebooks/cpg_rl_v3_4f_colab.ipynb` | v3.4f 訓練 notebook | 新增（產生物） |
| `task7/tests/test_mjx_model.py` | 模型不變量 | 改：加 v3f 增益斷言 |
| `task7/tests/test_rl_env_v3.py` | env 單元測試 | 改：加力矩空間與 `gains` 開關測試 |
| `task7/outputs/replay_factory_v3f.md`、`task7/outputs/g0_v34f_*.txt` | 驗收結果 | 新增（產生物） |

---

## Task 1: 釘住 v3.3 現況（golden 回歸）

先做這個。後面每一次重構都靠它證明「v3.3 沒被動到」。

**Files:**
- Create: `task7/tests/test_rl_env_v3_golden.py`
- Create: `task7/tests/data/rl_env_v3_golden.npz`（由測試自己產生，第一次跑時寫檔）

**Interfaces:**
- Consumes: 現有 `rl_env_v3.DualModeEnv()`（無參數＝v3.3）
- Produces: `task7/tests/data/rl_env_v3_golden.npz`，鍵 `obs`、`reward`、`qpos`、`ctrl`，每個 shape `(3, 12, ...)`（3 個指令 × 12 步）

- [ ] **Step 1: 寫 golden 測試**

建立 `task7/tests/test_rl_env_v3_golden.py`：

```python
"""釘住 v3.3（gains="kp250"）的 rollout 數值。

v3.4f 的重構把 scene／撓度／護欄／輪空間參數化，**預設路徑必須逐位元不變**。
這支測試是那件事唯一的證據。golden 檔不存在時會自己產生並 skip，
產生後要 commit；之後任何一步讓它失敗，就是真的動到 v3.3 了 —— 先查清楚再決定，不要放寬容差。
"""
import sys
from pathlib import Path

import jax
import jax.numpy as jnp
import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))

import rl_env_v3 as v3  # noqa: E402

GOLDEN = Path(__file__).resolve().parent / "data" / "rl_env_v3_golden.npz"
CMDS = ((0.5, 0.0, 0.0), (0.0, 0.08, 0.0), (0.0, 0.0, 1.3))   # 輪行／平移／原地轉，三條路徑都踩到
STEPS = 12


def _rollout():
    """固定 seed、固定動作序列跑三個指令，回傳 (obs, reward, qpos, ctrl) 四個 array。"""
    env = v3.DualModeEnv(ref=dict(cyc_amp_rand=False))
    jit_reset, jit_step = jax.jit(env.reset), jax.jit(env.step)
    O, R, Q, C = [], [], [], []
    for ci, cmd in enumerate(CMDS):
        s = jit_reset(jax.random.PRNGKey(ci))
        s = s.replace(info={**s.info, "cmd": jnp.array(cmd), "cmd2": jnp.array(cmd), "t_switch": 10 ** 6})
        o, r, q, c = [], [], [], []
        for i in range(STEPS):
            # 非零、可重現的動作：三條路徑的殘差／sway／輪殘差都要被踩到
            a = jnp.sin(jnp.arange(v3.ACT_DIM) * 0.7 + i * 0.3) * 0.5
            s = jit_step(s, a)
            o.append(np.asarray(s.obs)); r.append(float(s.reward))
            q.append(np.asarray(s.pipeline_state.qpos)); c.append(np.asarray(s.pipeline_state.ctrl))
        O.append(o); R.append(r); Q.append(q); C.append(c)
    return np.array(O), np.array(R), np.array(Q), np.array(C)


def test_v33_rollout_is_bitwise_unchanged():
    obs, rew, qpos, ctrl = _rollout()
    if not GOLDEN.exists():
        GOLDEN.parent.mkdir(parents=True, exist_ok=True)
        np.savez(GOLDEN, obs=obs, reward=rew, qpos=qpos, ctrl=ctrl)
        pytest.skip(f"golden 不存在，已產生 {GOLDEN} —— 請 commit 後重跑")
    g = np.load(GOLDEN)
    for name, got in (("obs", obs), ("reward", rew), ("qpos", qpos), ("ctrl", ctrl)):
        np.testing.assert_allclose(got, g[name], rtol=0, atol=0,
                                   err_msg=f"{name} 與 golden 不同 —— v3.3 預設路徑被動到了")


def test_golden_covers_all_three_wheel_paths():
    """golden 的三個指令必須真的讓輪子動，否則它擋不住輪控制律的回歸。"""
    g = np.load(GOLDEN) if GOLDEN.exists() else pytest.skip("golden 尚未產生")
    ctrl = g["ctrl"][:, :, np.asarray(v3.WHEEL_ACT_IDX)]
    assert np.abs(ctrl).max(axis=(1, 2)).min() > 0.1, f"有指令的輪 ctrl 全是 0：{np.abs(ctrl).max(axis=(1, 2))}"
```

- [ ] **Step 2: 跑一次產生 golden（會 skip）**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3_golden.py -q
```
預期：`1 skipped, 1 passed`（第一支 skip 並印出「已產生」，第二支通過）。
若第二支失敗（輪 ctrl 全 0），代表 `CMDS` 沒踩到輪子 —— 停下來檢查 `wheel_cmd`，不要改測試門檻。

- [ ] **Step 3: 再跑一次驗證 golden 可重現**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3_golden.py -q
```
預期：`2 passed`。若這裡就不可重現（同機同版本兩次跑出不同數字），**停下來**回報 —— 後面所有「不變」的保證都建立在這個前提上。

- [ ] **Step 4: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/tests/test_rl_env_v3_golden.py task7/tests/data/rl_env_v3_golden.npz
git commit -m "test(task7): 釘住 v3.3 rollout 的 golden 值 —— v3.4f 重構的回歸保護

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 2: v3f 模型（原廠增益）

**Files:**
- Modify: `task7/model/zgws/make_mjx_model.py`（`WHEEL_MODEL_V3P` 那行之後、`build_all()` 內）
- Modify: `task7/inference/max_model.py:30` 附近
- Modify: `task7/tests/test_mjx_model.py`（檔尾）
- Create: `task7/model/zgws/zgws_mjx_v3f.xml`、`task7/model/zgws/scene_flat_mjx_v3f.xml`（產生物）

**Interfaces:**
- Consumes: `make_mjx_model.build(dst, kp3, kd3, wheel_model)`、`build_scene(dst, model_xml)`、`max_model.KP3`（=[60,120,120]）、`max_model.KD3`（=[1,1,1]）
- Produces: `max_model.SCENE_MJX_V3F`（str 路徑）、`make_mjx_model.WHEEL_MODEL_V3F`（dict）

- [ ] **Step 1: 寫失敗的測試**

在 `task7/tests/test_mjx_model.py` 檔尾加：

```python
V3F_SCENE = _ROOT / "model" / "zgws" / "scene_flat_mjx_v3f.xml"


def test_v3f_model_uses_factory_action_gains():
    """v3.4f 模型＝原廠錄檔動作段的增益（spec §0.1，17 檔實測）。

    腿 60/120/120 kd 1.0、輪純速度伺服 kv 0.1（原廠 kp 0）。
    輪關節的 frictionloss／damping／armature 是 M11 實機辨識的硬體特性，不隨增益改。
    """
    m = mujoco.MjModel.from_xml_path(str(V3F_SCENE))
    kp = m.actuator_gainprm[mm.LEG_ACT_IDX, 0]
    kd = -m.actuator_biasprm[mm.LEG_ACT_IDX, 2]
    np.testing.assert_allclose(kp, np.tile([60.0, 120.0, 120.0], 4))
    np.testing.assert_allclose(kd, np.full(12, 1.0))
    # 輪：速度伺服（位置項為 0）、kv 0.1
    np.testing.assert_allclose(m.actuator_biasprm[mm.WHEEL_ACT_IDX, 1], 0.0)
    np.testing.assert_allclose(-m.actuator_biasprm[mm.WHEEL_ACT_IDX, 2], 0.1)
    # 輪關節物理照 M11，與 v3 模型一致
    v3m = mujoco.MjModel.from_xml_path(mm.SCENE_MJX_V3)
    np.testing.assert_allclose(m.dof_frictionloss[mm.WHEEL_QVEL_IDX], v3m.dof_frictionloss[mm.WHEEL_QVEL_IDX])
    np.testing.assert_allclose(m.dof_damping[mm.WHEEL_QVEL_IDX], v3m.dof_damping[mm.WHEEL_QVEL_IDX])
    np.testing.assert_allclose(m.dof_armature[mm.WHEEL_QVEL_IDX], v3m.dof_armature[mm.WHEEL_QVEL_IDX])


def test_v3f_differs_from_v3_only_in_gains():
    """v3f 與 v3 只能差增益 —— 質量、慣量、關節、碰撞幾何一項都不准變。"""
    a = mujoco.MjModel.from_xml_path(mm.SCENE_MJX_V3)
    b = mujoco.MjModel.from_xml_path(str(V3F_SCENE))
    np.testing.assert_allclose(a.body_mass, b.body_mass, rtol=0, atol=0)
    np.testing.assert_allclose(a.body_inertia, b.body_inertia, rtol=0, atol=0)
    np.testing.assert_allclose(a.jnt_range, b.jnt_range, rtol=0, atol=0)
    np.testing.assert_allclose(a.geom_size, b.geom_size, rtol=0, atol=0)
    assert (a.nq, a.nv, a.nu) == (b.nq, b.nv, b.nu)
    assert a.opt.timestep == b.opt.timestep
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_mjx_model.py -q -k v3f
```
預期：FAIL，訊息含 `scene_flat_mjx_v3f.xml` 找不到（以及 `SCENE_MJX_V3F` AttributeError）。

- [ ] **Step 3: 加模型常數**

`task7/inference/max_model.py`，在 `SCENE_MJX_V3P` 那行之後加：

```python
SCENE_MJX_V3F = str(_MODEL_DIR / "scene_flat_mjx_v3f.xml")       # ★ v3.4f：腿增益也照原廠動作段（60/120/120 kd 1.0）、輪 kv 0.1
```

- [ ] **Step 4: 加產生器設定**

`task7/model/zgws/make_mjx_model.py`，在 `WHEEL_MODEL_V3P = ...` 那行之後加：

```python
# ★ v3.4f：輪增益照原廠錄檔動作段（kp 0、kd 0.1）。輪關節的摩擦／阻尼／慣量是 M11 實機辨識的硬體特性，不隨增益改。
WHEEL_MODEL_V3F = dict(WHEEL_MODEL_V3, kv=0.1)
```

在 `build_all()` 內，產完 `zgws_mjx_v3p.xml` 的那段之後加：

```python
    # ★ v3.4f 訓練模型：腿與輪的增益**完全等於**原廠錄檔動作段（trip21 17 檔實測，spec §0.1）
    xml = HERE / "zgws_mjx_v3f.xml"
    build(dst=str(xml), kp3=KP3, kd3=KD3, wheel_model=WHEEL_MODEL_V3F)
    build_scene(str(HERE / "scene_flat_mjx_v3f.xml"), xml.name)
    print(f"[產生] {xml.name}  kp={KP3.tolist()} kd={KD3.tolist()} 輪 kv={WHEEL_MODEL_V3F['kv']}")
```

- [ ] **Step 5: 產生模型**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/model/zgws/make_mjx_model.py
```
預期輸出含 `[產生] zgws_mjx_v3f.xml  kp=[60.0, 120.0, 120.0] kd=[1.0, 1.0, 1.0] 輪 kv=0.1`。

⚠️ `make_mjx_model.py` 會**逐字元覆蓋所有**產生的 XML。跑完必須確認其他模型沒被動到：

```bash
cd /home/huang/rbtdog_sim && git status --short task7/model/zgws/
```
預期：只有 `zgws_mjx_v3f.xml` 與 `scene_flat_mjx_v3f.xml` 兩個 `??` 新檔。若有既有 XML 顯示 `M`，**停下來**先查為什麼（產生器的其他預設值被動到了）。

- [ ] **Step 6: 跑測試確認通過**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_mjx_model.py -q
```
預期：全部 PASS。

- [ ] **Step 7: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/model/zgws/make_mjx_model.py task7/model/zgws/zgws_mjx_v3f.xml task7/model/zgws/scene_flat_mjx_v3f.xml task7/inference/max_model.py task7/tests/test_mjx_model.py
git commit -m "feat(task7): v3f 模型 —— 腿 60/120/120 kd 1.0、輪 kv 0.1，完全照原廠動作段增益

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 3: 力矩空間的輪控制律（純函式）

kv 0.1 下，運動學差速算的 v_des（約 5 rad/s）只產生 0.5 N·m，推不動。控制律要改成直接指定力矩。

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`wheel_ctrl` 附近，約 287 行）
- Modify: `task7/tests/test_rl_env_v3.py`（檔尾）

**Interfaces:**
- Consumes: 模組常數 `TAU_FF = 0.13`、`V_DEAD = 0.3`
- Produces: `wheel_ctrl_tau(tau_cmd, v_meas, kv) -> jnp.ndarray`，語意＝「送進速度伺服後實際產生的力矩 = tau_cmd + τ_f·sign(tau_cmd)」，`|tau_cmd| < TAU_FF` 時輸出 0 力矩

- [ ] **Step 1: 寫失敗的測試**

在 `task7/tests/test_rl_env_v3.py` 檔尾加：

```python
def test_wheel_ctrl_tau_produces_requested_torque():
    """力矩空間控制律的定義性質：ctrl 餵進 τ = kv·(ctrl − v) 之後，力矩要等於要求值 ＋ 摩擦前饋。

    kv 0.1 時 v_des 幾乎沒有力矩權限（差 5 rad/s 只有 0.5 N·m），所以 v3.4f 直接指定力矩。
    """
    kv = 0.1
    tau_cmd = np.array([2.0, -1.5, 0.5, -0.3])
    v_meas = np.array([12.0, -8.0, 0.0, 3.0])
    ctrl = np.asarray(v3.wheel_ctrl_tau(jnp.array(tau_cmd), jnp.array(v_meas), kv))
    tau_out = kv * (ctrl - v_meas)
    np.testing.assert_allclose(tau_out, tau_cmd + np.sign(tau_cmd) * v3.TAU_FF, rtol=1e-6)


def test_wheel_ctrl_tau_dead_zone_holds_measured_speed():
    """|tau_cmd| 小於輪摩擦時要輸出零力矩（ctrl = 實測速度），不要用前饋去推一個推不動的指令。"""
    kv = 0.1
    v_meas = jnp.array([5.0, -5.0, 0.0, 1.0])
    ctrl = v3.wheel_ctrl_tau(jnp.array([0.05, -0.12, 0.0, 0.129]), v_meas, kv)
    np.testing.assert_allclose(np.asarray(ctrl), np.asarray(v_meas), rtol=0, atol=0)


def test_wheel_ctrl_tau_matches_velocity_law_in_the_clean_region():
    """G_w = 1、kv = 1 時，力矩空間與 v3.3 速度空間在「非死區、同號」的區域等價。

    兩者的死區判準不同（0.3 rad/s vs 0.13 N·m），所以只在乾淨區域比。
    這一項是 v3.4f 輪行族 G_w 起點取 1.0 的依據。
    """
    v_des = jnp.array([6.0, -6.0, 4.0, -4.0])
    v_meas = jnp.array([1.0, -1.0, 0.5, -0.5])
    tau_cmd = 1.0 * (v_des - v_meas)          # G_w = 1
    got = v3.wheel_ctrl_tau(tau_cmd, v_meas, 1.0)
    want = v3.wheel_ctrl(v_des)
    np.testing.assert_allclose(np.asarray(got), np.asarray(want), rtol=1e-6)
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k wheel_ctrl_tau
```
預期：3 個 FAIL，`AttributeError: module 'rl_env_v3' has no attribute 'wheel_ctrl_tau'`。

- [ ] **Step 3: 實作**

`task7/inference/rl_env_v3.py`，在 `def wheel_ctrl(v_des):` 那個函式之後加：

```python
def wheel_ctrl_tau(tau_cmd, v_meas, kv):
    """v3.4f 力矩空間：回傳送給速度伺服的 ctrl，使實際力矩 = tau_cmd ＋ τ_f·sign（克服輪摩擦）。

    原廠輪子 kp 0／kd 0.1 ＋ 大幅速度目標（反推 v_des 20–50 rad/s 對實際 12–17，spec §0.2），
    等於把 PD 當力矩源。kv 0.1 下速度殘差沒有力矩權限，所以殘差與週期偏移都改成直接指定力矩。
    |tau_cmd| < τ_f 視為推不動 → 輸出零力矩（ctrl = 實測速度）。
    """
    dead = jnp.abs(tau_cmd) < TAU_FF
    tau = jnp.where(dead, 0.0, tau_cmd + jnp.sign(tau_cmd) * TAU_FF)
    return v_meas + tau / kv
```

- [ ] **Step 4: 跑測試確認通過**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k wheel_ctrl
```
預期：4 passed（3 新的 ＋ 既有的 `test_wheel_cmd_gate_lateral_pattern_and_residual` 不在此 filter 內時為 3 passed，以實際輸出為準，只要沒有 FAIL）。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v3.py
git commit -m "feat(task7): 加力矩空間輪控制律 wheel_ctrl_tau —— kv 0.1 下速度殘差沒有力矩權限

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 4: 把四項寫死的 kp250 假設參數化（v3.3 行為不變）

這個 task 不改任何行為，只把常數搬進 `REF` / env 屬性，讓 Task 5 能整組切換。**Task 1 的 golden 是這個 task 唯一的驗收標準。**

**Files:**
- Modify: `task7/inference/rl_env_v3.py`
- Modify: `task7/tests/test_rl_env_v3.py`

**Interfaces:**
- Consumes: Task 3 的 `wheel_ctrl_tau`
- Produces:
  - `REF` 新增鍵：`z_sag`（float）、`nominal_height`（float）、`settle_h_range`（tuple）、`err_bar3`（3-tuple）、`wheel_space`（`"vel"` | `"tau"`）、`wheel_outer_gain`（float）
  - `foot_targets(..., duty, post_y, z_sag=Z_SAG)` 多一個尾端參數
  - `err_barrier_j(err12, bar12=ERR_BAR12)` 多一個尾端參數
  - `cycle_offsets(...)` 回傳鍵 `wheel` 改名 `wheel_tau`，單位由 rad/s 改為 **N·m（錄檔原值）**
  - `DualModeEnv._wheel_ctrl(wheel_q, v_meas, wheel_theta=None)`、`DualModeEnv._ctrl(q_des, w_ctrl)`（簽名改變）
  - `DualModeEnv.err_bar12`（jnp array, shape (12,)）、`DualModeEnv.wheel_space`（str）

- [ ] **Step 1: 寫失敗的測試**

在 `task7/tests/test_rl_env_v3.py` 檔尾加：

```python
def test_ref_exposes_gain_dependent_constants():
    """撓度、站高、護欄、輪空間都必須是 REF 的鍵，v3.4f 才切得動整組。"""
    for k in ("z_sag", "nominal_height", "settle_h_range", "err_bar3", "wheel_space", "wheel_outer_gain"):
        assert k in v3.REF, f"REF 缺鍵 {k}"
    assert v3.REF["z_sag"] == v3.Z_SAG == 0.036                 # v3.3：kp250 撓度 36 mm
    assert v3.REF["err_bar3"] == (0.60, 0.45, 0.45)             # v3.3 護欄
    assert v3.REF["wheel_space"] == "vel"                       # v3.3：速度空間
    assert v3.REF["wheel_outer_gain"] == 1.0


def test_foot_targets_z_sag_is_a_parameter():
    """抬高補償要能由外面給，否則 kp120 的 72 mm 換不進去。"""
    args = dict(theta=jnp.full(4, 1.0), amp=jnp.ones(4), g=jnp.ones(4), vec=jnp.zeros((4, 2)),
                lift4=jnp.full(4, 0.03), sway=jnp.zeros(2), u=1.0, foot_x=jnp.zeros(4), foot_z=0.0,
                duty=0.5, post_y=jnp.zeros(4))
    a = np.asarray(v3.foot_targets(**args, z_sag=0.0))
    b = np.asarray(v3.foot_targets(**args, z_sag=0.072))
    swing = np.sin(v3.duty_remap(args["theta"], args["duty"])) > 0
    assert swing.any(), "測試設定沒有任何一腿在擺動相，量不到 z_sag"
    np.testing.assert_allclose((b - a)[np.asarray(swing), 2],
                               0.072 * np.asarray(np.sin(v3.duty_remap(args["theta"], args["duty"])))[np.asarray(swing)],
                               rtol=1e-5)


def test_err_barrier_takes_per_joint_bar():
    """護欄要能由外面給：ABAD 放寬到 1.05 之後，0.8 的誤差不該再被罰。"""
    err = jnp.array([0.8, 0.0, 0.0] * 4)
    assert float(v3.err_barrier_j(err)) > 0.0                                   # 預設 0.60：罰
    wide = jnp.tile(jnp.array([1.05, 0.45, 0.45]), 4)
    assert float(v3.err_barrier_j(err, wide)) == 0.0                            # 放寬後：不罰


def test_cycle_offsets_returns_wheel_torque_in_newton_metres():
    """cycle_offsets 的輪輸出改成錄檔原始 τ（N·m），由呼叫端決定要不要除 kv。"""
    out = v3.cycle_offsets(jnp.zeros(()), jnp.array([0.0, 0.0, 1.3]), v3.activity(jnp.array([0.0, 0.0, 1.3])))
    assert "wheel_tau" in out and "wheel" not in out
    assert float(jnp.max(jnp.abs(out["wheel_tau"]))) < 6.0, "錄檔輪 τ 峰值只有 ±4.6 N·m，超過代表單位錯了"
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "gain_dependent or z_sag_is_a_parameter or per_joint_bar or newton_metres"
```
預期：4 FAIL（KeyError / TypeError / AssertionError）。

- [ ] **Step 3: REF 加鍵**

`task7/inference/rl_env_v3.py`，在 `REF = dict(...)` 內 `trans_s=1.0,` 那行之前加：

```python
    # ---- 隨增益變的四項（v3.4f 用 REF_FACTORY 整組覆寫；spec §4／§5）
    z_sag=0.036,                        # kp250 承重撓度（M8 實機 36 mm）；kp120 是 72 mm
    nominal_height=0.54, settle_h_range=(0.42, 0.60),
    err_bar3=(0.60, 0.45, 0.45),        # 分關節誤差護欄（ABAD, HIP, KNEE）
    wheel_space="vel",                  # "vel" = 速度伺服 kd 1.0（M11 實機已驗）；"tau" = 力矩空間（原廠 kd 0.1）
    wheel_outer_gain=1.0,               # 力矩空間下輪行族的外環增益 τ = G_w·(v_des − v)；1.0 ＝ kd 1.0 的等效行為
```

- [ ] **Step 4: `foot_targets` 與 `err_barrier_j` 加參數**

把 `def foot_targets(theta, amp, g, vec, lift4, sway, u, foot_x, foot_z, duty, post_y):` 改成：

```python
def foot_targets(theta, amp, g, vec, lift4, sway, u, foot_x, foot_z, duty, post_y, z_sag=Z_SAG):
```

同一函式內把 `dz = jnp.where(s > 0, (lift4 + Z_SAG * g) * s, 0.0) * ramp` 改成：

```python
    dz = jnp.where(s > 0, (lift4 + z_sag * g) * s, 0.0) * ramp
```

把 `def err_barrier_j(err12):` 與其 body 改成：

```python
def err_barrier_j(err12, bar12=ERR_BAR12):
    return jnp.sum(jnp.maximum(jnp.abs(err12) - bar12, 0.0) ** 2)
```

- [ ] **Step 5: `cycle_offsets` 改回傳原始 τ**

把 `cycle_offsets` 結尾的

```python
    wheel = blend(tau) / KV_WHEEL * ref["cyc_wheel"]
```
```python
    return dict(delta=delta, wheel=wheel, hz=hz * ref["cyc_hz_scale"], on=wl * on_l + (1.0 - wl) * on_t)
```

改成：

```python
    wheel_tau = blend(tau) * ref["cyc_wheel"]          # 錄檔原始 τ（N·m）；速度空間由呼叫端除 kv
```
```python
    return dict(delta=delta, wheel_tau=wheel_tau, hz=hz * ref["cyc_hz_scale"], on=wl * on_l + (1.0 - wl) * on_t)
```

並把 docstring 第一行的 `wheel(4) 輪速偏移 rad/s` 改成 `wheel_tau(4) 輪力矩偏移 N·m`。

> **為什麼這是逐位元不變的**：v3.3 的 `KV_WHEEL` 是 1.0，`(x/1.0)*c` 與 `x*c` 在 IEEE-754 下完全相等（除以 1.0 是精確運算）；呼叫端再除 `self.kv_wheel`（=1.0）同理。

- [ ] **Step 6: env 拆出 `_wheel_ctrl`、`_ctrl` 簡化**

把 `DualModeEnv._ctrl` 整個換成兩個方法：

```python
    def _wheel_ctrl(self, wheel_q, v_meas, wheel_theta=None):
        """wheel_q：速度空間＝v_des(rad/s)、力矩空間＝tau_cmd(N·m)。→ 輪致動器 ctrl。"""
        if self.wheel_pos:
            # 位置環：ctrl = 累加目標角；前饋等價 = 加 τ_ff/kp 的角度偏移
            dead = jnp.abs(wheel_q) < V_DEAD
            return jnp.where(dead, wheel_theta, wheel_theta + jnp.sign(wheel_q) * TAU_FF / self.kp_wheel)
        if self.wheel_space == "tau":
            return wheel_ctrl_tau(wheel_q, v_meas, self.kv_wheel)
        return wheel_ctrl(wheel_q)

    def _ctrl(self, q_des, w_ctrl):
        return jnp.zeros(16).at[LEG_ACT_IDX].set(q_des).at[WHEEL_ACT_IDX].set(w_ctrl)
```

在 `__init__` 內，`self.wheel_pos = wheel_pos` 那行之後加：

```python
        self.wheel_space = self.ref["wheel_space"]
```

在 `__init__` 結尾（`self._init_q = self._settled_qpos(m)` 之前）加：

```python
        self.err_bar12 = jnp.tile(jnp.array(self.ref["err_bar3"]), 4)
```

- [ ] **Step 7: `_settled_qpos` 用 REF 的站高**

把 `_settled_qpos` 內

```python
        d.qpos[:3] = [0.0, 0.0, NOMINAL_HEIGHT + 0.01]
```
```python
        assert 0.42 < d.qpos[2] < 0.60, f"站定高度 {d.qpos[2]:.3f} m 不合理"
```

改成：

```python
        d.qpos[:3] = [0.0, 0.0, self.ref["nominal_height"] + 0.01]
```
```python
        lo, hi = self.ref["settle_h_range"]
        assert lo < d.qpos[2] < hi, f"站定高度 {d.qpos[2]:.3f} m 不在 ({lo}, {hi})"
```

- [ ] **Step 8: `reset` 與 `step` 接上**

`reset` 內把

```python
        data = data.replace(ctrl=self._ctrl(self._init_q[LEG_QPOS_IDX], jnp.zeros(4), wheel_theta))
```

改成：

```python
        data = data.replace(ctrl=self._ctrl(self._init_q[LEG_QPOS_IDX],
                                            self._wheel_ctrl(jnp.zeros(4), jnp.zeros(4), wheel_theta)))
```

`step` 內把

```python
        feet = foot_targets(theta, amp, P["g"], P["vec"], lift4, sway, u_mode, A["foot_x"], A["foot_z"], P["duty"], P["post_y"])
```

改成：

```python
        feet = foot_targets(theta, amp, P["g"], P["vec"], lift4, sway, u_mode, A["foot_x"], A["foot_z"], P["duty"], P["post_y"], self.ref["z_sag"])
```

把

```python
        # ---- 輪速：差速×gate ＋ 平移圖案 ＋ 原廠 τ 週期偏移 ＋ 殘差（rad/s）
        wheel_v = wheel_cmd(cmd, P["A"], A["wres"], self.ref) + u4 * CY["wheel"]
        wheel_theta = info["wheel_theta"] + wheel_v * CTRL_DT              # 位置環變體用的累加目標角
```

改成：

```python
        # ---- 輪：差速×gate ＋ 平移圖案 ＋ 原廠 τ 週期偏移 ＋ 殘差
        #   速度空間（v3.3）：全部在 rad/s，錄檔 τ 除 kv 換算
        #   力矩空間（v3.4f，原廠 kd 0.1）：差速走外環 τ = G_w·(v_des − v)，錄檔 τ 與殘差直接是 N·m
        v_nom = wheel_cmd(cmd, P["A"], jnp.zeros(4), self.ref)
        v_wheel_meas = data.qvel[WHEEL_QVEL_IDX]
        if self.wheel_space == "tau":
            wheel_q = self.ref["wheel_outer_gain"] * (v_nom - v_wheel_meas) + A["wres"] + u4 * CY["wheel_tau"]
        else:
            wheel_q = v_nom + A["wres"] + u4 * CY["wheel_tau"] / self.kv_wheel
        wheel_theta = info["wheel_theta"] + wheel_q * CTRL_DT             # 位置環變體用的累加目標角
```

把

```python
        c = dict(theta=theta, amp=amp, vec=P["vec"] * amp[:, None], wheel=wheel_v, cyc_abad=d3[:, 0], cyc_knee=d3[:, 2])
```

改成：

```python
        c = dict(theta=theta, amp=amp, vec=P["vec"] * amp[:, None], wheel=wheel_q, cyc_abad=d3[:, 0], cyc_knee=d3[:, 2])
```

把

```python
        ctrl = self._ctrl(q_des, wheel_v, wheel_theta)
```

改成：

```python
        ctrl = self._ctrl(q_des, self._wheel_ctrl(wheel_q, v_wheel_meas, wheel_theta))
```

把 reward 區的

```python
        r_h = jnp.exp(-400.0 * (data.qpos[2] - NOMINAL_HEIGHT) ** 2)
```

改成：

```python
        r_h = jnp.exp(-400.0 * (data.qpos[2] - self.ref["nominal_height"]) ** 2)
```

把

```python
            "t_taubar": w["W_TAUBAR"] * tau_barrier(tau_pk12), "t_errbar": w["W_ERRBAR"] * err_barrier_j(err12),
```

改成：

```python
            "t_taubar": w["W_TAUBAR"] * tau_barrier(tau_pk12), "t_errbar": w["W_ERRBAR"] * err_barrier_j(err12, self.err_bar12),
```

⚠️ `data` 在 `step` 裡會被 `qvel` kick 改寫。`v_wheel_meas` 必須在 **kick 之前**取（外環回授用的是這一控制步開始時的實測輪速）。上面的插入位置（緊接 `CY = cycle_offsets(...)` 那段之後、`data = state.pipeline_state` 之前）**取不到 `data`**，所以實作時把
`data = state.pipeline_state` 這一行**往上搬**到 `wheel_q` 計算之前，kick 那三行維持在原位（在 `wheel_q` 之後）。改完確認順序是：
`data = state.pipeline_state` → `v_wheel_meas = ...` → `wheel_q = ...` → `rng/kick` → `ctrl = ...`。

- [ ] **Step 9: 跑新測試**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "gain_dependent or z_sag_is_a_parameter or per_joint_bar or newton_metres"
```
預期：4 passed。

- [ ] **Step 10: ★ 跑 golden 證明 v3.3 沒被動到**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3_golden.py -q
```
預期：2 passed。
**FAIL 的話不要放寬容差。** 先用下面這行印出差在哪一格，回頭找對應的改動：

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -c "
import sys; sys.path.insert(0,'task7/inference'); sys.path.insert(0,'task7/tests')
import numpy as np, test_rl_env_v3_golden as G
o,r,q,c = G._rollout(); g = np.load(G.GOLDEN)
for n,v in (('obs',o),('reward',r),('qpos',q),('ctrl',c)):
    d = np.abs(v-g[n]); print(n, 'max diff', d.max(), 'at', np.unravel_index(d.argmax(), d.shape))"
```

- [ ] **Step 11: 跑整個 v3 測試檔**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q
```
預期：全部 PASS（約 5 分鐘）。若既有測試引用了 `CY["wheel"]` 或 `foot_targets` 的舊簽名而失敗，**改測試去對新介面**（那是介面更名的預期後果），但不要改任何門檻數字。

- [ ] **Step 12: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v3.py
git commit -m "refactor(task7): 撓度／站高／護欄／輪空間參數化 —— v3.3 行為逐位元不變（golden 過）

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 5: `gains` 開關

**Files:**
- Modify: `task7/inference/rl_env_v3.py`
- Modify: `task7/tests/test_rl_env_v3.py`

**Interfaces:**
- Consumes: Task 2 的 `mm.SCENE_MJX_V3F`、Task 4 的 REF 鍵
- Produces:
  - `v3.REF_FACTORY`（dict）、`v3.GAIN_SETS`（dict，鍵 `"kp250"` / `"factory"`）
  - `DualModeEnv(scene=None, wheel_pos=False, ref=None, weights=None, gains="kp250")`
  - `make_domain_randomize(gains="kp250") -> callable`；模組級 `domain_randomize` 保留為 `make_domain_randomize("kp250")`

- [ ] **Step 1: 寫失敗的測試**

在 `task7/tests/test_rl_env_v3.py` 檔尾加：

```python
def test_gain_sets_match_factory_recording():
    """GAIN_SETS 的兩組要分別對上「我們 kp250 線」與「原廠動作段」（spec §0.1）。"""
    import max_model as mm
    assert set(v3.GAIN_SETS) == {"kp250", "factory"}
    np.testing.assert_allclose(v3.GAIN_SETS["factory"]["kp3"], [60.0, 120.0, 120.0])
    np.testing.assert_allclose(v3.GAIN_SETS["factory"]["kd3"], [1.0, 1.0, 1.0])
    assert v3.GAIN_SETS["factory"]["scene"] == mm.SCENE_MJX_V3F
    np.testing.assert_allclose(v3.GAIN_SETS["kp250"]["kp3"], [60.0, 250.0, 250.0])


def test_factory_env_reads_gains_from_model_and_switches_ref():
    """gains="factory" 要真的載到 v3f 模型，而且 REF 的四項一起換過去。"""
    env = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False))
    assert env.wheel_space == "tau"
    assert abs(env.kv_wheel - 0.1) < 1e-9
    kp = np.asarray(env._mj.actuator_gainprm[v3.mm.LEG_ACT_IDX, 0])
    np.testing.assert_allclose(kp, np.tile([60.0, 120.0, 120.0], 4))
    np.testing.assert_allclose(np.asarray(env.err_bar12), np.tile([1.05, 0.45, 0.45], 4))
    assert env.ref["z_sag"] == 0.072
    assert (env.obs_dim, env.action_size) == (88, 24)          # 介面與 v3.3 相同，狗端推論路徑不變


def test_factory_env_steps_and_wheel_torque_is_in_range():
    """v3f env 能跑，而且原地轉時輪子拿得到力矩權限（kd 0.1 下速度殘差只有 0.2 N·m 是不夠的）。"""
    env = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False))
    jit_reset, jit_step = jax.jit(env.reset), jax.jit(env.step)
    s = jit_reset(jax.random.PRNGKey(0))
    s = s.replace(info={**s.info, "cmd": jnp.array([0.0, 0.0, 1.3]), "cmd2": jnp.array([0.0, 0.0, 1.3]), "t_switch": 10 ** 6})
    tau = []
    for _ in range(20):
        s = jit_step(s, jnp.zeros(v3.ACT_DIM))
        tau.append(np.asarray(s.pipeline_state.actuator_force[v3.WHEEL_ACT_IDX]))
        assert np.isfinite(np.asarray(s.obs)).all()
    pk = np.abs(np.array(tau)).max()
    assert 0.5 < pk < 33.0, f"輪力矩峰 {pk:.2f} N·m 不合理（原廠錄檔動作段是 ±2~4.6）"


def test_kp250_env_is_still_the_default():
    """不給 gains 時必須完全是 v3.3。"""
    env = v3.DualModeEnv()
    assert env.wheel_space == "vel"
    assert abs(env.kv_wheel - 1.0) < 1e-9
    np.testing.assert_allclose(np.asarray(env.err_bar12), np.tile([0.60, 0.45, 0.45], 4))


def test_domain_randomize_uses_the_right_nominal_gains():
    """DR 的標稱增益要跟著模型走，否則 factory 線會被隨機化拉回 kp250 附近。"""
    env = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False))
    dr = v3.make_domain_randomize("factory")
    sys2, _ = dr(env.sys, jax.random.split(jax.random.PRNGKey(0), 4))
    kp = np.asarray(sys2.actuator_gainprm[:, v3.mm.LEG_ACT_IDX, 0])
    hip = kp[:, 1]                                              # HIP 標稱 120，DR ×0.8–1.2
    assert hip.min() >= 120 * 0.8 - 1e-3 and hip.max() <= 120 * 1.2 + 1e-3, f"HIP kp 範圍 {hip.min()}–{hip.max()}"
    abad = kp[:, 0]                                             # ABAD 標稱 60，DR ×0.7–1.0
    assert abad.min() >= 60 * 0.7 - 1e-3 and abad.max() <= 60 * 1.0 + 1e-3, f"ABAD kp 範圍 {abad.min()}–{abad.max()}"
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "gain_sets or factory_env or still_the_default or right_nominal"
```
預期：5 FAIL（`AttributeError: GAIN_SETS` 等）。

- [ ] **Step 3: 加 `REF_FACTORY` 與 `GAIN_SETS`**

`task7/inference/rl_env_v3.py`，在 `KV_WHEEL, TAU_FF, V_DEAD = ...` 那行之前（`REF = dict(...)` 結束之後）加：

```python
SCENE_V3F = str(Path(mm.SCENE_MJX_KP250).with_name("scene_flat_mjx_v3f.xml"))

# ---- v3.4f：馬達增益完全照原廠錄檔動作段（spec 2026-09-15-cpg-rl-v3.4f-factory-gains-design.md）
#   腿 kp 250→120 讓承重撓度 36→72 mm（M8 實機），命令相關的常數要跟著重掃（spec §4）；
#   輪 kv 1.0→0.1 讓速度殘差沒有力矩權限，改走力矩空間（spec §3）。
#   z_sag／lat_cmd_gain／wheel_outer_gain 的值由 G0 掃定（Task 8），這裡是起點。
REF_FACTORY = dict(
    z_sag=0.072,                        # M8 實機：kp120 撓度 72 mm（kp250 是 36）
    err_bar3=(1.05, 0.45, 0.45),        # 原廠 ABAD 命令差實測 max 1.02 rad；髖膝 max 0.32 < 0.45 不動（spec §0.3）
    wheel_space="tau",
    wheel_outer_gain=1.0,               # 起點＝kd 1.0 的等效行為
)

GAIN_SETS = {
    "kp250": dict(scene=SCENE_V3, kp3=mm.KP3_A, kd3=mm.KD3_A, ref={}),          # v3.3（預設）
    "factory": dict(scene=SCENE_V3F, kp3=mm.KP3, kd3=mm.KD3, ref=REF_FACTORY),  # v3.4f
}
```

- [ ] **Step 4: `__init__` 吃 `gains`**

把 `DualModeEnv.__init__` 的簽名與開頭幾行改成：

```python
    def __init__(self, scene: str = None, wheel_pos: bool = False, ref: dict = None, weights: dict = None, gains: str = "kp250"):
        """`wheel_pos=False`（預設）：純速度伺服（M11 實機已驗的 kd 1.0）；True：位置＋速度伺服（實機未驗，v3.1 不用）。
        `gains="kp250"`＝v3.3；`"factory"`＝馬達增益完全照原廠錄檔動作段（v3.4f）。`ref`／`weights` 覆寫 REF／W（掃參數用）。"""
        assert gains in GAIN_SETS, f"gains 只能是 {set(GAIN_SETS)}"
        self.gains = gains
        G = GAIN_SETS[gains]
        self.w = dict(W, **(weights or {}))
        self.ref = dict(REF, **G["ref"], **(ref or {}))
        self.wheel_pos = wheel_pos
        self.wheel_space = self.ref["wheel_space"]
        if scene is None:
            scene = SCENE_V3P if wheel_pos else G["scene"]
```

（其餘行不動。）

- [ ] **Step 5: `domain_randomize` 變工廠函式**

把模組尾端的

```python
_KP_NOM_j = jnp.array(np.tile(np.asarray(mm.KP3_A), 4))
_KD_NOM = float(np.asarray(mm.KD3_A)[1])
ABAD12 = jnp.array([0, 3, 6, 9])


def domain_randomize(sys, rng):
```

改成：

```python
ABAD12 = jnp.array([0, 3, 6, 9])


def make_domain_randomize(gains: str = "kp250"):
    """回傳對應增益組的 domain_randomize。標稱 kp/kd 必須跟著模型走，
    否則 factory 線會被隨機化拉回 kp250 附近（DR 是乘在標稱值上的）。"""
    G = GAIN_SETS[gains]
    _KP_NOM_j = jnp.array(np.tile(np.asarray(G["kp3"]), 4))
    _KD_NOM = float(np.asarray(G["kd3"])[1])

    def domain_randomize(sys, rng):
```

把原本 `domain_randomize` 的整個 body 往內縮排一層（body 的最後是 `return sys, in_axes`），然後在函式外、模組層級加：

```python
    return domain_randomize


domain_randomize = make_domain_randomize("kp250")     # 舊名保留：v3.3 的 notebook 不用改
```

⚠️ 縮排是這一步唯一容易出錯的地方。改完先用這行確認語法與行為：

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -c "
import sys; sys.path.insert(0,'task7/inference'); import rl_env_v3 as v3
print('ok', callable(v3.domain_randomize), callable(v3.make_domain_randomize('factory')))"
```
預期：`ok True True`

- [ ] **Step 6: 跑新測試**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3.py -q -k "gain_sets or factory_env or still_the_default or right_nominal"
```
預期：5 passed。
若 `test_factory_env_steps_and_wheel_torque_is_in_range` 失敗且輪力矩 < 0.5 N·m，代表力矩沒接上（多半是 `wheel_space` 沒讀到或 `wheel_q` 還走速度分支）——回 Task 4 Step 8 對一次順序。

- [ ] **Step 7: golden ＋ 全檔測試**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_rl_env_v3_golden.py task7/tests/test_rl_env_v3.py -q
```
預期：全部 PASS。

- [ ] **Step 8: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v3.py
git commit -m "feat(task7): DualModeEnv 加 gains 開關 —— factory 線走 v3f 模型、力矩空間輪控制、ABAD 護欄 1.05

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 6: 工具串接（g0_v3 與 replay_factory）

**Files:**
- Modify: `task7/inference/diag/g0_v3.py`
- Modify: `task7/inference/diag/replay_factory.py`
- Modify: `task7/tests/test_replay_factory.py`

**Interfaces:**
- Consumes: Task 5 的 `DualModeEnv(gains=...)`、Task 3 的 `wheel_ctrl_tau`
- Produces: `g0_v3.py --gains {kp250,factory}`、`replay_factory.py --actuator`、`replay_factory.make_model(mu=None, scene=None, actuator=False)`

- [ ] **Step 1: g0_v3 加 `--gains`**

`task7/inference/diag/g0_v3.py`，在 `ap.add_argument("--wheel-pos", ...)` 那行之後加：

```python
    ap.add_argument("--gains", default="kp250", choices=("kp250", "factory"), help="factory＝馬達增益完全照原廠（v3.4f）")
```

把 `env = v3.DualModeEnv(wheel_pos=a.wheel_pos, ref=dict(ref, cyc_amp_rand=False))` 改成：

```python
        env = v3.DualModeEnv(wheel_pos=a.wheel_pos, gains=a.gains, ref=dict(ref, cyc_amp_rand=False))
```

把它下面那行 print 的 `wheel_pos {env.wheel_pos}` 改成 `gains {env.gains} wheel_pos {env.wheel_pos}`。

另外把 `--lat-gain`／`--lift-lat` 的鄰居補上兩個 Task 8 要掃的旋鈕，在 `--mu` 那行之後加：

```python
    ap.add_argument("--z-sag", type=float, default=None, dest="z_sag"); ap.add_argument("--wheel-gain", type=float, default=None, dest="wheel_gain")
```

在 `if a.lat_gain is not None:` 那個區塊之後加：

```python
    if a.z_sag is not None:
        base["z_sag"] = a.z_sag
    if a.wheel_gain is not None:
        base["wheel_outer_gain"] = a.wheel_gain
```

- [ ] **Step 2: 驗 g0 兩條線都跑得起來（短跑）**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 40 --cases STAND --gains kp250
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 40 --cases STAND --gains factory
```
預期：各印一行，開頭含 `gains kp250` / `gains factory`，`obs 88 act 24`，`done@None`。
`factory` 那行的 `h`（站定高度）會明顯低於 `kp250`——那正是撓度翻倍，是預期的，Task 8 處理。

- [ ] **Step 3: replay_factory 加致動器路徑（寫測試）**

在 `task7/tests/test_replay_factory.py` 檔尾加：

```python
def test_actuator_path_produces_recorded_torque():
    """走「kv 0.1 致動器」的路徑，實際產生的輪力矩要等於錄檔 τ ＋ 摩擦前饋。

    這是 v3.4f 力矩空間控制律的端對端驗證：若這裡不成立，
    G0 與訓練用的輪力矩就不是錄檔那個值（而 qfrc_applied 那條舊路是對的，兩者會對不起來）。
    """
    import mujoco
    import rl_env_v3 as v3

    m = rf.make_model(scene=mm.SCENE_MJX_V3F, actuator=True)
    d = mujoco.MjData(m)
    tau_rec = np.array([1.5, -1.0, 0.8, -0.2])
    d.qvel[mm.WHEEL_QVEL_IDX] = [10.0, -10.0, 3.0, 0.0]
    mujoco.mj_forward(m, d)
    v_meas = d.qvel[mm.WHEEL_QVEL_IDX].copy()
    d.ctrl[mm.WHEEL_ACT_IDX] = np.asarray(v3.wheel_ctrl_tau(tau_rec, v_meas, 0.1))
    mujoco.mj_forward(m, d)
    got = d.actuator_force[mm.WHEEL_ACT_IDX]
    want = tau_rec + np.sign(tau_rec) * v3.TAU_FF
    np.testing.assert_allclose(got, want, rtol=1e-5, atol=1e-6)
```

- [ ] **Step 4: 跑測試確認失敗**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_replay_factory.py -q -k actuator_path_produces
```
預期：FAIL，`make_model() got an unexpected keyword argument 'scene'`。

- [ ] **Step 5: 實作 `make_model` 的新參數**

`task7/inference/diag/replay_factory.py`，把 `make_model` 改成：

```python
def make_model(mu: float | None = None, scene: str | None = None, actuator: bool = False) -> mujoco.MjModel:
    """`scene` 預設 `scene_flat_mjx.xml`（腿 60/120/120 kd 1.0 ＝ 原廠動作段）。
    `actuator=True`：輪子保留致動器（v3.4f 的 kv 0.1 速度伺服，力矩由 ctrl 指定）；
    False（預設）：關掉輪致動器、錄檔 τ 直接進 qfrc_applied（v3.1 步驟 0 的做法）。"""
    m = mujoco.MjModel.from_xml_path(scene or mm.SCENE_MJX)
    kp = m.actuator_gainprm[mm.LEG_ACT_IDX, 0]
    assert np.allclose(kp, np.tile([60.0, 120.0, 120.0], 4)), f"腿增益不是原廠動作組：{kp}"
    assert np.allclose(-m.actuator_biasprm[mm.LEG_ACT_IDX, 2], 1.0)
    if not actuator:
        m.actuator_gainprm[mm.WHEEL_ACT_IDX, :] = 0.0
        m.actuator_biasprm[mm.WHEEL_ACT_IDX, :] = 0.0
    # 輪關節照 M11 實測（與 scene_flat_mjx_v3 同）：τ_f 0.13、b 0.015、armature 0.004
    m.dof_frictionloss[mm.WHEEL_QVEL_IDX] = 0.13
    m.dof_damping[mm.WHEEL_QVEL_IDX] = 0.015
    m.dof_armature[mm.WHEEL_QVEL_IDX] = 0.004
    if mu is not None:
        m.geom_friction[:, 0] = mu
    return m
```

- [ ] **Step 6: 把 `--scene` / `--actuator` 接到 CLI 與回放迴圈**

在 `replay_factory.py` 的 argparse 區加（與 `--mu` 同一批）：

```python
    ap.add_argument("--scene", default=None, help="預設 scene_flat_mjx.xml；v3.4f 驗證用 task7/model/zgws/scene_flat_mjx_v3f.xml")
    ap.add_argument("--actuator", action="store_true", help="輪子走 kv 0.1 致動器（v3.4f 力矩空間），不用 qfrc_applied")
```

找到呼叫 `make_model(` 的地方，改成 `make_model(mu=..., scene=a.scene, actuator=a.actuator)`（把 `a.scene`／`a.actuator` 沿呼叫鏈傳下去）。

在逐 tick 施力的迴圈裡，把原本「寫 `d.qfrc_applied[wheel dof] = tau`」的那一段改成分支：

```python
        if actuator:
            d.ctrl[mm.WHEEL_ACT_IDX] = np.asarray(
                v3.wheel_ctrl_tau(tau_row, d.qvel[mm.WHEEL_QVEL_IDX], -m.actuator_biasprm[mm.WHEEL_ACT_IDX[0], 2]))
        else:
            d.qfrc_applied[mm.WHEEL_QVEL_IDX] = tau_row
```

（檔頭 import 區加 `import rl_env_v3 as v3`；`tau_row` 是該 tick 的四輪 τ，沿用原本變數名。）

- [ ] **Step 7: 跑測試確認通過**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_replay_factory.py -q
```
預期：全部 PASS。

- [ ] **Step 8: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/inference/diag/g0_v3.py task7/inference/diag/replay_factory.py task7/tests/test_replay_factory.py
git commit -m "feat(task7): g0_v3 --gains、replay_factory --scene/--actuator —— v3.4f 的驗收路徑

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 7: v3.4f 的 Colab notebook

**Files:**
- Modify: `task7/notebooks/build_nb_v3.py`
- Create: `task7/notebooks/cpg_rl_v3_4f_colab.ipynb`（產生物）
- Modify: `task7/tests/test_notebook_v2.py`（notebook 結構測試放在這個檔）

**Interfaces:**
- Consumes: Task 5 的 `DualModeEnv(gains="factory")`、`make_domain_randomize("factory")`
- Produces: `build_nb_v3.py --gains factory` → `cpg_rl_v3_4f_colab.ipynb`

- [ ] **Step 1: 寫失敗的測試**

在 `task7/tests/test_notebook_v2.py` 檔尾加：

```python
def test_v34f_notebook_trains_the_factory_gain_env():
    """v3.4f notebook 必須每一處都用 factory 增益：env、eval_env、domain randomization。

    漏掉 domain_randomize 會是最難發現的一種錯：訓練照跑、曲線照畫，
    但隨機化是乘在 kp250 標稱值上，等於根本沒在訓原廠增益。
    """
    import json
    from pathlib import Path
    nb = Path(__file__).resolve().parents[1] / "notebooks" / "cpg_rl_v3_4f_colab.ipynb"
    assert nb.exists(), "尚未產生 cpg_rl_v3_4f_colab.ipynb"
    src = "\n".join("".join(c["source"]) for c in json.loads(nb.read_text(encoding="utf-8"))["cells"])
    assert 'DualModeEnv(gains="factory"' in src
    assert 'make_domain_randomize("factory")' in src
    assert src.count('gains="factory"') >= 2, "env 與 eval_env 都要帶 gains"
    assert "cpg_rl_v3_4f_params.pkl" in src
    assert "v3.3" not in src.split("## ")[0], "標題段不要還寫 v3.3"
```

- [ ] **Step 2: 跑測試確認失敗**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_notebook_v2.py -q -k v34f
```
預期：FAIL，`尚未產生 cpg_rl_v3_4f_colab.ipynb`。

- [ ] **Step 3: build_nb_v3.py 加 `--gains`**

在 `build_nb_v3.py` 檔頭 `OUT = ...` / `WEIGHTS = ...` 兩行改成由參數決定：

```python
import argparse

HERE = Path(__file__).resolve().parent
_ap = argparse.ArgumentParser()
_ap.add_argument("--gains", default="kp250", choices=("kp250", "factory"))
ARGS = _ap.parse_args()
FACTORY = ARGS.gains == "factory"
OUT = HERE / ("cpg_rl_v3_4f_colab.ipynb" if FACTORY else "cpg_rl_v3_colab.ipynb")
WEIGHTS = "cpg_rl_v3_4f_params.pkl" if FACTORY else "cpg_rl_v3_params.pkl"
GAINS_KW = 'gains="factory", ' if FACTORY else ''
```

把 `md0` 的定義改成兩選一（在原本 `md0 = f"""..."""` 之後加）：

```python
if FACTORY:
    md0 = f"""# CPG-RL **v3.4f**：智元 D1 Max · 馬達增益完全照原廠 · MJX · Colab GPU（2026-09-15）

與 v3.3 的唯一差別是**馬達增益**：腿 ABAD 60 / HIP 120 / KNEE 120、kd 1.0，輪 kv 0.1
—— 逐檔掃 trip21 17 檔確認，這就是原廠錄檔動作段在用的那組（`scene_flat_mjx_v3f.xml`）。
連帶三項：撓度 36→72 mm（`z_sag` 0.072）、ABAD 誤差護欄 0.60→1.05（原廠命令差實測 max 1.02 rad）、
輪子改力矩空間（kv 0.1 下速度殘差只剩 ±0.2 N·m，沒有權限）。
設計：`docs/superpowers/specs/2026-09-15-cpg-rl-v3.4f-factory-gains-design.md`。

**停損**：同 v3.3 —— 1 億步時 `進度 yaw` < 3.5 或 len < 600 → 停。
**⚠️ `TAU_BAR` 58 在 kp 減半後大概率不再觸發**（spec §5）：訓練曲線要盯 `tau_pk`，
如果它明顯超過原廠的 40–60 而 `t_taubar` 一直是 0，代表這一版少了一個約束，要把 `TAU_BAR` 調到 35–40 重訓。
"""
```

把 `import_src` 內的 `env = v3.DualModeEnv()` 改成：

```python
import_src = f'''import jax, jax.numpy as jnp, numpy as np, mujoco
import rl_env_v3 as v3
env = v3.DualModeEnv({GAINS_KW})
print("obs", env.obs_dim, "act", env.action_size, "gains", env.gains, "wheel_space", env.wheel_space)
assert env.wheel_pos is False
print("REF", {{k: v for k, v in env.ref.items() if not hasattr(v, "shape")}})
assert (env.obs_dim, env.action_size) == (88, 24)
'''
```

在 `train_src` 裡，把 `v3.DualModeEnv()` 與 `v3.DualModeEnv(ref=dict(cyc_amp_rand=False))` 分別改成帶 `{GAINS_KW}`，並把 `randomization_fn` 指到 `v3.make_domain_randomize("{ARGS.gains}")`。

> 實作提示：`train_src` 目前是一段長字串。改成 f-string 時，字串內原有的 `{` `}`（dict、format）都要改成 `{{` `}}`。改完務必用下一步的 `--gains kp250` 逐字元比對確認沒改壞。

- [ ] **Step 4: ★ 先證明 kp250 的 notebook 沒被改壞**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/notebooks/build_nb_v3.py --gains kp250 && git diff --stat task7/notebooks/cpg_rl_v3_colab.ipynb
```
預期：`git diff --stat` **沒有輸出**（v3.3 的 notebook 逐字元不變）。
有 diff 就是 f-string 轉換弄壞了，回 Step 3 修到沒有 diff 為止。

- [ ] **Step 5: 產生 v3.4f notebook**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/notebooks/build_nb_v3.py --gains factory
```
預期：`→ .../cpg_rl_v3_4f_colab.ipynb (9 cells)`

- [ ] **Step 6: 跑測試確認通過**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/test_notebook_v2.py -q
```
預期：全部 PASS。

- [ ] **Step 7: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/notebooks/build_nb_v3.py task7/notebooks/cpg_rl_v3_4f_colab.ipynb task7/tests/test_notebook_v2.py
git commit -m "feat(task7): v3.4f Colab notebook —— env/eval_env/DR 三處都用 factory 增益

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

## Task 8: 驗收與撓度重掃（跑實驗、填數字、寫文件）

這個 task 不是寫功能，是**跑實驗定常數**。spec §7 的順序不能跳：前一關不過就不進下一關。

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`REF_FACTORY` 填入掃出來的值）
- Create: `task7/outputs/replay_factory_v3f.md`、`task7/outputs/g0_v34f_final.txt`
- Modify: `docs/superpowers/specs/2026-09-15-cpg-rl-v3.4f-factory-gains-design.md`（§11 執行紀錄）
- Modify: `task7/HANDOFF.md`（頂節）

**Interfaces:**
- Consumes: Task 6 的兩個工具
- Produces: `REF_FACTORY` 的定案值；驗收結果檔

- [ ] **Step 1: 關卡一 —— 原廠回放回歸**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/inference/diag/replay_factory.py \
  --window 1.5 --starts 3 --scene task7/model/zgws/scene_flat_mjx_v3f.xml --actuator \
  2>&1 | tee task7/outputs/replay_factory_v3f.md
```

對照 v3.1 spec §8.1 的舊數字：

| 檔 | 舊（qfrc 路徑） | 這次要落在 |
|---|---|---|
| turn_left / turn_right 偏航比 | 0.52–0.76 | 0.45 以上 |
| arc_left / arc_right 偏航比 | 0.42–0.52 | 0.35 以上 |
| fwd_fast 輪速比 | 1.08–1.13 | 0.95–1.25 |
| 摔 | 全部「否」 | 全部「否」 |

**不過就停。** 兩條路（qfrc 直接施力 vs kv 0.1 致動器）算的是同一個力矩（Task 6 的測試已釘住單點），
若端對端差很多，先跑不帶 `--actuator` 的同一組當對照，把差異定位到「致動器路徑」還是「模型換了」：

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/inference/diag/replay_factory.py --window 1.5 --starts 3
```

- [ ] **Step 2: 關卡二 —— G0 站定高度與撓度**

先量站著（`STAND`）與純輪行（`WHEEL`）在三個 `z_sag` 下的高度與離地：

```bash
cd /home/huang/rbtdog_sim && for z in 0.036 0.072 0.100; do
  conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 500 --gains factory --cases STAND,WHEEL --z-sag $z
done 2>&1 | tee task7/outputs/g0_v34f_zsag.txt
```

判準：
- `STAND` 的 `h` 要落在 0.42–0.60（否則 `_settled_qpos` 的 assert 會擋掉訓練）；若整排都低於 0.42，把 `REF_FACTORY` 加上 `nominal_height` 與 `settle_h_range` 改成量到的值。
- `WHEEL vx0.5` 要 `vx ≥ 0.45`、`lift` 四腿都 < 10 mm、`done@None`。
- 選 `lift` 能讓擺動腿真的離地（≥ 12 mm）而 `tau_pk` 最低的那個 `z_sag`。

**⚠️ 若三個 `z_sag` 下 `STAND` 全部 `done@` 有值（站不住），這一版就停在這裡**（spec §8 風險 1）：
把結果寫成 `task7/docs/results/N_原廠增益模擬驗證_2026-09-15.md`，結論寫「原廠增益在我們的模擬器裡站不住」，**不要上 Colab**，跳到 Step 6。

- [ ] **Step 3: 關卡二續 —— 輪行外環與平移**

```bash
cd /home/huang/rbtdog_sim && for g in 1.0 3.0 10.0; do
  conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 500 --gains factory --cases WHEEL,ARC --wheel-gain $g --z-sag <Step2 選定值>
done 2>&1 | tee task7/outputs/g0_v34f_wheelgain.txt

cd /home/huang/rbtdog_sim && for lg in 1.0 1.5 2.0; do
  conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 500 --gains factory --cases LAT --lat-vy 0.04,0.30 --lat-gain $lg --z-sag <Step2 選定值>
done 2>&1 | tee task7/outputs/g0_v34f_latgain.txt
```

判準（沿用 spec v3.1 §5.1／§8.2 的門檻，不放寬）：
- `WHEEL vx0.5`：`vx ≥ 0.45`
- `ARC vx0.5 wz0.5`：`yaw ≥ 20°/s`、`lift[1]`（內側前腿 FL）> 12 mm
- `LAT`：`|vy| ≥ 指令 60%`、`tau_pk ≤ 85`、`roll_max ≤ 8°`、`roll_std ≤ 1.5`
- `TURN wz1.3` 走原廠週期，**開迴路預期會倒**，只記 `done@` 與倒前偏航，不當門檻

- [ ] **Step 4: 定案並寫回 `REF_FACTORY`**

把 Step 2／3 選定的值填進 `task7/inference/rl_env_v3.py` 的 `REF_FACTORY`，每個值後面寫上依據（哪一次掃、對照哪個數字）。
然後跑定案的六指令全套，存檔：

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python task7/inference/diag/g0_v3.py --steps 500 --gains factory 2>&1 | tee task7/outputs/g0_v34f_final.txt
```

- [ ] **Step 5: 全套測試 ＋ golden**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog python -m pytest task7/tests/ -q 2>&1 | tail -5
```
預期：全部 PASS（含 Task 1 的 golden）。測試總數應該是原本的 821 再加上這次新增的那些。

- [ ] **Step 6: 寫結果文件與更新交接**

1. `docs/superpowers/specs/2026-09-15-cpg-rl-v3.4f-factory-gains-design.md` 的 §11 執行紀錄填入：
   replay 回歸數字、G0 掃描表、`REF_FACTORY` 定案值與依據、`TAU_BAR` 觸發率。
2. `task7/HANDOFF.md` 頂節加一段 v3.4f 的狀態：現在到哪一關、下一步是什麼、停損條件。
3. 若 Step 2 判定「站不住」，改寫 `task7/docs/results/N_原廠增益模擬驗證_2026-09-15.md` 並在 HANDOFF 標明這條線收在哪。

- [ ] **Step 7: Commit**

```bash
cd /home/huang/rbtdog_sim
git add task7/inference/rl_env_v3.py task7/outputs/ task7/HANDOFF.md docs/superpowers/specs/2026-09-15-cpg-rl-v3.4f-factory-gains-design.md task7/docs/results/
git commit -m "feat(task7): v3.4f 常數定案與驗收 —— replay 回歸過、G0 掃出 REF_FACTORY

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

- [ ] **Step 8: push，交給使用者跑 Colab**

```bash
cd /home/huang/rbtdog_sim && git push origin main && git log --oneline -1
```

回報給使用者的內容要包含：replay 回歸比值、G0 六指令結果、`REF_FACTORY` 定案值、
notebook 路徑 `task7/notebooks/cpg_rl_v3_4f_colab.ipynb`、停損條件（1 億步 yaw < 3.5 或 len < 600）。

---

## 附：spec 覆蓋對照

| spec 節 | 由哪個 task 實現 |
|---|---|
| §0.1／§0.2 事實（已完成的量測） | 已寫進 spec，Task 3 的控制律與 Task 2 的模型是它的產物 |
| §0.3 誤差分位數 | Task 5 的 `REF_FACTORY["err_bar3"]` |
| §2 模型 | Task 2 |
| §3 力矩空間控制律 | Task 3（純函式）＋ Task 4 Step 8（接線） |
| §4 撓度重掃 | Task 4（參數化）＋ Task 8 Step 2–4（掃值） |
| §5 護欄 | Task 4（參數化）＋ Task 5（`REF_FACTORY`）；`TAU_BAR` 觸發率在 Task 7 notebook 提醒、Task 8 Step 6 記錄 |
| §6 `gains` 開關與 notebook | Task 5 ＋ Task 7；「v3.3 逐位元不變」由 Task 1 的 golden 保證 |
| §7 驗收順序 | Task 8 Step 1–5 |
| §8 風險與備案 | Task 8 Step 2 的停損、Task 8 Step 6 的文件 |
| §9 上機 | 明確不做（另開 spec） |
| §10 檔案 | 本計畫的檔案結構表 |
