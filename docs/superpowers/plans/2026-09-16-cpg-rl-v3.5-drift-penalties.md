# CPG-RL v3.5 慢漂懲罰 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v3.3 的 env 上加兩個低通慢漂懲罰（ABAD、機身速度）與平移權重／取樣調整，產出 v3.5 的 Colab notebook，且 v3.3 預設路徑逐位元不變。

**Architecture:** 兩個純函數 `ema()`／`drift_terms()` 放在 `rl_env_v3.py`，env 的 `reset`／`step` 只多兩個 EMA 狀態、兩個 reward 項、兩個 metrics。新權重全部經 `W35` 字典覆寫；預設 `W` 的新權重是 0.0，所以 v3.3 reward 精確不變（golden 測試是證據）。notebook 產生器加 `--weights v35` 開關，v3.3／v3.4f 兩本輸出逐位元不變。

**Tech Stack:** JAX／MJX（brax 0.14.2）、pytest、conda env `rbtdog`（所有 python 指令用 `conda run -n rbtdog --no-capture-output python ...`，從 repo 根目錄 `/home/huang/rbtdog_sim` 執行）。

## Global Constraints

- spec：`docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md`
- `tests/test_rl_env_v3_golden.py` 必須不改一字就過；失敗＝動到 v3.3，先查再說，**不放寬容差**。
- `notebooks/cpg_rl_v3_colab.ipynb`、`notebooks/cpg_rl_v3_4f_colab.ipynb` 重建後 `git status` 必須乾淨（逐位元不變）。
- 新權重值：`W_ABADBIAS=30.0`、`W_DRIFT=40.0`、`W_VYREL=6.0`、`P_VY=0.60`；預設 `W` 中 `W_ABADBIAS=0.0`、`W_DRIFT=0.0`。低通係數沿用 `BIAS_EMA=0.02`。
- obs 維持 88、動作維持 24。
- 每個 task 結尾 commit，訊息結尾附 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` 與 `Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b`。
- 不 push，除非 Task 7 明說（Colab 用 `--depth 1` clone main，push 才拿得到）。

---

### Task 1: 純函數 `ema`／`drift_terms` 與權重表 `W35`

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`KNEE12` 那行附近加 `ABAD12`；`W` 字典加兩個 0 權重；`W` 之後加 `W35`；`err_barrier_j` 之前加兩個函數）
- Create: `task7/tests/test_rl_env_v35.py`
- Modify: `docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md`（§2.1「~1%」→「約 6%」，§3.2「< 3%」→「< 10%」；一階低通 k=0.02 在 2.5 Hz 的增益算出來是 0.064，不是 1%）

**Interfaces:**
- Produces: `v3.ema(prev, x, k)` → 同形狀 array；`v3.drift_terms(abad_ema (4,), drift_ema (2,), w: dict)` → `(t_abadbias, t_drift)` 兩個純量；`v3.W35: dict`；`v3.ABAD12 = jnp.array([0, 3, 6, 9])`。

- [ ] **Step 1: 寫失敗的測試**

`task7/tests/test_rl_env_v35.py`：

```python
"""v3.5（spec docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md）：ABAD 慢漂與速度漂移兩個低通懲罰。"""
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))
jax = pytest.importorskip("jax")
jnp = jax.numpy
import rl_env_v3 as v3  # noqa: E402


def test_ema_filters_cycle_but_keeps_constant():
    """k=0.02 @ 50 Hz：2.5 Hz 週期擺被壓到 10% 以下（理論 6.4%），常值 5 s 內收斂到 1%。"""
    k, fs = 0.02, 50.0
    t = np.arange(int(5 * fs)) / fs
    e, out = 0.0, []
    for x in 0.4 * np.sin(2 * np.pi * 2.5 * t):
        e = float(v3.ema(e, x, k)); out.append(e)
    assert max(abs(v) for v in out[-int(fs):]) < 0.10 * 0.4
    e = 0.0
    for _ in range(int(5 * fs)):
        e = float(v3.ema(e, 0.26, k))
    assert abs(e - 0.26) < 0.01 * 0.26


def test_drift_terms_default_zero_and_w35_magnitudes():
    ab = jnp.array([0.26, 0.0, 0.0, 0.0]); dr = jnp.array([0.08, 0.0])     # 一腿 15°、往後漂 0.08 m/s
    ta, td = v3.drift_terms(ab, dr, v3.W)
    assert float(ta) == 0.0 and float(td) == 0.0
    ta, td = v3.drift_terms(ab, dr, v3.W35)
    assert abs(float(ta) - 30.0 * 0.26 ** 2) < 1e-6      # ≈ 2.03／步
    assert abs(float(td) - 40.0 * 0.08 ** 2) < 1e-6      # ≈ 0.256／步


def test_w35_values_pinned():
    assert v3.W["W_ABADBIAS"] == 0.0 and v3.W["W_DRIFT"] == 0.0
    assert v3.W35["W_ABADBIAS"] == 30.0 and v3.W35["W_DRIFT"] == 40.0
    assert v3.W35["W_VYREL"] == 6.0 and v3.W35["P_VY"] == 0.60
    changed = ("W_ABADBIAS", "W_DRIFT", "W_VYREL", "P_VY")
    assert {k: v for k, v in v3.W35.items() if k not in changed} == {k: v for k, v in v3.W.items() if k not in changed}
    assert list(v3.ABAD12.tolist()) == [0, 3, 6, 9]
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v35.py -q 2>&1 | tail -5`
Expected: 3 failed，錯誤含 `AttributeError: module 'rl_env_v3' has no attribute 'ema'`（或 `W35`）。

- [ ] **Step 3: 實作**

`task7/inference/rl_env_v3.py`，`KNEE12 = jnp.array([2, 5, 8, 11])` 那行下面加：

```python
ABAD12 = jnp.array([0, 3, 6, 9])
```

`W = dict(...)` 的最後一行 `P_SWITCH=0.4, RAMP_STEPS=50, BIAS_EMA=0.02)` 改成：

```python
         P_SWITCH=0.4, RAMP_STEPS=50, BIAS_EMA=0.02,
         W_ABADBIAS=0.0, W_DRIFT=0.0)     # v3.5 兩個慢漂懲罰；預設 0 → v3.3 reward 逐位元不變（golden）
W35 = dict(W, W_ABADBIAS=30.0, W_DRIFT=40.0, W_VYREL=6.0, P_VY=0.60)   # v3.5（spec 2026-09-16 §2）：DualModeEnv(weights=v3.W35)
```

`def err_barrier_j(...)` 之前加：

```python
def ema(prev, x, k):
    """一階低通 prev + k·(x − prev)。k=0.02 @ 50 Hz ≈ 1 s 時間常數；2.5 Hz 週期擺剩 6%，慢漂全留。"""
    return prev + k * (x - prev)


def drift_terms(abad_ema, drift_ema, w):
    """v3.5：ABAD 慢漂（4 腿偏離站姿的低通）與機身速度低頻誤差（vx, vy）的平方懲罰 → (t_abadbias, t_drift)。
    權重 0 時精確為 0.0（0.0·有限數）。"""
    return w["W_ABADBIAS"] * jnp.sum(abad_ema ** 2), w["W_DRIFT"] * jnp.sum(drift_ema ** 2)
```

spec §2.1 把「2.5 Hz 週期擺動經 1 s 低通後幅度剩 ~1%」改成「剩約 6%（一階低通 k=0.02 在 2.5 Hz 的增益 0.064；±25° 擺剩 ±1.6°，平方乘 30 每步 0.02，可忽略）」；§3.2 的「< 3%」改「< 10%」。

- [ ] **Step 4: 跑測試確認通過，golden 也要過**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v35.py task7/tests/test_rl_env_v3_golden.py -q 2>&1 | tail -3`
Expected: `4 passed`（golden 1 ＋ 新 3）。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v35.py docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md
git commit -m "feat(task7): v3.5 純函數 ema/drift_terms 與 W35 權重表 —— 預設權重 0，v3.3 不變

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

### Task 2: env 接線 —— 兩個 EMA 狀態、兩個 reward 項、兩個 metrics

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`T_KEYS`、`METRIC_KEYS`、`reset` 的 `info`、`step` 的 reward 段／`info.update`／`metrics`）
- Test: `task7/tests/test_rl_env_v35.py`（加一個 rollout 測試）

**Interfaces:**
- Consumes: Task 1 的 `ema`、`drift_terms`、`ABAD12`、`W35`。
- Produces: `info["abad_ema"] (4,)`、`info["drift_ema"] (2,)`；`metrics["t_abadbias"]`、`metrics["t_drift"]`（每步懲罰值）、`metrics["abad_bias"]`（四腿 |ema| 最大值，度）、`metrics["vx_drift"]`（drift_ema[0]，m/s）。`T_KEYS` 尾端加 `"t_abadbias", "t_drift"`；`METRIC_KEYS` 尾端（`+ T_KEYS` 之前）加 `"abad_bias", "vx_drift"`。

- [ ] **Step 1: 寫失敗的測試**

`task7/tests/test_rl_env_v35.py` 末尾加：

```python
def _run(env, cmd, steps):
    jr, js = jax.jit(env.reset), jax.jit(env.step)
    s = jr(jax.random.PRNGKey(0))
    s = s.replace(info={**s.info, "cmd": jnp.array(cmd), "cmd2": jnp.array(cmd), "t_switch": 10 ** 6})
    a = jnp.zeros(v3.ACT_DIM); out = []
    for _ in range(steps):
        s = js(s, a)
        out.append({k: float(s.metrics[k]) for k in ("t_abadbias", "t_drift", "abad_bias", "vx_drift", "reward")})
    return out


def test_env_terms_zero_by_default_and_exactly_subtracted_in_w35():
    """零動作原地轉 30 步：預設 W 兩項精確為 0、診斷量照算；W35 下 reward 剛好少了兩項（物理同 seed 同動作完全一樣）。"""
    M = _run(v3.DualModeEnv(ref=dict(cyc_amp_rand=False)), (0.0, 0.0, 1.3), 30)
    assert all(m["t_abadbias"] == 0.0 and m["t_drift"] == 0.0 for m in M)
    assert M[-1]["abad_bias"] > 0.0 and np.isfinite(M[-1]["vx_drift"])
    M35 = _run(v3.DualModeEnv(ref=dict(cyc_amp_rand=False), weights=v3.W35), (0.0, 0.0, 1.3), 30)
    assert M35[-1]["t_abadbias"] > 0.0
    for m, m35 in zip(M, M35):
        assert abs(m35["reward"] - (m["reward"] - m35["t_abadbias"] - m35["t_drift"])) < 1e-4     # wz 指令無 vy → t_vyrel=0，差只有這兩項
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v35.py -q -k exactly_subtracted 2>&1 | tail -5`
Expected: FAIL，`KeyError: 't_abadbias'`。

- [ ] **Step 3: 實作**

`task7/inference/rl_env_v3.py`：

(a) `T_KEYS` 最後的 `"t_mode", "t_vz")` 改 `"t_mode", "t_vz", "t_abadbias", "t_drift")`。

(b) `METRIC_KEYS` 的 `"sway_y", "roll_bias") + T_KEYS` 改 `"sway_y", "roll_bias", "abad_bias", "vx_drift") + T_KEYS`。

(c) `reset` 的 `info` 字典，`"roll_ema": jnp.zeros(()), "sway_ema": jnp.zeros(()),` 那行後面加一行：

```python
                "abad_ema": jnp.zeros(4), "drift_ema": jnp.zeros(2),
```

(d) `step` 裡 `sway_ema = info["sway_ema"] + w["BIAS_EMA"] * (sway[1] - info["sway_ema"])` 之後加：

```python
        abad_ema = ema(info["abad_ema"], q12[ABAD12] - STANCE_Q12_j[ABAD12], w["BIAS_EMA"])   # v3.5：ABAD 偏離站姿的慢漂（cyc_recenter=True 下站姿＝名目中心）
        drift_ema = ema(info["drift_ema"], vb[0:2] - cmd[0:2], w["BIAS_EMA"])                # v3.5：機身 vx/vy 低頻追蹤誤差（濾掉踏步 ±0.3 的高頻擺）
        t_abadbias, t_drift = drift_terms(abad_ema, drift_ema, w)
```

(e) `T = {` 字典最後一行 `"t_mode": w["W_MODE"] * lift_pen, "t_vz": w["W_VZ"] * data.qvel[2] ** 2,` 後面加：

```python
            "t_abadbias": t_abadbias, "t_drift": t_drift,        # v3.5；不乘 k_post（漂移就在踏步時發生）
```

(f) `neg = (...)` 最後 `+ T["t_kneev"] + T["t_mode"] + T["t_vz"])` 改 `+ T["t_kneev"] + T["t_mode"] + T["t_vz"] + T["t_abadbias"] + T["t_drift"])`。

(g) `info.update({...})` 裡 `"om_prev": om, "roll_ema": roll_ema, "sway_ema": sway_ema,` 改 `"om_prev": om, "roll_ema": roll_ema, "sway_ema": sway_ema, "abad_ema": abad_ema, "drift_ema": drift_ema,`。

(h) `metrics = {...}` 裡 `"sway_y": sway[1] * 1000.0, "roll_bias": roll_ema * 57.29578, **T}` 改：

```python
                   "sway_y": sway[1] * 1000.0, "roll_bias": roll_ema * 57.29578,
                   "abad_bias": jnp.max(jnp.abs(abad_ema)) * 57.29578, "vx_drift": drift_ema[0], **T}
```

- [ ] **Step 4: 跑測試，golden 與既有 v3 測試一起**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v35.py task7/tests/test_rl_env_v3_golden.py task7/tests/test_rl_env_v3.py -q 2>&1 | tail -3`
Expected: 全 passed，golden 沒動。若 golden 失敗：先確認是 reward 而不是 obs／qpos／ctrl（印 npz 差異），reward 差若是 `-0.0` vs `0.0` 以外的值就是接線錯，不放寬。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v35.py
git commit -m "feat(task7): v3.5 env 接線 —— abad_ema/drift_ema 兩個低通狀態、t_abadbias/t_drift 兩項、abad_bias/vx_drift 兩個 metrics；golden 過

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

### Task 3: 攤帳工具 `reward_audit_v35.py` 並跑過門檻（spec §3.3）

**Files:**
- Create: `task7/inference/diag/reward_audit_v35.py`
- Create（執行產出）: `task7/outputs/reward_audit_v35.md`

**Interfaces:**
- Consumes: `local_infer_v3.load_policy(path, obs_dim)`；Task 2 的 metrics `t_abadbias`、`t_drift`、`abad_bias`、`vx_drift`。
- Produces: 退出碼 0＝三個指令全過門檻；1＝有沒過的。

- [ ] **Step 1: 寫工具**

`task7/inference/diag/reward_audit_v35.py`：

```python
"""v3.5 攤帳（spec §3.3）：拿 v3.3 的 params_2 在 W35 權重下跑三個指令各 10 s（seed 0），看新兩項是不是「咬在對的地方」。
門檻：原地轉 t_abadbias ≥ 0.5（BL 漂 8–15°）、平移 0.20 t_drift ≥ 0.15（漂 0.08 m/s）、直走兩項都 ≤ 0.05。
    conda run -n rbtdog --no-capture-output python task7/inference/diag/reward_audit_v35.py
輸出 outputs/reward_audit_v35.md；退出碼 1 = 沒過，不要上 Colab。
"""
import argparse, sys
from pathlib import Path

import jax, jax.numpy as jnp, numpy as np

INF = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(INF))
import rl_env_v3 as v3          # noqa: E402
import local_infer_v3 as L      # noqa: E402

CASES = (("原地左轉 1.3", (0.0, 0.0, 1.3)), ("左平移 0.20", (0.0, 0.20, 0.0)), ("直走 0.5", (0.5, 0.0, 0.0)))
# (下限, 上限)；None = 不管
GATE = {"原地左轉 1.3": {"t_abadbias": (0.5, None)},
        "左平移 0.20": {"t_drift": (0.15, None)},
        "直走 0.5": {"t_abadbias": (None, 0.05), "t_drift": (None, 0.05)}}
KEYS = ("t_abadbias", "t_drift", "abad_bias", "vx_drift", "reward")


def run_case(env, jr, js, pol, cmd, steps):
    s = jr(jax.random.PRNGKey(0))
    s = s.replace(info={**s.info, "cmd": jnp.array(cmd), "cmd2": jnp.array(cmd), "t_switch": 10 ** 6})
    M, fell = [], False
    for _ in range(steps):
        s = js(s, pol(s.obs))
        M.append({k: float(s.metrics[k]) for k in KEYS})
        if float(s.done) > 0:
            fell = True; break
    seg = M[steps // 2:] or M                       # 後 5 s 平均
    return {k: float(np.mean([m[k] for m in seg])) for k in KEYS} | {"fell": fell, "n": len(M)}


def judge(name, r):
    bad = []
    for k, (lo, hi) in GATE[name].items():
        if lo is not None and r[k] < lo: bad.append(f"{k} {r[k]:.3f} < {lo}")
        if hi is not None and r[k] > hi: bad.append(f"{k} {r[k]:.3f} > {hi}")
    return bad


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--weights", default=str(INF.parent / "weights" / "cpg_rl_v3_params_2.pkl"))
    ap.add_argument("--secs", type=float, default=10.0)
    a = ap.parse_args()
    env = v3.DualModeEnv(ref=dict(cyc_amp_rand=False), weights=v3.W35)
    jr, js = jax.jit(env.reset), jax.jit(env.step)
    pol = L.load_policy(a.weights, env.obs_dim); steps = int(a.secs / v3.CTRL_DT)
    rows, all_ok = [], True
    for name, cmd in CASES:
        r = run_case(env, jr, js, pol, cmd, steps); bad = judge(name, r); all_ok &= not bad
        rows.append((name, r, bad))
        print(f"{'PASS' if not bad else 'FAIL'} {name:10s} t_abadbias {r['t_abadbias']:.3f} t_drift {r['t_drift']:.3f} | ABAD 漂 {r['abad_bias']:.1f}° vx 漂 {r['vx_drift']:+.3f} | R/步 {r['reward']:+.2f} 摔 {r['fell']}"
              + (f"  ← {'; '.join(bad)}" if bad else ""), flush=True)
    wname = Path(a.weights).stem
    out = ["# v3.5 攤帳（spec §3.3）：v3.3 權重 `" + wname + "` 在 W35 下，後 5 s 每步平均", "",
           "| 指令 | t_abadbias | t_drift | ABAD 漂 ° | vx 漂 m/s | reward/步 | 摔 | 門檻 |", "|---|---|---|---|---|---|---|---|"]
    for name, r, bad in rows:
        out.append(f"| {name} | {r['t_abadbias']:.3f} | {r['t_drift']:.3f} | {r['abad_bias']:.1f} | {r['vx_drift']:+.3f} | {r['reward']:+.2f} | {r['fell']} | {'PASS' if not bad else 'FAIL：' + '; '.join(bad)} |")
    out += ["", "門檻：原地轉 t_abadbias ≥ 0.5、平移 t_drift ≥ 0.15、直走兩項 ≤ 0.05。全過 → 新 reward 咬在對的地方、可上 Colab。"]
    p = INF.parent / "outputs" / "reward_audit_v35.md"; p.write_text("\n".join(out) + "\n", encoding="utf-8"); print("→", p)
    return 0 if all_ok else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 執行**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python task7/inference/diag/reward_audit_v35.py 2>&1 | grep -v "Warning\|warp"; echo EXIT $?`
Expected: 三行 PASS，`EXIT 0`，並產生 `task7/outputs/reward_audit_v35.md`。
若 FAIL：原地轉 `t_abadbias` 太小 → 印出的 `ABAD 漂` 若仍是 8–15° 代表權重要往上（改 `W35["W_ABADBIAS"]`，同步改 Task 1 測試裡的 30.0 與 spec）；直走那行超過 0.05 → 先看是哪一項，`t_drift` 超過代表直走低頻 vx 誤差比預期大，把 `W_DRIFT` 降到 20 再跑。任何調整都要回 Task 1 的測試改釘住的值並在 spec §8 記錄。

- [ ] **Step 3: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/diag/reward_audit_v35.py task7/outputs/reward_audit_v35.md
git commit -m "feat(task7): v3.5 攤帳工具 reward_audit_v35 —— v3.3 權重在 W35 下三指令門檻全過

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

### Task 4: G0 回歸（零動作物理不變）

**Files:**
- Create（執行產出）: `task7/outputs/g0_v35_regress.txt`

- [ ] **Step 1: 跑 G0，與 v3.3 基準比**

`outputs/g0_v33_final.txt` 前 7 行來自 `g0_v3.py --steps 500 --lat-vy 0.04`（後兩行是 `--cyc-amp-turn 0.3／0.5` 另跑的）。

Run:
```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python task7/inference/diag/g0_v3.py --steps 500 --lat-vy 0.04 2>&1 | grep -E "^(PASS|FAIL)" > task7/outputs/g0_v35_regress.txt
diff <(head -7 task7/outputs/g0_v33_final.txt | sed 's/ ([0-9]*s)$//') <(head -7 task7/outputs/g0_v35_regress.txt | sed 's/ ([0-9]*s)$//') && echo "G0 逐字相同"
```
Expected: 印 `G0 逐字相同`。若有差：先確認差在哪一欄。`reward`／`R` 欄不該變（g0 用預設 `W`）；vx／yaw／roll 任何一欄變了就是 Task 2 動到物理，回頭查（golden 應該也會先抓到）。

- [ ] **Step 2: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/outputs/g0_v35_regress.txt
git commit -m "test(task7): v3.5 G0 回歸 —— 零動作七 case 與 v3.3 基準逐字相同

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

### Task 5: notebook 產生器 `--weights v35` 與 `cpg_rl_v3_5_colab.ipynb`

**Files:**
- Modify: `task7/notebooks/build_nb_v3.py`
- Create（產生）: `task7/notebooks/cpg_rl_v3_5_colab.ipynb`
- Create: `task7/tests/test_notebook_v35.py`

**Interfaces:**
- Produces: `python3 task7/notebooks/build_nb_v3.py --weights v35` → `cpg_rl_v3_5_colab.ipynb`，權重檔名 `cpg_rl_v3_5_params.pkl`；`--gains factory --weights v35` → `cpg_rl_v3_5f_colab.ipynb`／`cpg_rl_v3_5f_params.pkl`（本次不產生，只保證能組合）。
- 約束：不帶 `--weights` 時輸出與現在逐位元相同。

- [ ] **Step 1: 寫失敗的測試**

`task7/tests/test_notebook_v35.py`：

```python
"""v3.5 notebook：由 build_nb_v3.py --weights v35 產生，env 要帶 W35、progress 要印 abad/drift。"""
import json
from pathlib import Path

NB = Path(__file__).resolve().parents[1] / "notebooks" / "cpg_rl_v3_5_colab.ipynb"


def _code_cells():
    nb = json.loads(NB.read_text(encoding="utf-8"))
    return ["".join(c["source"]) for c in nb["cells"] if c["cell_type"] == "code"]


def test_v35_notebook_uses_w35_and_prints_drift():
    assert NB.exists(), "先跑 python3 task7/notebooks/build_nb_v3.py --weights v35"
    cells = _code_cells()
    src = "\n".join(cells)
    assert src.count("v3.DualModeEnv(weights=v3.W35)") == 2                       # import 格 + train 格
    assert "v3.DualModeEnv(weights=v3.W35, ref=dict(cyc_amp_rand=False))" in src  # eval_env
    assert "abad {ps('abad_bias'):.1f}" in src and "drift {ps('vx_drift'):+.3f}" in src
    assert "vy {ps('t_vyrel'):.2f}/6" in src
    assert 'model.save_params("cpg_rl_v3_5_params.pkl", params)' in src


def test_v33_notebook_untouched_by_builder_change():
    """v3.3 那本的 env 行與 progress 行不能帶 v3.5 的東西。"""
    nb33 = NB.with_name("cpg_rl_v3_colab.ipynb")
    src = "\n".join("".join(c["source"]) for c in json.loads(nb33.read_text(encoding="utf-8"))["cells"] if c["cell_type"] == "code")
    assert "W35" not in src and "abad_bias" not in src and "vy {ps('t_vyrel'):.2f}/3" in src
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && python3 -m pytest task7/tests/test_notebook_v35.py -q 2>&1 | tail -3`
Expected: 第一個 FAIL（`assert NB.exists()`），第二個 PASS。

- [ ] **Step 3: 改產生器**

`task7/notebooks/build_nb_v3.py`：

(a) `_ap.add_argument("--gains", ...)` 下面加：

```python
_ap.add_argument("--weights", default="v33", choices=("v33", "v35"), help="v35＝DualModeEnv(weights=v3.W35)（慢漂懲罰，spec 2026-09-16）")
```

(b) `FACTORY = ARGS.gains == "factory"` 之後、`OUT = ...` 之前，把 `OUT`／`WEIGHTS` 兩行換成：

```python
V35 = ARGS.weights == "v35"
_tag = {(False, False): "v3", (True, False): "v3_4f", (False, True): "v3_5", (True, True): "v3_5f"}[(FACTORY, V35)]
OUT = HERE / f"cpg_rl_{_tag}_colab.ipynb"
WEIGHTS = f"cpg_rl_{_tag}_params.pkl"
```

（檢查：`(False, False)` → `cpg_rl_v3_colab.ipynb`／`cpg_rl_v3_params.pkl`，`(True, False)` → `cpg_rl_v3_4f_colab.ipynb`／`cpg_rl_v3_4f_params.pkl`，與原本一致。）

(c) `GAINS_KW`／`GAINS_KW2` 兩行之後加：

```python
W_KW = "weights=v3.W35" if V35 else ""
ENV_ARGS = ", ".join(x for x in (GAINS_KW, W_KW) if x)        # '' | 'gains="factory"' | 'weights=v3.W35' | 'gains="factory", weights=v3.W35'
ENV_ARGS2 = ENV_ARGS + ", " if ENV_ARGS else ""              # 後面還有參數時用
PROG_EXTRA = "abad {{ps('abad_bias'):.1f}}° drift {{ps('vx_drift'):+.3f}} | " if V35 else ""   # 進 f-string 後變單層大括號
VY_DEN = "6" if V35 else "3"
```

(d) `import_src` 裡 `env = v3.DualModeEnv({GAINS_KW})` 改 `env = v3.DualModeEnv({ENV_ARGS})`。

(e) `train_src` 裡 `env = v3.DualModeEnv({GAINS_KW})` 改 `env = v3.DualModeEnv({ENV_ARGS})`；`eval_env = v3.DualModeEnv({GAINS_KW2}ref=dict(cyc_amp_rand=False))` 改 `eval_env = v3.DualModeEnv({ENV_ARGS2}ref=dict(cyc_amp_rand=False))`。

(f) `train_src` 的 progress 那段：`vy {{ps('t_vyrel'):.2f}}/3` 改 `vy {{ps('t_vyrel'):.2f}}/{VY_DEN}`；`knee_v {{ps('knee_v'):.1f}} | len {{L:.0f}} | "` 改 `knee_v {{ps('knee_v'):.1f}} | {PROG_EXTRA}len {{L:.0f}} | "`。

(g) `if FACTORY:` 的 md0 區塊之後加：

```python
if V35:
    md0 = f"""# CPG-RL **v3.5{"f" if FACTORY else ""}**：智元 D1 Max · 慢漂懲罰 · MJX · Colab GPU（2026-09-16）

在 v3.3（`weights/cpg_rl_v3_params_2.pkl`：原地轉 ±75°/s 達原廠 97%、九指令 0 摔）之上修兩個使用者看到的問題：
- **原地轉一條腿慢慢內收再踏出去**：左後腿 ABAD 6–8 s 漂到 +15°（跨種子同腿）。原廠圖案本來就對角不對稱（使用者決定保留），
  踏步時沒有東西把站立腳拉回名目 → 加 `t_abadbias`：四腿 ABAD 偏離站姿的 1 s 低通平方 × 30。
- **平移往後漂**：兩個方向都往後 0.08 m/s（policy 用前後換側向）→ 加 `t_drift`：機身 vx/vy 低頻誤差平方 × 40。
- 平移效率：`W_VYREL` 3→6、`P_VY` 0.45→0.60。
其餘（產生器、obs 88、動作 24、護欄、DR{"、原廠增益" if FACTORY else ""}）與 v3.{"4f" if FACTORY else "3"} 相同。
設計：`docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md`；攤帳 `outputs/reward_audit_v35.md`。

**停損**：同 v3.3 —— 1 億步 `進度 yaw` < 3.5 或 len < 600 → 停；**另加**：1 億步 `abad` 沒比 step 0 低、或 `drift` 沒往 0 走 → 新 reward 沒被學到，停下來查。
v3.3 在 1 億步時 3.3 沒過線、160M 才破到 4.2 —— 沒存中繼檢查點，決定跑完就別關分頁。
**注意**：G0 格的原地轉是開迴路原廠週期，**預期會倒**，那格只 assert 其他四個指令。
"""
```

(h) `save_src` **不改**（改了 v3.3／v3.4f 兩本就不再逐位元相同）。

- [ ] **Step 4: 產生三本、確認舊兩本逐位元不變、測試通過**

Run:
```bash
cd /home/huang/rbtdog_sim && python3 task7/notebooks/build_nb_v3.py && python3 task7/notebooks/build_nb_v3.py --gains factory && python3 task7/notebooks/build_nb_v3.py --weights v35
git status --short task7/notebooks/
python3 -m pytest task7/tests/test_notebook_v35.py task7/tests/test_notebook_v2.py -q 2>&1 | tail -3
```
Expected: `git status` 只列 ` M task7/notebooks/build_nb_v3.py` 與 `?? task7/notebooks/cpg_rl_v3_5_colab.ipynb`（v3／v3_4f 兩本沒出現）；pytest 全 passed。

- [ ] **Step 5: 用 python 實際 exec 一次 v3.5 的 import 格與 G0 格（本機 CPU，約 2 分鐘），確認語法與 env 參數對**

Run:
```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python - <<'EOF' 2>&1 | grep -v "Warning\|warp" | tail -8
import json, sys
sys.path.insert(0, "task7/inference")
nb = json.load(open("task7/notebooks/cpg_rl_v3_5_colab.ipynb", encoding="utf-8"))
cells = ["".join(c["source"]) for c in nb["cells"] if c["cell_type"] == "code"]
exec(cells[3])        # import 格（clone 格是 cells[2]，跳過）
exec(cells[4].replace("steps=500", "steps=60"))   # G0 格縮短到 1.2 s 只驗語法與 assert 路徑不炸（TURN 允許摔）
EOF
```
Expected: 印 `obs 88 act 24 wheel_pos False`、REF 字典、五個指令各一行、最後不炸（60 步時 WHEEL 的 vx 可能還沒到 0.45 而 assert 失敗 —— 那是縮短造成的，只要錯在 `assert R["WHEEL"]` 那行而不是更早就算過；若要完整驗就用 `steps=500`，約 8 分鐘）。

- [ ] **Step 6: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/notebooks/build_nb_v3.py task7/notebooks/cpg_rl_v3_5_colab.ipynb task7/tests/test_notebook_v35.py
git commit -m "feat(task7): v3.5 Colab notebook —— build_nb_v3 --weights v35，env/eval_env 帶 W35、progress 印 abad/drift；v3.3/v3.4f 兩本逐位元不變

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

### Task 6: 驗收工具 `local_infer_v3.py` 加「ABAD 漂」「vx 漂」兩欄

**Files:**
- Modify: `task7/inference/local_infer_v3.py`（`rollout` 的 metrics 鍵與回傳、`main` 的 `agg`／print／md 表頭與列）
- Modify: `task7/tests/test_local_infer_v3.py`（若不存在就建，只測 `rollout` 回傳鍵）

**Interfaces:**
- Consumes: Task 2 的 `metrics["abad_bias"]`、`metrics["vx_drift"]`（eval env 用預設 `W` 也會算）。
- Produces: `rollout(...)` 回傳字典多 `abad_bias`（後 5 s 平均，度）、`vx_drift`（後 5 s 平均，m/s）；md 表在「抬腳 mm」之後多兩欄 `ABAD 漂 °`、`vx 漂`。

- [ ] **Step 1: 寫失敗的測試**

若 `task7/tests/test_local_infer_v3.py` 不存在，建立；存在則加在末尾：

```python
"""local_infer_v3：rollout 回傳鍵（v3.5 加 abad_bias／vx_drift）。"""
import sys
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))
jax = pytest.importorskip("jax")
jnp = jax.numpy
import rl_env_v3 as v3          # noqa: E402
import local_infer_v3 as L      # noqa: E402


def test_rollout_reports_drift_columns():
    env = v3.DualModeEnv(ref=dict(cyc_amp_rand=False)); jr, js = jax.jit(env.reset), jax.jit(env.step)
    r = L.rollout(env, jr, js, (0.0, 0.0, 1.3), 40, None, seed=0)      # 零動作 0.8 s，只驗鍵與型別
    assert set(("abad_bias", "vx_drift")) <= set(r)
    assert r["abad_bias"] >= 0.0 and isinstance(r["vx_drift"], float)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_local_infer_v3.py -q -k drift_columns 2>&1 | tail -3`
Expected: FAIL，`assert {'abad_bias', 'vx_drift'} <= {...}`。

- [ ] **Step 3: 實作**

`task7/inference/local_infer_v3.py`：

(a) `rollout` 裡 `M.append({k: float(s.metrics[k]) for k in ("vx", "vy", "wz", "roll", "pitch", "tau_pk", "height")}` 的鍵 tuple 改成 `("vx", "vy", "wz", "roll", "pitch", "tau_pk", "height", "abad_bias", "vx_drift")`。

(b) `rollout` 的 `return dict(` 裡 `act_abs=float(np.abs(np.tanh(np.array(A))).mean()), Q=Q)` 改：

```python
        act_abs=float(np.abs(np.tanh(np.array(A))).mean()),
        abad_bias=float(g("abad_bias")[-int(5.0 / v3.CTRL_DT):].mean()) if n > h else 0.0,   # 後 5 s 四腿 |ABAD 慢漂| 最大值的平均，度（v3.5 spec §5）
        vx_drift=float(g("vx_drift")[-int(5.0 / v3.CTRL_DT):].mean()) if n > h else 0.0,     # 後 5 s 機身 vx 低頻誤差平均，m/s
        Q=Q)
```

（注意 `n > h` 為 False 時 `h = 0`，`g(k)` 仍可用，但摔在 2 s 內的回合數字沒意義，所以給 0.0。）

(c) `main` 裡 `agg = {k: float(np.mean([r[k] for r in ok])) for k in (... "act_abs")}` 的 tuple 尾端加 `"abad_bias", "vx_drift"`。

(d) `main` 的 print 那行 `| lift {agg['lift']} | |a| {agg['act_abs']:.2f}"` 改 `| lift {agg['lift']} | ABAD漂 {agg['abad_bias']:.1f}° vx漂 {agg['vx_drift']:+.3f} | |a| {agg['act_abs']:.2f}"`。

(e) md 表頭 `"| 指令 | 摔 | vx | vy | 偏航 °/s（±std） | roll std/峰 ° | 膝 τ 峰/RMS | 髖 τ 峰/RMS | ABAD τ 峰/RMS | 抬腳 mm | \\|a\\| | 零動作 ... |"` 改成在 `抬腳 mm |` 後面插 ` ABAD 漂 ° | vx 漂 |`，分隔列 `"|---|---|---|---|---|---|---|---|---|---|---|---|---|"` 多加兩個 `---|`（共 15 欄）。

(f) md 列 `| {g['lift']} | {g['act_abs']:.2f} | {bs} | {fs} |")` 改 `| {g['lift']} | {g['abad_bias']:.1f} | {g['vx_drift']:+.3f} | {g['act_abs']:.2f} | {bs} | {fs} |")`。

(g) 判讀規則那行尾端加：`ABAD 漂（原地轉）< 5°、vx 漂（平移）|·| < 0.03 m/s（v3.5 spec §5）。`

- [ ] **Step 4: 跑測試；再用 params_2 只跑原地轉與平移各一種子，確認新欄數字與 spec §0 一致**

Run:
```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_local_infer_v3.py -q 2>&1 | tail -3
conda run -n rbtdog --no-capture-output python task7/inference/local_infer_v3.py --weights task7/weights/cpg_rl_v3_params_2.pkl --seeds 1 --no-baseline --only 左 2>&1 | grep -E "^(原地|左)"
```
Expected: 測試 passed；`原地左轉 1.3` 那行 `ABAD漂` 在 7–12° 之間（spec §0.1 是 6–10 s 平均 +15／+7.6）、`左平移 0.20` 的 `vx漂` 在 −0.05 到 −0.10 之間（spec §0.4 是 −0.077）。這步會覆寫 `outputs/eval_cpg_rl_v3_params_2.md` 成只有兩列的表 —— **跑完用 `git checkout task7/outputs/eval_cpg_rl_v3_params_2.md` 還原**，正式表等 v3.5 權重回來再重生成。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git checkout task7/outputs/eval_cpg_rl_v3_params_2.md
git add task7/inference/local_infer_v3.py task7/tests/test_local_infer_v3.py
git commit -m "feat(task7): local_infer_v3 加 ABAD 漂／vx 漂 兩欄（v3.5 驗收 spec §5）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
```

---

### Task 7: 全測試、spec §8 執行紀錄、HANDOFF、push

**Files:**
- Modify: `docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md`（§8）
- Modify: `task7/HANDOFF.md`（頂節「最後更新」與 v3.3 那節下加 v3.5 狀態）

- [ ] **Step 1: 跑 task7 全測試**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests -q 2>&1 | tail -3`
Expected: 全 passed（830+ 項；原本就 skip 的照舊 skip）。有 fail 就回對應 task 修，不改測試遷就。

- [ ] **Step 2: spec §8 執行紀錄**

把 `## 8. 執行紀錄` 底下的「（實作時填）」換成：

```markdown
（2026-09-16 完成，計畫 `docs/superpowers/plans/2026-09-16-cpg-rl-v3.5-drift-penalties.md`）

- golden 不動就過（commit <Task 2 的 hash>）；G0 七 case 與 v3.3 基準逐字相同（`outputs/g0_v35_regress.txt`）。
- 攤帳 `outputs/reward_audit_v35.md`：<把三行 PASS 的數字貼進來：原地轉 t_abadbias x.xx（ABAD 漂 x.x°）、平移 t_drift x.xx（vx 漂 −0.0xx）、直走 x.xx／x.xx>。
- 低通衰減修正：2.5 Hz 剩 6.4% 不是 1%（§2.1 已改）；對懲罰量級沒影響（每步 0.02）。
- notebook `notebooks/cpg_rl_v3_5_colab.ipynb`；v3.3／v3.4f 兩本重建後逐位元不變。
- 驗收工具兩欄已加；v3.3 params_2 上讀到原地轉 ABAD 漂 x.x°、平移 vx 漂 −0.0xx（與 §0 一致）。
- 下一步：v3.4f 訓完 → Colab 跑 v3.5（2 億步）；回來後 `local_infer_v3.py --mesh --video` 對 §5 表。
```

（尖括號內的數字由實際輸出填，不留尖括號。）

- [ ] **Step 3: HANDOFF 頂節**

`task7/HANDOFF.md` 第 3 行「最後更新」改成：

```markdown
- 最後更新：**2026-09-16 下午（v3.5 慢漂懲罰做完、G0／golden／攤帳全過、notebook 已 push 等 Colab；v3.4f 訓練中；v3.3 params_2 是目前最佳權重）**
```

並在 `### ★★ v3.3 訓完（2026-09-16 上午）` 那節之前插入：

```markdown
### ★★★ v3.5：兩個慢漂懲罰（2026-09-16 下午做完，等 Colab）

spec `docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md`（§8 執行紀錄）。
使用者看 v3.3 影片的兩個問題 → 量到的根因 → 修法：
- 原地轉左後腿 ABAD 6–8 s 漂到 +15°（跨種子同腿；原廠圖案對角不對稱是真的、使用者決定保留）→ `t_abadbias`（ABAD 偏離站姿 1 s 低通平方 ×30）
- 平移兩向都往後漂 0.08 m/s（policy 用前後換側向）→ `t_drift`（機身 vx/vy 低頻誤差平方 ×40）；`W_VYREL` 3→6、`P_VY` 0.45→0.60
程式形式：`DualModeEnv(weights=v3.W35)`；預設 `W` 兩個新權重是 0 → **v3.3 逐位元不變**（golden）。
notebook `notebooks/cpg_rl_v3_5_colab.ipynb`（`build_nb_v3.py --weights v35`；加 `--gains factory` 就是 v3.5f）。
**上 Colab 前的證據**：攤帳 `outputs/reward_audit_v35.md`（v3.3 權重在新 reward 下：原地轉 ABAD 項、平移漂移項明顯非零，直走兩項 ≈ 0）、G0 回歸 `outputs/g0_v35_regress.txt`。
**訓完看**：`local_infer_v3.py` 新兩欄 —— 原地轉 ABAD 漂 < 5°、平移 vx 漂 |·| < 0.03、平移 0.20 達成率 ≥ 85%、原地轉偏航不退、0 摔。
停損同 v3.3，另加 1 億步 `abad`／`drift` 沒往下走就停。
```

- [ ] **Step 4: Commit 並 push（Colab 用 --depth 1 clone main）**

```bash
cd /home/huang/rbtdog_sim && git add docs/superpowers/specs/2026-09-16-cpg-rl-v3.5-drift-penalties-design.md task7/HANDOFF.md
git commit -m "docs(task7): v3.5 spec §8 執行紀錄 + HANDOFF —— 等 Colab

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_0154GCUWdnXY7JFLTidBXG5b"
git push origin main && git log --oneline -1
```
Expected: push 成功。v3.4f 那個 Colab 執行階段已 clone 完，不受影響。
