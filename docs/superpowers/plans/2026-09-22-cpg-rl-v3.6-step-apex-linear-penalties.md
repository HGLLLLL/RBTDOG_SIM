# CPG-RL v3.6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v3.5f 之上加「平移每週期抬腳頂點獎勵」、把兩個二次懲罰改成死區＋線性、DR 加橫向質心偏移、驗收工具關掉隨機推力並加步數欄，產出 `notebooks/cpg_rl_v3_6f_colab.ipynb` 可上 Colab。

**Architecture:** 所有新行為掛在 `rl_env_v3.W36` 的鍵上（`W_STEP`、`PEN_SHAPE` 等），鍵不存在＝舊行為，`tests/test_rl_env_v3_golden.py` 釘住 v3.3 預設路徑逐位元不變。抬腳頂點靠 `info` 裡兩個 (4,) 陣列（`apex_run`／`apex_last`）在 `phi_cyc` 回繞時交接。DR 與推力各加一個 Python 層參數，預設值走原本的程式路徑。

**Tech Stack:** Python 3、JAX／MJX、brax 0.14.2（Colab）、pytest、conda env `rbtdog`。本機跑 env 用 `conda run -n rbtdog --no-capture-output python ...`；本機 jax 0.10 **不能**跑 `ppo.train`。

Spec：`docs/superpowers/specs/2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md`。

## Global Constraints

- `rl_env_v3.DualModeEnv()` 預設路徑（`weights=None`、`gains="kp250"`、`push=True`、DR `com_y_mm=0`）**逐位元不變**：每個 task 結束都跑 `tests/test_rl_env_v3_golden.py`，失敗不得放寬容差。
- `W36 = dict(W35, W_STEP=1.5, STEP_APEX=0.021, PEN_SHAPE="hinge", W_DRIFT_L=8.0, DRIFT_DZ=0.010, W_ABADBIAS_L=6.0, ABAD_DZ=0.0436)`；預設 `W` 加 `W_STEP=0.0, STEP_APEX=0.021`（不加 `PEN_SHAPE`）。
- 抬腳目標 `tgt = STEP_APEX · clip(|cmd_vy| / 0.15, 0.5, 1.0)`；只算主動腿 `w_k = clip((s_k/max s − 0.9)/0.1, 0, 1)`；`t_step = W_STEP · u4 · wl · r_step`（`wl = _lat_weight(P["A"])`）。
- 質心 DR：`body_ipos[base_link, y] += U(−1,1) · (com_y_mm/1000) · (整機質量／base 質量)`；新亂數鍵用 `jax.random.fold_in(rng, 99)`，**不能**改 `jax.random.split(rng, 10)` 的個數（會改到現有 notebook 的 DR 抽樣）。
- 驗收工具預設**無推力**；摔欄同時印有推力那組。
- 所有 pytest 指令從 repo 根目錄跑：`conda run -n rbtdog --no-capture-output python -m pytest task7/tests/<file> -q`。
- commit 訊息格式照 repo 慣例：`feat(task7): ...`／`test(task7): ...`／`docs(task7): ...`，結尾加 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。

## 檔案地圖

| 檔案 | 責任 | Task |
|---|---|---|
| `task7/inference/rl_env_v3.py` | env：`W36`、`t_step`、apex 追蹤、hinge `drift_terms`、`push`、`make_domain_randomize(com_y_mm)` | 1–4 |
| `task7/tests/test_rl_env_v36.py` | 上面四件事的單元測試（新檔） | 1–4 |
| `task7/inference/local_infer_v3.py` | 驗收：`--push`、`--preset v36`、步/秒欄、摔欄兩組 | 5 |
| `task7/tests/test_local_infer_v3.py` | 新 rollout 鍵 | 5 |
| `task7/outputs/eval_cpg_rl_v3_5f_params.md` | v3.5f 無推力乾淨基準（重跑覆寫） | 5 |
| `task7/inference/diag/reward_audit_v35.py` | 攤帳：`--weights-set v36`、零動作列、`t_step` 門檻 | 6 |
| `task7/outputs/reward_audit_v36.md` | 攤帳證據 | 6 |
| `task7/outputs/g0_v36_regress.txt` | G0 回歸證據 | 7 |
| `task7/notebooks/build_nb_v3.py`、`task7/notebooks/cpg_rl_v3_6f_colab.ipynb` | Colab notebook | 8 |
| `task7/tests/test_notebook_v36.py` | notebook 字串檢查 | 8 |
| `task7/HANDOFF.md`、spec §8 | 交接 | 9 |

---

### Task 1: `W36` ＋ 抬腳頂點追蹤 ＋ `t_step`

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`W`／`W35` 附近 126–138 行；`T_KEYS` 138；`reset` 的 `info` 546–556；`step` 的 `phi_cyc` 之後 ~592、`clr` 之後 ~633、`T` dict 675–687、`pos` 688、`info.update` 696）
- Create: `task7/tests/test_rl_env_v36.py`

**Interfaces:**
- Produces: `v3.W36: dict`；`v3.step_apex_update(phi_new, phi_old, clr, apex_run, apex_last) -> (apex_run, apex_last)`；`v3.step_apex_reward(apex_last, s_leg, cmd_vy, w) -> r_step`（純函式，方便測）；`info["apex_run"]`、`info["apex_last"]` (4,)；`T["t_step"]`、`METRIC_KEYS` 含 `"t_step"`。

- [ ] **Step 1: 寫失敗的測試**

```python
# task7/tests/test_rl_env_v36.py
"""v3.6（spec docs/superpowers/specs/2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md）：
抬腳頂點獎勵、死區線性懲罰、橫向質心 DR、驗收關推力。"""
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "inference"))
jax = pytest.importorskip("jax")
jnp = jax.numpy
import rl_env_v3 as v3  # noqa: E402


def test_w36_values_pinned():
    assert v3.W["W_STEP"] == 0.0 and v3.W["STEP_APEX"] == 0.021 and "PEN_SHAPE" not in v3.W
    assert v3.W36["W_STEP"] == 1.5 and v3.W36["STEP_APEX"] == 0.021
    assert v3.W36["PEN_SHAPE"] == "hinge" and v3.W36["W_DRIFT_L"] == 8.0 and v3.W36["DRIFT_DZ"] == 0.010
    assert v3.W36["W_ABADBIAS_L"] == 6.0 and v3.W36["ABAD_DZ"] == 0.0436
    changed = ("W_STEP", "PEN_SHAPE", "W_DRIFT_L", "DRIFT_DZ", "W_ABADBIAS_L", "ABAD_DZ")
    assert {k: v for k, v in v3.W36.items() if k not in changed} == {k: v for k, v in v3.W35.items() if k not in changed}
    assert "t_step" in v3.T_KEYS and "t_step" in v3.METRIC_KEYS


def test_apex_update_hands_over_on_wrap():
    """合成 clr 序列跨兩次回繞：apex_last = 上一週期最大；回繞當步 apex_run 重置成當步 clr。"""
    run, last = jnp.zeros(4), jnp.zeros(4)
    phi = [0.0, 2.0, 4.0, 6.0, 0.5, 2.5, 4.5, 6.1, 0.2]           # 回繞在 index 4、8
    clr = [0.0, 0.010, 0.021, 0.005, 0.003, 0.015, 0.008, 0.002, 0.030]
    for i in range(1, len(phi)):
        run, last = v3.step_apex_update(jnp.float32(phi[i]), jnp.float32(phi[i - 1]), jnp.full(4, clr[i]), run, last)
        if i == 4:
            assert float(last[0]) == pytest.approx(0.021) and float(run[0]) == pytest.approx(0.003)
        if i == 7:
            assert float(last[0]) == pytest.approx(0.021) and float(run[0]) == pytest.approx(0.015)
    assert float(last[0]) == pytest.approx(0.015) and float(run[0]) == pytest.approx(0.030)


def test_apex_reward_primary_legs_only_and_saturates():
    s_lat = jnp.array([0.7, 1.0, 0.7, 1.0])
    r = v3.step_apex_reward(jnp.array([0.0, 0.021, 0.0, 0.021]), s_lat, 0.20, v3.W36)
    assert float(r) == pytest.approx(1.0)
    r = v3.step_apex_reward(jnp.array([0.0, 0.0105, 0.0, 0.021]), s_lat, 0.20, v3.W36)
    assert float(r) == pytest.approx(0.75)
    r = v3.step_apex_reward(jnp.array([0.050, 0.0, 0.050, 0.0]), s_lat, 0.20, v3.W36)      # 次要腿抬 50 mm 不算分
    assert float(r) == pytest.approx(0.0)
    r = v3.step_apex_reward(jnp.array([0.0, 0.050, 0.0, 0.050]), s_lat, 0.20, v3.W36)      # 超過目標飽和
    assert float(r) == pytest.approx(1.0)
    # 目標隨指令縮：vy 0.12 → 16.8 mm；vy 0.04 → 10.5 mm
    assert float(v3.step_apex_reward(jnp.array([0.0, 0.0168, 0.0, 0.0168]), s_lat, 0.12, v3.W36)) == pytest.approx(1.0)
    assert float(v3.step_apex_reward(jnp.array([0.0, 0.0105, 0.0, 0.0105]), s_lat, 0.04, v3.W36)) == pytest.approx(1.0)
    # 斜走：s 整體縮過（0.4×），正規化後主動腿仍是 1.0
    assert float(v3.step_apex_reward(jnp.array([0.0, 0.021, 0.0, 0.021]), s_lat * 0.4, 0.15, v3.W36)) == pytest.approx(1.0)


def _env_rollout(env, cmd, steps, seed=0):
    jr, js = jax.jit(env.reset), jax.jit(env.step)
    s = jr(jax.random.PRNGKey(seed))
    s = s.replace(info={**s.info, "cmd": jnp.array(cmd), "cmd2": jnp.array(cmd), "t_switch": 10 ** 6})
    out = []
    for _ in range(steps):
        s = js(s, jnp.zeros(v3.ACT_DIM)); out.append(s)
    return out


def test_t_step_zero_by_default_and_positive_in_lateral_with_w36():
    """預設 W：t_step 恆 0。W36＋factory 零動作左平移 0.20：兩個週期後 t_step > 0（零動作主動側兩腿抬 21 mm）；原地轉 t_step = 0。"""
    env0 = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False))
    S = _env_rollout(env0, (0.0, 0.20, 0.0), 80)
    assert all(float(s.metrics["t_step"]) == 0.0 for s in S)
    assert set(S[-1].info) >= {"apex_run", "apex_last"} and S[-1].info["apex_last"].shape == (4,)
    env = v3.DualModeEnv(gains="factory", weights=v3.W36, ref=dict(cyc_amp_rand=False))
    S = _env_rollout(env, (0.0, 0.20, 0.0), 150)                           # 3 s；2.1 Hz → 6 個週期
    ts = [float(s.metrics["t_step"]) for s in S]
    assert max(ts[-50:]) > 0.5, ts[-50:]
    S = _env_rollout(env, (0.0, 0.0, 1.3), 60)
    assert all(float(s.metrics["t_step"]) == 0.0 for s in S)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py -q -k "w36 or apex or t_step" 2>&1 | tail -5`
Expected: FAIL，`AttributeError: module 'rl_env_v3' has no attribute 'W36'`／`KeyError: 'W_STEP'`。

- [ ] **Step 3: 實作**

`task7/inference/rl_env_v3.py`，`W` dict 的最後一行與 `W35` 之後改成：

```python
         CYC_TURN_SYM=False,      # v3.5：True → env 把 ref["cyc_turn_sym"] 打開（放 W 裡是為了讓「權重預設」一個開關就帶齊訓練設定）
         W_STEP=0.0, STEP_APEX=0.021)   # v3.6：平移每週期抬腳頂點獎勵；預設 0 → 逐位元不變（golden）
W35 = dict(W, W_ABADBIAS=30.0, W_DRIFT=40.0, W_HEADLIN=1.0, W_VYREL=6.0, P_VY=0.60, CYC_TURN_SYM=True)   # v3.5（spec 2026-09-16 §2）：DualModeEnv(weights=v3.W35)
# v3.6（spec 2026-09-22 §2）：t_step ＋ t_drift／t_abadbias 改死區線性（PEN_SHAPE="hinge"；二次式接近目標時梯度消失，v3.5 spec §9.1）
W36 = dict(W35, W_STEP=1.5, STEP_APEX=0.021, PEN_SHAPE="hinge", W_DRIFT_L=8.0, DRIFT_DZ=0.010, W_ABADBIAS_L=6.0, ABAD_DZ=0.0436)
T_KEYS = ("t_vx", "t_vy", "t_yaw", "t_yawi", "t_yawlin", "t_yawrel", "t_vyrel", "t_head", "t_h", "t_lift", "t_stance", "t_roll", "t_pitch", "t_rollrate",
          "t_pitchrate", "t_bias", "t_act", "t_omdot", "t_qres", "t_tau", "t_taubar", "t_errbar", "t_kneev", "t_mode", "t_vz", "t_abadbias", "t_drift", "t_headlin", "t_step")
```

在 `drift_terms` 之後加兩個純函式：

```python
def step_apex_update(phi_new, phi_old, clr, apex_run, apex_last):
    """v3.6：每週期抬腳頂點。phi_cyc 回繞（新 < 舊）＝週期結束：上一週期的 running max 交給 apex_last、apex_run 從當步 clr 重新起算。
    phi 不前進（u4 ≤ 0.01）時不會回繞，apex_last 保持。"""
    wrap = phi_new < phi_old
    return (jnp.where(wrap, clr, jnp.maximum(apex_run, clr)), jnp.where(wrap, apex_run, apex_last))


def step_apex_reward(apex_last, s_leg, cmd_vy, w):
    """v3.6：主動腿（正規化活動度 ≥ 0.9 → 1，0.7 → 0）上一週期頂點對目標的線性分數，到目標飽和。
    目標隨指令縮：週期表的抬高跟幅度一起縮，零動作 vy 0.12 只抬 7 mm，低速逼 21 mm 會拿平衡去換（spec §2.1）。"""
    s_n = s_leg / jnp.maximum(jnp.max(s_leg), 1e-6)
    w_leg = jnp.clip((s_n - 0.9) / 0.1, 0.0, 1.0)
    tgt = w["STEP_APEX"] * jnp.clip(jnp.abs(cmd_vy) / 0.15, 0.5, 1.0)
    return jnp.sum(w_leg * jnp.clip(apex_last / tgt, 0.0, 1.0)) / jnp.maximum(jnp.sum(w_leg), 1e-3)
```

`reset` 的 `info` dict，在 `"abad_ema": jnp.zeros(4), "drift_ema": jnp.zeros(2),` 那行後加：

```python
                "apex_run": jnp.zeros(4), "apex_last": jnp.zeros(4),
```

`step` 裡 `clr = self._wheel_clearance(data)` 那行之後加：

```python
        apex_run, apex_last = step_apex_update(phi_cyc, info["phi_cyc"], clr, info["apex_run"], info["apex_last"])   # v3.6
```

`r_stance = ...` 之後（`c_act` 之前）加：

```python
        r_step = step_apex_reward(apex_last, s_leg, cmd[1], w)                            # v3.6：每週期抬腳頂點（只在平移族）
        t_step = w["W_STEP"] * u4 * _lat_weight(P["A"]) * r_step
```

`T` dict 的 `"t_headlin": w["W_HEADLIN"] * r_headlin,` 後加 `"t_step": t_step,`；`pos` 的和加 `+ T["t_step"]`（放在 `T["t_headlin"]` 之後）。
`info.update({...})` 裡加 `"apex_run": apex_run, "apex_last": apex_last,`。

- [ ] **Step 4: 跑測試確認通過 ＋ golden**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py task7/tests/test_rl_env_v3_golden.py task7/tests/test_rl_env_v35.py -q 2>&1 | tail -5`
Expected: 全 PASS（golden 不動；`t_step` 0.0 加進 `pos` 不改位元）。若 golden 失敗：先查是不是 `0.0 * nan`（`apex_last / tgt` 不會 nan，tgt ≥ 0.0105），不要放寬容差。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v36.py
git commit -m "feat(task7): v3.6 t_step —— 平移每週期抬腳頂點線性獎勵（policy 靠側滑不踏步；t_lift 在原廠週期模式恆 0）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: `drift_terms` 死區＋線性

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`drift_terms`，~263 行）
- Test: `task7/tests/test_rl_env_v36.py`

**Interfaces:**
- Consumes: `v3.W36`（Task 1）。
- Produces: `drift_terms(abad_ema, drift_ema, w)` 在 `w.get("PEN_SHAPE") == "hinge"` 時回傳 hinge 值，否則原式。

- [ ] **Step 1: 寫失敗的測試**（加到 `test_rl_env_v36.py` 末尾）

```python
def test_drift_terms_hinge_values_and_w35_unchanged():
    ab = jnp.array([0.11, 0.0, 0.0, 0.0]); dr = jnp.array([0.038, 0.0])
    ta, td = v3.drift_terms(ab, dr, v3.W36)
    assert float(ta) == pytest.approx(6.0 * (0.11 - 0.0436), abs=1e-6)     # 0.398／步
    assert float(td) == pytest.approx(8.0 * (0.038 - 0.010), abs=1e-6)     # 0.224／步
    ta, td = v3.drift_terms(jnp.array([0.04, -0.04, 0.0, 0.0]), jnp.array([0.009, -0.009]), v3.W36)
    assert float(ta) == 0.0 and float(td) == 0.0                            # 死區內精確 0
    ta35, td35 = v3.drift_terms(ab, dr, v3.W35)
    assert float(ta35) == float(30.0 * jnp.sum(ab ** 2)) and float(td35) == float(40.0 * jnp.sum(dr ** 2))
    ta0, td0 = v3.drift_terms(ab, dr, v3.W)
    assert float(ta0) == 0.0 and float(td0) == 0.0
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py -q -k hinge 2>&1 | tail -3`
Expected: FAIL（`ta` 是 30·0.11² = 0.363，不是 0.398）。

- [ ] **Step 3: 實作**

```python
def drift_terms(abad_ema, drift_ema, w):
    """v3.5：ABAD 慢漂（4 腿偏離站姿的低通）與機身速度低頻誤差（vx, vy）的懲罰 → (t_abadbias, t_drift)。
    預設二次式（權重 0 時精確為 0.0）；v3.6 `PEN_SHAPE="hinge"`：死區＋線性 —— 二次式在接近目標時梯度自己消失
    （0.038 m/s 時只剩 0.058／步），線性到目標仍有斜率；死區留給原廠圖案本來就有的 ±3° 不對稱（spec 2026-09-22 §2.2）。"""
    if w.get("PEN_SHAPE") == "hinge":
        ta = w["W_ABADBIAS_L"] * jnp.sum(jnp.maximum(jnp.abs(abad_ema) - w["ABAD_DZ"], 0.0))
        td = w["W_DRIFT_L"] * jnp.sum(jnp.maximum(jnp.abs(drift_ema) - w["DRIFT_DZ"], 0.0))
        return ta, td
    return w["W_ABADBIAS"] * jnp.sum(abad_ema ** 2), w["W_DRIFT"] * jnp.sum(drift_ema ** 2)
```

- [ ] **Step 4: 跑測試 ＋ golden**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py task7/tests/test_rl_env_v3_golden.py task7/tests/test_rl_env_v35.py -q 2>&1 | tail -3`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v36.py
git commit -m "feat(task7): v3.6 t_drift／t_abadbias 改死區＋線性（PEN_SHAPE=hinge；二次式接近目標時梯度消失）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: DR 橫向質心偏移

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`make_domain_randomize`，~739–779 行）
- Test: `task7/tests/test_rl_env_v36.py`

**Interfaces:**
- Produces: `make_domain_randomize(gains="kp250", com_y_mm=0.0)`；`v3.COM_Y_SCALE: float`（整機質量／base 質量，≈ 2.28）。

- [ ] **Step 1: 寫失敗的測試**

```python
def test_dr_com_y_shift_zero_mean_and_default_untouched():
    env = v3.DualModeEnv(gains="factory")
    base = int(v3._BASE_ID)
    n = 64
    keys = jax.random.split(jax.random.PRNGKey(3), n)
    sys_b, in_axes = v3.make_domain_randomize("factory", com_y_mm=5.0)(env.sys, keys)
    assert in_axes.body_ipos == 0 and sys_b.body_ipos.shape == (n, env.sys.nbody, 3)
    dy = np.asarray(sys_b.body_ipos[:, base, 1] - env.sys.body_ipos[base, 1])
    lim = 0.005 * v3.COM_Y_SCALE                                   # base 要移 ±11.4 mm，整機才是 ±5 mm
    assert abs(v3.COM_Y_SCALE - 38.82 / 17.03) < 0.05
    assert np.all(np.abs(dy) <= lim + 1e-9) and dy.std() > 0.4 * lim and abs(dy.mean()) < 0.3 * lim
    # 其他 body 沒動
    others = np.asarray(sys_b.body_ipos)[:, [i for i in range(env.sys.nbody) if i != base], :]
    assert np.allclose(others, np.asarray(env.sys.body_ipos)[None, [i for i in range(env.sys.nbody) if i != base], :])
    # 預設 com_y_mm=0：body_ipos 不在 in_axes、值逐位元同
    sys_0, in_axes_0 = v3.make_domain_randomize("factory")(env.sys, keys)
    assert in_axes_0.body_ipos is None and np.array_equal(np.asarray(sys_0.body_ipos), np.asarray(env.sys.body_ipos))
    # 舊的十個抽樣沒被新鍵擾動：kp 抽樣兩種呼叫逐位元同
    assert np.array_equal(np.asarray(sys_0.actuator_gainprm), np.asarray(sys_b.actuator_gainprm))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py -q -k com_y 2>&1 | tail -3`
Expected: FAIL，`TypeError: make_domain_randomize() got an unexpected keyword argument 'com_y_mm'`。

- [ ] **Step 3: 實作**

`_BASE_ID` 那行之後加：

```python
_M_BASE = mujoco.MjModel.from_xml_path(SCENE_V3)
COM_Y_SCALE = float(_M_BASE.body_subtreemass[_BASE_ID] / _M_BASE.body_mass[_BASE_ID])   # 整機 38.82 / base 17.03 ≈ 2.28：base 質心要移這麼多倍整機才移 1 倍
```

（`_BASE_ID` 已經用 `mujoco.MjModel.from_xml_path(SCENE_V3)` 算過一次，改成先存 `_M_BASE` 再取 id，不要載兩次。）

`make_domain_randomize` 改成：

```python
def make_domain_randomize(gains: str = "kp250", com_y_mm: float = 0.0):
    """回傳對應增益組的 domain_randomize。標稱 kp/kd 必須跟著模型走，
    否則 factory 線會被隨機化拉回 kp250 附近（DR 是乘在標稱值上的）。
    `com_y_mm > 0`（v3.6）：base_link 質心 y 每回合抽 U(−1,1)·com_y_mm·COM_Y_SCALE，整機質心 ±com_y_mm、零均值，
    洗掉「模型質心偏左 1.5 mm → policy 學方向專屬補償」（v3.5 spec §9.4）。預設 0 走原路徑。"""
    G = GAIN_SETS[gains]
    _KP_NOM_j = jnp.array(np.tile(np.asarray(G["kp3"]), 4))
    _KD_NOM = float(np.asarray(G["kd3"])[1])
    dy_max = com_y_mm / 1000.0 * COM_Y_SCALE

    def domain_randomize(sys, rng):
        """v2 那組 ＋ 輪 frictionloss 0.10–0.18、damping 0.005–0.03、ABAD kp ×0.7–1.0（v3.3 由 0.4 縮起）。"""
        @jax.vmap
        def per_env(rng):
            k = jax.random.split(rng, 10)            # ⚠️ 個數不能改：改了現有 notebook 的抽樣就變了；新鍵用 fold_in
            gf = sys.geom_friction.at[:, 0].set(jax.random.uniform(k[0], minval=0.4, maxval=1.4))
            s_kp = jax.random.uniform(k[1], minval=0.8, maxval=1.2)
            s_ab = jax.random.uniform(k[2], minval=0.7, maxval=1.0)      # v3.3：×0.4 太寬，平移滑步速度變異蓋掉 vy 訊號
            kv = jax.random.uniform(k[3], minval=0.5, maxval=2.0) * _KD_NOM
            kp_leg = _KP_NOM_j * s_kp
            kp_leg = kp_leg.at[ABAD12].set(_KP_NOM_j[ABAD12] * s_ab)
            gain = sys.actuator_gainprm.at[LEG_ACT_IDX, 0].set(kp_leg)
            bias = sys.actuator_biasprm.at[LEG_ACT_IDX, 1].set(-kp_leg).at[LEG_ACT_IDX, 2].set(-kv)
            bm = sys.body_mass * jax.random.uniform(k[4], (sys.nbody,), minval=0.9, maxval=1.1)
            bm = bm.at[_BASE_ID].add(jax.random.uniform(k[5], minval=0.0, maxval=5.0))
            fl = sys.dof_frictionloss
            fl = fl.at[_LEG_DOF].multiply(jax.random.uniform(k[6], minval=0.5, maxval=1.5))
            fl = fl.at[_ABAD_DOF].set(sys.dof_frictionloss[_ABAD_DOF] * jax.random.uniform(k[7], minval=0.6, maxval=1.4))
            fl = fl.at[_WHEEL_DOF].set(jax.random.uniform(k[8], minval=0.10, maxval=0.18))
            dmp = sys.dof_damping.at[_WHEEL_DOF].set(jax.random.uniform(k[9], minval=0.005, maxval=0.03))
            if dy_max > 0:
                dy = jax.random.uniform(jax.random.fold_in(rng, 99), minval=-1.0, maxval=1.0) * dy_max
                ipos = sys.body_ipos.at[_BASE_ID, 1].add(dy)
                return gf, gain, bias, bm, fl, dmp, ipos
            return gf, gain, bias, bm, fl, dmp

        out = per_env(rng)
        gf, gain, bias, bm, fl, dmp = out[:6]
        in_axes = jax.tree_util.tree_map(lambda x: None, sys)
        in_axes = in_axes.replace(geom_friction=0, actuator_gainprm=0, actuator_biasprm=0, body_mass=0,
                                  dof_frictionloss=0, dof_damping=0)
        sys = sys.replace(geom_friction=gf, actuator_gainprm=gain, actuator_biasprm=bias, body_mass=bm,
                          dof_frictionloss=fl, dof_damping=dmp)
        if dy_max > 0:
            in_axes = in_axes.replace(body_ipos=0)
            sys = sys.replace(body_ipos=out[6])
        return sys, in_axes

    return domain_randomize
```

- [ ] **Step 4: 跑測試 ＋ golden**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py task7/tests/test_rl_env_v3_golden.py -q 2>&1 | tail -3`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v36.py
git commit -m "feat(task7): v3.6 DR 加橫向質心偏移（整機 ±5 mm 零均值；新鍵用 fold_in，舊十個抽樣不變）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: env `push` 參數

**Files:**
- Modify: `task7/inference/rl_env_v3.py`（`__init__` 簽名 ~427；`step` 推力段 ~615）
- Test: `task7/tests/test_rl_env_v36.py`

**Interfaces:**
- Produces: `DualModeEnv(..., push: bool = True)`；`env.push_vel: float`（`PUSH_VEL` 或 0.0）。

- [ ] **Step 1: 寫失敗的測試**

```python
def test_push_false_removes_kick_at_step_99():
    """STAND 零動作：push=True 在第 99 步（PUSH_EVERY 100）機身速度跳；push=False 沒有。"""
    def kick(env, seed):
        S = _env_rollout(env, (0.0, 0.0, 0.0), v3.PUSH_EVERY + 1, seed)
        v = np.array([[float(s.metrics["vx"]), float(s.metrics["vy"])] for s in S])
        return float(np.linalg.norm(v[v3.PUSH_EVERY - 1] - v[v3.PUSH_EVERY - 2]))
    env_p = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False))
    env_n = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False), push=False)
    assert env_p.push_vel == v3.PUSH_VEL and env_n.push_vel == 0.0
    assert max(kick(env_p, sd) for sd in range(3)) > 0.05
    assert max(kick(env_n, sd) for sd in range(3)) < 0.005
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py -q -k push 2>&1 | tail -3`
Expected: FAIL，`TypeError: __init__() got an unexpected keyword argument 'push'`。

- [ ] **Step 3: 實作**

`__init__` 簽名改成 `def __init__(self, scene: str = None, wheel_pos: bool = False, ref: dict = None, weights: dict = None, gains: str = "kp250", push: bool = True):`，docstring 加一句 `` `push=False`（v3.6 驗收用）：關掉每 PUSH_EVERY 步的隨機推力；訓練預設開。``，`self.gains = gains` 之後加 `self.push_vel = PUSH_VEL if push else 0.0`。

`step` 推力段 `mag = jax.random.uniform(k_push, minval=0.0, maxval=PUSH_VEL)` 改成 `mag = jax.random.uniform(k_push, minval=0.0, maxval=self.push_vel)`。

- [ ] **Step 4: 跑測試 ＋ golden**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py task7/tests/test_rl_env_v3_golden.py -q 2>&1 | tail -3`
Expected: 全 PASS（`maxval` 是同一個 Python float 0.6，golden 不動）。

- [ ] **Step 5: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/rl_env_v3.py task7/tests/test_rl_env_v36.py
git commit -m "feat(task7): DualModeEnv 加 push 開關（驗收要關掉每 2 s 的訓練推力；直走的頓點就是它）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 驗收工具 `local_infer_v3.py`：`--push`、`--preset v36`、步/秒欄、摔欄兩組 ＋ 重跑 v3.5f 乾淨基準

**Files:**
- Modify: `task7/inference/local_infer_v3.py`
- Test: `task7/tests/test_local_infer_v3.py`
- Regenerate: `task7/outputs/eval_cpg_rl_v3_5f_params.md`

**Interfaces:**
- Consumes: `DualModeEnv(push=...)`（Task 4）、`v3.W36`（Task 1）。
- Produces: `rollout(...)` 回傳多一鍵 `steps_s: list[4]`（後 8 s 每腿每秒 `clr` 由 ≤ 8 mm 上穿 > 8 mm 的次數）；CLI `--push`（store_true）、`--preset v36`；表多一欄「步/秒 @8mm」，摔欄格式 `x/3｜推 y/3`。

- [ ] **Step 1: 寫失敗的測試**（加到 `test_local_infer_v3.py`）

```python
def test_rollout_reports_steps_per_second():
    env = v3.DualModeEnv(gains="factory", ref=dict(cyc_amp_rand=False), push=False); jr, js = jax.jit(env.reset), jax.jit(env.step)
    r = L.rollout(env, jr, js, (0.0, 0.20, 0.0), 250, None, seed=0)      # 零動作左平移 5 s：主動側 FL／RL 應有 ≥ 1.5 步/秒
    assert len(r["steps_s"]) == 4 and all(isinstance(x, float) for x in r["steps_s"])
    assert r["steps_s"][1] >= 1.5 and r["steps_s"][3] >= 1.5 and r["steps_s"][0] < 0.5
    r0 = L.rollout(env, jr, js, (0.5, 0.0, 0.0), 60, None, seed=0)
    assert r0["steps_s"] == [0.0, 0.0, 0.0, 0.0]


def test_cli_presets_and_push_flag():
    import argparse
    p = L.build_parser()
    a = p.parse_args(["--preset", "v36"])
    assert a.preset == "v36" and a.push is False
    assert L.preset_weights("v36") is v3.W36 and L.preset_weights("v35") is v3.W35 and L.preset_weights("v33") is None
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_local_infer_v3.py -q 2>&1 | tail -3`
Expected: FAIL，`KeyError: 'steps_s'`／`AttributeError: build_parser`。

- [ ] **Step 3: 實作**

`local_infer_v3.py`：

(a) `rollout` 的 return dict 加（放在 `lift=` 那行之後）：

```python
        steps_s=_steps_per_sec(np.array([m["clr"] for m in M[h:]]), max(1, n - h) * v3.CTRL_DT) if n > h else [0.0, 0.0, 0.0, 0.0],
```

模組層加：

```python
STEP_THR_MM = 8.0     # 步/秒門檻：clr 由 ≤ 8 mm 上穿 > 8 mm 算一步（原廠平移抬 21 mm；v3.5f 滑步 p95 只有 2–8 mm）


def _steps_per_sec(clr, secs):
    """(T,4) clr[mm] → 每腿每秒上穿 STEP_THR_MM 的次數。"""
    up = (clr[1:] > STEP_THR_MM) & (clr[:-1] <= STEP_THR_MM)
    return [float(x) for x in (up.sum(0) / secs).round(2)]
```

(b) 把 `main()` 裡的 `ap = argparse.ArgumentParser()` 到 `a = ap.parse_args()` 抽成模組函式 `build_parser()`（`main` 改成 `a = build_parser().parse_args()`），並：
- `--preset` 的 `choices=("v33", "v35", "v36")`，help 加 `v36＝weights=W36（抬腳頂點獎勵＋hinge 懲罰，2026-09-22）`。
- 加 `ap.add_argument("--push", action="store_true", help="統計 rollout 也開訓練用的隨機推力（預設關：推力每 2 s 一次，會污染 roll／航向／vx 漂；摔欄永遠另跑一組有推力的）")`。
- 加模組函式：

```python
def preset_weights(preset):
    return {"v33": None, "v35": v3.W35, "v36": v3.W36}[preset]
```

(c) `main()` 的 env 行改成兩個 env：

```python
    W_ = preset_weights(a.preset)
    env = v3.DualModeEnv(gains=a.gains, weights=W_, ref=dict(cyc_amp_rand=False), push=a.push); jr, js = jax.jit(env.reset), jax.jit(env.step)
    env_p = env if a.push else v3.DualModeEnv(gains=a.gains, weights=W_, ref=dict(cyc_amp_rand=False), push=True)   # 摔欄用：有推力那組
    jrp, jsp = (jr, js) if a.push else (jax.jit(env_p.reset), jax.jit(env_p.step))
```

迴圈裡 `R = [...]` 之後加：

```python
        RP = R if a.push else [rollout(env_p, jrp, jsp, cmd, steps, pol, seed=k) for k in range(a.seeds)]
```

`agg["lift"]` 那行之後加 `agg["steps_s"] = np.mean([r["steps_s"] for r in ok], 0).round(1).tolist()`；
`agg["falls"]` 改成：

```python
        t_falls = ",".join("%.1fs" % r["t_fall"] for r in RP if r["fell"])
        agg["falls"] = "%d/%d｜推 %d/%d" % (sum(r["fell"] for r in R), len(R), sum(r["fell"] for r in RP), len(RP)) + ("（%s）" % t_falls if t_falls else "")
```

print 行在 `lift {agg['lift']}` 後加 ` 步/秒 {agg['steps_s']}`。

(d) md 表頭：標題行的「3 種子 × 10 s」後加 `；推力 {"開" if a.push else "關（摔欄另跑有推力）"}`；欄名 `| 抬腳 mm |` 後插 `| 步/秒 @8mm |`，分隔線多一個 `---`，資料列 `{g['lift']} |` 後插 ` {g['steps_s']} |`。
判讀規則字串末尾加：`平移主動側兩腿 步/秒 ≥ 1.5 且抬腳 ≥ 15 mm（v3.6 spec §5）。`
輸出檔名：`out = HERE.parent / "outputs" / f"eval_{wname}{'_push' if a.push else ''}.md"`。

- [ ] **Step 4: 跑測試**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_local_infer_v3.py -q 2>&1 | tail -3`
Expected: 全 PASS。

- [ ] **Step 5: 重跑 v3.5f 無推力乾淨基準**

Run:
```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python task7/inference/local_infer_v3.py --gains factory --preset v35 --weights task7/weights/cpg_rl_v3_5f_params.pkl --seeds 3 2>&1 | grep -v -i 'warn\|warp'
```
Expected：印九行、寫 `task7/outputs/eval_cpg_rl_v3_5f_params.md`；直走 roll std 應比舊表 0.36 低、平移 步/秒 主動側 < 1.0（滑步）、摔欄 `0/3｜推 0/3`。約 5 分鐘。
把新表的平移／直走列與舊表（`git show e5e4842:task7/outputs/eval_cpg_rl_v3_5f_params.md`）差異記到 spec §8（Task 9 做）。

- [ ] **Step 6: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/local_infer_v3.py task7/tests/test_local_infer_v3.py task7/outputs/eval_cpg_rl_v3_5f_params.md
git commit -m "feat(task7): local_infer_v3 預設關推力＋摔欄兩組＋步/秒欄＋--preset v36；v3.5f 無推力乾淨基準重跑

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 攤帳 `reward_audit_v35.py --weights-set v36`

**Files:**
- Modify: `task7/inference/diag/reward_audit_v35.py`
- Create（執行產出）: `task7/outputs/reward_audit_v36.md`

**Interfaces:**
- Consumes: `v3.W36`、`metrics["t_step"]`、`L.load_policy`、`L.preset_weights`。
- Produces: CLI `--weights-set {v35,v36}`（預設 v35，舊行為與檔名不變）；v36 多一列「左平移 0.20 零動作」；退出碼 1＝沒過。

- [ ] **Step 1: 改程式**

```python
# 模組層：KEYS 加 "t_step"
KEYS = ("t_abadbias", "t_drift", "t_headlin", "t_step", "abad_bias", "vx_drift", "head_deg", "reward")

# v36 的案例與門檻（spec 2026-09-22 §3.3）；(name, cmd, use_policy)
CASES36 = (("原地左轉 1.3", (0.0, 0.0, 1.3), True), ("左平移 0.20", (0.0, 0.20, 0.0), True),
           ("左平移 0.20 零動作", (0.0, 0.20, 0.0), False), ("直走 0.5", (0.5, 0.0, 0.0), True))
GATE36 = {"原地左轉 1.3": {"t_abadbias": (0.3, 0.8), "t_step": (None, 1e-6)},
          "左平移 0.20": {"t_step": (None, 1.0), "t_drift": (0.15, 0.35)},
          "左平移 0.20 零動作": {"t_step": (1.2, None)},                 # 名目贏過現況（v3.2 教訓）；另在 main 裡檢查 ≥ policy + 0.4
          "直走 0.5": {"t_step": (None, 0.05), "t_abadbias": (None, 0.05), "t_drift": (None, 0.05)}}
```

`run_case(env, jr, js, pol, cmd, steps)`：`s = js(s, pol(s.obs))` 改成 `s = js(s, pol(s.obs) if pol is not None else jnp.zeros(v3.ACT_DIM))`。
`judge(name, r)` 改成 `judge(name, r, gate)`，內部用 `gate[name]`。

`main()`：
- `ap.add_argument("--weights-set", default="v35", choices=("v35", "v36"), dest="wset")`；`--weights` 預設不變。
- `W_ = v3.W36 if a.wset == "v36" else v3.W35`；env 行 `weights=W_`，並加 `push=False`（攤帳看的是 reward 項，不要推力）。
- 案例：`cases = CASES36 if a.wset == "v36" else tuple((n, c, True) for n, c in CASES)`；`gate = GATE36 if a.wset == "v36" else GATE`。
- 迴圈 `for name, cmd, use_pol in cases: r = run_case(env, jr, js, pol if use_pol else None, cmd, steps); bad = judge(name, r, gate)`。
- 迴圈後（v36 時）交叉檢查：

```python
    if a.wset == "v36":
        R_ = {n: r for n, r, _ in rows}
        gap = R_["左平移 0.20 零動作"]["t_step"] - R_["左平移 0.20"]["t_step"]
        if gap < 0.4:
            all_ok = False; rows.append(("零動作 − policy 的 t_step", {k: 0.0 for k in KEYS} | {"t_step": gap, "fell": False}, [f"gap {gap:.2f} < 0.4：名目沒有明確贏過現況"]))
```

- print／md：欄加 `t_step`（`{r['t_step']:.3f}`）；標題 `在 {a.wset.upper()} 下`；門檻說明字串 v36 版：`門檻（spec 2026-09-22 §3.3）：左平移 policy t_step ≤ 1.0、零動作 t_step ≥ 1.2 且比 policy 高 ≥ 0.4、t_drift 0.15–0.35；原地轉 t_abadbias 0.3–0.8、t_step = 0；直走三項 ≤ 0.05。`
- 輸出檔：v36 → `outputs/reward_audit_v36.md`（不分 gains；v35 路徑檔名不變）。

- [ ] **Step 2: 跑攤帳（v3.5f 權重在 W36 下）**

Run:
```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python task7/inference/diag/reward_audit_v35.py --weights-set v36 --gains factory --weights task7/weights/cpg_rl_v3_5f_params.pkl 2>&1 | grep -E '^(PASS|FAIL|→)'
```
Expected：四列 PASS，寫 `task7/outputs/reward_audit_v36.md`。預期值：左平移 policy `t_step` 0.3–0.8、零動作 1.3–1.5、`t_drift` ≈ 0.22、原地轉 `t_abadbias` ≈ 0.4–0.6。
**若 FAIL**：不要改門檻湊過。`t_step` 零動作 < 1.2 → 查 `steps_s` 是否主動側兩腿真的 21 mm（Task 5 的基準表）；policy > 1.0 → 每週期頂點比 p95 高很多，回 spec §0.1 重量、考慮把 `STEP_THR_MM`／目標一起檢討後再決定。

- [ ] **Step 3: 確認 v35 路徑沒變**

Run: `cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python task7/inference/diag/reward_audit_v35.py --gains factory --weights task7/weights/cpg_rl_v3_4f_params.pkl 2>&1 | grep -E '^(PASS|FAIL)' && git diff --stat task7/outputs/reward_audit_v35f.md`
Expected：三列 PASS；`reward_audit_v35f.md` 只有新增的 `t_step` 欄（值 0.000）不同——這是可接受的差異，其餘數字逐字同（推力關了會讓數字變！所以 v35 路徑的 env 保持 `push=True`：把 `push=(a.wset == "v36") is False` 寫清楚 → `push=a.wset != "v36"`）。

- [ ] **Step 4: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/inference/diag/reward_audit_v35.py task7/outputs/reward_audit_v36.md task7/outputs/reward_audit_v35f.md
git commit -m "feat(task7): 攤帳加 --weights-set v36（零動作列、t_step 門檻、名目贏過現況檢查）—— v3.5f 權重在 W36 下全過

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: G0 回歸（預設路徑逐字同）

**Files:**
- Create（執行產出）: `task7/outputs/g0_v36_regress.txt`

- [ ] **Step 1: 跑 G0 並比對**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python task7/inference/diag/g0_v3.py --steps 500 --lat-vy 0.04 2>&1 | grep -E "^(PASS|FAIL)" > task7/outputs/g0_v36_regress.txt
diff <(sed 's/ ([0-9]*s)$//' task7/outputs/g0_v35_regress.txt) <(sed 's/ ([0-9]*s)$//' task7/outputs/g0_v36_regress.txt) && echo "G0 逐字相同"
```
Expected：印 `G0 逐字相同`（七 case；TURN 與 LAT vy0.04 本來就 FAIL）。不同→ 停下來查是哪個 task 動到預設路徑（golden 12 步過但 500 步不同＝數值上很小的差異在累積，也算動到）。

- [ ] **Step 2: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/outputs/g0_v36_regress.txt
git commit -m "test(task7): v3.6 G0 回歸 —— 預設路徑七 case 與 g0_v35_regress.txt 逐字同

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: notebook `cpg_rl_v3_6f_colab.ipynb`

**Files:**
- Modify: `task7/notebooks/build_nb_v3.py`
- Create（產出）: `task7/notebooks/cpg_rl_v3_6f_colab.ipynb`
- Create: `task7/tests/test_notebook_v36.py`

**Interfaces:**
- Consumes: `v3.W36`、`v3.make_domain_randomize("factory", com_y_mm=5.0)`、`metrics["t_step"]`。
- Produces: `python3 task7/notebooks/build_nb_v3.py --gains factory --weights v36` → `cpg_rl_v3_6f_colab.ipynb`，權重檔名 `cpg_rl_v3_6f_params.pkl`。

- [ ] **Step 1: 寫失敗的測試**

```python
# task7/tests/test_notebook_v36.py
"""v3.6f notebook：build_nb_v3.py --gains factory --weights v36；env 帶 W36、DR 帶 com_y_mm=5.0、progress 印 step。"""
import json
import subprocess
from pathlib import Path

NBDIR = Path(__file__).resolve().parents[1] / "notebooks"
NB = NBDIR / "cpg_rl_v3_6f_colab.ipynb"


def _code_src(path):
    nb = json.loads(path.read_text(encoding="utf-8"))
    return "\n".join("".join(c["source"]) for c in nb["cells"] if c["cell_type"] == "code")


def _md_src(path):
    nb = json.loads(path.read_text(encoding="utf-8"))
    return "\n".join("".join(c["source"]) for c in nb["cells"] if c["cell_type"] == "markdown")


def test_v36f_notebook_env_dr_progress_save():
    assert NB.exists(), "先跑 python3 task7/notebooks/build_nb_v3.py --gains factory --weights v36"
    src = _code_src(NB)
    assert src.count('v3.DualModeEnv(gains="factory", weights=v3.W36)') == 2
    assert 'v3.DualModeEnv(gains="factory", weights=v3.W36, ref=dict(cyc_amp_rand=False))' in src
    assert 'randomization_fn=v3.make_domain_randomize("factory", com_y_mm=5.0)' in src
    assert "step {ps('t_step'):.2f}" in src and "abad {ps('abad_bias'):.1f}" in src and "head {ps('head_abs'):.1f}" in src
    assert "vy {ps('t_vyrel'):.2f}/6" in src
    assert 'model.save_params("cpg_rl_v3_6f_params.pkl", params)' in src
    assert "RESUME = None" in src
    md = _md_src(NB)
    assert "v3.6f" in md and "t_step" in md and "2026-09-22" in md and "< 0.3" in md


def test_older_notebooks_unchanged_by_builder():
    """重新產生 v3.3／v3.4f／v3.5／v3.5f 四本，git 不能看到差異。"""
    for args in (["--weights", "v33"], ["--gains", "factory", "--weights", "v33"], ["--weights", "v35"], ["--gains", "factory", "--weights", "v35"]):
        subprocess.run(["python3", str(NBDIR / "build_nb_v3.py"), *args], check=True, capture_output=True)
    r = subprocess.run(["git", "diff", "--exit-code", "--stat", "--", str(NBDIR / "cpg_rl_v3_colab.ipynb"), str(NBDIR / "cpg_rl_v3_4f_colab.ipynb"),
                        str(NBDIR / "cpg_rl_v3_5_colab.ipynb"), str(NBDIR / "cpg_rl_v3_5f_colab.ipynb")], capture_output=True, text=True)
    assert r.returncode == 0, r.stdout
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_notebook_v36.py -q 2>&1 | tail -3`
Expected: 第一個 FAIL（notebook 不存在）；第二個 PASS（還沒改 builder）。

- [ ] **Step 3: 改 builder**

`build_nb_v3.py` 開頭那段變數改成：

```python
_ap.add_argument("--weights", default="v33", choices=("v33", "v35", "v36"),
                 help="v35＝DualModeEnv(weights=v3.W35)（慢漂懲罰＋線性航向，spec 2026-09-16）；v36＝W36（抬腳頂點獎勵＋hinge 懲罰＋質心 DR，spec 2026-09-22）")
ARGS = _ap.parse_args()
FACTORY = ARGS.gains == "factory"
V35 = ARGS.weights == "v35"
V36 = ARGS.weights == "v36"
VNEW = V35 or V36                                   # v3.5 起共用的東西（RESUME、vy 分母 6、progress 三欄）
_tag = {(False, "v33"): "v3", (True, "v33"): "v3_4f", (False, "v35"): "v3_5", (True, "v35"): "v3_5f", (False, "v36"): "v3_6", (True, "v36"): "v3_6f"}[(FACTORY, ARGS.weights)]
OUT = HERE / f"cpg_rl_{_tag}_colab.ipynb"
WEIGHTS = f"cpg_rl_{_tag}_params.pkl"
GAINS_KW = 'gains="factory"' if FACTORY else ''
GAINS_KW2 = 'gains="factory", ' if FACTORY else ''    # 後面還有其他參數時用
W_KW = {"v33": "", "v35": "weights=v3.W35", "v36": "weights=v3.W36"}[ARGS.weights]
ENV_ARGS = ", ".join(x for x in (GAINS_KW, W_KW) if x)
ENV_ARGS2 = ENV_ARGS + ", " if ENV_ARGS else ""
PROG_EXTRA = ("abad {ps('abad_bias'):.1f}° drift {ps('vx_drift'):+.3f} head {ps('head_abs'):.1f}° | " if VNEW else "") + ("step {ps('t_step'):.2f} | " if V36 else "")
VY_DEN = "6" if VNEW else "3"
RESTORE_KW = ", restore_params=_restore" if VNEW else ""
```

`DR_FN` 改成：

```python
DR_FN = ('v3.make_domain_randomize("factory", com_y_mm=5.0)' if V36 else f'v3.make_domain_randomize("{ARGS.gains}")') if FACTORY else "v3.domain_randomize"
```

（v36 只做 factory 線；`--weights v36` 不配 `--gains factory` 時 `DR_FN` 仍是 `v3.domain_randomize`，加一行 `assert not V36 or FACTORY, "v3.6 只跑原廠增益線"`。）

`RESUME_SRC` 的條件 `if V35 else ''` 改 `if VNEW else ''`。

`if V35:` 的 md0 區塊之後加：

```python
if V36:
    md0 = """# CPG-RL **v3.6f**：智元 D1 Max · 平移抬腳頂點獎勵＋線性懲罰＋質心 DR · MJX · Colab GPU（2026-09-22）

在 v3.5f（`weights/cpg_rl_v3_5f_params.pkl`：九指令 0 摔、左轉 105% 原廠、航向 −32.7°→+4.5°）之上修四件事：
- **平移不踏步、靠側滑**：原廠週期模式下 `t_lift`／`t_mode`／`t_stance` 恆 0，policy 把抬腳壓到 4–19 mm（原廠 21）→ 加 `t_step`：
  主動側兩腿**每週期抬腳頂點**對目標（21 mm，隨指令縮）的線性分數 × 1.5，只在平移族。
- **`t_drift`／`t_abadbias` 改死區＋線性**（`PEN_SHAPE="hinge"`）：二次式在 0.038 m/s 時每步只剩 0.058、推不到目標。
- **DR 加橫向質心偏移**（整機 ±5 mm 零均值）：模型質心偏左 1.5 mm 讓 v3.5f 右轉只有左轉的 79%，policy 學的是方向專屬補償。
- 驗收工具改預設無推力（之前每 2 s 一次的訓練推力污染了 roll／航向／vx 漂）。
設計：`docs/superpowers/specs/2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md`；攤帳 `outputs/reward_audit_v36.md`。

**停損**：同 v3.5f —— 1 億步 `進度 yaw` < 3.5 或 len < 600 → 停；**另加**：1 億步 `step` < 0.3（平移族約佔回合四成、飽和 ≈ 0.6）→ 抬腳學不起來，停下來查 `W_STEP`。
`abad`／`drift`／`head` 三欄跨指令平均不可信（v3.5 spec §9.2），只看方向。
**注意**：G0 格的原地轉是開迴路原廠週期，**預期會倒**，那格只 assert 其他四個指令。
"""
```

- [ ] **Step 4: 產生 notebook、跑測試**

```bash
cd /home/huang/rbtdog_sim && python3 task7/notebooks/build_nb_v3.py --gains factory --weights v36
conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_notebook_v36.py task7/tests/test_notebook_v35.py -q 2>&1 | tail -3
git status --short task7/notebooks/
```
Expected：`→ .../cpg_rl_v3_6f_colab.ipynb (9 cells)`；全 PASS；`git status` 只有 `?? cpg_rl_v3_6f_colab.ipynb` 與 `M build_nb_v3.py`（舊四本無差異）。

- [ ] **Step 5: notebook 本機能跑的格子先跑過**（import 格＋G0 格；train 格本機跑不動）

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python - <<'PY' 2>&1 | grep -v -i 'warn\|warp' | tail -12
import json, sys
sys.path.insert(0, "task7/inference")
nb = json.load(open("task7/notebooks/cpg_rl_v3_6f_colab.ipynb", encoding="utf-8"))
code = [ "".join(c["source"]) for c in nb["cells"] if c["cell_type"] == "code"]
exec(code[4]); exec(code[5])      # import 格、G0 格（跳過 install／version／clone）
PY
```
Expected：`obs 88 act 24 gains factory wheel_space tau`、五個指令的 dict、`G0 全過 → 可以訓練`。

- [ ] **Step 6: Commit**

```bash
cd /home/huang/rbtdog_sim && git add task7/notebooks/build_nb_v3.py task7/notebooks/cpg_rl_v3_6f_colab.ipynb task7/tests/test_notebook_v36.py
git commit -m "feat(task7): v3.6f Colab notebook —— build_nb_v3 --weights v36（W36、質心 DR、progress 印 step、停損加 step < 0.3）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 9: 交接文件 ＋ push

**Files:**
- Modify: `task7/HANDOFF.md`（頂節）、`docs/superpowers/specs/2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md`（加 §8 執行紀錄）

- [ ] **Step 1: spec §8 執行紀錄**

在 spec 末尾加 `## 8. 執行紀錄（2026-09-22）`，內容照實填：golden／G0 回歸結果、攤帳四列數字（從 `outputs/reward_audit_v36.md` 抄）、v3.5f 無推力基準對舊表的差（直走 roll std、平移航向、步/秒欄）、notebook 檔名、下一步「Colab 跑 `cpg_rl_v3_6f_colab.ipynb`，回來 `local_infer_v3.py --preset v36 --gains factory --weights task7/weights/cpg_rl_v3_6f_params.pkl --mesh --video ...`，對 spec §5 表」。

- [ ] **Step 2: HANDOFF 頂節**

`task7/HANDOFF.md`：「最後更新」行改 2026-09-22；「▶▶ 下次上班從這裡開始」下方**最上面**加一節：

```markdown
### ★★★★★ v3.6f 已備好，下一步：Colab 跑 `notebooks/cpg_rl_v3_6f_colab.ipynb`（2026-09-22）

spec `docs/superpowers/specs/2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md`（§0 量到的事實、§8 執行紀錄）。
四件事：①`t_step` 平移每週期抬腳頂點線性獎勵（v3.5f 平移靠側滑、主動側步/秒 0–0.8 對原廠 2.1；原廠週期模式下 t_lift 恆 0 是漏洞）
②`t_drift`／`t_abadbias` 改死區＋線性 ③DR 橫向質心 ±5 mm ④驗收工具預設關推力（直走每 2 s 的頓點就是訓練推力）＋ 步/秒欄。
上 Colab 前證據：golden 過、G0 `outputs/g0_v36_regress.txt` 逐字同、攤帳 `outputs/reward_audit_v36.md` 四列過（零動作 t_step 贏過 policy）。
停損：1 億步 進度 yaw < 3.5 或 len < 600 或 `step` < 0.3 → 停。
訓完：`local_infer_v3.py --preset v36 --gains factory --weights task7/weights/cpg_rl_v3_6f_params.pkl --seeds 3 --mesh --video task7/outputs/eval_v36f_mesh.mp4`，對 spec §5 表。
⚠️ 2026-09-22 前的所有 `outputs/eval_*.md` 都含每 2 s 的推力，roll／航向／vx 漂數字偏差；v3.5f 已重跑無推力版當基準。
```

- [ ] **Step 3: 全測試 ＋ commit ＋ push**

```bash
cd /home/huang/rbtdog_sim && conda run -n rbtdog --no-capture-output python -m pytest task7/tests/test_rl_env_v36.py task7/tests/test_rl_env_v35.py task7/tests/test_rl_env_v3_golden.py task7/tests/test_local_infer_v3.py task7/tests/test_notebook_v35.py task7/tests/test_notebook_v36.py -q 2>&1 | tail -3
git add task7/HANDOFF.md docs/superpowers/specs/2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md
git commit -m "docs(task7): v3.6 spec §8 執行紀錄 ＋ HANDOFF —— 等 Colab 跑 v3.6f

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
git push origin main
```
Expected：全 PASS；push 成功（Colab notebook 是 clone main 的，**沒 push 等於沒做**）。
