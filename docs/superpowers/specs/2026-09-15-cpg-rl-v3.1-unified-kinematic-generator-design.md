# CPG-RL v3.1：統一運動學產生器（輪行／弧線／原地轉／平移／三軸疊加）—— 設計（2026-09-15）

取代 `2026-09-09-cpg-rl-v3-dual-mode-design.md` 的 §1（模式產生器）與 §7 的兩條錯誤結論。v3.0 從未訓練，`inference/rl_env_v3.py` 就地改成 v3.1，不留舊版。

## 0. 為什麼要改（2026-09-15 重看 trip21 錄檔的結論）

L 報告（`task7/docs/results/L_原廠步態參考錄製_2026-09-09.md`）與 v3 spec §7 有兩處結論錯了，錯因是**整檔平均**把起停與靜止段一起平均掉：

| 動作 | 之前的結論 | 錄檔逐秒攤開後 |
|---|---|---|
| 平移 | 移動側兩腿踏步、另一側輪胎側滑；模擬站不住 | **四腿都踏**（2.1 Hz）。移動側每步側向 +35～+48 mm、擺動 0.10 s；另一側**抬起原地重放**（機身座標 dy ≈ 0、擺動 0.20 s，機身在它擺動時被帶過去）。輪：移動側 fl +4／bl −3 rad/s，另一側 ≈ 0 |
| 邊走邊轉（弧線） | 純差速輪行、不踏步、差速比 1.3–1.7 | 穩態偏航 18–52°/s（不是 7–13）。**內側前腿持續踏步** 2.2–2.4 Hz、抬 18–26 mm、側向重放 40–80 mm；快弧線時對角的外側後腿也踏（0.5 倍頻次）。內側前腳往中線收 60–100 mm、內側後腳往外撐 30–50 mm。輪速外前／內後差 4–5 倍 |
| 原地轉 | 對角一對同相踏步、另一對站姿 | **四腿都踏**（2.5 Hz）：主導對角對抬 23 mm，另兩腿抬 16 mm；相位 {fl 0, br 0.06, fr 0.25, bl 0.59}（左轉） |

模擬站不住平移的真正原因：v3.0 產生器只讓移動側兩腿 `active=1`，另一側釘死 → 身體要側移只能靠釘死輪側滑。是實作漏掉，不是物理做不到。

**四種動作是同一套運動學**：每腳每週期在地面上的位移 D_k = T·(v + ω × r_k)。純前進位移全在 x，輪子滾得掉，所以不抬腳；平移／原地轉／弧線有 y 分量，輪子做不到，所以抬腳。錄檔的每步位移方向全部符合這條公式（左轉 fl (−21, +39)、br (+16, −25)；左移 fl (−23, +38)、bl (+20, +29)）。

分析腳本（scratch，將收進 `inference/ref_extract.py`）：擺動事件法 = 足端 FK 高度相對 1 s 滾動最低點抬 > 12 mm、時長 0.04–0.35 s。

## 0.1 增益：原廠動作時用的是軟增益＋大幅命令，我們不照抄腿的部分

錄檔 `kp/kd/ff` 欄（M6 唯讀 `joint_cmd`）：

| | 腿 ABAD／HIP／KNEE kp | 腿 kd | 輪 kp／kd | 輪前饋 |
|---|---|---|---|---|
| 原廠所有動作段（含踏步） | 60／120／120 | 1.0 | 0／0.1 | 0.2–0.5 N·m |
| 原廠靜止 | 250／250／250 | 5.0 | 20／0.1 | 0 |
| 我們模擬 v3.0 | 60／250／250 | 2.0 | 60 位置環／1.0 | 0.13 |
| 我們實機 trip17／19 | 60／250／250 | 5.0 | 0／0.5 | 0 |

原廠在 kp 120 下的**命令**遠超實際位置（des 對 q 做 FK）：原地轉 fl 命令抬 152 mm、實際 23 mm、ABAD 目標差最大 58°；平移 122 對 24 mm；弧線內前腿 80 對 16 mm；純前進 19 對 16 mm。它是把 PD 當力矩致動器用（學習式策略的典型手法）。輪子 kd 0.1 幾乎等於力矩控制：前進時前輪 +1.3～1.7 N·m 拉、**後輪 −1.0 N·m 煞車**，四輪等速；弧線時內側後輪煞得更重。

決定（使用者 2026-09-15 選項 1）：
- **腿留 kp 60／250／250、kd 2.0**（模擬）。kp 120 配運動學目標在 trip16／E 報告已證明撓度 72 mm、沒有可用窗口；要用 kp 120 得連命令方式一起換成 RL 出大幅度目標，另開線。
- **輪子回到速度伺服 kd 1.0 ＋ 前饋 0.13（M11 實機已驗，`scene_flat_mjx_v3.xml`）**，拿掉 v3.0 的 kp 60 位置環（實機未驗、且與原廠相反）。差速轉不動的根因是**力矩權限不夠**（kd 1.0 × 0.6 rad/s 誤差 = 0.6 N·m 推不動刮地阻力），不是要更硬：RL 輪殘差改成 **速度偏移 ±2 rad/s（等價 ±2 N·m）**，允許後輪負力矩。
- **先做原廠回放驗證**（§5 步驟 0）：把錄檔的 des／kp／kd／腿前饋、輪子的實測 τ 直接在 MuJoCo 開迴路回放，檢查模擬器的輪地接觸能不能重現原廠動作。這決定 G0 門檻該信到什麼程度。

## 1. 範圍

做：
1. `inference/ref_extract.py` 加擺動事件法；四腿相位、抬高比、弧線內前腳數字、原廠增益表寫進 `outputs/ref_gait_dataset.json`；env 常數對資料集有測試。
2. `inference/diag/replay_factory.py`：原廠錄檔在 MuJoCo 開迴路回放（§5 步驟 0）。
3. `inference/rl_env_v3.py` 改成 v3.1 統一運動學產生器（§2–§4）；`tests/test_rl_env_v3.py` 改寫；`diag/g0_v3.py` 加斜走、掃描。
4. `notebooks/build_nb_v3.py` 重建 notebook；L 報告 §0／§1 與 v3 spec §7 加勘誤；圖 P12 重畫；HANDOFF 更新。

不做（另開 spec）：狗上 M9 的 v3 推論路徑、`local_infer_v3.py`（G0 過之後）、腿 kp 120 路線、平移以外的四腿蟹行。

## 2. 產生器

指令 cmd = (vx, vy, wz)，單位 m/s、m/s、rad/s。沒有離散模式，全部是 cmd 的連續函數。

### 2.1 運動學

腳 k 的站姿位置 r_k = (x_k, y_k)（機身座標，由 `max_model` 的 HIP_X／ABAD_TO_HIP_X／ABAD_TO_FOOT_Y 加站姿足端偏移算出；前腳 x ≈ +0.43、後腳 ≈ −0.42、y ≈ ±0.17 m）。步週期 T = 1／f。

```
D_k = T · (vx − wz·y_k,  vy + wz·x_k)         # 每週期地面位移 (m)
vec_k = (KX · D_k.x,  D_k.y)                  # 踏步位移：x 分量大部分交給輪子
KX = 0.3                                      # 錄檔：原地轉 fl 每步 dx −21 mm，公式全量 −73 mm
```

`foot_targets` 沿用：swing 往 +vec_k、stance 往 −vec_k、抬高只加在 swing。四腿共用同一條 vec_k 公式就是「無側滑蟹行」：stance 腳相對機身以 −v 移動，swing 腳每週期補回 +D。

### 2.2 活動度（誰抬腳、抬多高）

```
a_lat   = clip(|vy| / 0.08, 0, 1)
a_turn  = clip(|wz| / 1.3, 0, 1)
a_wheel = clip(|vx| / 0.25, 0, 1)
s4      = max(a_lat, a_turn · (1 − a_wheel))            # 四腿踏步（平移、原地轉）
s_arc   = a_turn · a_wheel                              # 弧線：內側前腿 ＋ 對角外側後腿
```

每腿活動度 s_k：

| 情境 | 腿 | s_k |
|---|---|---|
| 四腿踏步（s4） | 主導腿 | s4 × 1.0 |
| | 次要腿 | s4 × 0.7 |
| 弧線（s_arc） | 內側前腿 | s_arc × 1.0 |
| | 對角外側後腿 | s_arc × 0.5 |
| | 其餘 | 0 |

s_k = max(四腿項, 弧線項)。主導腿：平移＝移動側兩腿；原地轉＝主導對角對（左轉 fl+br、右轉 fr+bl）。0.7 與 0.5 是錄檔抬高比（23 mm 對 16 mm）與踏步頻次比。

抬高 lift_k = s_k × LIFT，LIFT 名目 0.023 m，命令值再加 Z_SAG 0.036（kp250 撓度，不變）。純 vx 時 s_k 全 0，不抬腳；`u_mode` 混合變數由 max_k s_k 取代（仍保留 1 s 斜率限制，避免指令跳變時目標跳）。

### 2.3 相位

四腿一組偏移（週期分數），以左向為基準、右向左右鏡像（fl↔fr、bl↔br）：

| | fl | fr | bl | br |
|---|---|---|---|---|
| 原地左轉 PH_TURN | 0 | 0.25 | 0.59 | 0.06 |
| 左平移 PH_LAT | 0 | 0.19 | 0.51 | 0.72 |

弧線用 PH_TURN（弧線是原地轉的主導對角對在 vx 大時的殘留：錄檔快弧線 fl 0、br 0.21）。vy 與 wz 同時非零時，每腿偏移取 PH_LAT 與 PH_TURN 依 a_lat：a_turn 權重的圓周插值。偏移目標改變時每步限速 0.02 週期。

方向鏡像的判定：平移看 sign(vy)，旋轉看 sign(wz)。

### 2.4 步頻與 duty

名目 f = 2.0 Hz、duty 0.5（RL ω ±28%、抬高 ±40% 不變）。原廠 2.5／2.1 Hz、duty 0.6–0.8 是 kp 250 做不到的（v3 spec §7 掃描），留在 `REF["factory_*"]` 對照。G0 掃 duty {0.5, 0.6, 0.7}。

### 2.5 輪速

```
diff_k  = (vx − wz · sign(y_k) · L_eff/2) / r          # 差速，L_eff 0.375（資料擬合值）、r 0.096
gate_k  = 1 + (1 − a_wheel) · (1 − 2·dom_k)            # 原地轉：主導對角對 0、另兩輪 2（力矩守恆）；vx 大時全部 1
wheel_k = diff_k · gate_k + a_lat · LAT_WHEEL_k · Ω_lat(vy) + res_k
```

- dom_k ∈ {0, 1}：原地轉主導對角對。錄檔左轉 fl/br ≈ 0、fr +4.1／bl −4.1。
- LAT_WHEEL：左移 fl +1.0、bl −0.67、其餘 0（右移鏡像），Ω_lat = 3.3 × |vy|/0.06 rad/s。這條運動學推不出，照錄檔抄。
- res_k：RL 輪殘差，**速度偏移 ±2 rad/s**（kd 1.0 下等價 ±2 N·m），取代 v3.0 的 ±20% 比例殘差。
- 控制律：`wheel_ctrl`（kd 1.0、前饋 0.13、死區 0.3），模型 `scene_flat_mjx_v3.xml`。`wheel_pos` 參數保留但預設 False；`_v3p` 模型不刪、不用。

### 2.6 弧線姿態偏移

站姿足端 y 目標加 s_arc × POSTURE_ARC：內側前腳 −0.06 m（往中線）、內側後腳 +0.03 m（往外），其餘 0。REF 參數，G0 開關比較，沒幫助就設 0。

## 3. 動作、觀測、reward、指令抽樣

- **動作 12 維不變**：`[ω_scale, amp×4, lift, sway_x, sway_y, wheel_res×4]`。wheel_res 語意改為 ±2 rad/s 偏移。
- **obs 76 維不變**：原 `[u_mode, 1−u_mode]` 兩格改為 `[s4, s_arc]`。其餘欄位與雜訊／延遲同 v3.0。
- **reward**：模式紀律改連續版。腿 k 抬 > 10 mm 的懲罰權重 (1 − s_k)；抬腿獎勵權重 s_k；站姿獎勵權重 (1 − max_k s_k)。vx／vy／wz 追蹤、姿態、偏置、護欄（58 N·m／0.45 rad／90 N·m 終止）全部不變。不加逐點模仿獎勵，模仿由產生器承擔。
- **指令抽樣**：三軸獨立。vx ∈ [−0.4, 0.9]（65% 非零）、vy ∈ ±[0.03, 0.10]（30% 非零）、wz ∈ ±[0.2, 1.3]（50% 非零）；全零 5% 站立；40% episode 中途切指令。斜走與邊走邊轉自然出現。

## 4. 測試（`tests/test_rl_env_v3.py`）

1. 運動學：左轉 fl 的 vec 方向 (−, +)、br (+, −)；左移四腿 vec.y 相同且 > 0；純 vx 時 vec.y = 0、s_k = 0。
2. 活動度表：五組指令（直走、弧線、原地轉、平移、斜走）→ s_k 與表一致；左右鏡像。
3. 相位：PH_TURN／PH_LAT 鏡像；插值端點；限速。
4. 輪速：原地轉 gate 主導對 0、另兩輪 2；vx 0.5 時 gate 全 1；殘差上下限 ±2。
5. 常數對資料集（頻率、抬高、相位、LAT_WHEEL、增益表）。
6. obs 布局釘死、reset/step 形狀、短 rollout G0（直走 vx ≥ 0.45 且不抬腳）。

## 5. 驗收

### 步驟 0：原廠回放（`inference/diag/replay_factory.py`）

MuJoCo（非 MJX）載 `zgws_mjx.xml` 幾何，腿致動器改成 kp 60／120／120、kd 1.0 的 PD（照錄檔），輪子改成 **motor 直接施加錄檔實測 τ**（原廠輪 v_des 沒錄到，kd 0.1 下 τ 就是它的控制輸出）。
從動作段起點：qpos 腿 = 錄檔 q、des = q 定住 1 s，再逐 tick（500 Hz）餵錄檔 des／ff，回放 1.5 s 視窗，每檔取 3 個起點。
比對：偏航率（gyro_z）、四腳抬高（FK）、roll／pitch、四輪速。
判讀：
- 原地轉回放偏航 ≥ 50% 原廠（≥ 38°/s）、抬腳 15–30 mm、不摔 → 模擬器輪地模型可信，G0 門檻照 §5.1。
- 偏航 < 25% 或輪子被刮地鎖死 → 先調 floor `condim`／摩擦（μ 0.6–1.0 掃）、輪 `frictionloss`，再回來；G0 的偏航門檻依回放結果打折。
- 開迴路回放閉迴路策略會發散，只看前 1.5 s；三個起點一致才算數。

### 5.1 G0 零動作（200 步，`diag/g0_v3.py`）

| 指令 | 門檻 |
|---|---|
| 直走 vx 0.5 | vx ≥ 0.45、任何腿抬 < 10 mm |
| 弧線 vx 0.5, wz 0.5 | 偏航 ≥ 20°/s（運動學 28.6）、內側前腿踏步（clr_step > 12 mm） |
| 原地轉 wz 1.3 | 偏航 ≥ 27°/s（不比 v3.0 差）、四腿踏步、膝 ≤ 75 N·m |
| 左平移 vy 0.08 | \|vy\| ≥ 0.04、站滿 10 s、膝 ≤ 75、roll < 3° |
| 斜走 vx 0.3 + vy 0.06 | 兩軸各到指令 50% 以上 |
| 全部 | 不終止、峰值力矩不觸 90 N·m |

掃描：duty {0.5, 0.6, 0.7} × 姿態偏移 {開, 關}。G0 全過才上 Colab。

### 5.2 Colab 後：對標原廠運控（使用者 2026-09-15 要求）

`local_infer_v3.py`（另開）對每個動作跑 10 s，比 `outputs/ref_gait_dataset.md` 對標表（`ref_extract.factory_benchmark`，動作段）：

| 原廠動作 | v m/s | 偏航 °/s | roll std／峰 ° | pitch std／峰 ° | 膝 τ 峰／p98／RMS N·m | 髖 τ | ABAD τ 峰／RMS | 抬腳 mm |
|---|---|---|---|---|---|---|---|---|
| 前進 0.17–0.44 | — | 0 | 0.9–1.1／1.9–2.9 | 0.1–0.2／0.3–0.6 | 28／19–21／15–17 | 17–23／14–17／10–11 | 14–16／7 | 0–14 |
| 弧線 | 0.22–0.40 | 7–13（穩態 18–52） | 0.6–0.8／1.2–2.2 | 0.2–0.4／0.7–1.4 | 32–36／24–29／15–17 | 20–26／15–18／9–12 | 31–51／9–12 | 14–18 |
| 原地轉 | 0 | ±68–77 | 0.7／1.9–3.3 | 0.8–1.0／2.9–3.8 | 37–38／33–34／16 | 22–24／16–17／7 | 60／22–23 | 17–19 |
| 平移 | 側向 ~0.08–0.15 | 0 | 0.5–0.7／1.7–3.3 | 0.9–1.1／2.8–3.3 | 28–34／25–26／14–15 | 16–17／12–15／5–8 | 48–51／18–21 | 16–22 |

判讀規則：速度／偏航率到原廠 80% 以上；roll std 不超過原廠 1.5 倍；膝／髖力矩峰值不超過原廠 1.3 倍（原廠是 kp120，我們 kp250 撓度不同，RMS 才是可比的量）；抬腳 15–40 mm。
注意原廠 ABAD 力矩峰 40–60 N·m 是它 60° 命令差造成的，我們的 ABAD 目標是 ≤ 原廠。

## 6. 風險與備案

- **平移前對同時離地**：PH_LAT 前兩腿只差 0.19，2.0 Hz／duty 0.5 下有 ~0.15 s 只剩後輪撐地。G0 摔 → duty 0.65，仍不行 → 前對相位拉到 0.35（順序不變）。
- **弧線外側後輪刮地**：skid-steer 物理，模擬 μ 1.0 可能比真地板卡。備案：弧線也讓內側後腿踏 0.5、DR 加 μ 0.6–1.0。步驟 0 的回放會先看到這件事。
- **原廠 2.5 Hz 做不到**仍在：2.0 Hz 原地轉若仍只有原廠 1/3，是伺服剛度問題，交給實機 kp 驗證，不在這輪。
- **輪殘差 ±2 rad/s 上機**：M11 律 τ = kd·(v_des − v) + 0.13·sign，kd 1.0，殘差直接加進 v_des；M9 的 `wheel_kd` 防呆已在。

## 7. 檔案

| 檔 | 動作 |
|---|---|
| `inference/ref_extract.py` | 加擺動事件法、四腿相位／抬高比／弧線／增益表 → `outputs/ref_gait_dataset.json` |
| `inference/diag/replay_factory.py` | 新：原廠回放 |
| `inference/rl_env_v3.py` | 就地改 v3.1：`kin_step_vec`、`activity`、`phase_offsets`、`wheel_cmd`、`step_pattern` 重寫；`wheel_pos` 預設 False |
| `inference/diag/g0_v3.py` | 加斜走指令、duty／姿態偏移掃描、門檻判定 |
| `tests/test_rl_env_v3.py` | 改寫 §4 |
| `notebooks/build_nb_v3.py` → `cpg_rl_v3_colab.ipynb` | 重建（G0 三個數字改成五個） |
| `docs/results/L_*.md`、v3 spec §7 | 加勘誤段落指向本文件 §0 |
| `docs/figs/gen_figP12_v3_arch.py` | 重畫：模式選擇器 → 活動度 |
| `task7/HANDOFF.md` | 更新起點 |

## 8. 執行紀錄

### 8.0 與正文的差異（實作時定案）

- 步向量不用 KX 0.3，改 `rot_step_frac`：錄檔量到原地轉每步側向位移只有運動學要求的 **0.19**（39 mm 對 196 mm），x／y 比例已是運動學比例（−0.54 對 −0.45），所以旋轉項整個乘一個比例、其餘靠輪子刮地。名目值由 G0 掃 0.2／0.5／1.0 決定。
- 抬高名目 0.040 不是 0.023：v3.0 G0 已證明 kp250 下命令抬高要到 40 mm（＋Z_SAG）踏步腿才離地 10 mm；原廠 22 mm 留 `factory_lift`。
- 站立 5% 不強制：三軸獨立抽樣（vx 65%、vy 30%、wz 50% 非零）自然有約 12% 全零。
- 資料集 v3.1 段（擺動事件法，門檻 12 mm）：原地左轉相位 fl 0／fr 0.23／bl 0.58／br 0.03、次要腿抬高比 0.71；左平移 fl 0／fr 0.18／bl 0.54／br 0.72、抬高比 0.75；弧線內側前腿 2.26 Hz；動作段增益 60／120／120、kd 1.0、輪 kp 0 kd 0.1、前饋 p98 0.29 N·m。

### 8.1 原廠回放（步驟 0，`inference/diag/replay_factory.py` → `outputs/replay_factory_trip21.md`）

設定：腿 kp 60／120／120、kd 1.0（`scene_flat_mjx.xml`）、ctrl = 錄檔 des；輪致動器關掉、直接施加錄檔實測 τ；輪關節 M11 參數；起點給錄檔的初速（四輪平均 × r、gyro_z、四輪速）；1.5 s 視窗 × 3 起點。

| 檔 | 偏航 模擬／原廠 °/s（三起點） | 比 | 摔 |
|---|---|---|---|
| turn_left | 45／72、44／85、54／75 | 0.52–0.72 | 否 |
| turn_right | 37／59、64／84、45／68 | 0.63–0.76 | 否 |
| arc_left／arc_right（穩態起點） | 12／23、8／19 | 0.42–0.52 | 否 |
| fwd_fast 輪速 模擬／原廠 rad/s | 11.6／10.7、5.1／4.5 | 1.08–1.13 | 否 |

**結論：原地轉比中位 ≈ 0.66 ≥ 0.5 → 輪地模型可信，G0 偏航門檻照 §5.1 不打折。** 兩個附帶觀察：
1. 同樣命令下模擬的腳**離地間隙**只有 0–26 mm（原廠 FK 抬高 15–28 mm），但模擬 FK 抬高 30–70 mm、機身下沉更多 → 模擬對抬腳偏悲觀（和 v3.0 要 40 mm 名目抬高一致），實機可能比模擬好抬。
2. 模擬偏航少 1/3 是在完全相同的力矩與命令下發生的，代表模擬的刮地阻力比真地板大一點；DR 已含 μ 範圍，不再另調。
3. 沒給初速時前進段輪速只有原廠四成、減速段反向：開迴路回放一定要給初速，`replay()` 的 `v0` 參數。

### 8.2 G0 掃描與定案（`inference/diag/g0_v3.py`，輸出 `outputs/g0_v31_*.txt`）

**修掉的 bug（都在掃描中發現）**：
1. 相位偏移是週期分數卻直接加在弧度 θ 上（0.25 → 0.04 週期，四腿幾乎同相一起騰空）。v3.0 就有；v3.0 §7 的踏步掃描結論全部作廢。
2. 抬高被活動度打兩次折（`u·step_off` 再乘 `s_k·lift`）：vy 0.03 時 51 mm 命令剩 19 mm，腳離不了地。
3. 抬高原本隨指令大小縮（`s_k·lift`）：改成腿權重 × 名目（原廠 22 mm 不隨速度縮）。這一改弧線與斜走才過。

**族別拆開**（旋轉族／平移族各自步頻、duty、相位組、抬高，依 a_lat:a_turn 混合）：

| 族 | 相位 | 步頻 | duty | 抬高 | 理由 |
|---|---|---|---|---|---|
| 旋轉（原地轉、弧線） | 對角小跑 FL+RR 0／FR+RL 0.5 | 1.8 Hz | 0.5 | 35 mm | 原廠四腿相位配 duty 0.8 才有三腳著地；我們 duty 0.5 套上去三腳同時離地。原廠相位 2.5 Hz／duty 0.7–0.8 會踏步但偏航 ≈ 0。小跑 2.0 Hz 偏航 34 但力矩 90；1.8 Hz／rot 0.4 → 28°/s、力矩 74 |
| 平移（平移、斜走） | 原廠 fl 0／fr .19／bl .51／br .72 | 2.1 Hz（＝原廠） | 0.7 | 25 mm | 1.3 Hz 爬行側傾累積 10 s 內倒；2.1 Hz 配側向命令增益 1.5 才 10 s 站住。抬高 35 mm 時 vy 0.08 力矩峰 92 > 終止線，25 mm → 82 |

**側向命令增益 1.5**：原廠平移 ABAD 命令擺幅 60°（腳側向命令 ±220 mm 對實際 40 mm）；位置伺服要靠命令放大推側向（同 Z_SAG）。增益 1.0 → vy 0.058、2.0 → 0.125（過衝）、1.5 → 0.090（指令 0.08）。
**沒採用**：四腳外張 30 mm（側傾 10→7° 但 vy −25%、斜走失敗）、爬行式身體側移（反而倒退）、`rot_step_frac` 0.5（力矩 96）。

**原廠命令週期重播**（平均一個週期、kp250、10 s）：平移雙向不倒、側傾 2.4–3.3°、側速 0.14–0.20；原地轉 2.1 s 倒（開迴路）。證明差在命令形狀不在增益。

**最終 G0（`outputs/g0_v31_final.txt`，200 步；平移另跑 500 步）**：

| 指令 | 判定 | 數字 |
|---|---|---|
| 直走 0.5 | PASS | vx 0.49、不抬腳、τ 17 |
| 弧線 0.5＋0.5 | PASS | 偏航 +25°/s、內側前腿抬 33 mm、τ 65 |
| 原地轉 1.3 | PASS | 偏航 +28±55°/s、四腿抬 37–74 mm、τ 74 |
| 平移 0.04／0.06／0.08 | 10 s 不倒 | vy 0.029／0.059／0.080；roll 平均 2.5–2.9°、峰 6–7°；τ 71／75／82 |
| 斜走 0.3＋0.06 | PASS | vx 0.33、vy 0.070 |
| 站立 | PASS | |

**門檻修訂（使用者 2026-09-15 接受）**：平移 |vy| ≥ 指令 60%、力矩 ≤ 85、側傾峰 ≤ 8°（原廠 2–3° 是閉迴路結果，交給 RL 的 roll 罰項與 sway 動作）。其餘照 §5.1。


## 9. v3.2：四腿踏步族改用原廠命令週期（2026-09-15 下午定案，使用者要求最終效果對標原廠）

**為什麼**：v3.1 運動學產生器的原地轉是對角小跑 28°/s、平移側傾峰 6–7°；RL 殘差改不了節奏與形狀，訓完不會像原廠。
原廠**命令週期**（平均一個週期的 des／輪 τ）在 MuJoCo 用我們的 kp 250 重播：平移雙向 10 s 不倒、roll std 0.6–0.7°、0.14–0.20 m/s、抬腳 18–39 mm（§8.2）；原地轉開迴路 2–4.5 s 倒（偏航 40–61°/s），要 RL 閉迴路。

**範圍**：只換四腿踏步族（平移、原地轉、斜走的踏步部分）的名目。輪行、弧線（v3.1 內側前腿踏步）、活動度、obs／動作維度、reward 骨架不動。

**資料**：`ref_extract.cmd_cycle(path)` → `outputs/ref_cmd_cycles.json`：lat_left 161123、lat_right 161223、turn_left 161316、turn_right 161342。
以 fl 擺動起點切週期、100 點正規化、平均：`des`(100×12，MJCF 序、控制器座標)、`q_mean`(12)、`q`(100×12)、`tau_w`(100×4，正=向前滾)、`freq_hz`、`n_cycles`。

**產生器**：
- 關節目標 = `STANCE_Q12` + amp_k·(des(φ) − q_mean)（保留原廠命令深度、中心對我們站姿）。G0 另比「直接用原廠絕對 des」。
- φ 以該檔 `freq_hz` 推進（平移 2.1、原地轉 2.5 Hz）× RL ω 尺度；方向由 sign(vy)／sign(wz) 選檔，切換 1 s 混合；平移與旋轉同時非零依 a_lat:a_turn 混合偏移。
- 幅度：平移 clip(|vy|/0.10, 0.3, 1.2)、原地轉 clip(|wz|/1.3, 0.3, 1.2) × REF `cyc_amp_lat`／`cyc_amp_turn`（原地轉先掃 0.5–1.0，kp250 膝峰 99 要壓到 ≤ 85）。
- 輪 = v3.1 `wheel_cmd` ＋ τ(φ)/kv 速度偏移 ＋ RL 殘差。
- 動作 12 維語意：ω 尺度；4 個 amp = 每腿週期偏移幅度（±50%）；第 6 維 = 髖／膝偏移共同幅度（±40%）；sway 2、輪殘差 4 不變。
- obs cpg 區 24 格：`vec.x／vec.y` 8 格改放每腿命令 ABAD 偏移與膝偏移（rad）。維度不變。
- 護欄：誤差護欄分關節 ABAD 0.6 rad、髖膝 0.45 rad（原廠 ABAD 命令差本就 30°，我們 ABAD 同為 kp 60）；力矩 58／終止 90 不動。

**驗收（G0 開迴路 10 s）**：平移 0.08：|vy| ≥ 0.06、roll std ≤ 1.5°、力矩 ≤ 85、不倒。原地轉 1.3：偏航 ≥ 40°/s 且站滿 10 s；幅度 0.5–1.0 都站不滿 5 s → 原地轉退回 v3.1 對角小跑（`REF["turn_gen"]="kin"`），平移用週期，記在 §9.1。其餘四 case 不退步。重出影片。

**不做**：M9 端護欄與 numpy 版（上機前另做）、弧線改週期。

### 9.1 v3.2 執行紀錄（2026-09-15 下午；`outputs/g0_v32_final.txt`）

- **原地轉退回 v3.1 對角小跑**：原廠週期在 kp250 下幅度 0.4–1.0、播放速度 0.7–1.0、輪偏移 1–2.5× 全部在 3.4–6.8 s 內翻倒、偏航 ≈ 0（開迴路沒有原廠策略的即時修正）。小跑 10 s：偏航逐 2 s 為 17／28／14／13／20°/s、側傾 4 s 後累積到 4–5°、力矩 ≤ 74。**這是 RL 要收的主要缺口，訓完不會像原廠 77°/s。**
- **平移用原廠週期，對中到我們站姿**（`cyc_recenter=True`；絕對命令模式不動）。幅度對側速在 kp250 下很陡（滑步區）：有效幅度 0.64→0.04、0.80→0.17、0.96→0.30 m/s，0.86 在 10 s 內翻倒；
  取 amp = 0.64 + 1.23·(|vy| − 0.04)，夾 [0.55, 0.78]。u4 只當開關（s4 ≥ 0.3 全開），幅度不乘活動度（否則低速指令被砍半不動）。
- **10 s 結果**：vy 0.04／0.06／0.08／0.10 → 0.036／0.048／0.062／0.081 m/s；roll std 0.2–0.4°、峰 ≤ 2.6°；力矩峰 36–43 N·m（原廠膝 28–34）；斜走 vx 0.33／vy 0.049。
- **⚠️ 腳離地只有 2–4 mm**（原廠實機 16–22 mm）：同樣命令模擬裡是滑步、實機會抬（§8.1 觀察 1：模擬機身下沉較多）。這是已知 sim2real 差距，上機前用 M6 錄我們自己的命令對照。
- 誤差護欄改分關節（ABAD 0.6／髖膝 0.45 rad），M9 端護欄上機前要同步（原廠 ABAD 命令差 30°）。
- 中途發現並修掉：`cyc_vy_ref` 與 `cyc_amp_lat` 重複縮放（掃描時標的 1.0 實為 0.8）；u4 乘活動度造成的雙重打折。

### 9.2 v3.2 第一輪訓練結果與 reward 修正（2026-09-15 下午，Colab 1 億步；`outputs/eval_cpg_rl_v3_params.md`）

**結果**：五類指令 0 摔（3 種子 × 10 s），但策略把踏步動作關掉：原地轉偏航 −8／+8°/s（零動作 +19）、平移 vy 0.015（零動作 0.058）、斜走側向 0.007。訓練曲線 len 卡在 800、每步獎勵 4000 萬步後持平。

**攤帳（`reward_breakdown`，每步平均，策略 vs 零動作）**：
- 原地轉：零動作小跑被罰 roll 0.78＋bias 0.33＋力矩護欄 0.26＋pitch 0.16＋角速度 0.2 ≈ 1.7，轉動的獎勵只多 0.3（高斯核在誤差 ≥ 0.5 rad/s 全 0、線性項權重 1）。站著總分 3.76 對名目 1.05。
- 平移：策略 5.60 對名目 5.67 持平 —— 側移賺的 vy 追蹤（+0.5）被前後漂移（vx）與偏航漂移（航向積分）吃掉，地形平坦，PPO 停在原地。

**修正（v3.2b，`W` 與 `step()`）**：
1. 相對進度項 `t_yawrel`（8.0）、`t_vyrel`（3.0）：進度 = 沿指令方向分量／指令大小，**開根號**（25% 的追蹤已值一半），**只在有該軸指令時作用**（否則站著不動在所有輪行回合白拿分）；偏航用 **0.5 s 低通**（`wz_ema`，α 0.04），否則小跑 ±60°/s 的來回擺也能拿分。
2. 四腿踏步時姿態類懲罰（roll／pitch／角速度／偏置）乘 (1 − 0.6·u_mode)。
3. `W_VY` 2→3、`W_HEAD` 1→0.5。
驗證（同一支攤帳工具）：名目 vs 站著不動 —— 平移 8.51 對 7.13（+1.38）、原地轉 6.08 對 5.26（+0.82）。名目現在明確贏過不動。

**右向不對稱的真相**：右平移／右轉零動作會倒，鏡像左向週期後仍倒 → 不是表；模型對稱（重心 y +1.5 mm、四輪正向力 93–97 N、運動學平移左右 0.084／0.098）。關掉每 2 s 的 0.6 m/s 隨機推力後左右完全對稱（+0.053／−0.050、側傾 ≤ 1.5°）；推力開時左右各約 1/3 種子會倒。**原廠週期的滑步對推力敏感，這是 RL 要學的**；§9.1「10 s 站住」是種子 0 的結果。右向平移改用鏡像左向（`_mirror_cycle`），左右一致。

### 9.3 v3.2b 第二輪訓練驗收（2026-09-15 傍晚；權重 `weights/cpg_rl_v3_params_1.pkl`；`outputs/eval_cpg_rl_v3_params_1.md`、影片 `outputs/eval_v3b_small.mp4`）

3 種子 × 10 s，**九個指令 0 摔**（零動作的右轉、右平移會倒，策略把它們救回來）。

| 指令 | 策略 | 零動作 | 原廠 |
|---|---|---|---|
| 直走 0.5 | vx 0.50、roll std 0.20、膝峰 22 | 0.49 | roll std 1.1、膝峰 28 |
| 弧線 0.5+0.5 | +22°/s、roll std 0.47、膝峰 41 | +24 | 穩態 18–52°/s、roll std 0.8、膝峰 36 |
| 原地左／右轉 1.3 | +22／−23°/s（±53 擺）、roll std 1.3／0.8、膝峰 55／51 | +19／−21（右倒） | ±77°/s、roll std 0.7、膝峰 38 |
| 左／右平移 0.08 | **+0.080／−0.076**、roll std 0.5／0.7、膝峰 37／43 | 0.058／−0.155（右倒） | 0.08–0.15、roll std 0.5–0.7、膝峰 28–34 |
| 左平移 0.04 | 0.029 | 0.033 | |
| 斜走 0.3+0.06 | vx 0.31、vy 0.045 | 0.31／0.046 | |

對標判讀（§5.2）：直走、弧線、平移、斜走在速度、側傾、膝力矩 RMS 都到原廠同量級（平移膝峰 37–43 對原廠 28–34，×1.3 內）；**原地轉只有原廠 30%**（小跑名目天花板，v3.3 原廠週期＋關節殘差才有機會）；**平移抬腳 1–7 mm**（模擬滑步，實機可能抬，M6 錄檔對照）。
訓練曲線 2700 萬步後 plateau（每步獎勵 8.2、len 750–780）；下一輪：ABAD 剛度 DR ×0.4–1.0 → ×0.7–1.0、學習率衰減、原地轉換 v3.3。

## 10. v3.3：原地轉用原廠命令週期＋RL 關節殘差（2026-09-15 晚，使用者核准「開始實做下一版本」）

在 v3.2b 上改四件事：
1. **原地轉名目 = 原廠命令週期**（`step_gen_turn="cycle"`），幅度每回合抽 U(0.3, 0.9)（`cyc_amp_rand`；小幅度站得住讓 RL 先學平衡，大幅度轉得快），eval／G0 固定 0.7。開迴路會倒：幅度 0.3／0.5／0.7 各在 7.9／5.2／3.7 s 翻（`outputs/g0_v33_final.txt`），這是預期，靠 RL。
2. **動作加 12 個關節目標殘差**：第 12–23 維，±0.12 rad、每步限速 0.01 rad；罰項 `t_qres` 0.5·Σqres²。動作 24 維、obs 88 維（last_a 24）。
3. ABAD 剛度 DR ×0.4–1.0 → ×0.7–1.0（平移滑步速度變異蓋掉 vy 訊號）。
4. 訓練：2 億步、`learning_rate_schedule="ADAPTIVE_KL"`（desired_kl 0.01、lr 1e-5–1e-3）、eval_env 幅度固定。

其他四指令 G0 不變（直走 0.50、弧線 +23、平移 0.061／0.036、斜走 0.33／0.049）。退路：`REF["step_gen_turn"]="kin"` 回 v3.2b 小跑。
停損：1 億步時 `進度 yaw` < 3.5 或 len < 600 → 停，回 v3.2b。
狗端影響：obs 88／動作 24、分關節護欄 ABAD 0.6（M9 v3 推論路徑要照這版）。

### 10.1 原廠平移速度修正（2026-09-15 晚，使用者現場觀察 0.3–0.4 m/s）

之前的 0.08–0.15 m/s 是「腳重放 40 mm × 2.1 Hz」推的，沒有定位資料。IMU 加速度積分（前後靜止段去漂移）：ref_left +0.52、ref_right −0.17／−0.51 m/s；同法對前進段對照輪里程低估 25–70%，所以絕對值不準，但量級是 **0.3–0.5 m/s**，與現場一致。
機身每週期滑 150 mm 以上、腳只重放 40 mm → 原廠平移本來就是輪子被推著側滑為主，模擬的「滑步」不是缺陷。
改動：`a_ref.vy` 0.08 → 0.15、`CMD_VY` (0.03, 0.10) → (0.04, 0.30)、幅度上限 0.78 → 1.0（全幅 0.96 ≈ 0.30 m/s）。
零動作 10 s：vy 0.30 → **0.300**、roll std 0.8°、抬腳 9–24 mm、膝峰 44（原廠 28–34）；vy 0.15（幅度 0.78）在 9.5 s 被推力推倒（中幅度最脆弱，RL 要補）；0.08 不變。
對標表的平移速度改為 0.3–0.5；`factory_benchmark.v_body` 對平移無效（輪里程）。

### 10.2 分關節等力矩縮放（2026-09-15 晚，使用者問「ABAD 增益相同這件事有沒有用」）

ABAD 增益兩邊同為 60，髖膝我們 250 對原廠 120。命令偏移 × kp_原廠/kp_我們（ABAD 1.0、髖膝 0.48）= 等力矩重播。G0 開迴路 10 s：

| | 原地轉 幅度 1.0 | 平移 0.08 | 平移 0.30 |
|---|---|---|---|
| 統一縮放 | 2.6 s 倒、τ 96（> 終止線 90） | 0.062 | 0.300 站住 |
| 等力矩換算 | 5.6 s 倒、τ 77 | **0.016** | **0.131、6.3 s 倒** |

平移不能用等力矩：髖膝偏移雖小，負責卸重讓側滑發生，砍半就不動；理論的前提（兩台實際關節角相同）不成立，撓度 36 對 72 mm。原地轉可用：力矩峰全幅度都在 90 下，RL 探索高幅度不會被力矩終止砍。
→ `cyc_joint_scale_turn=(1, 0.48, 0.48)`、`cyc_joint_scale_lat=(1, 1, 1)`。原地轉幅度 0.5 → τ 62、7.3 s；1.0 → τ 77、5.6 s；偏航仍 ≈ 0（開迴路）。
