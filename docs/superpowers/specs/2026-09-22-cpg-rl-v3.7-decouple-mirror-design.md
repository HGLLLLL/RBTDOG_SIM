# CPG-RL v3.7：平移抬高／跨距解耦 ＋ 原地轉鏡像推論 —— 設計與證據

- 日期：2026-09-22 下午
- 基底：v3.6f（spec `2026-09-22-cpg-rl-v3.6-step-apex-linear-penalties-design.md` §9 是它的結果）
- 使用者要求：「你確定這樣修改會進步嗎，請你做完整的實驗測試」→ 全部實驗在 `task7/outputs/v37_pretest.md`（E1–E12），本檔只放結論與設計。
- 使用者定案：實驗有明確方向後開始做 Colab notebook → `notebooks/cpg_rl_v3_7f_colab.ipynb`。

## 0. 實驗結論（會進步的證據，與不會進步的證據）

| 問題 | 實驗 | 結論 |
|---|---|---|
| 平移抬腳只到 10 mm | E1／E6：抬高與跨距在週期表裡耦合；髖膝固定、ABAD 隨指令 → 抬 20–25 mm 且速度可控 | ✅ 解耦產生器，零動作全速度、摩擦 0.4／1.4、ABAD kp ×0.7、推力、20 s、斜走、切換都不倒（E9） |
| 一行 (0.7,1.4,1.4) 夠不夠 | E1：低速 2 倍超速 | ❌ 不夠，會逼 policy 再壓抬腳 → 改成 ABAD 倍率隨指令（E6 曲線擬合） |
| 髖膝倍率多少 | E11／E12／E13：往後漂與航向偏轉隨膝倍率線性增加，但速度上限也跟著（1.6 → 0.16、1.75 → 0.18、1.87 → 0.21）；髖膝分開沒有更好 | **1.75**（抬 24–30、0.20 跑 0.18、膝峰 46–51、漂 −0.065） |
| 右轉弱＝質心偏左？ | E5：質心 +1.5／0／−1.5 mm，左右差都 22–28%；E7：零動作倒地時間左右相同 | ❌ 假說推翻；質心 DR 無害但沒用 |
| 右轉弱在產生器？ | 左右週期表（關節、輪力矩）逐位元鏡像 | ❌ |
| 右轉弱在 policy？ | E8：8 種子左 +83.6±0.9、右 −58.8±7.3 | ✅ policy 不對稱 |
| 鏡像推論能不能救 | E10：v3.6f 右轉 −62±10 → **−84±1**（＝左轉），v3.5f −64 → −80；平移鏡像互有好壞 | ✅ 只套原地轉，不用重訓；驗收表 `outputs/eval_cpg_rl_v3_6f_params_mirror.md` |
| 名目側速上限 | E6／E9／E13：2.1 Hz 踏步、抬 1.75× ≈ 0.18 m/s | 指令上限 0.30 → 0.22，避免再教它側滑 |

## 1. 改動（全部掛在 `W37`／REF 旗標，預設關；golden 與 G0 逐字不變）

1. **REF `cyc_lat_decouple=True`**（`W37["CYC_LAT_DECOUPLE"]` 打開）：平移族
   `delta = 表 × [k_abad(|vy|), 1.75, 1.75]`，`k_abad = clip(0.3 + 5.6·(|vy| − 0.12), 0.3, 1.0)`；
   step 裡 `delta × [amp_k, lift, lift]`（policy 的 amp 只縮 ABAD、lift 只縮髖膝）。原地轉／弧線／直走不受影響（E9 末兩列）。
2. **`CMD_VY=(0.04, 0.22)`**（名目上限 ≈ 0.18）。
3. **`mirror_obs`／`mirror_act`／`mirror_policy(turn_only=True)`**：部署與驗收（`local_infer_v3.py --mirror`）。obs 88 的映射見函式 docstring；θ 相位錨在 0 號腿，鏡像後用 θ₀−θ₁ 轉回。
4. `t_step`、hinge 懲罰、質心 DR、obs／動作維度同 v3.6f。

## 2. 上 Colab 前證據（2026-09-22）

- golden 過；G0 `outputs/g0_v37_regress.txt` 與 v3.5 逐字同；`tests/test_rl_env_v37.py` 6 條（鏡像 involution、零動作左右 obs 互為鏡像 ×3、解耦倍率、解耦 env 零動作抬腳與速度）。
- 攤帳 `outputs/reward_audit_v37.md`（v3.6f 權重零樣本套新產生器）：零動作 `t_step` 1.46、policy 1.34（policy 沒把名目的抬腳壓掉）、直走／原地轉 `t_step` 0；零樣本不倒。
- notebook import 格與 G0 格本機跑過。

## 3. 訓練與停損

`cpg_rl_v3_7f_colab.ipynb`，2 億步。停損：1 億步 `進度 yaw` < 3.5 或 len < 600；**`step` < 0.45**（名目 1.5 × 平移族約四成回合 ≈ 0.6；低於 0.45 代表 policy 又在壓抬腳）。

## 4. 驗收（`local_infer_v3.py --preset v37 --gains factory --mirror`，無推力）

同 v3.6 spec §5，另：平移主動側步/秒 ≥ 1.5、每週期頂點 ≥ 15 mm、vy 達成 85–115%（0.30 那列名目上限 71%，看 0.20／0.12）、
原地左右偏航差 < 5%（鏡像後）、平移航向 < 8°、vx 漂 < 0.02。

## 5. 風險

- 名目往後漂 −0.065／航向 −48°/10 s 要 policy 扛：hinge `t_drift` 在 0.05 給 0.32/步、`t_headlin` 線性，v3.6f 零樣本已壓到 +0.028。若訓完 vx 漂 > 0.02，下一步是在產生器加固定前向輪偏置（不是外環增益，E11 證明那會倒）。
- 膝峰 0.08–0.16 指令 46–52，貼 1.3 倍線；policy 通常再降 10%。
- 鏡像推論的 sim2real：實機 IMU 偏置／質心不對稱會讓鏡像後的 obs 分布略偏；上機時左右各跑一次比較。
