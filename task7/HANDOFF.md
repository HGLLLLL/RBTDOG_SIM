# task7 交接：D1 Max 現況與下一步

- 最後更新：2026-08-26
- 分支：`feat/d1-edu-cpg-rl`（最後 commit `a84c435`）
- **接手先讀這份，再讀 `README.md`。**

---

## 一句話現況

**D1 Max 的底層控制鏈已經全部打通並實機驗證：**
寫得進 `joint_cmd`、四顆輪馬達都驅動過、摩擦量到了、
模擬↔實機的座標換算式也驗證完畢。

### ★★ 2026-08-26：**16 顆馬達全部可由我們直接控制**（實機驗證）

吊帶懸吊，`M5_leg_pose.py` **S0–S6 六階全部走完**：
12 個腿關節符號全對、腿之間無耦合、四顆輪同時驅動。
`stand` / `crouch` / `home` / `stand+knee_back` **四種姿勢 × 12 關節 = 48 組全部通過**
（比值 0.68–1.43、**零反號**）。
並且**量到腿關節靜摩擦：HIP 掙脫門檻 = 1.505 N·m**（正反兩向相減分離出來，不依賴模型）。

**唯一還沒做的是落地承重** —— 吊掛只驗了「指揮得到角度」，**沒驗撐得住 41 kg**。

結果：`docs/實機腿關節與16顆全控結果_2026-08-26.md` ← **先讀這份**
設計：`docs/腿關節與姿勢控制_設計_2026-08-26.md`
流程：`docs/現場操作卡_腿關節與姿勢.md`

**S5（姿勢切換 crouch/home/knee-back）還沒做** —— 力矩最大的一階。

---

## 已完成（每一項都有實機證據）

| 項目 | 結果 | 文件 |
|---|---|---|
| 控制介面調查 | 官方 SDK 只到高層；底層是 `/dev/shm/joint_cmd` | `docs/D1Max_控制方式調查_2026-08-25.md` |
| 官方 MJCF | 取得並驗證，`model/zgws/` | `model/zgws/SOURCE.md` |
| 原廠運控參數 | 從 MATRiX 解出，**與實機檔逐字元相同**（只差平台開關） | `docs/D1Max_原廠運控參數_MATRiX解包_2026-08-25.md` |
| 第一趟偵察 | SHM 三塊、運控架構、ROS2 拓樸 | `docs/實機偵察結果_第一趟_2026-08-25.md` |
| 第二趟偵察 | SHM 結構解碼、`ros2_control` 80 個介面 | `docs/實機偵察結果_第二趟_2026-08-25.md` |
| **寫入驗證** | M1 **16/16**，零出力 | `docs/實機寫入結果_第三趟_2026-08-25.md` |
| **單顆馬達驅動** | 左前輪轉 22.08° | `docs/實機單顆馬達驅動結果_2026-08-25.md` |
| **四顆輪 + 摩擦** | 全部成功，摩擦 **0.13–0.18 N·m** | `docs/實機四輪驅動結果_2026-08-25.md` |
| **座標換算式** | `馬達 = sign × 控制器 + offset`，且**控制器座標系 == MJCF** | `docs/座標換算式驗證結果_2026-08-25.md` |
| MJCF 填入摩擦 | 四輪 `frictionloss="0.15"`，速度反而 +4~10% | `model/zgws/SOURCE.md` |
| **★ 腿關節驅動** | **12 個腿關節符號全對**，無耦合 | `docs/實機腿關節與16顆全控結果_2026-08-26.md` |
| **★ 16 顆全控** | **腿維持站姿 + 四輪同時轉** | 同上 |
| **★ 四種姿勢切換** | stand / crouch / home / knee_back，**48 組全對** | 同上 §3.5 |
| **腿關節摩擦** | **殘差 0.5–0.9 N·m、掙脫門檻 >0.86**（MJCF 仍是 0） | 同上 §2 |
| 輪摩擦交叉驗證 | 兩天兩種條件：0.153 vs 0.170 → MJCF 的 0.15 站得住 | 同上 §3 |

---

## 部署到實機需要知道的關鍵事實

```
控制鏈：mc_ctrl → /dev/shm/joint_cmd → joint_shm_controller → zsi_actuator_driver → 馬達

座標鏈：MJCF 關節角 ＝ 控制器角 → × side_sign + offset → joint_cmd（馬達座標系）
```

| 事實 | 值 |
|---|---|
| `joint_cmd` 結構 | base=752、stride=112，`name[64]` + pos/vel/effort/kp/kd（f64） |
| **心跳（必寫）** | `base+0` 的 u64，~1 kHz。**不寫會被判過期而清零** |
| 寫入順序 | **先 payload、最後心跳**（心跳＝「這幀備妥了」的旗標） |
| SHM 腿序 | `fl, fr, bl, br` ← **與動作設定檔的 `FR,FL,RR,RL` 不同，按名稱對應** |
| 原廠 RL 增益 | ABAD 60 / HIP 120 / KNEE 120 / Kd 1.0；輪 60 / 0.5 |
| **逾時保護** | `joint_cmd_timeout: 500 ms`。逾時後的處置**有兩種說法，以實測為準** ↓ |
| 四元數順序 | **xyzw** |
| 輪半徑 | 0.0961 m（碰撞網格實算） |
| 輪摩擦 | 0.15 N·m 純庫倫（`damping` 應維持 0） |
| 站姿（遙控器） | `stand`：hip ±0.6 / knee ∓1.2，機身離地約 0.535 m |
| 膝模式切換 | 只翻**後兩腿**的 hip/knee 號，前腿不動。預設是**後腿往前彎** |
| **吊起來時的自然下垂** | hip/knee ≈ 0，但 **abad = ∓0.28 rad（±16°）** —— 左右輪在機身正下方互相頂住 |
| **腿關節靜摩擦（實測）** | **HIP 1.505**（高）、KNEE 1.5–1.7（中）、ABAD **≥1.25**（下界）。**MJCF 已填 1.5** |
| ⚠️ **摩擦死區** | 角度 = f/k：ABAD **≥±0.08**、HIP **±0.106**、KNEE **±0.31** rad（≥±4.5° / ±6.1° / **±18°**）。
  判斷「腿有沒有自由懸掛」要拿這個當尺 —— **關節可以停在真平衡點 ±6°（膝 ±15°）內任何位置** |

### ⚠️ 逾時處置：兩種說法互相矛盾，**尚未釘死**

| 說法 | 來源 | 安全後果 |
|---|---|---|
| **把指令區清成 0** | **實測**：M1 首次寫入失敗讀回全 0（`docs/實機寫入結果_第三趟` §2） | 腿**失力**，自由落體 |
| 套 `estop_kd=35` | 讀設定檔推的，**沒有觀測佐證** | 阻尼停止，腿慢慢垂 |

**目前一律當成「會清零」來設計**（保守的那一邊）。實際後果是：
任何「要維持一段時間」的指令都必須**持續餵心跳**，寫一次就不管的話 0.5 秒後會失效。
`M5_leg_pose.py` 的 `Keepalive` 就是為此而生（2026-08-26 抓到的真 bug）。

**建議用一次低風險實驗釘死它**：寫入後停止心跳，觀察 `joint_cmd` 的 `kd` 欄
變成 0 還是 35。這是零風險的唯讀觀察，值得早點做。

---

## 工具（都在 `realbot/`，純標準函式庫，自動存 log 到狗上 `~/m_logs/`）

| 腳本 | 用途 | 風險 |
|---|---|---|
| `recon_d1max.sh` / `recon2_d1max.sh` | 唯讀偵察 | 零 |
| `shm_io.py` | SHM 讀寫底層（**其他都 import 它**） | — |
| `shm_decode.py` | 離線解碼 `.bin` 快照（需 numpy，跑在本機） | 零 |
| `M0_probe.py` | 寫入前前提檢查 | 零 |
| `M1_zero_write.py` | 零出力寫入驗證 | 極低 |
| `M2_wheel_spin.py` | 單顆輪驅動 | 低（須墊高離地） |
| `M3_wheel_tour.py` | 多顆輪巡迴（錄影用） | 低（同上） |
| `M4_pose_capture.py` | 姿勢擷取與座標驗證 | 零 |
| **`coord.py`** | **座標換算／限位／姿勢的單一事實來源**（其他都從這裡取） | — |
| **`M5_leg_pose.py`** | **腿關節微動 → 單腿 → 全身姿勢 → 16 顆全控** | **中～高，必須吊掛** |
| `push_to_dog.sh` | 傳檔 + sha256 雙向比對 + 清 `__pycache__` | 零 |
| **`estop_max.sh`** | **急停：殺我們的程式 → 確認死透 → 才解凍 `mc_ctrl`**。第二個終端機備著 | 零 |
| `M_faultwatch.py` | 閃紅燈故障取證 | 零 |

⚠️ **每次上機都重傳全部會用到的腳本**，版本不一致的症狀會偽裝成硬體問題。

現場操作卡在 `docs/現場*.md`，設計成**離線可用**（連狗的 WiFi 就沒網路）。

---

## 下一步（風險由低到高）

1. **[零風險]** MJCF 的 knee 限位 ±2.791 → **±2.801**
   實測支持：癱平時膝頂在 ±2.80 的機械停點，與 URDF 一致，MJCF 略緊 0.01 rad
2. **[零風險]** 掃 `--kd` 找**靜摩擦掙脫門檻**（目前 0.15 是動摩擦，MuJoCo 靜動不分家）
3. **[零風險]** 訂閱 `/joint_shm_controller/joint_cmd_echo`，佐證 controller 確實消費指令
4. ✅ **已完成**：單關節微動 → 全 12 腿關節 → 16 顆全控（2026-08-26）
5. **[零風險]** **ABAD 的掙脫門檻要在 stand 姿勢重測**（那裡輪子相距 150 mm，確定分開）。
   目前只有下界 1.25。⚠️ 它靜止在 −0.31，已深入預演算出的輪對輪接觸區（∓0.277），
   所以現有數字可能量到的是「關節摩擦 + 輪子互頂」
6. ✅ **已完成**：MJCF 腿關節填入 1.5、CPG 重掃（`x_off` 配平點 −30 → **−40 mm**）
7. ~~釐清 IMU roll 的矛盾~~ —— **已釐清：弱的一方是我的擬合，不是 IMU**（結果文件 §6）
8. **[高]** 落地測試 → 跑自己的步態。⚠️ 吊掛只驗了「指揮得到角度」，**沒驗承重**

✅ 已完成：S5 四種姿勢各 12/12（stand / crouch / home / knee_back）

⚠️ **原廠對「第三方寫入 `/dev/shm/joint_cmd`」的態度仍未確認。**
2026-08-26 使用者決定**自負風險進行**。這一條沒有被解決，只是被接受了。

---

## CPG 步態那條線（模擬）—— ★ 已走起來，但**尚未 commit**

結果文件：`docs/D1Max_CPG步態_模擬結果_2026-08-25.md`（未進版控）。
影片 `outputs/cpg_{walk,walk_fast,trot}_max.mp4`（.mp4 被 gitignore）。

**建議步態 `walk`**：duty 0.80、ω1.4、d_step 0.10、**x_off −30 mm**、g_c 0.08
→ **0.21 m/s**（路徑 0.27）、離地 ~102 mm、彈跳 27 mm、支撐腳 3.15、
超限/力矩飽和/IK縮限 **全 0%**、20 秒不跌倒。`pytest task7/tests/` **36 項全過**。

### ⚠️ 尚未 commit 的檔案（東西都在工作區，沒有遺失）

```
M  task7/inference/max_model.py      常數更新（NOMINAL_HEIGHT、STATIC_SAG）
M  task7/inference/cpg_max.py        yaw_deg、circ_std 修正、z_sag 補償
M  task7/inference/cpg_walk_max.py   調好的 GAITS、--sweep、路徑速度、輪摩擦旗標
?? task7/tests/test_cpg_max.py       CPG 測試（另兩支測試檔已在版控）
?? task7/docs/D1Max_CPG步態_模擬結果_2026-08-25.md
```

**`HEAD` 上的 `inference/` 還是調參前的版本**（`GAITS` 仍是 trot ω2.0 / walk duty 0.75），
所以直接 checkout HEAD 會退回「預設值 100% 跌倒」的狀態。**commit 前不要 discard 工作區。**

### 上面那五個既有問題 —— **全部已修**

由 frictionloss 那個 session 在 commit `ad1f068` 上發現，現已全部處理：

| 問題 | 狀態 |
|---|---|
| `trot`/`walk` 預設值 100% 跌倒 | ✅ 已重新掃參；三組預設都不跌倒 |
| `NOMINAL_HEIGHT` 過時 10 mm | ✅ 已更新為 0.4493，並拆出 `STATIC_SAG` |
| `speed` 走弧線嚴重低估 | ✅ 新增 `speed_path`（路徑長/時間），兩個都印 |
| `--sweep` 旗標不存在 | ✅ 已實作（`--sweep x_off=-0.04,-0.03,...`） |
| `--wheel-friction` 只能加不能減 | ✅ 守衛改成 `is not None`，`--wheel-friction 0` 可取無摩擦對照組 |

### ★ 併入實測輪摩擦 0.15 之後的重掃結論

四項判準**全部重掃驗證過**（不是只確認能跑）：

| 項目 | 結果 |
|---|---|
| `duty` ≤0.70 必跌、0.80 彈跳最小 | 不變 |
| `mu_y`=1.5 是唯一不跌的 | 不變 |
| 地面摩擦 0.3 跌、≥0.5 能走 | 不變 |
| **`x_off` 平均俯仰過零 −42 → −30 mm** | ★ **變了，預設已改** |

⚠️ **教訓：輪摩擦不是只讓速度整體平移，它會移動配平點。換輪摩擦一定要重掃 `x_off`。**

### 還沒做的（下次接手）

1. **偏航閉迴路** —— 唯一擋住「能直線走遠」的東西。開迴路慢漂 −0.5~−0.8°/s，
   60 秒累積 −50°、側偏 −5.5 m。建議拿 MJCF 的 `imu_quat` 做航向 P 控制 → 左右腿 `d_step` 差動。
2. **`trot` 不要用**：對皮米級擾動混沌（速度 0.11–0.46 m/s，差 4 倍），單次數字不可引用。
3. **腿關節摩擦仍是 0**（沒量過；輪的已經是實測值了）。
4. 只驗過平地；官方發布包有 20 幾個 `scene_terrain_*.xml` 可做零樣本地形測試。
5. 決定要不要 commit 上面那批檔案。

---

## ⚠️ 這個 task 最重要的一條方法論

**「診斷輸出騙人」在這個 task 出現了七次。** 每一次都是量測／診斷工具本身有錯，
而不是被量的東西有問題：

1. `set -u` + `source` 別人的環境腳本 → 失敗偽裝成正常結束（還被 `2>/dev/null` 吞掉）
2. 自動判讀的 grep 打到腳本自己印的區段標題 → 誤報命中
3. `set -o pipefail` + `cmd | grep -q` → SIGPIPE 造成誤報失敗
4. `tick_end` 在 `restore()` 之後才讀 → 心跳速率算錯 15%
5. 輪角沒解纏（讀數包裹在 ±π）→ 轉超過半圈時連方向都算反
6. 保護用了雜訊訊號（`velocity` 欄位）→ 假尖峰造成誤中止
7. 拿**馬達座標系**的角度去比 **URDF/MJCF 的限位** → 憑空生出「超限」

### 2026-08-26 又加了四次（寫 M5 的過程中）

8. **又一次拿馬達座標系去比 MJCF** —— 這回是 `effort`。第 7 次的同款錯誤，
   換個欄位就再犯一次。力矩的換算是 `τ_c = s·τ_m`（功率守恆），不換算會有一半關節反號
9. `soft_stop()` 只寫**一次**心跳 → 阻尼 0.5 秒後被 controller 清掉。
   「已切成純阻尼」這句話印在畫面上，但它只維持了 0.5 秒 —— **輸出本身沒錯，錯的是它描述的狀態早就沒了**
10. 對照表查詢**找到第一個字典就回傳** → τ₀ 靜靜退化成 0 → 微動判讀會把
    abad 的正常反向走誤報成「符號錯」。**沉默的降級比大聲的錯誤難抓**
11. `self._stop` 蓋掉 `threading.Thread._stop()` → `join()` 炸 TypeError，
    而且炸在收尾階段：**mc_ctrl 已凍結、SIGCONT 還沒送出去**

### 2026-08-26 實機又加了兩次

12. **力矩保護單筆就跳** → `bl3_knee` 一個 9.96 N·m 的尖峰造成中止，
    但同一刻我們的 PD 律最多只能產生 4.87，而誤差與速度都正常。
    **與第 6 號坑同源**（M3 被 `velocity` 假尖峰誤中止，修法是「連續 N 筆」）——
    我把那條教訓套在速度上，卻沒套在力矩上。
    ★ 判別法：**`kp·|err| + kd·|v|` 是我們所能產生的上限**。實測遠大於它 → 那力矩不是我們下的
13. **S6 的目的是輪子，而 M5 對輪子一個數字都沒記** → 「左前輪沒轉」只剩目視印象，
    事後完全無法判別是「真的卡住」還是「轉得慢沒看出來」。
    **工具沒量到它要測的東西，那一階就沒有證據。**

15. **我為了修第 13 條而加的「硬上限單筆即跳」，自己成了雜訊的入口。**
    下午 `fr3_knee` 吐一筆 **−33.2 N·m**（位置完全沒變、速度 0、我們最多只能給 0.55）
    就把它打中了。→ 硬上限也改成連續 2 筆。
    ★ **為了「不延遲」而開的快速通道，也要有最低限度的一致性檢查。**

16. **判讀門檻本身就是一個要對照的量。** 量靜摩擦時「滑多久才算開始滑」
    取 25 / 100 / 300 ms，HIP 的答案**完全不變**（可信），
    ABAD 卻是 −0.229 / −0.558 / 沒有 —— 那是**瞬間微滑**被當成掙脫，
    只印一個門檻會得出「ABAD 摩擦比 HIP 小一個量級」這種完全相反的結論。
    M5 現在三個門檻並排印。

**共同的解法：多印一個可以互相對照的量，比多印一個結論有用。**

第 9~10 兩次還多帶出一條：**「沉默的降級」要主動出聲。**
取不到對照值時不要只是留白，要印「取不到，所以本次少了哪個判準」——
現場沒有人會去追一個空欄位。

正面例子（第 7 次之後）：驗證座標系時，「四輪共面」看起來足以定案，
但同時算了「MJCF 限位」與「對文件姿勢的 RMS」——才發現**共面性對 V1/V3 沒有鑑別力**。
只看一個指標就會選錯。

其他反覆用到的：
- `pgrep -f` / `pkill -f` 的 pattern 別包含自己的命令列（task6 中過兩次，殺掉自己的 SSH）
- 比較兩個量之前，先確認**同座標系、同單位**
- 單次模擬結果可能是雜訊（trot 對微擾是混沌的，速度差 4 倍）——只採信穩健趨勢
