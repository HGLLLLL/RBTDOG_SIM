# D1 Max（中狗）CPG-RL —— 設計與交接

- 日期：2026-08-27
- Notebook：**`task7/notebooks/cpg_rl_max_colab.ipynb`**（丟 Colab GPU 直接跑）
- 基準步態：`task7/docs/基準步態凍結_D1Max_walk_2026-08-27.md`
- 訓練模型落差：`task7/docs/MJX模型對照_2026-08-27.md`
- 移植基底：`task6/notebooks/cpg_rl_d1w_colab.ipynb`（D1 EDU，reward 已迭代到 v4）

---

## 0. 為什麼做這件事

開迴路 `walk` **走得穩但走不直**：180 秒 × 12 擾動 0 跌倒，
但偏航 −0.5 ~ −0.9 °/s，180 秒累積 −51°、側偏 −10.7 m。

而且**調參繞不過去**（三個獨立證據）：

- `x_off` 在 20 mm 範圍內掃，偏航率 −0.858 ~ −0.814 °/s，**完全不變號**
- `d_step` 單調（0.100→−0.06、0.130→−0.85 °/s），**沒有過零點**
- `ω` 在 1.8 變號（+0.68）但 2.0 又變回來（−0.87）—— **那是分歧不是趨勢**

航向只能靠回授。而 RL 拿得到 gyro，這正是它該做的事。
**所以這一輪要打敗的主要是偏航，速度是附帶的。**

---

## 1. 觀測層：68 維

每一欄都必須是**實機凍結 `mc_ctrl` 後、直接讀 `/dev/shm` 拿得到的量**。
唯一定義在 `task7/inference/obs_max.py`，Colab 端與本機推論端共用。

| 欄位 | 維度 | 實機來源 | 驗證程度 |
|---|---|---|---|
| `gravity` | 3 | `/dev/shm/imu_central` quat（**xyzw**）轉機身系 | ⚠️ 見 §1.2 |
| `gyro` | 3 | `/dev/shm/imu_central` gyro | ⚠️ 見 §1.2 |
| `joint_pos` | 12 | `/dev/shm/joint_state` pos − `HOME12` | ★ 實機驗過是 **1 kHz 活的串流** |
| `joint_vel` | 12 | `/dev/shm/joint_state` vel | ★ 同上 |
| `cmd` | 2 | `(vx, wz)`，自產 | — |
| `last_action` | 12 | 自存 | — |
| `cpg` | 24 | `rx, rx_d, ry, ry_d, sin θ, cos θ`，自算 | — |

⚠️ `joint_state` 的 16 軸中**輪關節夾在腿關節之間**（`fl1,fl2,fl3,fl4,fr1,…`），
位址不連續，一律用 `LEG_QPOS_IDX` / `LEG_QVEL_IDX`。
⚠️ SHM 腿序是 `FL,FR,BL,BR`，動作設定檔是 `FR,FL,RR,RL` —— **一律按名稱對應**。

### 1.1 刻意排除的三項

| 排除 | 理由 |
|---|---|
| **機身線速度** | 底層沒有這個量。高層 SDK 有，但凍結 `mc_ctrl` 後高層就死了。reward 仍用模擬真值 —— reward 只在訓練時計算，訓練產物只有 policy 權重，推論時一行都不會執行到 |
| **足端觸地** | 沒有感測器。task6 在同型輪足（D1 EDU）實測四個候選訊號**全部不可用**：0.9 kg 的輪子讓**擺動相**的膝力矩與位置誤差**大於站立相**，訊號方向是反的（膝力矩站立相 p05=1.55 N·m、擺動相 p95=8.76）。D1 Max 的輪更重（**1.51 kg**），只會更嚴重 |
| **輪子角速度** | 實機讀得到，但實機**從未走過路**，輪速在真實地面（地毯 vs 磁磚）的行為零資料可對照，而 policy 又控制不了它（輪子固定阻尼）。放進去等於押一個沒驗證過的量 |

有測試釘住「機身線速度不在 obs 裡」（`test_obs_excludes_base_linear_velocity`），
擋的是「訓練時順手加一欄讓 reward 好學、上機才發現餵不出來」。

### 1.2 ⚠️ 上實機前的硬性前提：IMU 尚未驗證

`/dev/shm/imu_central` 目前**只有 2026-08-25 一次離線快照解碼過，
沒驗過是不是活的串流**（`joint_state` 的 1 kHz 活性是驗過的，IMU 沒有）。
而且 `xyzw` 順序是「取樣當下機身剛好水平」推出來的，**不是刻意做的平放實驗**。

四元數順序若錯 → 重力向量整個翻掉 → **policy 直接廢掉**，
而且症狀不是報錯，是「狗一走就往某個方向倒」，
現場會被誤判成「RL 沒訓練好」而去改訓練。

→ **上機前先跑 `task7/docs/現場操作卡_IMU平放複核.md`**（唯讀、零風險、15 分鐘）。
本次已補上 `shm_io.read_imu()` 與欄位位移的回歸測試（`test_shm_imu.py`）。

---

## 2. 動作空間：12 維

每腿 `(mux, muy, omega)` → `tanh` → 線性映射。輪子**不進動作空間**，維持純阻尼
（給位置增益實測造成 **+39° 偏航失控**）。

| | 範圍 | 依據 |
|---|---|---|
| `mu_x`, `mu_y` | 1.0 – 2.0 | 沿用 task4 / task6 |
| `omega` | 0.0 – 2.0 | ω=1.8 → 0.316 m/s 但彈跳 +48%、支撐腳 −0.3；ω=2.0 速度反而掉回 0.281 |

### ★ 與 task6 最關鍵的差異：`duty` 必須進 env

task6 的 env **沒有 `duty_remap`**（等效 duty=0.5），
而 D1 Max 實測 **`duty ≤ 0.70` 是 12/12 全跌**。
照抄 task6 的 env 會得到一台永遠站不住的狗，而且不會有任何錯誤訊息。

env 內完整沿用 task7 的軌跡鏈：

```
cpg_step → duty_remap(0.80) → foot_targets(x_off=−40mm, z_sag=32.5mm 只加擺動相)
         → 每腿各自的 home_foot + knee_sign → 解析式 IK → 12 個關節目標角
```

- **解析式 IK**，不是 task6 的線性化 Jacobian：這台抬腿 100 mm、連桿 0.26+0.28 m，
  線性化誤差會直接汙染「抬腿量」—— 而那正是 reward 的 `r_clr` 要獎勵的東西。
- notebook 內建 **JAX vs numpy 的逐點對照**（容差 1e-4 rad ＝ float32 捨入；
  開 `jax_enable_x64` 實測差 **2.2e-16**）。沒有這個檢查，移植錯一個號會安靜地
  變成「訓練與推論是兩台機器人」，而訓練曲線會很漂亮 —— 因為 policy 只是學會了另一台。

---

## 3. 訓練模型

★ **官方 MJCF 不能直接餵 MJX**：碰撞網格 `BASE_LINK` **98,569 頂點**、四輪各 31,730，
MJX 的 plane–convex 是逐頂點算的，2048 個平行環境會 OOM。

訓練用 `task7/model/zgws/zgws_mjx.xml`（產生器 `make_mjx_model.py`），四項改動：
碰撞幾何換成原始形狀、致動器換成 affine 位置伺服、拿掉全部 STL 相依、關掉自碰撞。

**落差已量（G1，`docs/MJX模型對照_2026-08-27.md`）**：

| | A 基準（原網格） | C3 訓練模型 | 差 |
|---|---|---|---|
| 跌倒 | 0/12 | 0/12 | — |
| 行進速度 | 0.148 | 0.147 | **−0.7%** |
| 彈跳 | 17.2 mm | 16.9 | −1.7% |
| 支撐腳 | 3.20 | 3.20 | **0%** |
| 離地 | 93.6 mm | 93.6 | **0%** |

★ 這是**量出來的不是設計出來的**：輪子用全寬圓柱差 −34%、用球差 −23%，
**冠頂半徑的窄圓盤（半寬 5 mm）**才差 −0.7%。

**另一個好處**：訓練模型零 STL 相依 → Colab **clone 完即可用**，
不必為了 54 MB 的網格去抓 2.1 GB 的官方發布包。

---

## 4. Reward、終止、domain randomization

### 4.1 Reward

移植 task6 reward v4 的全部懲罰項（權重都經實測校準）＋ 偏航率追蹤。

| 正項 | 權重 | |
|---|---|---|
| 前進速度追蹤 `exp(−(vx−cmd_vx)²/0.02)` | 1.5 | |
| **偏航率追蹤 `exp(−(wz−cmd_wz)²/0.05)`** | **1.0** | ★ 本輪主目標 |
| 側向速度 `exp(−vy²/0.02)` | 0.5 | |
| 機身高 `exp(−400(h−0.4825)²)` | 0.5 | 目標用**走路時**高度，不是靜態站立的 0.4493 |
| 抬腳 `r_clr` | 1.5 | 封頂在 `G_C`：抬到位就給滿分，再高不加分 |

| 罰項 | 權重 | 針對 |
|---|---|---|
| `grav[0]²` | 20.0 | 機身俯仰姿態 |
| `qvel[4]²` | 0.05 | 俯仰角速度 |
| `qvel[2]²` | 0.5 | 機身垂直彈跳（v3「打地鼠」打出來的洞） |
| `var(各腿 ω)` | 0.5 | ★ 封住「把各腿頻率拆開」這個規避管道 |
| `Σ(Δaction)²` | 0.01 | 動作抖動 |
| `Σtau²` | **3e-5** | 見下 |

★ **力矩懲罰係數是量出來反推的，不是沿用**：基準步態在這個動作空間裡是一個
**固定動作**（`mux/muy/ω` 用 `atanh` 反推），直接跑它量到 `Σtau²` 中位數 **1648**。
取 3e-5 讓這一項在「已知可行的步態」上值 **0.049 分**（正項總和上限 5.0，約 1%）。
task6 的 5e-6 只值 0.008 分，**等於這一項不存在** —— 那台的力矩上限只有 33 N·m。

⚠️⚠️ **其餘權重仍是照 D1 EDU（20.6 kg）訂的。這台 41 kg，第一輪訓練後必須依
`progress` 印出的實測值重新校準**，不要當成定案。task6 就是這樣從 v1 迭代到 v4 的。

**reward 可以用上帝視角，obs 不行**：`r_clr` 用 `geom_xpos` 真值，實機沒有這個訊號，
但 reward 只在訓練時計算、訓練完就丟棄。

### 4.2 終止

- 翻倒：`grav[2] > −0.4`（＝ `max_model.FALL_GRAV_Z`，**直接 import 不手抄**）
- 低姿：機身高 < **0.29 m**（由 task4 Go2 的 0.18/0.30 等比例換到走路高度 0.4825）

低姿護欄是必要的：「塌腰趴著慢慢挪」是很好爬的 local optimum。

★ task6 的註解寫「Colab 無法 import 本地模組，只能手抄，兩邊分岔不會有錯誤訊息」——
本檔 clone 了 repo 又把 `task7/inference` 加進 `sys.path`，**這個風險整個消失**。

### 4.3 指令範圍

| | 範圍 |
|---|---|
| `cmd_vx` | 0.05 – 0.40 m/s（基準開迴路 0.148；開迴路 ω=1.8 曾達 0.316） |
| `cmd_wz` | **60% 機率抽 0**（＝「給我走直」），其餘 40% 在 −0.4 – +0.4 rad/s 均勻抽 |

### 4.4 Domain randomization

| 項目 | 範圍 | 依據 |
|---|---|---|
| 地面摩擦 | 0.4 – 1.4 | 0.3 會跌、≥0.5 能走 |
| 機身 payload | 0 – 5 kg | 官方額定 5 kg，**同時吸收 MJCF 38.821 vs 規格書 41 kg 的 2.2 kg 缺口** |
| 連桿質量 | ±10% | |
| PD 增益 | kp ×0.8–1.2、kv 0.5–2.0 | ⚠️ 三個關節名目 Kp **不同**（60/120/120），是整組乘係數不是設同值 |
| 腿關節 `frictionloss` | ×0.5 – ×1.5 | ★ ABAD 的 1.85 掃描已證明是**下界不是量測值** |
| 輪 `frictionloss` | 0.10 – 0.25 | 兩天兩種條件量到 0.153 / 0.170 |
| IMU | gyro 雜訊 σ=0.10、每 episode 偏差 ±0.05、重力雜訊 σ=0.02 | |
| **動作延遲** | 每 episode 抽 0 或 1 個控制步 | ★ task6 沒有。實機鏈路「寫 shm → 1 kHz daemon 讀 → 馬達」一定有延遲，量沒量過所以用範圍蓋住 |
| 推撞 | 每 2 s 注入 0–0.6 m/s | |

---

## 5. 訓練規模

PPO（brax `0.14.2` / MJX），**版本鎖死不放寬** ——
brax 載到 activation 不匹配的權重**不會報錯**，只會讓 policy 靜默錯亂，
而 activation 由 brax 的**版本**決定。

`num_envs=2048`、`episode_length=1000`（20 s）、`batch_size=256`、
`num_minibatches=32`、`unroll_length=20`、`lr=3e-4`、`entropy_cost=1e-2`、
`discounting=0.97`、`normalize_observations=True`、
policy `(256,256,128)` / value `(256,256,256)`，先設 **60M steps**。

⚠️ **成本預告**：`SIM_DT=0.002`，每控制步 **10 個物理步**（task6 是 5）→
每步計算量約 **2 倍**。notebook 的 `progress` 在第一個 eval 就印出實測步率與
「60M 約需幾分鐘」，**當場決定要不要砍到 40M**，不要硬等。

已在本機用極小規模（8 env / 4096 步）實跑 PPO 驗過契約：
8 個 metrics 鍵全部出現在 `eval/episode_*`，`domain_randomize` 的 `in_axes` 結構也過。

---

## 6. 驗收關卡

| 關卡 | 內容 | 狀態 |
|---|---|---|
| **G1** | 訓練模型 vs 原始模型（walk，20 s × 12 擾動） | ✅ **通過**，差異全部在 ±2% 內 |
| **G2** | 測試（觀測層維度/順序、基準常數、MJX 模型不變量、IMU 位移） | ✅ **590 項全過** |
| **G3** | 訓練後在**原始網格模型**上 20 / 60 / 180 s × 12 擾動 | ⬜ 待訓練完成 |
| **G4** | `cmd_wz=0` 時的偏航率**顯著優於開迴路的 −0.5 ~ −0.9 °/s** | ⬜ 待訓練完成 |
| **G5** | 速度一律用 `speed_travel`（**不可用 `speed_path`**，高估 68%） | ⬜ |
| **G6** | 錄影 `task7/outputs/cpg_rl_max.mp4` | ⬜ |

### 已通過的一條額外驗證

`local_infer_max.py --dummy` 跑出來的**八個指標與開迴路 `rollout` 逐位相同**
（`speed_travel / bounce / pitch_mean / support / yaw / net_roll / min_lift / height`）。

這條之所以做得到，是因為**基準步態在這個動作空間裡就是一個固定動作** ——
所以整條推論鏈（obs → `act_to_cmd` → CPG → 解析 IK → PD）有**標準答案**可對，
而不是「跑起來沒炸就算過」。任何一個係數、順序、映射範圍接錯都會在這裡露餡。

---

## 7. 使用流程

```bash
# 1) 把分支推上 GitHub（Colab 用 --depth 1 clone，本機沒 push 的東西它看不到）
git push origin feat/d1-edu-cpg-rl

# 2) Colab：開 task7/notebooks/cpg_rl_max_colab.ipynb，執行階段選 GPU，全部執行
#    第 8 格（基準校準）會印出「env 有沒有重現開迴路基準」
#    第 9 格第一個 eval 會印出「60M 約需幾分鐘」

# 3) 下載 cpg_rl_max_params.pkl → 放到 task7/weights/

# 4) 本機驗收（★ 跑原始網格模型）
conda run --no-capture-output -n rbtdog \
  python task7/inference/local_infer_max.py \
  --params task7/weights/cpg_rl_max_params.pkl --secs 20 --video

# 沒有權重時先測管線（＝開迴路基準的固定動作，有標準答案）
conda run --no-capture-output -n rbtdog \
  python task7/inference/local_infer_max.py --dummy --secs 20
```

---

## 8. 未解決 —— **不要當作已完成**

1. **IMU 尚未複核**（§1.2）。不做不影響訓練，**做完才可以上實機**。
2. **gyro 軸向沒驗過**。IMU 操作卡刻意不含這一項（平放狀態量不準），
   建議第一次實機走路前用「繞 z 軸原地轉 90°、看 `gyro.z` 積分」補驗。
   ⚠️ 軸向錯誤**蓋不住**：`GYRO_NOISE`/`GYRO_BIAS` 隨機化的是雜訊不是軸向。
3. **只有平地**。斜坡／台階／不平地面完全沒測，也沒進訓練。
4. **輪子沒納入動作空間**。這台是輪足，輪子能出力，速度上限應該還有很大空間 ——
   但輪子位置增益實測造成 +39° 偏航失控，且沒有成功案例可對照，本輪不做。
5. **全向移動（`vy`）沒做**。ABAD 行程只有 −0.697 ~ +0.523 rad 且左右鏡像。
6. **實機一步都沒走過**，連承重站立（M7）都還沒做。
   本文所有結論**只在模擬內成立**。
7. **reward 權重除了 `W_TAU` 之外都還是 D1 EDU 的值**，第一輪訓練後要重新校準。
