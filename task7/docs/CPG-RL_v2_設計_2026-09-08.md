# CPG-RL v2（D1 Max）—— 設計、訓練前檢查結果、Colab 流程

- 日期：2026-09-08
- Spec：`docs/superpowers/specs/2026-09-08-cpg-rl-v2-retrain-design.md`（§1–§9 是完整設計，本文不重抄）
- Plan：`docs/superpowers/plans/2026-09-08-cpg-rl-v2-retrain.md`
- 前置：`docs/H_實機obs盤點_2026-09-08.md`（obs 慣例、延遲、雜訊、偏置全部實測定案）
- Notebook：`task7/notebooks/cpg_rl_max_colab.ipynb`（由 `build_nb_v2.py` 產生）
- Env：**`task7/inference/rl_env_max.py`**（本機 CPU 可跑 reset/step、pytest 釘住）

---

## 0. 三句話

> 基準換成實機走過的 **`A_kp250_walk`**（LS / kp250 / abad60 / kd2），動作加 **body sway (x,y)** 直攻搖擺根因
> （前腳擺動時質心在支撐三角形外 35 mm），obs 加進 H 文件實測的**延遲 15 ms／雜訊／IMU 偏轉 ±3°**，
> reward 依使用者優先序（姿態 > 前腳執行率與對稱 > 航向 > 速度）並加**力矩／誤差硬邊界**（58 N·m / 0.45 rad）。
> G0/G1/G2 全過，可開訓；G7 不過不上機。

## 1. 與 v1（2026-08-27）的差異

| | v1 | **v2** |
|---|---|---|
| 基準 | `BASELINE`（DS、kp 60/120/120、kd 1、x_off −40、g_c 0.08） | **`BASELINE_A`**（LS、kp 60/250/250、kd 2、x_off −30、g_c 0.048、z_sag 0.036）|
| 訓練模型 | `zgws_mjx.xml` | **`zgws_mjx_kp250.xml`**（`make_mjx_model --kp3/--kd3` 參數化；舊檔不動）|
| 動作 | 12（mux/muy/ω ×4） | **14**（＋ sway x,y，±60 mm，斜率 4 mm/步）|
| obs | 68 | **70**（`last_action` 14）|
| 延遲 | 抽 0/1 步 | **固定 1 步 ＋ 抽 0/1**（實測 +15 ms）；`joint_vel` 另延 1 步 |
| IMU | gyro 雜訊 0.10、偏置 0.05 | **安裝偏轉 ±3°、gyro 偏置 0.02、雜訊 0.002**（實測）|
| 關節雜訊 | 無 | **pos 0.001、vel 0.2/0.2/0.4**（步態工況實測）|
| reward 新增 | — | **roll / roll rate**、**擺動相 x 追蹤（執行率）**、**前後對稱 EMA**、**力矩／誤差護欄** |
| W_VZ | 0.5 | **2.0**（41 kg 校準；v1 彈跳退步 2.5×）|
| 終止新增 | — | **\|τ\| > 90 N·m 連續 3 步** |
| DR | ABAD 與 HIP/KNEE 同係數 | **ABAD kp／摩擦單獨 ×0.6–1.4** |
| env 位置 | notebook 內 | **repo 模組**，15 項單元測試 |
| 驗收工具 | 單次 | **12 擾動 ＋ 同擾動開迴路對照 ＋ G3–G7 自動判定** |

## 2. 訓練前檢查結果（本機，2026-09-08）

### G0 —— env 固定動作重現開迴路 A（`local_infer_max --dummy --perturb 3 --compare`）

`test_dummy_matches_open_loop_walk_a` 逐位相同（speed_travel / bounce / support / min_lift / roll_pk / exec_front）。
A 基準在**原始網格模型**的數字（之後 G4/G5 的對照）：

| speed_travel | roll_pk | roll_std | exec 前／後 | 峰值力矩 | 誤差峰值 | 60 s 偏航（換算）|
|---|---|---|---|---|---|---|
| 0.34 m/s | **3.86°** | 2.56° | **0.88 ／ 1.47** | 55.7 N·m（×1.2 = 66.8）| 0.259 rad | +4.4° |

兩個要記的觀察：**後腳執行率 1.47**（後腿每步自走 173 mm，是指令的 1.47 倍）——「對稱」要壓的不只前腳不足，也有後腳過衝；
**基準峰值力矩 55.7 已貼近護欄 58**（護欄是軟的：超 10 N·m 才 −1 分），校準格會印它。

### G1 —— kp250 訓練模型 vs 網格模型（`diag/g1_kp250.py`，20 s × 12 擾動）

| 指標 | 網格 | kp250 MJX | 差 |
|---|---|---|---|
| speed_travel | 0.3440 | 0.3451 | +0.3% |
| bounce | 0.0162 | 0.0164 | +1.3% |
| support | 2.695 | 2.676 | −0.7% |
| min_lift | 0.0618 | 0.0625 | +1.2% |
| roll_pk | 3.858 | 3.797 | −1.6% |
| exec_front | 0.878 | 0.886 | +0.9% |

跌倒 0/12 ／ 0/12。**✅ 全部在 ±2% 內**（門檻 ±5%）。

### G2 —— 測試 **756 項全綠**（新增 15：BASELINE_A 凍結、kp250 模型增益、obs 70、roll/err 峰值、
JAX vs numpy CPG/IK/FK 逐點、sway 斜率、護欄數值、env 30 步不 done、DR 形狀、推論端常數一致、G0 逐位、notebook 契約）。

## 3. Colab 流程

1. 本機：`git push origin main`（notebook 用 `--depth 1` clone `main`，沒 push 的它看不到）
2. Colab 開 `task7/notebooks/cpg_rl_max_colab.ipynb`，執行階段選 GPU，**全部執行**
3. 第 3 格印 clone 到的 commit —— 要對得上本機 `git log -1`
4. 第 5 格「基準校準」：基準動作 500 步不能 done；`tau_pk` 平均要 < 58、`err_pk` < 0.45；
   `roll`/`exec_f`/`exec_r` 與本機 G0 同量級
5. 第 6 格第一個 eval 印實測步率與「60M 約幾分」—— 超過 3 小時就把 `TIMESTEPS` 砍到 40M 重跑
6. 訓練中看：`roll` 要往下、`exec f/r` 往 1 靠、`tau_pk` 一路 < 58、`len` 維持 1000
7. 下載 `cpg_rl_max_v2_params.pkl` → `task7/weights/`
8. 本機驗收：
   ```
   conda run --no-capture-output -n rbtdog python task7/inference/local_infer_max.py \
       --params task7/weights/cpg_rl_max_v2_params.pkl --secs 60 --perturb 12 --compare --video
   # 再跑 --secs 180 --perturb 12 ；--wz 0.3 --secs 20 ；--wz -0.3 --secs 20 ；--vx 0.30 --secs 20
   ```
   G3–G7 一次印完；**G7 ❌ → 不上機**，回 `rl_env_max.py` 調權重重訓。

## 4. 驗收門檻（spec §7）

| | 內容 | 過關 |
|---|---|---|
| G3 | 20/60/180 s × 12 擾動 | 0 跌倒 |
| **G4** | roll 峰值與 std vs 開迴路 A（同擾動） | **兩者都降 ≥ 60%**（基準 3.86° / 2.56°）|
| **G5** | 前腳執行率、前後差 | ≥ 0.9、\|前−後\| < 0.15（基準 0.88 / 1.47）|
| G6 | `cmd_wz=0` 60 s 總偏航 | < 5°（基準 +4.4°）|
| **G7** | 峰值力矩 ×1.2、誤差 ×1.14 | < 70 N·m、< 0.6 rad（基準 66.8 / 0.295）|

## 5. 風險（spec §9 摘要）

- RL 版 sway 沒上過機；G7 是唯一守門員。
- 模擬 z 方向順從性高估 1.8×；G4 的 60% 是模擬內相對值，實機要再驗。
- 基準已貼近力矩護欄（55.7 vs 58）→ policy 若學不到比基準更省力的走法，護欄項會常態小額扣分；
  校準格印出來後若基準常態 > 58，把 `TAU_BAR` 調到 62（仍 ×1.2 < 75，需同步放寬 G7 的說法）——**先看數字再決定**。
- 狗上推論尚未實作（另開 spec）。
